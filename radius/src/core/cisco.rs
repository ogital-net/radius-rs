// Code generated by machine generator; DO NOT EDIT.

//! Utility for cisco packet.
//!
//! This module handles the packet according to the following definition:
//! ```text
//! //! # -*- text -*-
//! # Copyright (C) 2019 The FreeRADIUS Server project and contributors
//! # This work is licensed under CC-BY version 4.0 https://creativecommons.org/licenses/by/4.0
//! #
//! #        Accounting VSAs originally by
//! #        "Marcelo M. Sosa Lugones" <marcelo@sosa.com.ar>
//! #
//! # Version:    $Id$
//! #
//! #  For documentation on Cisco RADIUS attributes, see:
//! #
//! # http://www.cisco.com/univercd/cc/td/doc/product/access/acs_serv/vapp_dev/vsaig3.htm
//! #
//! #  For general documentation on Cisco RADIUS configuration, see:
//! #
//! # http://www.cisco.com/en/US/partner/tech/tk583/tk547/tsd_technology_support_sub-protocol_home.html
//! #
//!
//! VENDOR        Cisco                9
//!
//! #
//! #    Standard attribute
//! #
//! BEGIN-VENDOR    Cisco
//!
//! ATTRIBUTE    Cisco-AVPair                1    string
//! ATTRIBUTE    Cisco-NAS-Port                2    string
//!
//! #
//! #  T.37 Store-and-Forward attributes.
//! #
//! ATTRIBUTE    Cisco-Fax-Account-Id-Origin        3    string
//! ATTRIBUTE    Cisco-Fax-Msg-Id            4    string
//! ATTRIBUTE    Cisco-Fax-Pages                5    string
//! ATTRIBUTE    Cisco-Fax-Coverpage-Flag        6    string
//! ATTRIBUTE    Cisco-Fax-Modem-Time            7    string
//! ATTRIBUTE    Cisco-Fax-Connect-Speed            8    string
//! ATTRIBUTE    Cisco-Fax-Recipient-Count        9    string
//! ATTRIBUTE    Cisco-Fax-Process-Abort-Flag        10    string
//! ATTRIBUTE    Cisco-Fax-Dsn-Address            11    string
//! ATTRIBUTE    Cisco-Fax-Dsn-Flag            12    string
//! ATTRIBUTE    Cisco-Fax-Mdn-Address            13    string
//! ATTRIBUTE    Cisco-Fax-Mdn-Flag            14    string
//! ATTRIBUTE    Cisco-Fax-Auth-Status            15    string
//! ATTRIBUTE    Cisco-Email-Server-Address        16    string
//! ATTRIBUTE    Cisco-Email-Server-Ack-Flag        17    string
//! ATTRIBUTE    Cisco-Gateway-Id            18    string
//! ATTRIBUTE    Cisco-Call-Type                19    string
//! ATTRIBUTE    Cisco-Port-Used                20    string
//! ATTRIBUTE    Cisco-Abort-Cause            21    string
//!
//! #
//! #  Voice over IP attributes.
//! #
//! ATTRIBUTE    h323-remote-address            23    string
//! ATTRIBUTE    h323-conf-id                24    string
//! ATTRIBUTE    h323-setup-time                25    string
//! ATTRIBUTE    h323-call-origin            26    string
//! ATTRIBUTE    h323-call-type                27    string
//! ATTRIBUTE    h323-connect-time            28    string
//! ATTRIBUTE    h323-disconnect-time            29    string
//! ATTRIBUTE    h323-disconnect-cause            30    string
//! ATTRIBUTE    h323-voice-quality            31    string
//! ATTRIBUTE    h323-gw-id                33    string
//! ATTRIBUTE    h323-incoming-conf-id            35    string
//!
//! ATTRIBUTE    Cisco-Policy-Up                37    string
//! ATTRIBUTE    Cisco-Policy-Down            38    string
//!
//! ATTRIBUTE    Cisco-Relay-Information-Option        46    string
//! ATTRIBUTE    Cisco-DHCP-User-Class            47    string
//! ATTRIBUTE    Cisco-DHCP-Vendor-Class            48    string
//!
//! ATTRIBUTE    Cisco-DHCP-Relay-GiAddr            50    string
//! ATTRIBUTE    Cisco-Service-Name            51    string
//! ATTRIBUTE    Cisco-Parent-Session-Id            52    string
//!
//! ATTRIBUTE    Cisco-Sub-QoS-Pol-In            55    string
//! ATTRIBUTE    Cisco-Sub-QoS-Pol-Out            56    string
//! ATTRIBUTE    Cisco-In-ACL                57    string
//! ATTRIBUTE    Cisco-Out-ACL                58    string
//! ATTRIBUTE    Cisco-Sub-PBR-Policy-In            59    string
//! ATTRIBUTE    Cisco-Sub-Activate-Service        60    string
//! ATTRIBUTE    Cisco-IPv6-In-ACL            61    string
//! ATTRIBUTE    Cisco-IPv6-Out-ACL            62    string
//! ATTRIBUTE    Cisco-Sub-Deactivate-Service        63    string
//!
//! ATTRIBUTE    Cisco-DHCP-Subscriber-Id        65    string
//! ATTRIBUTE    Cisco-DHCPv6-Link-Address        66    string
//!
//! ATTRIBUTE    Cisco-DHCPv4-Option-To-AAA        90    string
//!
//! ATTRIBUTE    sip-conf-id                100    string
//! ATTRIBUTE    h323-credit-amount            101    string
//! ATTRIBUTE    h323-credit-time            102    string
//! ATTRIBUTE    h323-return-code            103    string
//! ATTRIBUTE    h323-prompt-id                104    string
//! ATTRIBUTE    h323-time-and-day            105    string
//! ATTRIBUTE    h323-redirect-number            106    string
//! ATTRIBUTE    h323-preferred-lang            107    string
//! ATTRIBUTE    h323-redirect-ip-address        108    string
//! ATTRIBUTE    h323-billing-model            109    string
//! ATTRIBUTE    h323-currency                110    string
//! ATTRIBUTE    subscriber                111    string
//! ATTRIBUTE    gw-rxd-cdn                112    string
//! ATTRIBUTE    gw-final-xlated-cdn            113    string
//! ATTRIBUTE    remote-media-address            114    string
//! ATTRIBUTE    release-source                115    string
//! ATTRIBUTE    gw-rxd-cgn                116    string
//! ATTRIBUTE    gw-final-xlated-cgn            117    string
//!
//! # SIP Attributes
//! ATTRIBUTE    call-id                    141    string
//! ATTRIBUTE    session-protocol            142    string
//! ATTRIBUTE    method                    143    string
//! ATTRIBUTE    prev-hop-via                144    string
//! ATTRIBUTE    prev-hop-ip                145    string
//! ATTRIBUTE    incoming-req-uri            146    string
//! ATTRIBUTE    outgoing-req-uri            147    string
//! ATTRIBUTE    next-hop-ip                148    string
//! ATTRIBUTE    next-hop-dn                149    string
//! ATTRIBUTE    sip-hdr                    150    string
//! ATTRIBUTE    dsp-id                    151    string
//!
//! #
//! #    Extra attributes sent by the Cisco, if you configure
//! #    "radius-server vsa accounting" (requires IOS11.2+).
//! #
//! ATTRIBUTE    Cisco-Multilink-ID            187    integer
//! ATTRIBUTE    Cisco-Num-In-Multilink            188    integer
//! ATTRIBUTE    Cisco-Pre-Input-Octets            190    integer
//! ATTRIBUTE    Cisco-Pre-Output-Octets            191    integer
//! ATTRIBUTE    Cisco-Pre-Input-Packets            192    integer
//! ATTRIBUTE    Cisco-Pre-Output-Packets        193    integer
//! ATTRIBUTE    Cisco-Maximum-Time            194    integer
//! ATTRIBUTE    Cisco-Disconnect-Cause            195    integer
//! ATTRIBUTE    Cisco-Data-Rate                197    integer
//! ATTRIBUTE    Cisco-PreSession-Time            198    integer
//! ATTRIBUTE    Cisco-PW-Lifetime            208    integer
//! ATTRIBUTE    Cisco-IP-Direct                209    integer
//! ATTRIBUTE    Cisco-PPP-VJ-Slot-Comp            210    integer
//! ATTRIBUTE    Cisco-PPP-Async-Map            212    integer
//! ATTRIBUTE    Cisco-IP-Pool-Definition        217    string
//! ATTRIBUTE    Cisco-Assign-IP-Pool            218    integer
//! ATTRIBUTE    Cisco-Route-IP                228    integer
//! ATTRIBUTE    Cisco-Link-Compression            233    integer
//! ATTRIBUTE    Cisco-Target-Util            234    integer
//! ATTRIBUTE    Cisco-Maximum-Channels            235    integer
//! ATTRIBUTE    Cisco-Data-Filter            242    integer
//! ATTRIBUTE    Cisco-Call-Filter            243    integer
//! ATTRIBUTE    Cisco-Idle-Limit            244    integer
//! ATTRIBUTE    Cisco-Subscriber-Password        249    string
//! ATTRIBUTE    Cisco-Account-Info            250    string
//! ATTRIBUTE    Cisco-Service-Info            251    string
//! ATTRIBUTE    Cisco-Command-Code            252    string
//! ATTRIBUTE    Cisco-Control-Info            253    string
//! ATTRIBUTE    Cisco-Xmit-Rate                255    integer
//!
//! VALUE    Cisco-Disconnect-Cause        No-Reason        0
//! VALUE    Cisco-Disconnect-Cause        No-Disconnect        1
//! VALUE    Cisco-Disconnect-Cause        Unknown            2
//! VALUE    Cisco-Disconnect-Cause        Call-Disconnect        3
//! VALUE    Cisco-Disconnect-Cause        CLID-Authentication-Failure 4
//! VALUE    Cisco-Disconnect-Cause        No-Modem-Available    9
//! VALUE    Cisco-Disconnect-Cause        No-Carrier        10
//! VALUE    Cisco-Disconnect-Cause        Lost-Carrier        11
//! VALUE    Cisco-Disconnect-Cause        No-Detected-Result-Codes 12
//! VALUE    Cisco-Disconnect-Cause        User-Ends-Session    20
//! VALUE    Cisco-Disconnect-Cause        Idle-Timeout        21
//! VALUE    Cisco-Disconnect-Cause        Exit-Telnet-Session    22
//! VALUE    Cisco-Disconnect-Cause        No-Remote-IP-Addr    23
//! VALUE    Cisco-Disconnect-Cause        Exit-Raw-TCP        24
//! VALUE    Cisco-Disconnect-Cause        Password-Fail        25
//! VALUE    Cisco-Disconnect-Cause        Raw-TCP-Disabled    26
//! VALUE    Cisco-Disconnect-Cause        Control-C-Detected    27
//! VALUE    Cisco-Disconnect-Cause        EXEC-Program-Destroyed    28
//! VALUE    Cisco-Disconnect-Cause        Close-Virtual-Connection 29
//! VALUE    Cisco-Disconnect-Cause        End-Virtual-Connection    30
//! VALUE    Cisco-Disconnect-Cause        Exit-Rlogin        31
//! VALUE    Cisco-Disconnect-Cause        Invalid-Rlogin-Option    32
//! VALUE    Cisco-Disconnect-Cause        Insufficient-Resources    33
//! VALUE    Cisco-Disconnect-Cause        Timeout-PPP-LCP        40
//! VALUE    Cisco-Disconnect-Cause        Failed-PPP-LCP-Negotiation 41
//! VALUE    Cisco-Disconnect-Cause        Failed-PPP-PAP-Auth-Fail 42
//! VALUE    Cisco-Disconnect-Cause        Failed-PPP-CHAP-Auth    43
//! VALUE    Cisco-Disconnect-Cause        Failed-PPP-Remote-Auth    44
//! VALUE    Cisco-Disconnect-Cause        PPP-Remote-Terminate    45
//! VALUE    Cisco-Disconnect-Cause        PPP-Closed-Event    46
//! VALUE    Cisco-Disconnect-Cause        NCP-Closed-PPP        47
//! VALUE    Cisco-Disconnect-Cause        MP-Error-PPP        48
//! VALUE    Cisco-Disconnect-Cause        PPP-Maximum-Channels    49
//! VALUE    Cisco-Disconnect-Cause        Tables-Full        50
//! VALUE    Cisco-Disconnect-Cause        Resources-Full        51
//! VALUE    Cisco-Disconnect-Cause        Invalid-IP-Address    52
//! VALUE    Cisco-Disconnect-Cause        Bad-Hostname        53
//! VALUE    Cisco-Disconnect-Cause        Bad-Port        54
//! VALUE    Cisco-Disconnect-Cause        Reset-TCP        60
//! VALUE    Cisco-Disconnect-Cause        TCP-Connection-Refused    61
//! VALUE    Cisco-Disconnect-Cause        Timeout-TCP        62
//! VALUE    Cisco-Disconnect-Cause        Foreign-Host-Close-TCP    63
//! VALUE    Cisco-Disconnect-Cause        TCP-Network-Unreachable    64
//! VALUE    Cisco-Disconnect-Cause        TCP-Host-Unreachable    65
//! VALUE    Cisco-Disconnect-Cause        TCP-Network-Admin-Unreachable 66
//! VALUE    Cisco-Disconnect-Cause        TCP-Port-Unreachable    67
//! VALUE    Cisco-Disconnect-Cause        Session-Timeout        100
//! VALUE    Cisco-Disconnect-Cause        Session-Failed-Security    101
//! VALUE    Cisco-Disconnect-Cause        Session-End-Callback    102
//! VALUE    Cisco-Disconnect-Cause        Invalid-Protocol    120
//! VALUE    Cisco-Disconnect-Cause        RADIUS-Disconnect    150
//! VALUE    Cisco-Disconnect-Cause        Local-Admin-Disconnect    151
//! VALUE    Cisco-Disconnect-Cause        SNMP-Disconnect        152
//! VALUE    Cisco-Disconnect-Cause        V110-Retries        160
//! VALUE    Cisco-Disconnect-Cause        PPP-Authentication-Timeout 170
//! VALUE    Cisco-Disconnect-Cause        Local-Hangup        180
//! VALUE    Cisco-Disconnect-Cause        Remote-Hangup        185
//! VALUE    Cisco-Disconnect-Cause        T1-Quiesced        190
//! VALUE    Cisco-Disconnect-Cause        Call-Duration        195
//! VALUE    Cisco-Disconnect-Cause        VPN-User-Disconnect    600
//! VALUE    Cisco-Disconnect-Cause        VPN-Carrier-Loss    601
//! VALUE    Cisco-Disconnect-Cause        VPN-No-Resources    602
//! VALUE    Cisco-Disconnect-Cause        VPN-Bad-Control-Packet    603
//! VALUE    Cisco-Disconnect-Cause        VPN-Admin-Disconnect    604
//! VALUE    Cisco-Disconnect-Cause        VPN-Tunnel-Shut        605
//! VALUE    Cisco-Disconnect-Cause        VPN-Local-Disconnect    606
//! VALUE    Cisco-Disconnect-Cause        VPN-Session-Limit    607
//! VALUE    Cisco-Disconnect-Cause        VPN-Call-Redirect    608
//!
//! END-VENDOR    Cisco
//! ```

use crate::core::avp::{AVPError, AVPType, AVP};
use crate::core::packet::Packet;

pub const CISCO_AV_PAIR_TYPE: AVPType = 1;
/// Delete all of `cisco_av_pair` values from a packet.
pub fn delete_cisco_av_pair(packet: &mut Packet) {
    packet.delete(CISCO_AV_PAIR_TYPE);
}
/// Add `cisco_av_pair` string value to a packet.
pub fn add_cisco_av_pair(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_AV_PAIR_TYPE, value));
}
/// Lookup a `cisco_av_pair` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_av_pair`, it returns `None`.
pub fn lookup_cisco_av_pair(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(CISCO_AV_PAIR_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `cisco_av_pair` string value from a packet.
pub fn lookup_all_cisco_av_pair(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_AV_PAIR_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_NAS_PORT_TYPE: AVPType = 2;
/// Delete all of `cisco_nas_port` values from a packet.
pub fn delete_cisco_nas_port(packet: &mut Packet) {
    packet.delete(CISCO_NAS_PORT_TYPE);
}
/// Add `cisco_nas_port` string value to a packet.
pub fn add_cisco_nas_port(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_NAS_PORT_TYPE, value));
}
/// Lookup a `cisco_nas_port` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_nas_port`, it returns `None`.
pub fn lookup_cisco_nas_port(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_NAS_PORT_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_nas_port` string value from a packet.
pub fn lookup_all_cisco_nas_port(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_NAS_PORT_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_ACCOUNT_ID_ORIGIN_TYPE: AVPType = 3;
/// Delete all of `cisco_fax_account_id_origin` values from a packet.
pub fn delete_cisco_fax_account_id_origin(packet: &mut Packet) {
    packet.delete(CISCO_FAX_ACCOUNT_ID_ORIGIN_TYPE);
}
/// Add `cisco_fax_account_id_origin` string value to a packet.
pub fn add_cisco_fax_account_id_origin(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_ACCOUNT_ID_ORIGIN_TYPE, value));
}
/// Lookup a `cisco_fax_account_id_origin` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_account_id_origin`, it returns `None`.
pub fn lookup_cisco_fax_account_id_origin(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_ACCOUNT_ID_ORIGIN_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_account_id_origin` string value from a packet.
pub fn lookup_all_cisco_fax_account_id_origin(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_ACCOUNT_ID_ORIGIN_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_MSG_ID_TYPE: AVPType = 4;
/// Delete all of `cisco_fax_msg_id` values from a packet.
pub fn delete_cisco_fax_msg_id(packet: &mut Packet) {
    packet.delete(CISCO_FAX_MSG_ID_TYPE);
}
/// Add `cisco_fax_msg_id` string value to a packet.
pub fn add_cisco_fax_msg_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_MSG_ID_TYPE, value));
}
/// Lookup a `cisco_fax_msg_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_msg_id`, it returns `None`.
pub fn lookup_cisco_fax_msg_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_MSG_ID_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_msg_id` string value from a packet.
pub fn lookup_all_cisco_fax_msg_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_MSG_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_PAGES_TYPE: AVPType = 5;
/// Delete all of `cisco_fax_pages` values from a packet.
pub fn delete_cisco_fax_pages(packet: &mut Packet) {
    packet.delete(CISCO_FAX_PAGES_TYPE);
}
/// Add `cisco_fax_pages` string value to a packet.
pub fn add_cisco_fax_pages(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_PAGES_TYPE, value));
}
/// Lookup a `cisco_fax_pages` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_pages`, it returns `None`.
pub fn lookup_cisco_fax_pages(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_PAGES_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_pages` string value from a packet.
pub fn lookup_all_cisco_fax_pages(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_PAGES_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_COVERPAGE_FLAG_TYPE: AVPType = 6;
/// Delete all of `cisco_fax_coverpage_flag` values from a packet.
pub fn delete_cisco_fax_coverpage_flag(packet: &mut Packet) {
    packet.delete(CISCO_FAX_COVERPAGE_FLAG_TYPE);
}
/// Add `cisco_fax_coverpage_flag` string value to a packet.
pub fn add_cisco_fax_coverpage_flag(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_COVERPAGE_FLAG_TYPE, value));
}
/// Lookup a `cisco_fax_coverpage_flag` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_coverpage_flag`, it returns `None`.
pub fn lookup_cisco_fax_coverpage_flag(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_COVERPAGE_FLAG_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_coverpage_flag` string value from a packet.
pub fn lookup_all_cisco_fax_coverpage_flag(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_COVERPAGE_FLAG_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_MODEM_TIME_TYPE: AVPType = 7;
/// Delete all of `cisco_fax_modem_time` values from a packet.
pub fn delete_cisco_fax_modem_time(packet: &mut Packet) {
    packet.delete(CISCO_FAX_MODEM_TIME_TYPE);
}
/// Add `cisco_fax_modem_time` string value to a packet.
pub fn add_cisco_fax_modem_time(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_MODEM_TIME_TYPE, value));
}
/// Lookup a `cisco_fax_modem_time` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_modem_time`, it returns `None`.
pub fn lookup_cisco_fax_modem_time(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_MODEM_TIME_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_modem_time` string value from a packet.
pub fn lookup_all_cisco_fax_modem_time(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_MODEM_TIME_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_CONNECT_SPEED_TYPE: AVPType = 8;
/// Delete all of `cisco_fax_connect_speed` values from a packet.
pub fn delete_cisco_fax_connect_speed(packet: &mut Packet) {
    packet.delete(CISCO_FAX_CONNECT_SPEED_TYPE);
}
/// Add `cisco_fax_connect_speed` string value to a packet.
pub fn add_cisco_fax_connect_speed(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_CONNECT_SPEED_TYPE, value));
}
/// Lookup a `cisco_fax_connect_speed` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_connect_speed`, it returns `None`.
pub fn lookup_cisco_fax_connect_speed(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_CONNECT_SPEED_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_connect_speed` string value from a packet.
pub fn lookup_all_cisco_fax_connect_speed(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_CONNECT_SPEED_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_RECIPIENT_COUNT_TYPE: AVPType = 9;
/// Delete all of `cisco_fax_recipient_count` values from a packet.
pub fn delete_cisco_fax_recipient_count(packet: &mut Packet) {
    packet.delete(CISCO_FAX_RECIPIENT_COUNT_TYPE);
}
/// Add `cisco_fax_recipient_count` string value to a packet.
pub fn add_cisco_fax_recipient_count(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_RECIPIENT_COUNT_TYPE, value));
}
/// Lookup a `cisco_fax_recipient_count` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_recipient_count`, it returns `None`.
pub fn lookup_cisco_fax_recipient_count(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_RECIPIENT_COUNT_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_recipient_count` string value from a packet.
pub fn lookup_all_cisco_fax_recipient_count(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_RECIPIENT_COUNT_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_PROCESS_ABORT_FLAG_TYPE: AVPType = 10;
/// Delete all of `cisco_fax_process_abort_flag` values from a packet.
pub fn delete_cisco_fax_process_abort_flag(packet: &mut Packet) {
    packet.delete(CISCO_FAX_PROCESS_ABORT_FLAG_TYPE);
}
/// Add `cisco_fax_process_abort_flag` string value to a packet.
pub fn add_cisco_fax_process_abort_flag(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_PROCESS_ABORT_FLAG_TYPE, value));
}
/// Lookup a `cisco_fax_process_abort_flag` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_process_abort_flag`, it returns `None`.
pub fn lookup_cisco_fax_process_abort_flag(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_PROCESS_ABORT_FLAG_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_process_abort_flag` string value from a packet.
pub fn lookup_all_cisco_fax_process_abort_flag(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_PROCESS_ABORT_FLAG_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_DSN_ADDRESS_TYPE: AVPType = 11;
/// Delete all of `cisco_fax_dsn_address` values from a packet.
pub fn delete_cisco_fax_dsn_address(packet: &mut Packet) {
    packet.delete(CISCO_FAX_DSN_ADDRESS_TYPE);
}
/// Add `cisco_fax_dsn_address` string value to a packet.
pub fn add_cisco_fax_dsn_address(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_DSN_ADDRESS_TYPE, value));
}
/// Lookup a `cisco_fax_dsn_address` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_dsn_address`, it returns `None`.
pub fn lookup_cisco_fax_dsn_address(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_DSN_ADDRESS_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_dsn_address` string value from a packet.
pub fn lookup_all_cisco_fax_dsn_address(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_DSN_ADDRESS_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_DSN_FLAG_TYPE: AVPType = 12;
/// Delete all of `cisco_fax_dsn_flag` values from a packet.
pub fn delete_cisco_fax_dsn_flag(packet: &mut Packet) {
    packet.delete(CISCO_FAX_DSN_FLAG_TYPE);
}
/// Add `cisco_fax_dsn_flag` string value to a packet.
pub fn add_cisco_fax_dsn_flag(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_DSN_FLAG_TYPE, value));
}
/// Lookup a `cisco_fax_dsn_flag` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_dsn_flag`, it returns `None`.
pub fn lookup_cisco_fax_dsn_flag(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_DSN_FLAG_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_dsn_flag` string value from a packet.
pub fn lookup_all_cisco_fax_dsn_flag(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_DSN_FLAG_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_MDN_ADDRESS_TYPE: AVPType = 13;
/// Delete all of `cisco_fax_mdn_address` values from a packet.
pub fn delete_cisco_fax_mdn_address(packet: &mut Packet) {
    packet.delete(CISCO_FAX_MDN_ADDRESS_TYPE);
}
/// Add `cisco_fax_mdn_address` string value to a packet.
pub fn add_cisco_fax_mdn_address(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_MDN_ADDRESS_TYPE, value));
}
/// Lookup a `cisco_fax_mdn_address` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_mdn_address`, it returns `None`.
pub fn lookup_cisco_fax_mdn_address(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_MDN_ADDRESS_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_mdn_address` string value from a packet.
pub fn lookup_all_cisco_fax_mdn_address(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_MDN_ADDRESS_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_MDN_FLAG_TYPE: AVPType = 14;
/// Delete all of `cisco_fax_mdn_flag` values from a packet.
pub fn delete_cisco_fax_mdn_flag(packet: &mut Packet) {
    packet.delete(CISCO_FAX_MDN_FLAG_TYPE);
}
/// Add `cisco_fax_mdn_flag` string value to a packet.
pub fn add_cisco_fax_mdn_flag(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_MDN_FLAG_TYPE, value));
}
/// Lookup a `cisco_fax_mdn_flag` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_mdn_flag`, it returns `None`.
pub fn lookup_cisco_fax_mdn_flag(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_MDN_FLAG_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_mdn_flag` string value from a packet.
pub fn lookup_all_cisco_fax_mdn_flag(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_MDN_FLAG_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_FAX_AUTH_STATUS_TYPE: AVPType = 15;
/// Delete all of `cisco_fax_auth_status` values from a packet.
pub fn delete_cisco_fax_auth_status(packet: &mut Packet) {
    packet.delete(CISCO_FAX_AUTH_STATUS_TYPE);
}
/// Add `cisco_fax_auth_status` string value to a packet.
pub fn add_cisco_fax_auth_status(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_FAX_AUTH_STATUS_TYPE, value));
}
/// Lookup a `cisco_fax_auth_status` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_fax_auth_status`, it returns `None`.
pub fn lookup_cisco_fax_auth_status(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_FAX_AUTH_STATUS_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_fax_auth_status` string value from a packet.
pub fn lookup_all_cisco_fax_auth_status(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_FAX_AUTH_STATUS_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_EMAIL_SERVER_ADDRESS_TYPE: AVPType = 16;
/// Delete all of `cisco_email_server_address` values from a packet.
pub fn delete_cisco_email_server_address(packet: &mut Packet) {
    packet.delete(CISCO_EMAIL_SERVER_ADDRESS_TYPE);
}
/// Add `cisco_email_server_address` string value to a packet.
pub fn add_cisco_email_server_address(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_EMAIL_SERVER_ADDRESS_TYPE, value));
}
/// Lookup a `cisco_email_server_address` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_email_server_address`, it returns `None`.
pub fn lookup_cisco_email_server_address(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_EMAIL_SERVER_ADDRESS_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_email_server_address` string value from a packet.
pub fn lookup_all_cisco_email_server_address(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_EMAIL_SERVER_ADDRESS_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_EMAIL_SERVER_ACK_FLAG_TYPE: AVPType = 17;
/// Delete all of `cisco_email_server_ack_flag` values from a packet.
pub fn delete_cisco_email_server_ack_flag(packet: &mut Packet) {
    packet.delete(CISCO_EMAIL_SERVER_ACK_FLAG_TYPE);
}
/// Add `cisco_email_server_ack_flag` string value to a packet.
pub fn add_cisco_email_server_ack_flag(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_EMAIL_SERVER_ACK_FLAG_TYPE, value));
}
/// Lookup a `cisco_email_server_ack_flag` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_email_server_ack_flag`, it returns `None`.
pub fn lookup_cisco_email_server_ack_flag(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_EMAIL_SERVER_ACK_FLAG_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_email_server_ack_flag` string value from a packet.
pub fn lookup_all_cisco_email_server_ack_flag(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_EMAIL_SERVER_ACK_FLAG_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_GATEWAY_ID_TYPE: AVPType = 18;
/// Delete all of `cisco_gateway_id` values from a packet.
pub fn delete_cisco_gateway_id(packet: &mut Packet) {
    packet.delete(CISCO_GATEWAY_ID_TYPE);
}
/// Add `cisco_gateway_id` string value to a packet.
pub fn add_cisco_gateway_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_GATEWAY_ID_TYPE, value));
}
/// Lookup a `cisco_gateway_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_gateway_id`, it returns `None`.
pub fn lookup_cisco_gateway_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_GATEWAY_ID_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_gateway_id` string value from a packet.
pub fn lookup_all_cisco_gateway_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_GATEWAY_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_CALL_TYPE_TYPE: AVPType = 19;
/// Delete all of `cisco_call_type` values from a packet.
pub fn delete_cisco_call_type(packet: &mut Packet) {
    packet.delete(CISCO_CALL_TYPE_TYPE);
}
/// Add `cisco_call_type` string value to a packet.
pub fn add_cisco_call_type(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_CALL_TYPE_TYPE, value));
}
/// Lookup a `cisco_call_type` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_call_type`, it returns `None`.
pub fn lookup_cisco_call_type(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_CALL_TYPE_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_call_type` string value from a packet.
pub fn lookup_all_cisco_call_type(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_CALL_TYPE_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_PORT_USED_TYPE: AVPType = 20;
/// Delete all of `cisco_port_used` values from a packet.
pub fn delete_cisco_port_used(packet: &mut Packet) {
    packet.delete(CISCO_PORT_USED_TYPE);
}
/// Add `cisco_port_used` string value to a packet.
pub fn add_cisco_port_used(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_PORT_USED_TYPE, value));
}
/// Lookup a `cisco_port_used` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_port_used`, it returns `None`.
pub fn lookup_cisco_port_used(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_PORT_USED_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_port_used` string value from a packet.
pub fn lookup_all_cisco_port_used(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_PORT_USED_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_ABORT_CAUSE_TYPE: AVPType = 21;
/// Delete all of `cisco_abort_cause` values from a packet.
pub fn delete_cisco_abort_cause(packet: &mut Packet) {
    packet.delete(CISCO_ABORT_CAUSE_TYPE);
}
/// Add `cisco_abort_cause` string value to a packet.
pub fn add_cisco_abort_cause(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_ABORT_CAUSE_TYPE, value));
}
/// Lookup a `cisco_abort_cause` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_abort_cause`, it returns `None`.
pub fn lookup_cisco_abort_cause(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_ABORT_CAUSE_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_abort_cause` string value from a packet.
pub fn lookup_all_cisco_abort_cause(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_ABORT_CAUSE_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_REMOTE_ADDRESS_TYPE: AVPType = 23;
/// Delete all of `h_323_remote_address` values from a packet.
pub fn delete_h_323_remote_address(packet: &mut Packet) {
    packet.delete(H_323_REMOTE_ADDRESS_TYPE);
}
/// Add `h_323_remote_address` string value to a packet.
pub fn add_h_323_remote_address(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_REMOTE_ADDRESS_TYPE, value));
}
/// Lookup a `h_323_remote_address` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_remote_address`, it returns `None`.
pub fn lookup_h_323_remote_address(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_REMOTE_ADDRESS_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_remote_address` string value from a packet.
pub fn lookup_all_h_323_remote_address(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_REMOTE_ADDRESS_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_CONF_ID_TYPE: AVPType = 24;
/// Delete all of `h_323_conf_id` values from a packet.
pub fn delete_h_323_conf_id(packet: &mut Packet) {
    packet.delete(H_323_CONF_ID_TYPE);
}
/// Add `h_323_conf_id` string value to a packet.
pub fn add_h_323_conf_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_CONF_ID_TYPE, value));
}
/// Lookup a `h_323_conf_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_conf_id`, it returns `None`.
pub fn lookup_h_323_conf_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(H_323_CONF_ID_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `h_323_conf_id` string value from a packet.
pub fn lookup_all_h_323_conf_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_CONF_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_SETUP_TIME_TYPE: AVPType = 25;
/// Delete all of `h_323_setup_time` values from a packet.
pub fn delete_h_323_setup_time(packet: &mut Packet) {
    packet.delete(H_323_SETUP_TIME_TYPE);
}
/// Add `h_323_setup_time` string value to a packet.
pub fn add_h_323_setup_time(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_SETUP_TIME_TYPE, value));
}
/// Lookup a `h_323_setup_time` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_setup_time`, it returns `None`.
pub fn lookup_h_323_setup_time(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_SETUP_TIME_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_setup_time` string value from a packet.
pub fn lookup_all_h_323_setup_time(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_SETUP_TIME_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_CALL_ORIGIN_TYPE: AVPType = 26;
/// Delete all of `h_323_call_origin` values from a packet.
pub fn delete_h_323_call_origin(packet: &mut Packet) {
    packet.delete(H_323_CALL_ORIGIN_TYPE);
}
/// Add `h_323_call_origin` string value to a packet.
pub fn add_h_323_call_origin(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_CALL_ORIGIN_TYPE, value));
}
/// Lookup a `h_323_call_origin` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_call_origin`, it returns `None`.
pub fn lookup_h_323_call_origin(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_CALL_ORIGIN_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_call_origin` string value from a packet.
pub fn lookup_all_h_323_call_origin(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_CALL_ORIGIN_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_CALL_TYPE_TYPE: AVPType = 27;
/// Delete all of `h_323_call_type` values from a packet.
pub fn delete_h_323_call_type(packet: &mut Packet) {
    packet.delete(H_323_CALL_TYPE_TYPE);
}
/// Add `h_323_call_type` string value to a packet.
pub fn add_h_323_call_type(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_CALL_TYPE_TYPE, value));
}
/// Lookup a `h_323_call_type` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_call_type`, it returns `None`.
pub fn lookup_h_323_call_type(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_CALL_TYPE_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_call_type` string value from a packet.
pub fn lookup_all_h_323_call_type(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_CALL_TYPE_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_CONNECT_TIME_TYPE: AVPType = 28;
/// Delete all of `h_323_connect_time` values from a packet.
pub fn delete_h_323_connect_time(packet: &mut Packet) {
    packet.delete(H_323_CONNECT_TIME_TYPE);
}
/// Add `h_323_connect_time` string value to a packet.
pub fn add_h_323_connect_time(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_CONNECT_TIME_TYPE, value));
}
/// Lookup a `h_323_connect_time` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_connect_time`, it returns `None`.
pub fn lookup_h_323_connect_time(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_CONNECT_TIME_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_connect_time` string value from a packet.
pub fn lookup_all_h_323_connect_time(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_CONNECT_TIME_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_DISCONNECT_TIME_TYPE: AVPType = 29;
/// Delete all of `h_323_disconnect_time` values from a packet.
pub fn delete_h_323_disconnect_time(packet: &mut Packet) {
    packet.delete(H_323_DISCONNECT_TIME_TYPE);
}
/// Add `h_323_disconnect_time` string value to a packet.
pub fn add_h_323_disconnect_time(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_DISCONNECT_TIME_TYPE, value));
}
/// Lookup a `h_323_disconnect_time` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_disconnect_time`, it returns `None`.
pub fn lookup_h_323_disconnect_time(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_DISCONNECT_TIME_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_disconnect_time` string value from a packet.
pub fn lookup_all_h_323_disconnect_time(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_DISCONNECT_TIME_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_DISCONNECT_CAUSE_TYPE: AVPType = 30;
/// Delete all of `h_323_disconnect_cause` values from a packet.
pub fn delete_h_323_disconnect_cause(packet: &mut Packet) {
    packet.delete(H_323_DISCONNECT_CAUSE_TYPE);
}
/// Add `h_323_disconnect_cause` string value to a packet.
pub fn add_h_323_disconnect_cause(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_DISCONNECT_CAUSE_TYPE, value));
}
/// Lookup a `h_323_disconnect_cause` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_disconnect_cause`, it returns `None`.
pub fn lookup_h_323_disconnect_cause(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_DISCONNECT_CAUSE_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_disconnect_cause` string value from a packet.
pub fn lookup_all_h_323_disconnect_cause(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_DISCONNECT_CAUSE_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_VOICE_QUALITY_TYPE: AVPType = 31;
/// Delete all of `h_323_voice_quality` values from a packet.
pub fn delete_h_323_voice_quality(packet: &mut Packet) {
    packet.delete(H_323_VOICE_QUALITY_TYPE);
}
/// Add `h_323_voice_quality` string value to a packet.
pub fn add_h_323_voice_quality(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_VOICE_QUALITY_TYPE, value));
}
/// Lookup a `h_323_voice_quality` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_voice_quality`, it returns `None`.
pub fn lookup_h_323_voice_quality(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_VOICE_QUALITY_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_voice_quality` string value from a packet.
pub fn lookup_all_h_323_voice_quality(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_VOICE_QUALITY_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_GW_ID_TYPE: AVPType = 33;
/// Delete all of `h_323_gw_id` values from a packet.
pub fn delete_h_323_gw_id(packet: &mut Packet) {
    packet.delete(H_323_GW_ID_TYPE);
}
/// Add `h_323_gw_id` string value to a packet.
pub fn add_h_323_gw_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_GW_ID_TYPE, value));
}
/// Lookup a `h_323_gw_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_gw_id`, it returns `None`.
pub fn lookup_h_323_gw_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(H_323_GW_ID_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `h_323_gw_id` string value from a packet.
pub fn lookup_all_h_323_gw_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_GW_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_INCOMING_CONF_ID_TYPE: AVPType = 35;
/// Delete all of `h_323_incoming_conf_id` values from a packet.
pub fn delete_h_323_incoming_conf_id(packet: &mut Packet) {
    packet.delete(H_323_INCOMING_CONF_ID_TYPE);
}
/// Add `h_323_incoming_conf_id` string value to a packet.
pub fn add_h_323_incoming_conf_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_INCOMING_CONF_ID_TYPE, value));
}
/// Lookup a `h_323_incoming_conf_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_incoming_conf_id`, it returns `None`.
pub fn lookup_h_323_incoming_conf_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_INCOMING_CONF_ID_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_incoming_conf_id` string value from a packet.
pub fn lookup_all_h_323_incoming_conf_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_INCOMING_CONF_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_POLICY_UP_TYPE: AVPType = 37;
/// Delete all of `cisco_policy_up` values from a packet.
pub fn delete_cisco_policy_up(packet: &mut Packet) {
    packet.delete(CISCO_POLICY_UP_TYPE);
}
/// Add `cisco_policy_up` string value to a packet.
pub fn add_cisco_policy_up(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_POLICY_UP_TYPE, value));
}
/// Lookup a `cisco_policy_up` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_policy_up`, it returns `None`.
pub fn lookup_cisco_policy_up(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_POLICY_UP_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_policy_up` string value from a packet.
pub fn lookup_all_cisco_policy_up(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_POLICY_UP_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_POLICY_DOWN_TYPE: AVPType = 38;
/// Delete all of `cisco_policy_down` values from a packet.
pub fn delete_cisco_policy_down(packet: &mut Packet) {
    packet.delete(CISCO_POLICY_DOWN_TYPE);
}
/// Add `cisco_policy_down` string value to a packet.
pub fn add_cisco_policy_down(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_POLICY_DOWN_TYPE, value));
}
/// Lookup a `cisco_policy_down` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_policy_down`, it returns `None`.
pub fn lookup_cisco_policy_down(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_POLICY_DOWN_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_policy_down` string value from a packet.
pub fn lookup_all_cisco_policy_down(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_POLICY_DOWN_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_RELAY_INFORMATION_OPTION_TYPE: AVPType = 46;
/// Delete all of `cisco_relay_information_option` values from a packet.
pub fn delete_cisco_relay_information_option(packet: &mut Packet) {
    packet.delete(CISCO_RELAY_INFORMATION_OPTION_TYPE);
}
/// Add `cisco_relay_information_option` string value to a packet.
pub fn add_cisco_relay_information_option(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_RELAY_INFORMATION_OPTION_TYPE, value));
}
/// Lookup a `cisco_relay_information_option` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_relay_information_option`, it returns `None`.
pub fn lookup_cisco_relay_information_option(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_RELAY_INFORMATION_OPTION_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_relay_information_option` string value from a packet.
pub fn lookup_all_cisco_relay_information_option(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_RELAY_INFORMATION_OPTION_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_DHCP_USER_CLASS_TYPE: AVPType = 47;
/// Delete all of `cisco_dhcp_user_class` values from a packet.
pub fn delete_cisco_dhcp_user_class(packet: &mut Packet) {
    packet.delete(CISCO_DHCP_USER_CLASS_TYPE);
}
/// Add `cisco_dhcp_user_class` string value to a packet.
pub fn add_cisco_dhcp_user_class(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_DHCP_USER_CLASS_TYPE, value));
}
/// Lookup a `cisco_dhcp_user_class` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_dhcp_user_class`, it returns `None`.
pub fn lookup_cisco_dhcp_user_class(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_DHCP_USER_CLASS_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_dhcp_user_class` string value from a packet.
pub fn lookup_all_cisco_dhcp_user_class(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_DHCP_USER_CLASS_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_DHCP_VENDOR_CLASS_TYPE: AVPType = 48;
/// Delete all of `cisco_dhcp_vendor_class` values from a packet.
pub fn delete_cisco_dhcp_vendor_class(packet: &mut Packet) {
    packet.delete(CISCO_DHCP_VENDOR_CLASS_TYPE);
}
/// Add `cisco_dhcp_vendor_class` string value to a packet.
pub fn add_cisco_dhcp_vendor_class(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_DHCP_VENDOR_CLASS_TYPE, value));
}
/// Lookup a `cisco_dhcp_vendor_class` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_dhcp_vendor_class`, it returns `None`.
pub fn lookup_cisco_dhcp_vendor_class(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_DHCP_VENDOR_CLASS_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_dhcp_vendor_class` string value from a packet.
pub fn lookup_all_cisco_dhcp_vendor_class(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_DHCP_VENDOR_CLASS_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_DHCP_RELAY_GI_ADDR_TYPE: AVPType = 50;
/// Delete all of `cisco_dhcp_relay_gi_addr` values from a packet.
pub fn delete_cisco_dhcp_relay_gi_addr(packet: &mut Packet) {
    packet.delete(CISCO_DHCP_RELAY_GI_ADDR_TYPE);
}
/// Add `cisco_dhcp_relay_gi_addr` string value to a packet.
pub fn add_cisco_dhcp_relay_gi_addr(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_DHCP_RELAY_GI_ADDR_TYPE, value));
}
/// Lookup a `cisco_dhcp_relay_gi_addr` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_dhcp_relay_gi_addr`, it returns `None`.
pub fn lookup_cisco_dhcp_relay_gi_addr(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_DHCP_RELAY_GI_ADDR_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_dhcp_relay_gi_addr` string value from a packet.
pub fn lookup_all_cisco_dhcp_relay_gi_addr(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_DHCP_RELAY_GI_ADDR_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_SERVICE_NAME_TYPE: AVPType = 51;
/// Delete all of `cisco_service_name` values from a packet.
pub fn delete_cisco_service_name(packet: &mut Packet) {
    packet.delete(CISCO_SERVICE_NAME_TYPE);
}
/// Add `cisco_service_name` string value to a packet.
pub fn add_cisco_service_name(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_SERVICE_NAME_TYPE, value));
}
/// Lookup a `cisco_service_name` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_service_name`, it returns `None`.
pub fn lookup_cisco_service_name(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_SERVICE_NAME_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_service_name` string value from a packet.
pub fn lookup_all_cisco_service_name(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_SERVICE_NAME_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_PARENT_SESSION_ID_TYPE: AVPType = 52;
/// Delete all of `cisco_parent_session_id` values from a packet.
pub fn delete_cisco_parent_session_id(packet: &mut Packet) {
    packet.delete(CISCO_PARENT_SESSION_ID_TYPE);
}
/// Add `cisco_parent_session_id` string value to a packet.
pub fn add_cisco_parent_session_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_PARENT_SESSION_ID_TYPE, value));
}
/// Lookup a `cisco_parent_session_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_parent_session_id`, it returns `None`.
pub fn lookup_cisco_parent_session_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_PARENT_SESSION_ID_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_parent_session_id` string value from a packet.
pub fn lookup_all_cisco_parent_session_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_PARENT_SESSION_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_SUB_QO_S_POL_IN_TYPE: AVPType = 55;
/// Delete all of `cisco_sub_qo_s_pol_in` values from a packet.
pub fn delete_cisco_sub_qo_s_pol_in(packet: &mut Packet) {
    packet.delete(CISCO_SUB_QO_S_POL_IN_TYPE);
}
/// Add `cisco_sub_qo_s_pol_in` string value to a packet.
pub fn add_cisco_sub_qo_s_pol_in(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_SUB_QO_S_POL_IN_TYPE, value));
}
/// Lookup a `cisco_sub_qo_s_pol_in` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_sub_qo_s_pol_in`, it returns `None`.
pub fn lookup_cisco_sub_qo_s_pol_in(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_SUB_QO_S_POL_IN_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_sub_qo_s_pol_in` string value from a packet.
pub fn lookup_all_cisco_sub_qo_s_pol_in(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_SUB_QO_S_POL_IN_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_SUB_QO_S_POL_OUT_TYPE: AVPType = 56;
/// Delete all of `cisco_sub_qo_s_pol_out` values from a packet.
pub fn delete_cisco_sub_qo_s_pol_out(packet: &mut Packet) {
    packet.delete(CISCO_SUB_QO_S_POL_OUT_TYPE);
}
/// Add `cisco_sub_qo_s_pol_out` string value to a packet.
pub fn add_cisco_sub_qo_s_pol_out(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_SUB_QO_S_POL_OUT_TYPE, value));
}
/// Lookup a `cisco_sub_qo_s_pol_out` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_sub_qo_s_pol_out`, it returns `None`.
pub fn lookup_cisco_sub_qo_s_pol_out(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_SUB_QO_S_POL_OUT_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_sub_qo_s_pol_out` string value from a packet.
pub fn lookup_all_cisco_sub_qo_s_pol_out(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_SUB_QO_S_POL_OUT_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_IN_ACL_TYPE: AVPType = 57;
/// Delete all of `cisco_in_acl` values from a packet.
pub fn delete_cisco_in_acl(packet: &mut Packet) {
    packet.delete(CISCO_IN_ACL_TYPE);
}
/// Add `cisco_in_acl` string value to a packet.
pub fn add_cisco_in_acl(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_IN_ACL_TYPE, value));
}
/// Lookup a `cisco_in_acl` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_in_acl`, it returns `None`.
pub fn lookup_cisco_in_acl(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(CISCO_IN_ACL_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `cisco_in_acl` string value from a packet.
pub fn lookup_all_cisco_in_acl(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_IN_ACL_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_OUT_ACL_TYPE: AVPType = 58;
/// Delete all of `cisco_out_acl` values from a packet.
pub fn delete_cisco_out_acl(packet: &mut Packet) {
    packet.delete(CISCO_OUT_ACL_TYPE);
}
/// Add `cisco_out_acl` string value to a packet.
pub fn add_cisco_out_acl(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_OUT_ACL_TYPE, value));
}
/// Lookup a `cisco_out_acl` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_out_acl`, it returns `None`.
pub fn lookup_cisco_out_acl(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(CISCO_OUT_ACL_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `cisco_out_acl` string value from a packet.
pub fn lookup_all_cisco_out_acl(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_OUT_ACL_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_SUB_PBR_POLICY_IN_TYPE: AVPType = 59;
/// Delete all of `cisco_sub_pbr_policy_in` values from a packet.
pub fn delete_cisco_sub_pbr_policy_in(packet: &mut Packet) {
    packet.delete(CISCO_SUB_PBR_POLICY_IN_TYPE);
}
/// Add `cisco_sub_pbr_policy_in` string value to a packet.
pub fn add_cisco_sub_pbr_policy_in(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_SUB_PBR_POLICY_IN_TYPE, value));
}
/// Lookup a `cisco_sub_pbr_policy_in` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_sub_pbr_policy_in`, it returns `None`.
pub fn lookup_cisco_sub_pbr_policy_in(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_SUB_PBR_POLICY_IN_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_sub_pbr_policy_in` string value from a packet.
pub fn lookup_all_cisco_sub_pbr_policy_in(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_SUB_PBR_POLICY_IN_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_SUB_ACTIVATE_SERVICE_TYPE: AVPType = 60;
/// Delete all of `cisco_sub_activate_service` values from a packet.
pub fn delete_cisco_sub_activate_service(packet: &mut Packet) {
    packet.delete(CISCO_SUB_ACTIVATE_SERVICE_TYPE);
}
/// Add `cisco_sub_activate_service` string value to a packet.
pub fn add_cisco_sub_activate_service(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_SUB_ACTIVATE_SERVICE_TYPE, value));
}
/// Lookup a `cisco_sub_activate_service` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_sub_activate_service`, it returns `None`.
pub fn lookup_cisco_sub_activate_service(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_SUB_ACTIVATE_SERVICE_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_sub_activate_service` string value from a packet.
pub fn lookup_all_cisco_sub_activate_service(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_SUB_ACTIVATE_SERVICE_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_I_PV_6_IN_ACL_TYPE: AVPType = 61;
/// Delete all of `cisco_i_pv_6_in_acl` values from a packet.
pub fn delete_cisco_i_pv_6_in_acl(packet: &mut Packet) {
    packet.delete(CISCO_I_PV_6_IN_ACL_TYPE);
}
/// Add `cisco_i_pv_6_in_acl` string value to a packet.
pub fn add_cisco_i_pv_6_in_acl(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_I_PV_6_IN_ACL_TYPE, value));
}
/// Lookup a `cisco_i_pv_6_in_acl` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_i_pv_6_in_acl`, it returns `None`.
pub fn lookup_cisco_i_pv_6_in_acl(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_I_PV_6_IN_ACL_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_i_pv_6_in_acl` string value from a packet.
pub fn lookup_all_cisco_i_pv_6_in_acl(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_I_PV_6_IN_ACL_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_I_PV_6_OUT_ACL_TYPE: AVPType = 62;
/// Delete all of `cisco_i_pv_6_out_acl` values from a packet.
pub fn delete_cisco_i_pv_6_out_acl(packet: &mut Packet) {
    packet.delete(CISCO_I_PV_6_OUT_ACL_TYPE);
}
/// Add `cisco_i_pv_6_out_acl` string value to a packet.
pub fn add_cisco_i_pv_6_out_acl(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_I_PV_6_OUT_ACL_TYPE, value));
}
/// Lookup a `cisco_i_pv_6_out_acl` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_i_pv_6_out_acl`, it returns `None`.
pub fn lookup_cisco_i_pv_6_out_acl(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_I_PV_6_OUT_ACL_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_i_pv_6_out_acl` string value from a packet.
pub fn lookup_all_cisco_i_pv_6_out_acl(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_I_PV_6_OUT_ACL_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_SUB_DEACTIVATE_SERVICE_TYPE: AVPType = 63;
/// Delete all of `cisco_sub_deactivate_service` values from a packet.
pub fn delete_cisco_sub_deactivate_service(packet: &mut Packet) {
    packet.delete(CISCO_SUB_DEACTIVATE_SERVICE_TYPE);
}
/// Add `cisco_sub_deactivate_service` string value to a packet.
pub fn add_cisco_sub_deactivate_service(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_SUB_DEACTIVATE_SERVICE_TYPE, value));
}
/// Lookup a `cisco_sub_deactivate_service` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_sub_deactivate_service`, it returns `None`.
pub fn lookup_cisco_sub_deactivate_service(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_SUB_DEACTIVATE_SERVICE_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_sub_deactivate_service` string value from a packet.
pub fn lookup_all_cisco_sub_deactivate_service(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_SUB_DEACTIVATE_SERVICE_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_DHCP_SUBSCRIBER_ID_TYPE: AVPType = 65;
/// Delete all of `cisco_dhcp_subscriber_id` values from a packet.
pub fn delete_cisco_dhcp_subscriber_id(packet: &mut Packet) {
    packet.delete(CISCO_DHCP_SUBSCRIBER_ID_TYPE);
}
/// Add `cisco_dhcp_subscriber_id` string value to a packet.
pub fn add_cisco_dhcp_subscriber_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_DHCP_SUBSCRIBER_ID_TYPE, value));
}
/// Lookup a `cisco_dhcp_subscriber_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_dhcp_subscriber_id`, it returns `None`.
pub fn lookup_cisco_dhcp_subscriber_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_DHCP_SUBSCRIBER_ID_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_dhcp_subscriber_id` string value from a packet.
pub fn lookup_all_cisco_dhcp_subscriber_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_DHCP_SUBSCRIBER_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_DHC_PV_6_LINK_ADDRESS_TYPE: AVPType = 66;
/// Delete all of `cisco_dhc_pv_6_link_address` values from a packet.
pub fn delete_cisco_dhc_pv_6_link_address(packet: &mut Packet) {
    packet.delete(CISCO_DHC_PV_6_LINK_ADDRESS_TYPE);
}
/// Add `cisco_dhc_pv_6_link_address` string value to a packet.
pub fn add_cisco_dhc_pv_6_link_address(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_DHC_PV_6_LINK_ADDRESS_TYPE, value));
}
/// Lookup a `cisco_dhc_pv_6_link_address` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_dhc_pv_6_link_address`, it returns `None`.
pub fn lookup_cisco_dhc_pv_6_link_address(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_DHC_PV_6_LINK_ADDRESS_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_dhc_pv_6_link_address` string value from a packet.
pub fn lookup_all_cisco_dhc_pv_6_link_address(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_DHC_PV_6_LINK_ADDRESS_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_DHC_PV_4_OPTION_TO_AAA_TYPE: AVPType = 90;
/// Delete all of `cisco_dhc_pv_4_option_to_aaa` values from a packet.
pub fn delete_cisco_dhc_pv_4_option_to_aaa(packet: &mut Packet) {
    packet.delete(CISCO_DHC_PV_4_OPTION_TO_AAA_TYPE);
}
/// Add `cisco_dhc_pv_4_option_to_aaa` string value to a packet.
pub fn add_cisco_dhc_pv_4_option_to_aaa(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_DHC_PV_4_OPTION_TO_AAA_TYPE, value));
}
/// Lookup a `cisco_dhc_pv_4_option_to_aaa` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_dhc_pv_4_option_to_aaa`, it returns `None`.
pub fn lookup_cisco_dhc_pv_4_option_to_aaa(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_DHC_PV_4_OPTION_TO_AAA_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_dhc_pv_4_option_to_aaa` string value from a packet.
pub fn lookup_all_cisco_dhc_pv_4_option_to_aaa(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_DHC_PV_4_OPTION_TO_AAA_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const SIP_CONF_ID_TYPE: AVPType = 100;
/// Delete all of `sip_conf_id` values from a packet.
pub fn delete_sip_conf_id(packet: &mut Packet) {
    packet.delete(SIP_CONF_ID_TYPE);
}
/// Add `sip_conf_id` string value to a packet.
pub fn add_sip_conf_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(SIP_CONF_ID_TYPE, value));
}
/// Lookup a `sip_conf_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `sip_conf_id`, it returns `None`.
pub fn lookup_sip_conf_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(SIP_CONF_ID_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `sip_conf_id` string value from a packet.
pub fn lookup_all_sip_conf_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(SIP_CONF_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_CREDIT_AMOUNT_TYPE: AVPType = 101;
/// Delete all of `h_323_credit_amount` values from a packet.
pub fn delete_h_323_credit_amount(packet: &mut Packet) {
    packet.delete(H_323_CREDIT_AMOUNT_TYPE);
}
/// Add `h_323_credit_amount` string value to a packet.
pub fn add_h_323_credit_amount(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_CREDIT_AMOUNT_TYPE, value));
}
/// Lookup a `h_323_credit_amount` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_credit_amount`, it returns `None`.
pub fn lookup_h_323_credit_amount(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_CREDIT_AMOUNT_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_credit_amount` string value from a packet.
pub fn lookup_all_h_323_credit_amount(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_CREDIT_AMOUNT_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_CREDIT_TIME_TYPE: AVPType = 102;
/// Delete all of `h_323_credit_time` values from a packet.
pub fn delete_h_323_credit_time(packet: &mut Packet) {
    packet.delete(H_323_CREDIT_TIME_TYPE);
}
/// Add `h_323_credit_time` string value to a packet.
pub fn add_h_323_credit_time(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_CREDIT_TIME_TYPE, value));
}
/// Lookup a `h_323_credit_time` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_credit_time`, it returns `None`.
pub fn lookup_h_323_credit_time(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_CREDIT_TIME_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_credit_time` string value from a packet.
pub fn lookup_all_h_323_credit_time(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_CREDIT_TIME_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_RETURN_CODE_TYPE: AVPType = 103;
/// Delete all of `h_323_return_code` values from a packet.
pub fn delete_h_323_return_code(packet: &mut Packet) {
    packet.delete(H_323_RETURN_CODE_TYPE);
}
/// Add `h_323_return_code` string value to a packet.
pub fn add_h_323_return_code(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_RETURN_CODE_TYPE, value));
}
/// Lookup a `h_323_return_code` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_return_code`, it returns `None`.
pub fn lookup_h_323_return_code(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_RETURN_CODE_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_return_code` string value from a packet.
pub fn lookup_all_h_323_return_code(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_RETURN_CODE_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_PROMPT_ID_TYPE: AVPType = 104;
/// Delete all of `h_323_prompt_id` values from a packet.
pub fn delete_h_323_prompt_id(packet: &mut Packet) {
    packet.delete(H_323_PROMPT_ID_TYPE);
}
/// Add `h_323_prompt_id` string value to a packet.
pub fn add_h_323_prompt_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_PROMPT_ID_TYPE, value));
}
/// Lookup a `h_323_prompt_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_prompt_id`, it returns `None`.
pub fn lookup_h_323_prompt_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_PROMPT_ID_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_prompt_id` string value from a packet.
pub fn lookup_all_h_323_prompt_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_PROMPT_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_TIME_AND_DAY_TYPE: AVPType = 105;
/// Delete all of `h_323_time_and_day` values from a packet.
pub fn delete_h_323_time_and_day(packet: &mut Packet) {
    packet.delete(H_323_TIME_AND_DAY_TYPE);
}
/// Add `h_323_time_and_day` string value to a packet.
pub fn add_h_323_time_and_day(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_TIME_AND_DAY_TYPE, value));
}
/// Lookup a `h_323_time_and_day` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_time_and_day`, it returns `None`.
pub fn lookup_h_323_time_and_day(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_TIME_AND_DAY_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_time_and_day` string value from a packet.
pub fn lookup_all_h_323_time_and_day(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_TIME_AND_DAY_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_REDIRECT_NUMBER_TYPE: AVPType = 106;
/// Delete all of `h_323_redirect_number` values from a packet.
pub fn delete_h_323_redirect_number(packet: &mut Packet) {
    packet.delete(H_323_REDIRECT_NUMBER_TYPE);
}
/// Add `h_323_redirect_number` string value to a packet.
pub fn add_h_323_redirect_number(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_REDIRECT_NUMBER_TYPE, value));
}
/// Lookup a `h_323_redirect_number` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_redirect_number`, it returns `None`.
pub fn lookup_h_323_redirect_number(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_REDIRECT_NUMBER_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_redirect_number` string value from a packet.
pub fn lookup_all_h_323_redirect_number(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_REDIRECT_NUMBER_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_PREFERRED_LANG_TYPE: AVPType = 107;
/// Delete all of `h_323_preferred_lang` values from a packet.
pub fn delete_h_323_preferred_lang(packet: &mut Packet) {
    packet.delete(H_323_PREFERRED_LANG_TYPE);
}
/// Add `h_323_preferred_lang` string value to a packet.
pub fn add_h_323_preferred_lang(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_PREFERRED_LANG_TYPE, value));
}
/// Lookup a `h_323_preferred_lang` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_preferred_lang`, it returns `None`.
pub fn lookup_h_323_preferred_lang(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_PREFERRED_LANG_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_preferred_lang` string value from a packet.
pub fn lookup_all_h_323_preferred_lang(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_PREFERRED_LANG_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_REDIRECT_IP_ADDRESS_TYPE: AVPType = 108;
/// Delete all of `h_323_redirect_ip_address` values from a packet.
pub fn delete_h_323_redirect_ip_address(packet: &mut Packet) {
    packet.delete(H_323_REDIRECT_IP_ADDRESS_TYPE);
}
/// Add `h_323_redirect_ip_address` string value to a packet.
pub fn add_h_323_redirect_ip_address(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_REDIRECT_IP_ADDRESS_TYPE, value));
}
/// Lookup a `h_323_redirect_ip_address` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_redirect_ip_address`, it returns `None`.
pub fn lookup_h_323_redirect_ip_address(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_REDIRECT_IP_ADDRESS_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_redirect_ip_address` string value from a packet.
pub fn lookup_all_h_323_redirect_ip_address(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_REDIRECT_IP_ADDRESS_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_BILLING_MODEL_TYPE: AVPType = 109;
/// Delete all of `h_323_billing_model` values from a packet.
pub fn delete_h_323_billing_model(packet: &mut Packet) {
    packet.delete(H_323_BILLING_MODEL_TYPE);
}
/// Add `h_323_billing_model` string value to a packet.
pub fn add_h_323_billing_model(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_BILLING_MODEL_TYPE, value));
}
/// Lookup a `h_323_billing_model` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_billing_model`, it returns `None`.
pub fn lookup_h_323_billing_model(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_BILLING_MODEL_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_billing_model` string value from a packet.
pub fn lookup_all_h_323_billing_model(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_BILLING_MODEL_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const H_323_CURRENCY_TYPE: AVPType = 110;
/// Delete all of `h_323_currency` values from a packet.
pub fn delete_h_323_currency(packet: &mut Packet) {
    packet.delete(H_323_CURRENCY_TYPE);
}
/// Add `h_323_currency` string value to a packet.
pub fn add_h_323_currency(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(H_323_CURRENCY_TYPE, value));
}
/// Lookup a `h_323_currency` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `h_323_currency`, it returns `None`.
pub fn lookup_h_323_currency(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(H_323_CURRENCY_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `h_323_currency` string value from a packet.
pub fn lookup_all_h_323_currency(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(H_323_CURRENCY_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const SUBSCRIBER_TYPE: AVPType = 111;
/// Delete all of `subscriber` values from a packet.
pub fn delete_subscriber(packet: &mut Packet) {
    packet.delete(SUBSCRIBER_TYPE);
}
/// Add `subscriber` string value to a packet.
pub fn add_subscriber(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(SUBSCRIBER_TYPE, value));
}
/// Lookup a `subscriber` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `subscriber`, it returns `None`.
pub fn lookup_subscriber(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(SUBSCRIBER_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `subscriber` string value from a packet.
pub fn lookup_all_subscriber(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(SUBSCRIBER_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const GW_RXD_CDN_TYPE: AVPType = 112;
/// Delete all of `gw_rxd_cdn` values from a packet.
pub fn delete_gw_rxd_cdn(packet: &mut Packet) {
    packet.delete(GW_RXD_CDN_TYPE);
}
/// Add `gw_rxd_cdn` string value to a packet.
pub fn add_gw_rxd_cdn(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(GW_RXD_CDN_TYPE, value));
}
/// Lookup a `gw_rxd_cdn` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `gw_rxd_cdn`, it returns `None`.
pub fn lookup_gw_rxd_cdn(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(GW_RXD_CDN_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `gw_rxd_cdn` string value from a packet.
pub fn lookup_all_gw_rxd_cdn(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(GW_RXD_CDN_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const GW_FINAL_XLATED_CDN_TYPE: AVPType = 113;
/// Delete all of `gw_final_xlated_cdn` values from a packet.
pub fn delete_gw_final_xlated_cdn(packet: &mut Packet) {
    packet.delete(GW_FINAL_XLATED_CDN_TYPE);
}
/// Add `gw_final_xlated_cdn` string value to a packet.
pub fn add_gw_final_xlated_cdn(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(GW_FINAL_XLATED_CDN_TYPE, value));
}
/// Lookup a `gw_final_xlated_cdn` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `gw_final_xlated_cdn`, it returns `None`.
pub fn lookup_gw_final_xlated_cdn(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(GW_FINAL_XLATED_CDN_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `gw_final_xlated_cdn` string value from a packet.
pub fn lookup_all_gw_final_xlated_cdn(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(GW_FINAL_XLATED_CDN_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const REMOTE_MEDIA_ADDRESS_TYPE: AVPType = 114;
/// Delete all of `remote_media_address` values from a packet.
pub fn delete_remote_media_address(packet: &mut Packet) {
    packet.delete(REMOTE_MEDIA_ADDRESS_TYPE);
}
/// Add `remote_media_address` string value to a packet.
pub fn add_remote_media_address(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(REMOTE_MEDIA_ADDRESS_TYPE, value));
}
/// Lookup a `remote_media_address` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `remote_media_address`, it returns `None`.
pub fn lookup_remote_media_address(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(REMOTE_MEDIA_ADDRESS_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `remote_media_address` string value from a packet.
pub fn lookup_all_remote_media_address(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(REMOTE_MEDIA_ADDRESS_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const RELEASE_SOURCE_TYPE: AVPType = 115;
/// Delete all of `release_source` values from a packet.
pub fn delete_release_source(packet: &mut Packet) {
    packet.delete(RELEASE_SOURCE_TYPE);
}
/// Add `release_source` string value to a packet.
pub fn add_release_source(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(RELEASE_SOURCE_TYPE, value));
}
/// Lookup a `release_source` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `release_source`, it returns `None`.
pub fn lookup_release_source(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(RELEASE_SOURCE_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `release_source` string value from a packet.
pub fn lookup_all_release_source(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(RELEASE_SOURCE_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const GW_RXD_CGN_TYPE: AVPType = 116;
/// Delete all of `gw_rxd_cgn` values from a packet.
pub fn delete_gw_rxd_cgn(packet: &mut Packet) {
    packet.delete(GW_RXD_CGN_TYPE);
}
/// Add `gw_rxd_cgn` string value to a packet.
pub fn add_gw_rxd_cgn(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(GW_RXD_CGN_TYPE, value));
}
/// Lookup a `gw_rxd_cgn` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `gw_rxd_cgn`, it returns `None`.
pub fn lookup_gw_rxd_cgn(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(GW_RXD_CGN_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `gw_rxd_cgn` string value from a packet.
pub fn lookup_all_gw_rxd_cgn(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(GW_RXD_CGN_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const GW_FINAL_XLATED_CGN_TYPE: AVPType = 117;
/// Delete all of `gw_final_xlated_cgn` values from a packet.
pub fn delete_gw_final_xlated_cgn(packet: &mut Packet) {
    packet.delete(GW_FINAL_XLATED_CGN_TYPE);
}
/// Add `gw_final_xlated_cgn` string value to a packet.
pub fn add_gw_final_xlated_cgn(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(GW_FINAL_XLATED_CGN_TYPE, value));
}
/// Lookup a `gw_final_xlated_cgn` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `gw_final_xlated_cgn`, it returns `None`.
pub fn lookup_gw_final_xlated_cgn(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(GW_FINAL_XLATED_CGN_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `gw_final_xlated_cgn` string value from a packet.
pub fn lookup_all_gw_final_xlated_cgn(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(GW_FINAL_XLATED_CGN_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CALL_ID_TYPE: AVPType = 141;
/// Delete all of `call_id` values from a packet.
pub fn delete_call_id(packet: &mut Packet) {
    packet.delete(CALL_ID_TYPE);
}
/// Add `call_id` string value to a packet.
pub fn add_call_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CALL_ID_TYPE, value));
}
/// Lookup a `call_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `call_id`, it returns `None`.
pub fn lookup_call_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(CALL_ID_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `call_id` string value from a packet.
pub fn lookup_all_call_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CALL_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const SESSION_PROTOCOL_TYPE: AVPType = 142;
/// Delete all of `session_protocol` values from a packet.
pub fn delete_session_protocol(packet: &mut Packet) {
    packet.delete(SESSION_PROTOCOL_TYPE);
}
/// Add `session_protocol` string value to a packet.
pub fn add_session_protocol(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(SESSION_PROTOCOL_TYPE, value));
}
/// Lookup a `session_protocol` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `session_protocol`, it returns `None`.
pub fn lookup_session_protocol(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(SESSION_PROTOCOL_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `session_protocol` string value from a packet.
pub fn lookup_all_session_protocol(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(SESSION_PROTOCOL_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const METHOD_TYPE: AVPType = 143;
/// Delete all of `method` values from a packet.
pub fn delete_method(packet: &mut Packet) {
    packet.delete(METHOD_TYPE);
}
/// Add `method` string value to a packet.
pub fn add_method(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(METHOD_TYPE, value));
}
/// Lookup a `method` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `method`, it returns `None`.
pub fn lookup_method(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(METHOD_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `method` string value from a packet.
pub fn lookup_all_method(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(METHOD_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const PREV_HOP_VIA_TYPE: AVPType = 144;
/// Delete all of `prev_hop_via` values from a packet.
pub fn delete_prev_hop_via(packet: &mut Packet) {
    packet.delete(PREV_HOP_VIA_TYPE);
}
/// Add `prev_hop_via` string value to a packet.
pub fn add_prev_hop_via(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(PREV_HOP_VIA_TYPE, value));
}
/// Lookup a `prev_hop_via` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `prev_hop_via`, it returns `None`.
pub fn lookup_prev_hop_via(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(PREV_HOP_VIA_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `prev_hop_via` string value from a packet.
pub fn lookup_all_prev_hop_via(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(PREV_HOP_VIA_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const PREV_HOP_IP_TYPE: AVPType = 145;
/// Delete all of `prev_hop_ip` values from a packet.
pub fn delete_prev_hop_ip(packet: &mut Packet) {
    packet.delete(PREV_HOP_IP_TYPE);
}
/// Add `prev_hop_ip` string value to a packet.
pub fn add_prev_hop_ip(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(PREV_HOP_IP_TYPE, value));
}
/// Lookup a `prev_hop_ip` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `prev_hop_ip`, it returns `None`.
pub fn lookup_prev_hop_ip(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(PREV_HOP_IP_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `prev_hop_ip` string value from a packet.
pub fn lookup_all_prev_hop_ip(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(PREV_HOP_IP_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const INCOMING_REQ_URI_TYPE: AVPType = 146;
/// Delete all of `incoming_req_uri` values from a packet.
pub fn delete_incoming_req_uri(packet: &mut Packet) {
    packet.delete(INCOMING_REQ_URI_TYPE);
}
/// Add `incoming_req_uri` string value to a packet.
pub fn add_incoming_req_uri(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(INCOMING_REQ_URI_TYPE, value));
}
/// Lookup a `incoming_req_uri` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `incoming_req_uri`, it returns `None`.
pub fn lookup_incoming_req_uri(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(INCOMING_REQ_URI_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `incoming_req_uri` string value from a packet.
pub fn lookup_all_incoming_req_uri(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(INCOMING_REQ_URI_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const OUTGOING_REQ_URI_TYPE: AVPType = 147;
/// Delete all of `outgoing_req_uri` values from a packet.
pub fn delete_outgoing_req_uri(packet: &mut Packet) {
    packet.delete(OUTGOING_REQ_URI_TYPE);
}
/// Add `outgoing_req_uri` string value to a packet.
pub fn add_outgoing_req_uri(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(OUTGOING_REQ_URI_TYPE, value));
}
/// Lookup a `outgoing_req_uri` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `outgoing_req_uri`, it returns `None`.
pub fn lookup_outgoing_req_uri(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(OUTGOING_REQ_URI_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `outgoing_req_uri` string value from a packet.
pub fn lookup_all_outgoing_req_uri(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(OUTGOING_REQ_URI_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const NEXT_HOP_IP_TYPE: AVPType = 148;
/// Delete all of `next_hop_ip` values from a packet.
pub fn delete_next_hop_ip(packet: &mut Packet) {
    packet.delete(NEXT_HOP_IP_TYPE);
}
/// Add `next_hop_ip` string value to a packet.
pub fn add_next_hop_ip(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(NEXT_HOP_IP_TYPE, value));
}
/// Lookup a `next_hop_ip` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `next_hop_ip`, it returns `None`.
pub fn lookup_next_hop_ip(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(NEXT_HOP_IP_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `next_hop_ip` string value from a packet.
pub fn lookup_all_next_hop_ip(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(NEXT_HOP_IP_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const NEXT_HOP_DN_TYPE: AVPType = 149;
/// Delete all of `next_hop_dn` values from a packet.
pub fn delete_next_hop_dn(packet: &mut Packet) {
    packet.delete(NEXT_HOP_DN_TYPE);
}
/// Add `next_hop_dn` string value to a packet.
pub fn add_next_hop_dn(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(NEXT_HOP_DN_TYPE, value));
}
/// Lookup a `next_hop_dn` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `next_hop_dn`, it returns `None`.
pub fn lookup_next_hop_dn(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(NEXT_HOP_DN_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `next_hop_dn` string value from a packet.
pub fn lookup_all_next_hop_dn(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(NEXT_HOP_DN_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const SIP_HDR_TYPE: AVPType = 150;
/// Delete all of `sip_hdr` values from a packet.
pub fn delete_sip_hdr(packet: &mut Packet) {
    packet.delete(SIP_HDR_TYPE);
}
/// Add `sip_hdr` string value to a packet.
pub fn add_sip_hdr(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(SIP_HDR_TYPE, value));
}
/// Lookup a `sip_hdr` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `sip_hdr`, it returns `None`.
pub fn lookup_sip_hdr(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(SIP_HDR_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `sip_hdr` string value from a packet.
pub fn lookup_all_sip_hdr(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(SIP_HDR_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const DSP_ID_TYPE: AVPType = 151;
/// Delete all of `dsp_id` values from a packet.
pub fn delete_dsp_id(packet: &mut Packet) {
    packet.delete(DSP_ID_TYPE);
}
/// Add `dsp_id` string value to a packet.
pub fn add_dsp_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(DSP_ID_TYPE, value));
}
/// Lookup a `dsp_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `dsp_id`, it returns `None`.
pub fn lookup_dsp_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(DSP_ID_TYPE).map(|v| v.encode_string())
}
/// Lookup all of the `dsp_id` string value from a packet.
pub fn lookup_all_dsp_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(DSP_ID_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_MULTILINK_ID_TYPE: AVPType = 187;
/// Delete all of `cisco_multilink_id` values from a packet.
pub fn delete_cisco_multilink_id(packet: &mut Packet) {
    packet.delete(CISCO_MULTILINK_ID_TYPE);
}
/// Add `cisco_multilink_id` integer value to a packet.
pub fn add_cisco_multilink_id(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_MULTILINK_ID_TYPE, value));
}
/// Lookup a `cisco_multilink_id` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_multilink_id`, it returns `None`.
pub fn lookup_cisco_multilink_id(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_MULTILINK_ID_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_multilink_id` integer value from a packet.
pub fn lookup_all_cisco_multilink_id(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_MULTILINK_ID_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_NUM_IN_MULTILINK_TYPE: AVPType = 188;
/// Delete all of `cisco_num_in_multilink` values from a packet.
pub fn delete_cisco_num_in_multilink(packet: &mut Packet) {
    packet.delete(CISCO_NUM_IN_MULTILINK_TYPE);
}
/// Add `cisco_num_in_multilink` integer value to a packet.
pub fn add_cisco_num_in_multilink(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_NUM_IN_MULTILINK_TYPE, value));
}
/// Lookup a `cisco_num_in_multilink` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_num_in_multilink`, it returns `None`.
pub fn lookup_cisco_num_in_multilink(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_NUM_IN_MULTILINK_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_num_in_multilink` integer value from a packet.
pub fn lookup_all_cisco_num_in_multilink(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_NUM_IN_MULTILINK_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_PRE_INPUT_OCTETS_TYPE: AVPType = 190;
/// Delete all of `cisco_pre_input_octets` values from a packet.
pub fn delete_cisco_pre_input_octets(packet: &mut Packet) {
    packet.delete(CISCO_PRE_INPUT_OCTETS_TYPE);
}
/// Add `cisco_pre_input_octets` integer value to a packet.
pub fn add_cisco_pre_input_octets(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_PRE_INPUT_OCTETS_TYPE, value));
}
/// Lookup a `cisco_pre_input_octets` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_pre_input_octets`, it returns `None`.
pub fn lookup_cisco_pre_input_octets(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_PRE_INPUT_OCTETS_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_pre_input_octets` integer value from a packet.
pub fn lookup_all_cisco_pre_input_octets(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_PRE_INPUT_OCTETS_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_PRE_OUTPUT_OCTETS_TYPE: AVPType = 191;
/// Delete all of `cisco_pre_output_octets` values from a packet.
pub fn delete_cisco_pre_output_octets(packet: &mut Packet) {
    packet.delete(CISCO_PRE_OUTPUT_OCTETS_TYPE);
}
/// Add `cisco_pre_output_octets` integer value to a packet.
pub fn add_cisco_pre_output_octets(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_PRE_OUTPUT_OCTETS_TYPE, value));
}
/// Lookup a `cisco_pre_output_octets` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_pre_output_octets`, it returns `None`.
pub fn lookup_cisco_pre_output_octets(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_PRE_OUTPUT_OCTETS_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_pre_output_octets` integer value from a packet.
pub fn lookup_all_cisco_pre_output_octets(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_PRE_OUTPUT_OCTETS_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_PRE_INPUT_PACKETS_TYPE: AVPType = 192;
/// Delete all of `cisco_pre_input_packets` values from a packet.
pub fn delete_cisco_pre_input_packets(packet: &mut Packet) {
    packet.delete(CISCO_PRE_INPUT_PACKETS_TYPE);
}
/// Add `cisco_pre_input_packets` integer value to a packet.
pub fn add_cisco_pre_input_packets(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_PRE_INPUT_PACKETS_TYPE, value));
}
/// Lookup a `cisco_pre_input_packets` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_pre_input_packets`, it returns `None`.
pub fn lookup_cisco_pre_input_packets(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_PRE_INPUT_PACKETS_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_pre_input_packets` integer value from a packet.
pub fn lookup_all_cisco_pre_input_packets(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_PRE_INPUT_PACKETS_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_PRE_OUTPUT_PACKETS_TYPE: AVPType = 193;
/// Delete all of `cisco_pre_output_packets` values from a packet.
pub fn delete_cisco_pre_output_packets(packet: &mut Packet) {
    packet.delete(CISCO_PRE_OUTPUT_PACKETS_TYPE);
}
/// Add `cisco_pre_output_packets` integer value to a packet.
pub fn add_cisco_pre_output_packets(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_PRE_OUTPUT_PACKETS_TYPE, value));
}
/// Lookup a `cisco_pre_output_packets` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_pre_output_packets`, it returns `None`.
pub fn lookup_cisco_pre_output_packets(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_PRE_OUTPUT_PACKETS_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_pre_output_packets` integer value from a packet.
pub fn lookup_all_cisco_pre_output_packets(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_PRE_OUTPUT_PACKETS_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_MAXIMUM_TIME_TYPE: AVPType = 194;
/// Delete all of `cisco_maximum_time` values from a packet.
pub fn delete_cisco_maximum_time(packet: &mut Packet) {
    packet.delete(CISCO_MAXIMUM_TIME_TYPE);
}
/// Add `cisco_maximum_time` integer value to a packet.
pub fn add_cisco_maximum_time(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_MAXIMUM_TIME_TYPE, value));
}
/// Lookup a `cisco_maximum_time` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_maximum_time`, it returns `None`.
pub fn lookup_cisco_maximum_time(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_MAXIMUM_TIME_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_maximum_time` integer value from a packet.
pub fn lookup_all_cisco_maximum_time(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_MAXIMUM_TIME_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_DISCONNECT_CAUSE_TYPE: AVPType = 195;
/// Delete all of `cisco_disconnect_cause` values from a packet.
pub fn delete_cisco_disconnect_cause(packet: &mut Packet) {
    packet.delete(CISCO_DISCONNECT_CAUSE_TYPE);
}
/// Add `cisco_disconnect_cause` value-defined integer value to a packet.
pub fn add_cisco_disconnect_cause(packet: &mut Packet, value: CiscoDisconnectCause) {
    packet.add(AVP::from_u32(CISCO_DISCONNECT_CAUSE_TYPE, value));
}
/// Lookup a `cisco_disconnect_cause` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_disconnect_cause`, it returns `None`.
pub fn lookup_cisco_disconnect_cause(
    packet: &Packet,
) -> Option<Result<CiscoDisconnectCause, AVPError>> {
    packet
        .lookup(CISCO_DISCONNECT_CAUSE_TYPE)
        .map(|v| Ok(v.encode_u32()? as CiscoDisconnectCause))
}
/// Lookup all of the `cisco_disconnect_cause` value-defined integer value from a packet.
pub fn lookup_all_cisco_disconnect_cause(
    packet: &Packet,
) -> Result<Vec<CiscoDisconnectCause>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_DISCONNECT_CAUSE_TYPE) {
        vec.push(avp.encode_u32()? as CiscoDisconnectCause)
    }
    Ok(vec)
}

pub const CISCO_DATA_RATE_TYPE: AVPType = 197;
/// Delete all of `cisco_data_rate` values from a packet.
pub fn delete_cisco_data_rate(packet: &mut Packet) {
    packet.delete(CISCO_DATA_RATE_TYPE);
}
/// Add `cisco_data_rate` integer value to a packet.
pub fn add_cisco_data_rate(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_DATA_RATE_TYPE, value));
}
/// Lookup a `cisco_data_rate` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_data_rate`, it returns `None`.
pub fn lookup_cisco_data_rate(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(CISCO_DATA_RATE_TYPE).map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_data_rate` integer value from a packet.
pub fn lookup_all_cisco_data_rate(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_DATA_RATE_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_PRE_SESSION_TIME_TYPE: AVPType = 198;
/// Delete all of `cisco_pre_session_time` values from a packet.
pub fn delete_cisco_pre_session_time(packet: &mut Packet) {
    packet.delete(CISCO_PRE_SESSION_TIME_TYPE);
}
/// Add `cisco_pre_session_time` integer value to a packet.
pub fn add_cisco_pre_session_time(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_PRE_SESSION_TIME_TYPE, value));
}
/// Lookup a `cisco_pre_session_time` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_pre_session_time`, it returns `None`.
pub fn lookup_cisco_pre_session_time(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_PRE_SESSION_TIME_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_pre_session_time` integer value from a packet.
pub fn lookup_all_cisco_pre_session_time(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_PRE_SESSION_TIME_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_PW_LIFETIME_TYPE: AVPType = 208;
/// Delete all of `cisco_pw_lifetime` values from a packet.
pub fn delete_cisco_pw_lifetime(packet: &mut Packet) {
    packet.delete(CISCO_PW_LIFETIME_TYPE);
}
/// Add `cisco_pw_lifetime` integer value to a packet.
pub fn add_cisco_pw_lifetime(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_PW_LIFETIME_TYPE, value));
}
/// Lookup a `cisco_pw_lifetime` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_pw_lifetime`, it returns `None`.
pub fn lookup_cisco_pw_lifetime(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_PW_LIFETIME_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_pw_lifetime` integer value from a packet.
pub fn lookup_all_cisco_pw_lifetime(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_PW_LIFETIME_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_IP_DIRECT_TYPE: AVPType = 209;
/// Delete all of `cisco_ip_direct` values from a packet.
pub fn delete_cisco_ip_direct(packet: &mut Packet) {
    packet.delete(CISCO_IP_DIRECT_TYPE);
}
/// Add `cisco_ip_direct` integer value to a packet.
pub fn add_cisco_ip_direct(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_IP_DIRECT_TYPE, value));
}
/// Lookup a `cisco_ip_direct` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_ip_direct`, it returns `None`.
pub fn lookup_cisco_ip_direct(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(CISCO_IP_DIRECT_TYPE).map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_ip_direct` integer value from a packet.
pub fn lookup_all_cisco_ip_direct(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_IP_DIRECT_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_PPP_VJ_SLOT_COMP_TYPE: AVPType = 210;
/// Delete all of `cisco_ppp_vj_slot_comp` values from a packet.
pub fn delete_cisco_ppp_vj_slot_comp(packet: &mut Packet) {
    packet.delete(CISCO_PPP_VJ_SLOT_COMP_TYPE);
}
/// Add `cisco_ppp_vj_slot_comp` integer value to a packet.
pub fn add_cisco_ppp_vj_slot_comp(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_PPP_VJ_SLOT_COMP_TYPE, value));
}
/// Lookup a `cisco_ppp_vj_slot_comp` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_ppp_vj_slot_comp`, it returns `None`.
pub fn lookup_cisco_ppp_vj_slot_comp(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_PPP_VJ_SLOT_COMP_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_ppp_vj_slot_comp` integer value from a packet.
pub fn lookup_all_cisco_ppp_vj_slot_comp(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_PPP_VJ_SLOT_COMP_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_PPP_ASYNC_MAP_TYPE: AVPType = 212;
/// Delete all of `cisco_ppp_async_map` values from a packet.
pub fn delete_cisco_ppp_async_map(packet: &mut Packet) {
    packet.delete(CISCO_PPP_ASYNC_MAP_TYPE);
}
/// Add `cisco_ppp_async_map` integer value to a packet.
pub fn add_cisco_ppp_async_map(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_PPP_ASYNC_MAP_TYPE, value));
}
/// Lookup a `cisco_ppp_async_map` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_ppp_async_map`, it returns `None`.
pub fn lookup_cisco_ppp_async_map(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_PPP_ASYNC_MAP_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_ppp_async_map` integer value from a packet.
pub fn lookup_all_cisco_ppp_async_map(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_PPP_ASYNC_MAP_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_IP_POOL_DEFINITION_TYPE: AVPType = 217;
/// Delete all of `cisco_ip_pool_definition` values from a packet.
pub fn delete_cisco_ip_pool_definition(packet: &mut Packet) {
    packet.delete(CISCO_IP_POOL_DEFINITION_TYPE);
}
/// Add `cisco_ip_pool_definition` string value to a packet.
pub fn add_cisco_ip_pool_definition(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_IP_POOL_DEFINITION_TYPE, value));
}
/// Lookup a `cisco_ip_pool_definition` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_ip_pool_definition`, it returns `None`.
pub fn lookup_cisco_ip_pool_definition(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_IP_POOL_DEFINITION_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_ip_pool_definition` string value from a packet.
pub fn lookup_all_cisco_ip_pool_definition(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_IP_POOL_DEFINITION_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_ASSIGN_IP_POOL_TYPE: AVPType = 218;
/// Delete all of `cisco_assign_ip_pool` values from a packet.
pub fn delete_cisco_assign_ip_pool(packet: &mut Packet) {
    packet.delete(CISCO_ASSIGN_IP_POOL_TYPE);
}
/// Add `cisco_assign_ip_pool` integer value to a packet.
pub fn add_cisco_assign_ip_pool(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_ASSIGN_IP_POOL_TYPE, value));
}
/// Lookup a `cisco_assign_ip_pool` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_assign_ip_pool`, it returns `None`.
pub fn lookup_cisco_assign_ip_pool(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_ASSIGN_IP_POOL_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_assign_ip_pool` integer value from a packet.
pub fn lookup_all_cisco_assign_ip_pool(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_ASSIGN_IP_POOL_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_ROUTE_IP_TYPE: AVPType = 228;
/// Delete all of `cisco_route_ip` values from a packet.
pub fn delete_cisco_route_ip(packet: &mut Packet) {
    packet.delete(CISCO_ROUTE_IP_TYPE);
}
/// Add `cisco_route_ip` integer value to a packet.
pub fn add_cisco_route_ip(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_ROUTE_IP_TYPE, value));
}
/// Lookup a `cisco_route_ip` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_route_ip`, it returns `None`.
pub fn lookup_cisco_route_ip(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(CISCO_ROUTE_IP_TYPE).map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_route_ip` integer value from a packet.
pub fn lookup_all_cisco_route_ip(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_ROUTE_IP_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_LINK_COMPRESSION_TYPE: AVPType = 233;
/// Delete all of `cisco_link_compression` values from a packet.
pub fn delete_cisco_link_compression(packet: &mut Packet) {
    packet.delete(CISCO_LINK_COMPRESSION_TYPE);
}
/// Add `cisco_link_compression` integer value to a packet.
pub fn add_cisco_link_compression(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_LINK_COMPRESSION_TYPE, value));
}
/// Lookup a `cisco_link_compression` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_link_compression`, it returns `None`.
pub fn lookup_cisco_link_compression(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_LINK_COMPRESSION_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_link_compression` integer value from a packet.
pub fn lookup_all_cisco_link_compression(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_LINK_COMPRESSION_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_TARGET_UTIL_TYPE: AVPType = 234;
/// Delete all of `cisco_target_util` values from a packet.
pub fn delete_cisco_target_util(packet: &mut Packet) {
    packet.delete(CISCO_TARGET_UTIL_TYPE);
}
/// Add `cisco_target_util` integer value to a packet.
pub fn add_cisco_target_util(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_TARGET_UTIL_TYPE, value));
}
/// Lookup a `cisco_target_util` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_target_util`, it returns `None`.
pub fn lookup_cisco_target_util(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_TARGET_UTIL_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_target_util` integer value from a packet.
pub fn lookup_all_cisco_target_util(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_TARGET_UTIL_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_MAXIMUM_CHANNELS_TYPE: AVPType = 235;
/// Delete all of `cisco_maximum_channels` values from a packet.
pub fn delete_cisco_maximum_channels(packet: &mut Packet) {
    packet.delete(CISCO_MAXIMUM_CHANNELS_TYPE);
}
/// Add `cisco_maximum_channels` integer value to a packet.
pub fn add_cisco_maximum_channels(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_MAXIMUM_CHANNELS_TYPE, value));
}
/// Lookup a `cisco_maximum_channels` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_maximum_channels`, it returns `None`.
pub fn lookup_cisco_maximum_channels(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_MAXIMUM_CHANNELS_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_maximum_channels` integer value from a packet.
pub fn lookup_all_cisco_maximum_channels(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_MAXIMUM_CHANNELS_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_DATA_FILTER_TYPE: AVPType = 242;
/// Delete all of `cisco_data_filter` values from a packet.
pub fn delete_cisco_data_filter(packet: &mut Packet) {
    packet.delete(CISCO_DATA_FILTER_TYPE);
}
/// Add `cisco_data_filter` integer value to a packet.
pub fn add_cisco_data_filter(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_DATA_FILTER_TYPE, value));
}
/// Lookup a `cisco_data_filter` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_data_filter`, it returns `None`.
pub fn lookup_cisco_data_filter(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_DATA_FILTER_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_data_filter` integer value from a packet.
pub fn lookup_all_cisco_data_filter(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_DATA_FILTER_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_CALL_FILTER_TYPE: AVPType = 243;
/// Delete all of `cisco_call_filter` values from a packet.
pub fn delete_cisco_call_filter(packet: &mut Packet) {
    packet.delete(CISCO_CALL_FILTER_TYPE);
}
/// Add `cisco_call_filter` integer value to a packet.
pub fn add_cisco_call_filter(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_CALL_FILTER_TYPE, value));
}
/// Lookup a `cisco_call_filter` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_call_filter`, it returns `None`.
pub fn lookup_cisco_call_filter(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(CISCO_CALL_FILTER_TYPE)
        .map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_call_filter` integer value from a packet.
pub fn lookup_all_cisco_call_filter(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_CALL_FILTER_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_IDLE_LIMIT_TYPE: AVPType = 244;
/// Delete all of `cisco_idle_limit` values from a packet.
pub fn delete_cisco_idle_limit(packet: &mut Packet) {
    packet.delete(CISCO_IDLE_LIMIT_TYPE);
}
/// Add `cisco_idle_limit` integer value to a packet.
pub fn add_cisco_idle_limit(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_IDLE_LIMIT_TYPE, value));
}
/// Lookup a `cisco_idle_limit` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_idle_limit`, it returns `None`.
pub fn lookup_cisco_idle_limit(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(CISCO_IDLE_LIMIT_TYPE).map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_idle_limit` integer value from a packet.
pub fn lookup_all_cisco_idle_limit(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_IDLE_LIMIT_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub const CISCO_SUBSCRIBER_PASSWORD_TYPE: AVPType = 249;
/// Delete all of `cisco_subscriber_password` values from a packet.
pub fn delete_cisco_subscriber_password(packet: &mut Packet) {
    packet.delete(CISCO_SUBSCRIBER_PASSWORD_TYPE);
}
/// Add `cisco_subscriber_password` string value to a packet.
pub fn add_cisco_subscriber_password(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_SUBSCRIBER_PASSWORD_TYPE, value));
}
/// Lookup a `cisco_subscriber_password` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_subscriber_password`, it returns `None`.
pub fn lookup_cisco_subscriber_password(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_SUBSCRIBER_PASSWORD_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_subscriber_password` string value from a packet.
pub fn lookup_all_cisco_subscriber_password(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_SUBSCRIBER_PASSWORD_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_ACCOUNT_INFO_TYPE: AVPType = 250;
/// Delete all of `cisco_account_info` values from a packet.
pub fn delete_cisco_account_info(packet: &mut Packet) {
    packet.delete(CISCO_ACCOUNT_INFO_TYPE);
}
/// Add `cisco_account_info` string value to a packet.
pub fn add_cisco_account_info(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_ACCOUNT_INFO_TYPE, value));
}
/// Lookup a `cisco_account_info` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_account_info`, it returns `None`.
pub fn lookup_cisco_account_info(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_ACCOUNT_INFO_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_account_info` string value from a packet.
pub fn lookup_all_cisco_account_info(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_ACCOUNT_INFO_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_SERVICE_INFO_TYPE: AVPType = 251;
/// Delete all of `cisco_service_info` values from a packet.
pub fn delete_cisco_service_info(packet: &mut Packet) {
    packet.delete(CISCO_SERVICE_INFO_TYPE);
}
/// Add `cisco_service_info` string value to a packet.
pub fn add_cisco_service_info(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_SERVICE_INFO_TYPE, value));
}
/// Lookup a `cisco_service_info` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_service_info`, it returns `None`.
pub fn lookup_cisco_service_info(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_SERVICE_INFO_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_service_info` string value from a packet.
pub fn lookup_all_cisco_service_info(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_SERVICE_INFO_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_COMMAND_CODE_TYPE: AVPType = 252;
/// Delete all of `cisco_command_code` values from a packet.
pub fn delete_cisco_command_code(packet: &mut Packet) {
    packet.delete(CISCO_COMMAND_CODE_TYPE);
}
/// Add `cisco_command_code` string value to a packet.
pub fn add_cisco_command_code(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_COMMAND_CODE_TYPE, value));
}
/// Lookup a `cisco_command_code` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_command_code`, it returns `None`.
pub fn lookup_cisco_command_code(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_COMMAND_CODE_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_command_code` string value from a packet.
pub fn lookup_all_cisco_command_code(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_COMMAND_CODE_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_CONTROL_INFO_TYPE: AVPType = 253;
/// Delete all of `cisco_control_info` values from a packet.
pub fn delete_cisco_control_info(packet: &mut Packet) {
    packet.delete(CISCO_CONTROL_INFO_TYPE);
}
/// Add `cisco_control_info` string value to a packet.
pub fn add_cisco_control_info(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(CISCO_CONTROL_INFO_TYPE, value));
}
/// Lookup a `cisco_control_info` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_control_info`, it returns `None`.
pub fn lookup_cisco_control_info(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(CISCO_CONTROL_INFO_TYPE)
        .map(|v| v.encode_string())
}
/// Lookup all of the `cisco_control_info` string value from a packet.
pub fn lookup_all_cisco_control_info(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_CONTROL_INFO_TYPE) {
        vec.push(avp.encode_string()?)
    }
    Ok(vec)
}

pub const CISCO_XMIT_RATE_TYPE: AVPType = 255;
/// Delete all of `cisco_xmit_rate` values from a packet.
pub fn delete_cisco_xmit_rate(packet: &mut Packet) {
    packet.delete(CISCO_XMIT_RATE_TYPE);
}
/// Add `cisco_xmit_rate` integer value to a packet.
pub fn add_cisco_xmit_rate(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(CISCO_XMIT_RATE_TYPE, value));
}
/// Lookup a `cisco_xmit_rate` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `cisco_xmit_rate`, it returns `None`.
pub fn lookup_cisco_xmit_rate(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(CISCO_XMIT_RATE_TYPE).map(|v| v.encode_u32())
}
/// Lookup all of the `cisco_xmit_rate` integer value from a packet.
pub fn lookup_all_cisco_xmit_rate(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(CISCO_XMIT_RATE_TYPE) {
        vec.push(avp.encode_u32()?)
    }
    Ok(vec)
}

pub type CiscoDisconnectCause = u32;
pub const CISCO_DISCONNECT_CAUSE_NO_REASON: CiscoDisconnectCause = 0;
pub const CISCO_DISCONNECT_CAUSE_NO_DISCONNECT: CiscoDisconnectCause = 1;
pub const CISCO_DISCONNECT_CAUSE_UNKNOWN: CiscoDisconnectCause = 2;
pub const CISCO_DISCONNECT_CAUSE_CALL_DISCONNECT: CiscoDisconnectCause = 3;
pub const CISCO_DISCONNECT_CAUSE_CLID_AUTHENTICATION_FAILURE: CiscoDisconnectCause = 4;
pub const CISCO_DISCONNECT_CAUSE_NO_MODEM_AVAILABLE: CiscoDisconnectCause = 9;
pub const CISCO_DISCONNECT_CAUSE_NO_CARRIER: CiscoDisconnectCause = 10;
pub const CISCO_DISCONNECT_CAUSE_LOST_CARRIER: CiscoDisconnectCause = 11;
pub const CISCO_DISCONNECT_CAUSE_NO_DETECTED_RESULT_CODES: CiscoDisconnectCause = 12;
pub const CISCO_DISCONNECT_CAUSE_USER_ENDS_SESSION: CiscoDisconnectCause = 20;
pub const CISCO_DISCONNECT_CAUSE_IDLE_TIMEOUT: CiscoDisconnectCause = 21;
pub const CISCO_DISCONNECT_CAUSE_EXIT_TELNET_SESSION: CiscoDisconnectCause = 22;
pub const CISCO_DISCONNECT_CAUSE_NO_REMOTE_IP_ADDR: CiscoDisconnectCause = 23;
pub const CISCO_DISCONNECT_CAUSE_EXIT_RAW_TCP: CiscoDisconnectCause = 24;
pub const CISCO_DISCONNECT_CAUSE_PASSWORD_FAIL: CiscoDisconnectCause = 25;
pub const CISCO_DISCONNECT_CAUSE_RAW_TCP_DISABLED: CiscoDisconnectCause = 26;
pub const CISCO_DISCONNECT_CAUSE_CONTROL_C_DETECTED: CiscoDisconnectCause = 27;
pub const CISCO_DISCONNECT_CAUSE_EXEC_PROGRAM_DESTROYED: CiscoDisconnectCause = 28;
pub const CISCO_DISCONNECT_CAUSE_CLOSE_VIRTUAL_CONNECTION: CiscoDisconnectCause = 29;
pub const CISCO_DISCONNECT_CAUSE_END_VIRTUAL_CONNECTION: CiscoDisconnectCause = 30;
pub const CISCO_DISCONNECT_CAUSE_EXIT_RLOGIN: CiscoDisconnectCause = 31;
pub const CISCO_DISCONNECT_CAUSE_INVALID_RLOGIN_OPTION: CiscoDisconnectCause = 32;
pub const CISCO_DISCONNECT_CAUSE_INSUFFICIENT_RESOURCES: CiscoDisconnectCause = 33;
pub const CISCO_DISCONNECT_CAUSE_TIMEOUT_PPP_LCP: CiscoDisconnectCause = 40;
pub const CISCO_DISCONNECT_CAUSE_FAILED_PPP_LCP_NEGOTIATION: CiscoDisconnectCause = 41;
pub const CISCO_DISCONNECT_CAUSE_FAILED_PPP_PAP_AUTH_FAIL: CiscoDisconnectCause = 42;
pub const CISCO_DISCONNECT_CAUSE_FAILED_PPP_CHAP_AUTH: CiscoDisconnectCause = 43;
pub const CISCO_DISCONNECT_CAUSE_FAILED_PPP_REMOTE_AUTH: CiscoDisconnectCause = 44;
pub const CISCO_DISCONNECT_CAUSE_PPP_REMOTE_TERMINATE: CiscoDisconnectCause = 45;
pub const CISCO_DISCONNECT_CAUSE_PPP_CLOSED_EVENT: CiscoDisconnectCause = 46;
pub const CISCO_DISCONNECT_CAUSE_NCP_CLOSED_PPP: CiscoDisconnectCause = 47;
pub const CISCO_DISCONNECT_CAUSE_MP_ERROR_PPP: CiscoDisconnectCause = 48;
pub const CISCO_DISCONNECT_CAUSE_PPP_MAXIMUM_CHANNELS: CiscoDisconnectCause = 49;
pub const CISCO_DISCONNECT_CAUSE_TABLES_FULL: CiscoDisconnectCause = 50;
pub const CISCO_DISCONNECT_CAUSE_RESOURCES_FULL: CiscoDisconnectCause = 51;
pub const CISCO_DISCONNECT_CAUSE_INVALID_IP_ADDRESS: CiscoDisconnectCause = 52;
pub const CISCO_DISCONNECT_CAUSE_BAD_HOSTNAME: CiscoDisconnectCause = 53;
pub const CISCO_DISCONNECT_CAUSE_BAD_PORT: CiscoDisconnectCause = 54;
pub const CISCO_DISCONNECT_CAUSE_RESET_TCP: CiscoDisconnectCause = 60;
pub const CISCO_DISCONNECT_CAUSE_TCP_CONNECTION_REFUSED: CiscoDisconnectCause = 61;
pub const CISCO_DISCONNECT_CAUSE_TIMEOUT_TCP: CiscoDisconnectCause = 62;
pub const CISCO_DISCONNECT_CAUSE_FOREIGN_HOST_CLOSE_TCP: CiscoDisconnectCause = 63;
pub const CISCO_DISCONNECT_CAUSE_TCP_NETWORK_UNREACHABLE: CiscoDisconnectCause = 64;
pub const CISCO_DISCONNECT_CAUSE_TCP_HOST_UNREACHABLE: CiscoDisconnectCause = 65;
pub const CISCO_DISCONNECT_CAUSE_TCP_NETWORK_ADMIN_UNREACHABLE: CiscoDisconnectCause = 66;
pub const CISCO_DISCONNECT_CAUSE_TCP_PORT_UNREACHABLE: CiscoDisconnectCause = 67;
pub const CISCO_DISCONNECT_CAUSE_SESSION_TIMEOUT: CiscoDisconnectCause = 100;
pub const CISCO_DISCONNECT_CAUSE_SESSION_FAILED_SECURITY: CiscoDisconnectCause = 101;
pub const CISCO_DISCONNECT_CAUSE_SESSION_END_CALLBACK: CiscoDisconnectCause = 102;
pub const CISCO_DISCONNECT_CAUSE_INVALID_PROTOCOL: CiscoDisconnectCause = 120;
pub const CISCO_DISCONNECT_CAUSE_RADIUS_DISCONNECT: CiscoDisconnectCause = 150;
pub const CISCO_DISCONNECT_CAUSE_LOCAL_ADMIN_DISCONNECT: CiscoDisconnectCause = 151;
pub const CISCO_DISCONNECT_CAUSE_SNMP_DISCONNECT: CiscoDisconnectCause = 152;
pub const CISCO_DISCONNECT_CAUSE_V110_RETRIES: CiscoDisconnectCause = 160;
pub const CISCO_DISCONNECT_CAUSE_PPP_AUTHENTICATION_TIMEOUT: CiscoDisconnectCause = 170;
pub const CISCO_DISCONNECT_CAUSE_LOCAL_HANGUP: CiscoDisconnectCause = 180;
pub const CISCO_DISCONNECT_CAUSE_REMOTE_HANGUP: CiscoDisconnectCause = 185;
pub const CISCO_DISCONNECT_CAUSE_T1_QUIESCED: CiscoDisconnectCause = 190;
pub const CISCO_DISCONNECT_CAUSE_CALL_DURATION: CiscoDisconnectCause = 195;
pub const CISCO_DISCONNECT_CAUSE_VPN_USER_DISCONNECT: CiscoDisconnectCause = 600;
pub const CISCO_DISCONNECT_CAUSE_VPN_CARRIER_LOSS: CiscoDisconnectCause = 601;
pub const CISCO_DISCONNECT_CAUSE_VPN_NO_RESOURCES: CiscoDisconnectCause = 602;
pub const CISCO_DISCONNECT_CAUSE_VPN_BAD_CONTROL_PACKET: CiscoDisconnectCause = 603;
pub const CISCO_DISCONNECT_CAUSE_VPN_ADMIN_DISCONNECT: CiscoDisconnectCause = 604;
pub const CISCO_DISCONNECT_CAUSE_VPN_TUNNEL_SHUT: CiscoDisconnectCause = 605;
pub const CISCO_DISCONNECT_CAUSE_VPN_LOCAL_DISCONNECT: CiscoDisconnectCause = 606;
pub const CISCO_DISCONNECT_CAUSE_VPN_SESSION_LIMIT: CiscoDisconnectCause = 607;
pub const CISCO_DISCONNECT_CAUSE_VPN_CALL_REDIRECT: CiscoDisconnectCause = 608;

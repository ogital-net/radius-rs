// Code generated by machine generator; DO NOT EDIT.
// Clippy: `#[must_use]` on functions returning `Option`/`Result` is intentional even though
// those types are already `#[must_use]`; the attribute surfaces better IDE messages.
#![allow(clippy::double_must_use)]

//! Utility for ascend packet.
//!
//! This module handles the packet according to the following definition:
//! ```text
//! //! # -*- text -*-
//! # Copyright (C) 2019 The FreeRADIUS Server project and contributors
//! # This work is licensed under CC-BY version 4.0 https://creativecommons.org/licenses/by/4.0
//! #
//! ##############################################################################
//! #
//! #    Ascend dictionary.
//! #
//! #    $Id$
//! #
//! ##############################################################################
//!
//! #
//! #    For 16-bit Ascend VSA's, see dictionary.lucent.  Those VSA's
//! #    are in the Lucent namespace, and belong in that file, rather
//! #    than here.
//! #
//! #
//! #  The Ascend-Data-Filter and Ascend-Call-Filter are case insensitive
//! #  strings, with the following format:
//! #
//! #  IP FILTERS:
//! #
//! #    ip dir action [ dstip n.n.n.n/nn ] [ srcip n.n.n.n/nn ]
//! #        [ proto [ dstport cmp value ] [ srcport cmd value ] [ est ] ]
//! #
//! # Fields in [...] are optional.
//! #    where:
//! #
//! #    ip:        Keyword to designate an IP filter. Actually this
//! #            has been determined by parseFilter.
//! #
//! #    dir:        Filter direction. "IN" or "OUT"
//! #
//! #    action:        Filter action. "FORWARD" or "DROP"
//! #
//! #    dstip:        Keyword for destination IP address.
//! #            n.n.n.n = IP address. /nn - netmask.
//! #
//! #    srcip:        Keyword for source IP address.
//! #            n.n.n.n = IP address. /nn - netmask.
//! #
//! #    proto:        Optional protocol field. Either a name or
//! #            number. Known names are in FilterProtoName[].
//! #
//! #    dstport:    Keyword for destination port. Only valid with tcp
//! #            or udp. 'cmp' are in FilterPortType[]. 'value' can be
//! #            a name or number.
//! #
//! #    srcport:    Keyword for source port. Only valid with tcp
//! #            or udp. 'cmp' are in FilterPortType[]. 'value' can be
//! #            a name or number.
//! #
//! #    est:        Keyword for TCP established. Valid only for tcp.
//! #
//! #  IPX FILTERS
//! #
//! #  ipx dir action [ srcipxnet nnnn srcipxnode mmmmm [srcipxsoc cmd value ]]
//! #                 [ dstipxnet nnnn dstipxnode mmmmm [dstipxsoc cmd value ]]
//! #
//! # Fields in [...] are optional.
//! #    where:
//! #
//! #    ipx:        Keyword to designate an IPX filter. Actually this
//! #            has been determined by parseFilter.
//! #
//! #    dir:        Filter direction. "IN" or "OUT"
//! #
//! #    action:        Filter action. "FORWARD" or "DROP"
//! #
//! #    srcipxnet:      Keyword for source IPX address.
//! #            nnnn = IPX Node address.
//! #
//! #    srcipxnode:     Keyword for source IPX Node address.
//! #                      mmmmm = IPX Node Address, could be FFFFFF.
//! #                      A vlid ipx node number should accompany ipx net number.
//! #
//! #    srcipxsoc:      Keyword for source IPX socket address.
//! #
//! #    cmd:            One of ">" or "<" or "=" or "!=".
//! #            (without the quotes)
//! #
//! #     value:          Socket value to be compared against, in hex.
//! #
//! #    dstipxnet:    Keyword for destination IPX address.
//! #            nnnn = IPX Node address.
//! #
//! #    dstipxnode:    Keyword for destination IPX Node address.
//! #              mmmmm = IPX Node Address, could be FFFFFF.
//! #            A vlid ipx node number should accompany ipx net number.
//! #
//! #    dstipxsoc:    Keyword for destination IPX socket address.
//! #
//! #    cmd:        One of ">" or "<" or "=" or "!=".
//! #            (without the quotes)
//! #
//! #    value:        Socket value to be compared against, in hex.
//! #
//! #  GENERIC FILTERS
//! #
//! #    generic dir action offset mask value [== or != ] [more]
//! #
//! # Fields in [...] are optional.
//! #    where:
//! #
//! #     generic:    Keyword to indicate a generic filter. This
//! #            has been determined by parseFilter.
//! #
//! #    dir:        Filter direction. "IN" or "OUT"
//! #
//! #    action:        Filter action. "FORWARD" or "DROP"
//! #
//! #    offset:        A Number. Specifies an offset into a frame
//! #            to start comparing.
//! #
//! #    mask:        A hexadecimal mask of bits to compare.
//! #
//! #    value:        A value to compare with the masked data.
//! #
//! #    compNeq:    Defines type of comparison. ( "==" or "!=")
//! #            Default is "==".
//! #
//! #    more:        Optional keyword MORE, to represent the attachment
//! #            to the next entry.
//!
//! VENDOR        Ascend                529
//!
//! #
//! #  Ascend vendor-specific attributes.
//! #
//! BEGIN-VENDOR    Ascend
//! ATTRIBUTE    Ascend-Max-Shared-Users            2    integer
//! ATTRIBUTE    Ascend-UU-Info                7    string
//! ATTRIBUTE    Ascend-CIR-Timer            9    integer
//! ATTRIBUTE    Ascend-FR-08-Mode            10    integer
//! ATTRIBUTE    Ascend-Destination-Nas-Port        11    integer
//! ATTRIBUTE    Ascend-FR-SVC-Addr            12    string
//! ATTRIBUTE    Ascend-NAS-Port-Format            13    integer
//! ATTRIBUTE    Ascend-ATM-Fault-Management        14    integer
//! ATTRIBUTE    Ascend-ATM-Loopback-Cell-Loss        15    integer
//! ATTRIBUTE    Ascend-Ckt-Type                16    integer
//! ATTRIBUTE    Ascend-SVC-Enabled            17    integer
//! ATTRIBUTE    Ascend-Session-Type            18    integer
//! ATTRIBUTE    Ascend-H323-Gatekeeper            19    ipaddr
//! ATTRIBUTE    Ascend-Global-Call-Id            20    string
//! ATTRIBUTE    Ascend-H323-Conference-Id        21    integer
//! ATTRIBUTE    Ascend-H323-Fegw-Address        22    ipaddr
//! ATTRIBUTE    Ascend-H323-Dialed-Time            23    integer
//! ATTRIBUTE    Ascend-Dialed-Number            24    string
//! ATTRIBUTE    Ascend-Inter-Arrival-Jitter        25    integer
//! ATTRIBUTE    Ascend-Dropped-Octets            26    integer
//! ATTRIBUTE    Ascend-Dropped-Packets            27    integer
//! ATTRIBUTE    Ascend-Auth-Delay            28    integer
//! ATTRIBUTE    Ascend-X25-Pad-X3-Profile        29    integer
//! ATTRIBUTE    Ascend-X25-Pad-X3-Parameters        30    string
//! ATTRIBUTE    Ascend-Tunnel-VRouter-Name        31    string
//! ATTRIBUTE    Ascend-X25-Reverse-Charging        32    integer
//! ATTRIBUTE    Ascend-X25-Nui-Prompt            33    string
//! ATTRIBUTE    Ascend-X25-Nui-Password-Prompt        34    string
//! ATTRIBUTE    Ascend-X25-Cug                35    string
//! ATTRIBUTE    Ascend-X25-Pad-Alias-1            36    string
//! ATTRIBUTE    Ascend-X25-Pad-Alias-2            37    string
//! ATTRIBUTE    Ascend-X25-Pad-Alias-3            38    string
//! ATTRIBUTE    Ascend-X25-X121-Address            39    string
//! ATTRIBUTE    Ascend-X25-Nui                40    string
//! ATTRIBUTE    Ascend-X25-Rpoa                41    string
//! ATTRIBUTE    Ascend-X25-Pad-Prompt            42    string
//! ATTRIBUTE    Ascend-X25-Pad-Banner            43    string
//! ATTRIBUTE    Ascend-X25-Profile-Name            44    string
//! ATTRIBUTE    Ascend-Recv-Name            45    string
//! ATTRIBUTE    Ascend-Bi-Directional-Auth        46    integer
//! ATTRIBUTE    Ascend-MTU                47    integer
//! ATTRIBUTE    Ascend-Call-Direction            48    integer
//! ATTRIBUTE    Ascend-Service-Type            49    integer
//! ATTRIBUTE    Ascend-Filter-Required            50    integer
//! ATTRIBUTE    Ascend-Traffic-Shaper            51    integer
//! ATTRIBUTE    Ascend-Access-Intercept-LEA        52    string
//! ATTRIBUTE    Ascend-Access-Intercept-Log        53    string
//! ATTRIBUTE    Ascend-Private-Route-Table-ID        54    string
//! ATTRIBUTE    Ascend-Private-Route-Required        55    integer
//! ATTRIBUTE    Ascend-Cache-Refresh            56    integer
//! ATTRIBUTE    Ascend-Cache-Time            57    integer
//! ATTRIBUTE    Ascend-Egress-Enabled            58    integer
//! ATTRIBUTE    Ascend-QOS-Upstream            59    string
//! ATTRIBUTE    Ascend-QOS-Downstream            60    string
//! ATTRIBUTE    Ascend-ATM-Connect-Vpi            61    integer
//! ATTRIBUTE    Ascend-ATM-Connect-Vci            62    integer
//! ATTRIBUTE    Ascend-ATM-Connect-Group        63    integer
//! ATTRIBUTE    Ascend-ATM-Group            64    integer
//! ATTRIBUTE    Ascend-IPX-Header-Compression        65    integer
//! ATTRIBUTE    Ascend-Calling-Id-Type-Of-Num        66    integer
//! ATTRIBUTE    Ascend-Calling-Id-Number-Plan        67    integer
//! ATTRIBUTE    Ascend-Calling-Id-Presentatn        68    integer
//! ATTRIBUTE    Ascend-Calling-Id-Screening        69    integer
//! ATTRIBUTE    Ascend-BIR-Enable            70    integer
//! ATTRIBUTE    Ascend-BIR-Proxy            71    integer
//! ATTRIBUTE    Ascend-BIR-Bridge-Group            72    integer
//! ATTRIBUTE    Ascend-IPSEC-Profile            73    string
//! ATTRIBUTE    Ascend-PPPoE-Enable            74    integer
//! ATTRIBUTE    Ascend-Bridge-Non-PPPoE            75    integer
//! ATTRIBUTE    Ascend-ATM-Direct            76    integer
//! ATTRIBUTE    Ascend-ATM-Direct-Profile        77    string
//! ATTRIBUTE    Ascend-Client-Primary-WINS        78    ipaddr
//! ATTRIBUTE    Ascend-Client-Secondary-WINS        79    ipaddr
//! ATTRIBUTE    Ascend-Client-Assign-WINS        80    integer
//! ATTRIBUTE    Ascend-Auth-Type            81    integer
//! ATTRIBUTE    Ascend-Port-Redir-Protocol        82    integer
//! ATTRIBUTE    Ascend-Port-Redir-Portnum        83    integer
//! ATTRIBUTE    Ascend-Port-Redir-Server        84    ipaddr
//! ATTRIBUTE    Ascend-IP-Pool-Chaining            85    integer
//! ATTRIBUTE    Ascend-Owner-IP-Addr            86    ipaddr
//! ATTRIBUTE    Ascend-IP-TOS                87    integer
//! ATTRIBUTE    Ascend-IP-TOS-Precedence        88    integer
//! ATTRIBUTE    Ascend-IP-TOS-Apply-To            89    integer
//! ATTRIBUTE    Ascend-Filter                90    string
//! ATTRIBUTE    Ascend-Telnet-Profile            91    string
//! ATTRIBUTE    Ascend-Dsl-Rate-Type            92    integer
//! ATTRIBUTE    Ascend-Redirect-Number            93    string
//! ATTRIBUTE    Ascend-ATM-Vpi                94    integer
//! ATTRIBUTE    Ascend-ATM-Vci                95    integer
//! ATTRIBUTE    Ascend-Source-IP-Check            96    integer
//! ATTRIBUTE    Ascend-Dsl-Rate-Mode            97    integer
//! ATTRIBUTE    Ascend-Dsl-Upstream-Limit        98    integer
//! ATTRIBUTE    Ascend-Dsl-Downstream-Limit        99    integer
//! ATTRIBUTE    Ascend-Dsl-CIR-Recv-Limit        100    integer
//! ATTRIBUTE    Ascend-Dsl-CIR-Xmit-Limit        101    integer
//! ATTRIBUTE    Ascend-VRouter-Name            102    string
//! ATTRIBUTE    Ascend-Source-Auth            103    string
//! ATTRIBUTE    Ascend-Private-Route            104    string
//! ATTRIBUTE    Ascend-Numbering-Plan-ID        105    integer
//! ATTRIBUTE    Ascend-FR-Link-Status-DLCI        106    integer
//! ATTRIBUTE    Ascend-Calling-Subaddress        107    string
//! ATTRIBUTE    Ascend-Callback-Delay            108    integer
//! ATTRIBUTE    Ascend-Endpoint-Disc            109    string
//! ATTRIBUTE    Ascend-Remote-FW            110    string
//! ATTRIBUTE    Ascend-Multicast-GLeave-Delay        111    integer
//! ATTRIBUTE    Ascend-CBCP-Enable            112    integer
//! ATTRIBUTE    Ascend-CBCP-Mode            113    integer
//! ATTRIBUTE    Ascend-CBCP-Delay            114    integer
//! ATTRIBUTE    Ascend-CBCP-Trunk-Group            115    integer
//! ATTRIBUTE    Ascend-Appletalk-Route            116    string
//! ATTRIBUTE    Ascend-Appletalk-Peer-Mode        117    integer
//! ATTRIBUTE    Ascend-Route-Appletalk            118    integer
//! ATTRIBUTE    Ascend-FCP-Parameter            119    string
//! ATTRIBUTE    Ascend-Modem-PortNo            120    integer
//! ATTRIBUTE    Ascend-Modem-SlotNo            121    integer
//! ATTRIBUTE    Ascend-Modem-ShelfNo            122    integer
//! ATTRIBUTE    Ascend-Call-Attempt-Limit        123    integer
//! ATTRIBUTE    Ascend-Call-Block-Duration        124    integer
//! ATTRIBUTE    Ascend-Maximum-Call-Duration        125    integer
//! ATTRIBUTE    Ascend-Temporary-Rtes            126    integer
//! ATTRIBUTE    Ascend-Tunneling-Protocol        127    integer
//! ATTRIBUTE    Ascend-Shared-Profile-Enable        128    integer
//! ATTRIBUTE    Ascend-Primary-Home-Agent        129    string
//! ATTRIBUTE    Ascend-Secondary-Home-Agent        130    string
//! ATTRIBUTE    Ascend-Dialout-Allowed            131    integer
//! ATTRIBUTE    Ascend-Client-Gateway            132    ipaddr
//! ATTRIBUTE    Ascend-BACP-Enable            133    integer
//! ATTRIBUTE    Ascend-DHCP-Maximum-Leases        134    integer
//! ATTRIBUTE    Ascend-Client-Primary-DNS        135    ipaddr
//! ATTRIBUTE    Ascend-Client-Secondary-DNS        136    ipaddr
//! ATTRIBUTE    Ascend-Client-Assign-DNS        137    integer
//! ATTRIBUTE    Ascend-User-Acct-Type            138    integer
//! ATTRIBUTE    Ascend-User-Acct-Host            139    ipaddr
//! ATTRIBUTE    Ascend-User-Acct-Port            140    integer
//! ATTRIBUTE    Ascend-User-Acct-Key            141    string
//! ATTRIBUTE    Ascend-User-Acct-Base            142    integer
//! ATTRIBUTE    Ascend-User-Acct-Time            143    integer
//! ATTRIBUTE    Ascend-Assign-IP-Client            144    ipaddr
//! ATTRIBUTE    Ascend-Assign-IP-Server            145    ipaddr
//! ATTRIBUTE    Ascend-Assign-IP-Global-Pool        146    string
//! ATTRIBUTE    Ascend-DHCP-Reply            147    integer
//! ATTRIBUTE    Ascend-DHCP-Pool-Number            148    integer
//! ATTRIBUTE    Ascend-Expect-Callback            149    integer
//! ATTRIBUTE    Ascend-Event-Type            150    integer
//! ATTRIBUTE    Ascend-Session-Svr-Key            151    string
//! ATTRIBUTE    Ascend-Multicast-Rate-Limit        152    integer
//! ATTRIBUTE    Ascend-IF-Netmask            153    ipaddr
//! ATTRIBUTE    Ascend-Remote-Addr            154    ipaddr
//! ATTRIBUTE    Ascend-Multicast-Client            155    integer
//! ATTRIBUTE    Ascend-FR-Circuit-Name            156    string
//! ATTRIBUTE    Ascend-FR-LinkUp            157    integer
//! ATTRIBUTE    Ascend-FR-Nailed-Grp            158    integer
//! ATTRIBUTE    Ascend-FR-Type                159    integer
//! ATTRIBUTE    Ascend-FR-Link-Mgt            160    integer
//! ATTRIBUTE    Ascend-FR-N391                161    integer
//! ATTRIBUTE    Ascend-FR-DCE-N392            162    integer
//! ATTRIBUTE    Ascend-FR-DTE-N392            163    integer
//! ATTRIBUTE    Ascend-FR-DCE-N393            164    integer
//! ATTRIBUTE    Ascend-FR-DTE-N393            165    integer
//! ATTRIBUTE    Ascend-FR-T391                166    integer
//! ATTRIBUTE    Ascend-FR-T392                167    integer
//! ATTRIBUTE    Ascend-Bridge-Address            168    string
//! ATTRIBUTE    Ascend-TS-Idle-Limit            169    integer
//! ATTRIBUTE    Ascend-TS-Idle-Mode            170    integer
//! ATTRIBUTE    Ascend-DBA-Monitor            171    integer
//! ATTRIBUTE    Ascend-Base-Channel-Count        172    integer
//! ATTRIBUTE    Ascend-Minimum-Channels            173    integer
//! ATTRIBUTE    Ascend-IPX-Route            174    string
//! ATTRIBUTE    Ascend-FT1-Caller            175    integer
//! ATTRIBUTE    Ascend-Backup                176    string
//! ATTRIBUTE    Ascend-Call-Type            177    integer
//! ATTRIBUTE    Ascend-Group                178    string
//! ATTRIBUTE    Ascend-FR-DLCI                179    integer
//! ATTRIBUTE    Ascend-FR-Profile-Name            180    string
//! ATTRIBUTE    Ascend-Ara-PW                181    string
//! ATTRIBUTE    Ascend-IPX-Node-Addr            182    string
//! ATTRIBUTE    Ascend-Home-Agent-IP-Addr        183    ipaddr
//! ATTRIBUTE    Ascend-Home-Agent-Password        184    string
//! ATTRIBUTE    Ascend-Home-Network-Name        185    string
//! ATTRIBUTE    Ascend-Home-Agent-UDP-Port        186    integer
//! ATTRIBUTE    Ascend-Multilink-ID            187    integer
//! ATTRIBUTE    Ascend-Num-In-Multilink            188    integer
//! ATTRIBUTE    Ascend-First-Dest            189    ipaddr
//! ATTRIBUTE    Ascend-Pre-Input-Octets            190    integer
//! ATTRIBUTE    Ascend-Pre-Output-Octets        191    integer
//! ATTRIBUTE    Ascend-Pre-Input-Packets        192    integer
//! ATTRIBUTE    Ascend-Pre-Output-Packets        193    integer
//! ATTRIBUTE    Ascend-Maximum-Time            194    integer
//! ATTRIBUTE    Ascend-Disconnect-Cause            195    integer
//! ATTRIBUTE    Ascend-Connect-Progress            196    integer
//! ATTRIBUTE    Ascend-Data-Rate            197    integer
//! ATTRIBUTE    Ascend-PreSession-Time            198    integer
//! ATTRIBUTE    Ascend-Token-Idle            199    integer
//! ATTRIBUTE    Ascend-Token-Immediate            200    integer
//! ATTRIBUTE    Ascend-Require-Auth            201    integer
//! ATTRIBUTE    Ascend-Number-Sessions            202    string
//! ATTRIBUTE    Ascend-Authen-Alias            203    string
//! ATTRIBUTE    Ascend-Token-Expiry            204    integer
//! ATTRIBUTE    Ascend-Menu-Selector            205    string
//! ATTRIBUTE    Ascend-Menu-Item            206    string
//! ATTRIBUTE    Ascend-PW-Warntime            207    integer
//! ATTRIBUTE    Ascend-PW-Lifetime            208    integer
//! ATTRIBUTE    Ascend-IP-Direct            209    ipaddr
//! ATTRIBUTE    Ascend-PPP-VJ-Slot-Comp            210    integer
//! ATTRIBUTE    Ascend-PPP-VJ-1172            211    integer
//! ATTRIBUTE    Ascend-PPP-Async-Map            212    integer
//! ATTRIBUTE    Ascend-Third-Prompt            213    string
//! ATTRIBUTE    Ascend-Send-Secret            214    string    encrypt=3
//! ATTRIBUTE    Ascend-Receive-Secret            215    string    encrypt=3
//! ATTRIBUTE    Ascend-IPX-Peer-Mode            216    integer
//! ATTRIBUTE    Ascend-IP-Pool-Definition        217    string
//! ATTRIBUTE    Ascend-Assign-IP-Pool            218    integer
//! ATTRIBUTE    Ascend-FR-Direct            219    integer
//! ATTRIBUTE    Ascend-FR-Direct-Profile        220    string
//! ATTRIBUTE    Ascend-FR-Direct-DLCI            221    integer
//! ATTRIBUTE    Ascend-Handle-IPX            222    integer
//! ATTRIBUTE    Ascend-Netware-timeout            223    integer
//! ATTRIBUTE    Ascend-IPX-Alias            224    integer
//! ATTRIBUTE    Ascend-Metric                225    integer
//! ATTRIBUTE    Ascend-PRI-Number-Type            226    integer
//! ATTRIBUTE    Ascend-Dial-Number            227    string
//! ATTRIBUTE    Ascend-Route-IP                228    integer
//! ATTRIBUTE    Ascend-Route-IPX            229    integer
//! ATTRIBUTE    Ascend-Bridge                230    integer
//! ATTRIBUTE    Ascend-Send-Auth            231    integer
//! ATTRIBUTE    Ascend-Send-Passwd            232    string
//! ATTRIBUTE    Ascend-Link-Compression            233    integer
//! ATTRIBUTE    Ascend-Target-Util            234    integer
//! ATTRIBUTE    Ascend-Maximum-Channels            235    integer
//! ATTRIBUTE    Ascend-Inc-Channel-Count        236    integer
//! ATTRIBUTE    Ascend-Dec-Channel-Count        237    integer
//! ATTRIBUTE    Ascend-Seconds-Of-History        238    integer
//! ATTRIBUTE    Ascend-History-Weigh-Type        239    integer
//! ATTRIBUTE    Ascend-Add-Seconds            240    integer
//! ATTRIBUTE    Ascend-Remove-Seconds            241    integer
//! ATTRIBUTE    Ascend-Data-Filter            242    abinary
//! ATTRIBUTE    Ascend-Call-Filter            243    abinary
//! ATTRIBUTE    Ascend-Idle-Limit            244    integer
//! ATTRIBUTE    Ascend-Preempt-Limit            245    integer
//! ATTRIBUTE    Ascend-Callback                246    integer
//! ATTRIBUTE    Ascend-Data-Svc                247    integer
//! ATTRIBUTE    Ascend-Force-56                248    integer
//! ATTRIBUTE    Ascend-Billing-Number            249    string
//! ATTRIBUTE    Ascend-Call-By-Call            250    integer
//! ATTRIBUTE    Ascend-Transit-Number            251    string
//! ATTRIBUTE    Ascend-Host-Info            252    string
//! ATTRIBUTE    Ascend-PPP-Address            253    ipaddr
//! ATTRIBUTE    Ascend-MPP-Idle-Percent            254    integer
//! ATTRIBUTE    Ascend-Xmit-Rate            255    integer
//!
//! # Ascend protocols
//! VALUE    Framed-Protocol            Ascend-ARA        255
//! VALUE    Framed-Protocol            Ascend-MPP        256
//! VALUE    Framed-Protocol            Ascend-EURAW        257
//! VALUE    Framed-Protocol            Ascend-EUUI        258
//! VALUE    Framed-Protocol            Ascend-X25        259
//! VALUE    Framed-Protocol            Ascend-COMB        260
//! VALUE    Framed-Protocol            Ascend-FR        261
//! VALUE    Framed-Protocol            Ascend-MP        262
//! VALUE    Framed-Protocol            Ascend-FR-CIR        263
//!
//! #
//! #    Ascend specific extensions
//! #    Used by ASCEND MAX/Pipeline products (see above)
//! #
//!
//! VALUE    Ascend-Source-IP-Check        Source-IP-Check-No    0
//! VALUE    Ascend-Source-IP-Check        Source-IP-Check-Yes    1
//! VALUE    Ascend-CBCP-Enable        CBCP-Not-Enabled    0
//! VALUE    Ascend-CBCP-Enable        CBCP-Enabled        1
//! VALUE    Ascend-CBCP-Mode        CBCP-No-Callback    1
//! VALUE    Ascend-CBCP-Mode        CBCP-User-Callback    2
//! VALUE    Ascend-CBCP-Mode        CBCP-Profile-Callback    3
//! VALUE    Ascend-CBCP-Mode        CBCP-Any-Or-No        7
//! VALUE    Ascend-CBCP-Mode        CBCP-Off        8
//! VALUE    Ascend-FR-Direct        FR-Direct-No        0
//! VALUE    Ascend-FR-Direct        FR-Direct-Yes        1
//! VALUE    Ascend-Handle-IPX        Handle-IPX-None        0
//! VALUE    Ascend-Handle-IPX        Handle-IPX-Client    1
//! VALUE    Ascend-Handle-IPX        Handle-IPX-Server    2
//! VALUE    Ascend-IPX-Peer-Mode        IPX-Peer-Router        0
//! VALUE    Ascend-IPX-Peer-Mode        IPX-Peer-Dialin        1
//! VALUE    Ascend-Call-Type        Switched        0
//! VALUE    Ascend-Call-Type        Nailed            1
//! VALUE    Ascend-Call-Type        Nailed/Mpp        2
//! VALUE    Ascend-Call-Type        Perm/Switched        3
//! VALUE    Ascend-Call-Type        AO/DI            6
//! VALUE    Ascend-Call-Type        MegaMax            7
//! VALUE    Ascend-FT1-Caller        FT1-No            0
//! VALUE    Ascend-FT1-Caller        FT1-Yes            1
//! VALUE    Ascend-PRI-Number-Type        Unknown-Number        0
//! VALUE    Ascend-PRI-Number-Type        Intl-Number        1
//! VALUE    Ascend-PRI-Number-Type        National-Number        2
//! VALUE    Ascend-PRI-Number-Type        Net-Specific-Number    3
//! VALUE    Ascend-PRI-Number-Type        Local-Number        4
//! VALUE    Ascend-PRI-Number-Type        Abbrev-Number        5
//!
//! VALUE    Ascend-Route-IP            Route-IP-No        0
//! VALUE    Ascend-Route-IP            Route-IP-Yes        1
//! VALUE    Ascend-Route-IPX        Route-IPX-No        0
//! VALUE    Ascend-Route-IPX        Route-IPX-Yes        1
//! VALUE    Ascend-Bridge            Bridge-No        0
//! VALUE    Ascend-Bridge            Bridge-Yes        1
//! VALUE    Ascend-TS-Idle-Mode        TS-Idle-None        0
//! VALUE    Ascend-TS-Idle-Mode        TS-Idle-Input        1
//! VALUE    Ascend-TS-Idle-Mode        TS-Idle-Input-Output    2
//!
//! VALUE    Ascend-Send-Auth        Send-Auth-None        0
//! VALUE    Ascend-Send-Auth        Send-Auth-PAP        1
//! VALUE    Ascend-Send-Auth        Send-Auth-CHAP        2
//! VALUE    Ascend-Send-Auth        Send-Auth-MS-CHAP    3
//!
//! VALUE    Ascend-Link-Compression        Link-Comp-None        0
//! VALUE    Ascend-Link-Compression        Link-Comp-Stac        1
//! VALUE    Ascend-Link-Compression        Link-Comp-Stac-Draft-9    2
//! VALUE    Ascend-Link-Compression        Link-Comp-MS-Stac    3
//! VALUE    Ascend-History-Weigh-Type    History-Constant    0
//! VALUE    Ascend-History-Weigh-Type    History-Linear        1
//! VALUE    Ascend-History-Weigh-Type    History-Quadratic    2
//! VALUE    Ascend-Callback            Callback-No        0
//! VALUE    Ascend-Callback            Callback-Yes        1
//! VALUE    Ascend-Expect-Callback        Expect-Callback-No    0
//! VALUE    Ascend-Expect-Callback        Expect-Callback-Yes    1
//! VALUE    Ascend-Data-Svc            Switched-Voice-Bearer    0
//! VALUE    Ascend-Data-Svc            Nailed-56KR        1
//! VALUE    Ascend-Data-Svc            Nailed-64K        2
//! VALUE    Ascend-Data-Svc            Switched-64KR        3
//! VALUE    Ascend-Data-Svc            Switched-56K        4
//! VALUE    Ascend-Data-Svc            Switched-384KR        5
//! VALUE    Ascend-Data-Svc            Switched-384K        6
//! VALUE    Ascend-Data-Svc            Switched-1536K        7
//! VALUE    Ascend-Data-Svc            Switched-1536KR        8
//! VALUE    Ascend-Data-Svc            Switched-128K        9
//! VALUE    Ascend-Data-Svc            Switched-192K        10
//! VALUE    Ascend-Data-Svc            Switched-256K        11
//! VALUE    Ascend-Data-Svc            Switched-320K        12
//! VALUE    Ascend-Data-Svc            Switched-384K-MR    13
//! VALUE    Ascend-Data-Svc            Switched-448K        14
//! VALUE    Ascend-Data-Svc            Switched-512K        15
//! VALUE    Ascend-Data-Svc            Switched-576K        16
//! VALUE    Ascend-Data-Svc            Switched-640K        17
//! VALUE    Ascend-Data-Svc            Switched-704K        18
//! VALUE    Ascend-Data-Svc            Switched-768K        19
//! VALUE    Ascend-Data-Svc            Switched-832K        20
//! VALUE    Ascend-Data-Svc            Switched-896K        21
//! VALUE    Ascend-Data-Svc            Switched-960K        22
//! VALUE    Ascend-Data-Svc            Switched-1024K        23
//! VALUE    Ascend-Data-Svc            Switched-1088K        24
//! VALUE    Ascend-Data-Svc            Switched-1152K        25
//! VALUE    Ascend-Data-Svc            Switched-1216K        26
//! VALUE    Ascend-Data-Svc            Switched-1280K        27
//! VALUE    Ascend-Data-Svc            Switched-1344K        28
//! VALUE    Ascend-Data-Svc            Switched-1408K        29
//! VALUE    Ascend-Data-Svc            Switched-1472K        30
//! VALUE    Ascend-Data-Svc            Switched-1600K        31
//! VALUE    Ascend-Data-Svc            Switched-1664K        32
//! VALUE    Ascend-Data-Svc            Switched-1728K        33
//! VALUE    Ascend-Data-Svc            Switched-1792K        34
//! VALUE    Ascend-Data-Svc            Switched-1856K        35
//! VALUE    Ascend-Data-Svc            Switched-1920K        36
//! VALUE    Ascend-Data-Svc            Switched-inherited    37
//! VALUE    Ascend-Data-Svc            Switched-restricted-bearer-x30 38
//! VALUE    Ascend-Data-Svc            Switched-clear-bearer-v110 39
//! VALUE    Ascend-Data-Svc            Switched-restricted-64-x30 40
//! VALUE    Ascend-Data-Svc            Switched-clear-56-v110    41
//! VALUE    Ascend-Data-Svc            Switched-modem        42
//! VALUE    Ascend-Data-Svc            Switched-atmodem    43
//! VALUE    Ascend-Data-Svc            Switched-V110-24-56    45
//! VALUE    Ascend-Data-Svc            Switched-V110-48-56    46
//! VALUE    Ascend-Data-Svc            Switched-V110-96-56    47
//! VALUE    Ascend-Data-Svc            Switched-V110-192-56    48
//! VALUE    Ascend-Data-Svc            Switched-V110-384-56    49
//! VALUE    Ascend-Data-Svc            Switched-V110-24-56R    50
//! VALUE    Ascend-Data-Svc            Switched-V110-48-56R    51
//! VALUE    Ascend-Data-Svc            Switched-V110-96-56R    52
//! VALUE    Ascend-Data-Svc            Switched-V110-192-56R    53
//! VALUE    Ascend-Data-Svc            Switched-V110-384-56R    54
//! VALUE    Ascend-Data-Svc            Switched-V110-24-64    55
//! VALUE    Ascend-Data-Svc            Switched-V110-48-64    56
//! VALUE    Ascend-Data-Svc            Switched-V110-96-64    57
//! VALUE    Ascend-Data-Svc            Switched-V110-192-64    58
//! VALUE    Ascend-Data-Svc            Switched-V110-384-64    59
//! VALUE    Ascend-Data-Svc            Switched-V110-24-64R    60
//! VALUE    Ascend-Data-Svc            Switched-V110-48-64R    61
//! VALUE    Ascend-Data-Svc            Switched-V110-96-64R    62
//! VALUE    Ascend-Data-Svc            Switched-V110-384-64R    64
//! VALUE    Ascend-Data-Svc            Switched-V110-192-64R    63
//!
//! VALUE    Ascend-Data-Svc            Switched-Pots        68
//! VALUE    Ascend-Data-Svc            Switched-ATM        69
//! VALUE    Ascend-Data-Svc            Switched-FR        70
//!
//! VALUE    Ascend-Force-56            Force-56-No        0
//! VALUE    Ascend-Force-56            Force-56-Yes        1
//! VALUE    Ascend-PW-Lifetime        Lifetime-In-Days    0
//! VALUE    Ascend-PW-Warntime        Days-Of-Warning        0
//! VALUE    Ascend-PPP-VJ-1172        PPP-VJ-1172        1
//! VALUE    Ascend-PPP-VJ-Slot-Comp        VJ-Slot-Comp-No        1
//! VALUE    Ascend-Require-Auth        Not-Require-Auth    0
//! VALUE    Ascend-Require-Auth        Require-Auth        1
//! VALUE    Ascend-Token-Immediate        Tok-Imm-No        0
//! VALUE    Ascend-Token-Immediate        Tok-Imm-Yes        1
//! VALUE    Ascend-DBA-Monitor        DBA-Transmit        0
//! VALUE    Ascend-DBA-Monitor        DBA-Transmit-Recv    1
//! VALUE    Ascend-DBA-Monitor        DBA-None        2
//! VALUE    Ascend-FR-Type            Ascend-FR-DTE        0
//! VALUE    Ascend-FR-Type            Ascend-FR-DCE        1
//! VALUE    Ascend-FR-Type            Ascend-FR-NNI        2
//! VALUE    Ascend-FR-Link-Mgt        Ascend-FR-No-Link-Mgt    0
//! VALUE    Ascend-FR-Link-Mgt        Ascend-FR-T1-617D    1
//! VALUE    Ascend-FR-Link-Mgt        Ascend-FR-Q-933A    2
//! VALUE    Ascend-FR-LinkUp        Ascend-LinkUp-Default    0
//! VALUE    Ascend-FR-LinkUp        Ascend-LinkUp-AlwaysUp    1
//! VALUE    Ascend-Multicast-Client        Multicast-No        0
//! VALUE    Ascend-Multicast-Client        Multicast-Yes        1
//! VALUE    Ascend-User-Acct-Type        Ascend-User-Acct-None    0
//! VALUE    Ascend-User-Acct-Type        Ascend-User-Acct-User    1
//! VALUE    Ascend-User-Acct-Type        Ascend-User-Acct-User-Default 2
//! VALUE    Ascend-User-Acct-Base        Base-10            0
//! VALUE    Ascend-User-Acct-Base        Base-16            1
//! VALUE    Ascend-DHCP-Reply        DHCP-Reply-No        0
//! VALUE    Ascend-DHCP-Reply        DHCP-Reply-Yes        1
//! VALUE    Ascend-Client-Assign-DNS    DNS-Assign-No        0
//! VALUE    Ascend-Client-Assign-DNS    DNS-Assign-Yes        1
//! VALUE    Ascend-Event-Type        Ascend-ColdStart    1
//! VALUE    Ascend-Event-Type        Ascend-Session-Event    2
//! VALUE    Ascend-BACP-Enable        BACP-No            0
//! VALUE    Ascend-BACP-Enable        BACP-Yes        1
//!
//! VALUE    Ascend-Dialout-Allowed        Dialout-Not-Allowed    0
//! VALUE    Ascend-Dialout-Allowed        Dialout-Allowed        1
//!
//! VALUE    Ascend-Shared-Profile-Enable    Shared-Profile-No    0
//! VALUE    Ascend-Shared-Profile-Enable    Shared-Profile-Yes    1
//!
//! VALUE    Ascend-Temporary-Rtes        Temp-Rtes-No        0
//! VALUE    Ascend-Temporary-Rtes        Temp-Rtes-Yes        1
//!
//! # Ascend Disconnect Cause Values
//!
//! VALUE    Ascend-Disconnect-Cause        No-Reason        0
//! VALUE    Ascend-Disconnect-Cause        Not-Applicable        1
//! VALUE    Ascend-Disconnect-Cause        Unknown            2
//! VALUE    Ascend-Disconnect-Cause        Call-Disconnected    3
//! VALUE    Ascend-Disconnect-Cause        CLID-Authentication-Failed 4
//! VALUE    Ascend-Disconnect-Cause        CLID-RADIUS-Timeout    5
//!
//! VALUE    Ascend-Disconnect-Cause        Modem-No-DCD        10
//! VALUE    Ascend-Disconnect-Cause        DCD-Detected-Then-Inactive 11
//! VALUE    Ascend-Disconnect-Cause        Modem-Invalid-Result-Codes 12
//!
//! VALUE    Ascend-Disconnect-Cause        TermSrv-User-Quit    20
//! VALUE    Ascend-Disconnect-Cause        TermSrv-Idle-Timeout    21
//! VALUE    Ascend-Disconnect-Cause        TermSrv-Exit-Telnet    22
//! VALUE    Ascend-Disconnect-Cause        TermSrv-No-IPaddr    23
//! VALUE    Ascend-Disconnect-Cause        TermSrv-Exit-Raw-TCP    24
//! VALUE    Ascend-Disconnect-Cause        TermSrv-Exit-Login-Failed 25
//! VALUE    Ascend-Disconnect-Cause        TermSrv-Exit-Raw-TCP-Disabled 26
//! VALUE    Ascend-Disconnect-Cause        TermSrv-CTRL-C-In-Login    27
//! VALUE    Ascend-Disconnect-Cause        TermSrv-Destroyed    28
//! VALUE    Ascend-Disconnect-Cause        TermSrv-User-Closed-VCon 29
//!
//! VALUE    Ascend-Disconnect-Cause        TermSrv-VCon-Destroyed    30
//! VALUE    Ascend-Disconnect-Cause        TermSrv-Exit-Rlogin    31
//! VALUE    Ascend-Disconnect-Cause        TermSrv-Bad-Rlogin-Option 32
//! VALUE    Ascend-Disconnect-Cause        TermSrv-Not-Enough-Resources 33
//!
//! VALUE    Ascend-Disconnect-Cause        MPP-No-NULL-Msg-Timeout    35
//!
//! VALUE    Ascend-Disconnect-Cause        PPP-LCP-Timeout        40
//! VALUE    Ascend-Disconnect-Cause        PPP-LCP-Negotion-Failed    41
//! VALUE    Ascend-Disconnect-Cause        PPP-PAP-Auth-Failed    42
//! VALUE    Ascend-Disconnect-Cause        PPP-CHAP-Auth-Failed    43
//! VALUE    Ascend-Disconnect-Cause        PPP-Rmt-Auth-Failed    44
//! VALUE    Ascend-Disconnect-Cause        PPP-Rcv-Terminate-Req    45
//! VALUE    Ascend-Disconnect-Cause        PPP-Rcv-Close-Event    46
//! VALUE    Ascend-Disconnect-Cause        PPP-No-NCPs-Open    47
//! VALUE    Ascend-Disconnect-Cause        PPP-MP-Bundle-Unknown    48
//! VALUE    Ascend-Disconnect-Cause        PPP-LCP-Close-MP-Add-Fail 49
//!
//! VALUE    Ascend-Disconnect-Cause        Session-Table-Full    50
//! VALUE    Ascend-Disconnect-Cause        Out-Of-Resources    51
//! VALUE    Ascend-Disconnect-Cause        Invalid-IP-Address    52
//! VALUE    Ascend-Disconnect-Cause        Hostname-Resolution-Failed 53
//! VALUE    Ascend-Disconnect-Cause        Bad-Or-Missing-Port-Number 54
//!
//! VALUE    Ascend-Disconnect-Cause        Host-Reset        60
//! VALUE    Ascend-Disconnect-Cause        Connection-Refused    61
//! VALUE    Ascend-Disconnect-Cause        Connection-Timeout    62
//! VALUE    Ascend-Disconnect-Cause        Connection-Closed    63
//! VALUE    Ascend-Disconnect-Cause        Network-Unreachable    64
//! VALUE    Ascend-Disconnect-Cause        Host-Unreachable    65
//! VALUE    Ascend-Disconnect-Cause        Network-Unreachable-Admin 66
//! VALUE    Ascend-Disconnect-Cause        Host-Unreachable-Admin    67
//! VALUE    Ascend-Disconnect-Cause        Port-Unreachable    68
//!
//! VALUE    Ascend-Disconnect-Cause        Session-Timeout        100
//! VALUE    Ascend-Disconnect-Cause        Invalid-Incoming-User    101
//! VALUE    Ascend-Disconnect-Cause        Disconnect-Due-To-Callback 102
//!
//! VALUE    Ascend-Disconnect-Cause        Proto-Disabled-Or-Unsupported 120
//!
//! VALUE    Ascend-Disconnect-Cause        Disconnect-Req-By-RADIUS 150
//! VALUE    Ascend-Disconnect-Cause        Disconnect-Req-By-Local-Admin 151
//!
//! VALUE    Ascend-Disconnect-Cause        V110-Timeout-Sync-Retry-Exceed 160
//!
//! VALUE    Ascend-Disconnect-Cause        PPP-Auth-Timeout-Exceeded 170
//! VALUE    Ascend-Disconnect-Cause        User-Executed-Do-Hangup    180
//! VALUE    Ascend-Disconnect-Cause        Remote-End-Hung-Up    185
//! VALUE    Ascend-Disconnect-Cause        Resource-Has-Been-Quiesced 190
//! VALUE    Ascend-Disconnect-Cause        Max-Call-Duration-Reached 195
//!
//! # ascend connect progress codes
//! VALUE    Ascend-Connect-Progress        No-Progress        0
//! VALUE    Ascend-Connect-Progress        Call-Up            10
//! VALUE    Ascend-Connect-Progress        Modem-Up        30
//! VALUE    Ascend-Connect-Progress        Modem-Awaiting-DCD    31
//! VALUE    Ascend-Connect-Progress        Modem-Awaiting-Codes    32
//! VALUE    Ascend-Connect-Progress        TermSrv-Started        40
//! VALUE    Ascend-Connect-Progress        TermSrv-Raw-TCP-Started    41
//! VALUE    Ascend-Connect-Progress        TermSrv-Telnet-Started    42
//! VALUE    Ascend-Connect-Progress        TermSrv-Raw-TCP-Connected 43
//! VALUE    Ascend-Connect-Progress        TermSrv-Telnet-Connected 44
//! VALUE    Ascend-Connect-Progress        TermSrv-Rlogin-Started    45
//! VALUE    Ascend-Connect-Progress        TermSrv-Rlogin-Connected 46
//! VALUE    Ascend-Connect-Progress        Modem-Outdial-Call-Up    50
//! VALUE    Ascend-Connect-Progress        LAN-Session-Up        60
//! VALUE    Ascend-Connect-Progress        LCP-Opening        61
//! VALUE    Ascend-Connect-Progress        CCP-Opening        62
//! VALUE    Ascend-Connect-Progress        IPNCP-Opening        63
//! VALUE    Ascend-Connect-Progress        BNCP-Opening        64
//! VALUE    Ascend-Connect-Progress        LCP-Opened        65
//! VALUE    Ascend-Connect-Progress        CCP-Opened        66
//! VALUE    Ascend-Connect-Progress        IPNCP-Opened        67
//! VALUE    Ascend-Connect-Progress        BNCP-Opened        68
//! VALUE    Ascend-Connect-Progress        LCP-State-Initial    69
//! VALUE    Ascend-Connect-Progress        LCP-State-Starting    70
//! VALUE    Ascend-Connect-Progress        LCP-State-Closed    71
//! VALUE    Ascend-Connect-Progress        LCP-State-Stopped    72
//! VALUE    Ascend-Connect-Progress        LCP-State-Closing    73
//! VALUE    Ascend-Connect-Progress        LCP-State-Stopping    74
//! VALUE    Ascend-Connect-Progress        LCP-State-Request-Sent    75
//! VALUE    Ascend-Connect-Progress        LCP-State-Ack-Received    76
//! VALUE    Ascend-Connect-Progress        LCP-State-Ack-Sent    77
//! VALUE    Ascend-Connect-Progress        IPXNCP-Opened        80
//! VALUE    Ascend-Connect-Progress        ATNCP-Opened        81
//! VALUE    Ascend-Connect-Progress        BACP-Opening        82
//! VALUE    Ascend-Connect-Progress        BACP-Opened        83
//! VALUE    Ascend-Connect-Progress        V110-Up            90
//! VALUE    Ascend-Connect-Progress        V110-State-Opened    91
//! VALUE    Ascend-Connect-Progress        V110-State-Carrier    92
//! VALUE    Ascend-Connect-Progress        V110-State-Reset    93
//! VALUE    Ascend-Connect-Progress        V110-State-Closed    94
//! VALUE    Ascend-ATM-Direct        ATM-Direct-No        0
//! VALUE    Ascend-ATM-Direct        ATM-Direct-Yes        1
//! VALUE    Ascend-ATM-Fault-Management    VC-End-To-End-Loopback    2
//! VALUE    Ascend-ATM-Fault-Management    VC-No-Loopback        0
//! VALUE    Ascend-ATM-Fault-Management    VC-Segment-Loopback    1
//! VALUE    Ascend-Appletalk-Peer-Mode    Appletalk-Peer-Dialin    1
//! VALUE    Ascend-Appletalk-Peer-Mode    Appletalk-Peer-Router    0
//! VALUE    Ascend-Auth-Type        Auth-Any        2
//! VALUE    Ascend-Auth-Type        Auth-CHAP        4
//! VALUE    Ascend-Auth-Type        Auth-Default        1
//! VALUE    Ascend-Auth-Type        Auth-MS-CHAP        5
//! VALUE    Ascend-Auth-Type        Auth-None        0
//! VALUE    Ascend-Auth-Type        Auth-PAP        3
//! VALUE    Ascend-BIR-Enable        BIR-Enable-No        0
//! VALUE    Ascend-BIR-Enable        BIR-Enable-Yes        1
//! VALUE    Ascend-BIR-Proxy        BIR-Proxy-No        0
//! VALUE    Ascend-BIR-Proxy        BIR-Proxy-Yes        1
//! VALUE    Ascend-Bi-Directional-Auth    Bi-Directional-Auth-Allowed 1
//! VALUE    Ascend-Bi-Directional-Auth    Bi-Directional-Auth-None 0
//! VALUE    Ascend-Bi-Directional-Auth    Bi-Directional-Auth-Required 2
//! VALUE    Ascend-Bridge-Non-PPPoE        Bridge-Non-PPPoE-No    0
//! VALUE    Ascend-Bridge-Non-PPPoE        Bridge-Non-PPPoE-Yes    1
//! VALUE    Ascend-Cache-Refresh        Refresh-No        0
//! VALUE    Ascend-Cache-Refresh        Refresh-Yes        1
//! VALUE    Ascend-Call-Direction        Ascend-Call-Direction-Incoming 0
//! VALUE    Ascend-Call-Direction        Ascend-Call-Direction-Outgoing 1
//! VALUE    Ascend-Calling-Id-Number-Plan    Data            3
//! VALUE    Ascend-Calling-Id-Number-Plan    ISDN-Telephony        1
//! VALUE    Ascend-Calling-Id-Number-Plan    National        8
//! VALUE    Ascend-Calling-Id-Number-Plan    Private            9
//! VALUE    Ascend-Calling-Id-Number-Plan    Telex            4
//! VALUE    Ascend-Calling-Id-Number-Plan    Unknown            0
//! VALUE    Ascend-Calling-Id-Presentatn    Allowed            0
//! VALUE    Ascend-Calling-Id-Presentatn    Number-Not-Available    2
//! VALUE    Ascend-Calling-Id-Presentatn    Restricted        1
//! VALUE    Ascend-Calling-Id-Screening    Network-Provided    3
//! VALUE    Ascend-Calling-Id-Screening    User-Not-Screened    0
//! VALUE    Ascend-Calling-Id-Screening    User-Provided-Failed    2
//! VALUE    Ascend-Calling-Id-Screening    User-Provided-Passed    1
//! VALUE    Ascend-Calling-Id-Type-Of-Num    Abbreviated-Number    6
//! VALUE    Ascend-Calling-Id-Type-Of-Num    International-Number    1
//! VALUE    Ascend-Calling-Id-Type-Of-Num    National-Number        2
//! VALUE    Ascend-Calling-Id-Type-Of-Num    Network-Specific    3
//! VALUE    Ascend-Calling-Id-Type-Of-Num    Subscriber-Number    4
//! VALUE    Ascend-Calling-Id-Type-Of-Num    Unknown            0
//! VALUE    Ascend-Ckt-Type            Ascend-PVC        0
//! VALUE    Ascend-Ckt-Type            Ascend-SVC        1
//! VALUE    Ascend-Client-Assign-WINS    WINS-Assign-No        0
//! VALUE    Ascend-Client-Assign-WINS    WINS-Assign-Yes        1
//!
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-1280000    10
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-1600000    9
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-1920000    8
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-2240000    7
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-2560000    6
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-2688000    5
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-3200000    4
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-4480000    3
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-5120000    2
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-6272000    1
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-640000    12
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-7168000    0
//! VALUE    Ascend-Dsl-Downstream-Limit    adslcap-dn-960000    11
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-128000    121
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-1280000    114
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-1600000    113
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-1920000    112
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-2240000    111
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-256000    120
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-2560000    110
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-2688000    109
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-3200000    108
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-384000    119
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-4480000    107
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-512000    118
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-5120000    106
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-6272000    105
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-640000    117
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-7168000    104
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-768000    116
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-8000000    103
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-8960000    102
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-9504000    101
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-960000    115
//! VALUE    Ascend-Dsl-Downstream-Limit    adsldmt-dn-auto        100
//! VALUE    Ascend-Dsl-Rate-Mode        Rate-Mode-AutoBaud    1
//! VALUE    Ascend-Dsl-Rate-Mode        Rate-Mode-Single    2
//! VALUE    Ascend-Dsl-Rate-Type        Rate-Type-AdslCap    2
//! VALUE    Ascend-Dsl-Rate-Type        Rate-Type-AdslDmt    4
//! VALUE    Ascend-Dsl-Rate-Type        Rate-Type-AdslDmtCell    3
//! VALUE    Ascend-Dsl-Rate-Type        Rate-Type-Disabled    0
//! VALUE    Ascend-Dsl-Rate-Type        Rate-Type-Sdsl        1
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-896000    153
//! VALUE    Ascend-Dsl-Upstream-Limit    adslcap-up-1088000    50
//! VALUE    Ascend-Dsl-Upstream-Limit    adslcap-up-272000    56
//! VALUE    Ascend-Dsl-Upstream-Limit    adslcap-up-408000    55
//! VALUE    Ascend-Dsl-Upstream-Limit    adslcap-up-544000    54
//! VALUE    Ascend-Dsl-Upstream-Limit    adslcap-up-680000    53
//! VALUE    Ascend-Dsl-Upstream-Limit    adslcap-up-816000    52
//! VALUE    Ascend-Dsl-Upstream-Limit    adslcap-up-952000    51
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-1088000    151
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-128000    160
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-256000    159
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-384000    158
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-512000    157
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-640000    156
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-768000    155
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-800000    154
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-928000    152
//! VALUE    Ascend-Dsl-Upstream-Limit    adsldmt-up-auto        150
//! VALUE    Ascend-Dsl-Upstream-Limit    sdsl-1168000        5
//! VALUE    Ascend-Dsl-Upstream-Limit    sdsl-144000        0
//! VALUE    Ascend-Dsl-Upstream-Limit    sdsl-1552000        6
//! VALUE    Ascend-Dsl-Upstream-Limit    sdsl-2320000        7
//! VALUE    Ascend-Dsl-Upstream-Limit    sdsl-272000        1
//! VALUE    Ascend-Dsl-Upstream-Limit    sdsl-400000        2
//! VALUE    Ascend-Dsl-Upstream-Limit    sdsl-528000        3
//! VALUE    Ascend-Dsl-Upstream-Limit    sdsl-784000        4
//! VALUE    Ascend-FR-Link-Status-DLCI    Ascend-FR-LMI-Dlci-0    0
//! VALUE    Ascend-FR-Link-Status-DLCI    Ascend-FR-LMI-Dlci-1023    1023
//! VALUE    Ascend-Filter-Required        Required-No        0
//! VALUE    Ascend-Filter-Required        Required-Yes        1
//! VALUE    Ascend-IP-Pool-Chaining        IP-Pool-Chaining-No    0
//! VALUE    Ascend-IP-Pool-Chaining        IP-Pool-Chaining-Yes    1
//! VALUE    Ascend-IP-TOS            IP-TOS-Cost        2
//! VALUE    Ascend-IP-TOS            IP-TOS-Disabled        1
//! VALUE    Ascend-IP-TOS            IP-TOS-Latency        16
//! VALUE    Ascend-IP-TOS            IP-TOS-Normal        0
//! VALUE    Ascend-IP-TOS            IP-TOS-Reliability    4
//! VALUE    Ascend-IP-TOS            IP-TOS-Throughput    8
//! VALUE    Ascend-IP-TOS-Apply-To        IP-TOS-Apply-To-Both    3072
//! VALUE    Ascend-IP-TOS-Apply-To        IP-TOS-Apply-To-Incoming 1024
//! VALUE    Ascend-IP-TOS-Apply-To        IP-TOS-Apply-To-Outgoing 2048
//! VALUE    Ascend-IP-TOS-Precedence    IP-TOS-Precedence-Pri-Five 160
//! VALUE    Ascend-IP-TOS-Precedence    IP-TOS-Precedence-Pri-Four 128
//! VALUE    Ascend-IP-TOS-Precedence    IP-TOS-Precedence-Pri-Normal 0
//! VALUE    Ascend-IP-TOS-Precedence    IP-TOS-Precedence-Pri-One 32
//! VALUE    Ascend-IP-TOS-Precedence    IP-TOS-Precedence-Pri-Seven 224
//! VALUE    Ascend-IP-TOS-Precedence    IP-TOS-Precedence-Pri-Six 192
//! VALUE    Ascend-IP-TOS-Precedence    IP-TOS-Precedence-Pri-Three 96
//! VALUE    Ascend-IP-TOS-Precedence    IP-TOS-Precedence-Pri-Two 64
//! VALUE    Ascend-IPX-Header-Compression    IPX-Header-Compression-No 0
//! VALUE    Ascend-IPX-Header-Compression    IPX-Header-Compression-Yes 1
//! VALUE    Ascend-NAS-Port-Format        1_2_2            3
//! VALUE    Ascend-NAS-Port-Format        2_4_5_5            2
//! VALUE    Ascend-NAS-Port-Format        2_4_6_4            1
//! VALUE    Ascend-NAS-Port-Format        Unknown            0
//! VALUE    Ascend-Numbering-Plan-ID    ISDN-Numbering-Plan    1
//! VALUE    Ascend-Numbering-Plan-ID    Private-Numbering-Plan    9
//! VALUE    Ascend-Numbering-Plan-ID    Unknown-Numbering-Plan    0
//! VALUE    Ascend-PPPoE-Enable        PPPoE-No        0
//! VALUE    Ascend-PPPoE-Enable        PPPoE-Yes        1
//! VALUE    Ascend-Port-Redir-Protocol    Ascend-Proto-TCP    6
//! VALUE    Ascend-Port-Redir-Protocol    Ascend-Proto-UDP    17
//! VALUE    Ascend-Private-Route-Required    Required-No        0
//! VALUE    Ascend-Private-Route-Required    Required-Yes        1
//! VALUE    Ascend-Route-Appletalk        Route-Appletalk-No    0
//! VALUE    Ascend-Route-Appletalk        Route-Appletalk-Yes    1
//! VALUE    Ascend-SVC-Enabled        Ascend-SVC-Enabled-No    0
//! VALUE    Ascend-SVC-Enabled        Ascend-SVC-Enabled-Yes    1
//!
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-ATM    20
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-Combinet 7
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-EuRaw 9
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-EuUi 10
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-FR    8
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-HdlcNrm 21
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-IpFax 19
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-MP    15
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-MPP    5
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-NetToNet 25
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-None 1
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-NotUsed 0
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-Other 2
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-PPP    3
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-PseuTunPPP 18
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-RawTcp 13
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-Slip 4
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-Telnet 11
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-TelnetBin 12
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-TermServer 14
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-VirtualConn 16
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-Visa2 23
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-VoIp 22
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-X25    6
//! VALUE    Ascend-Service-Type        Ascend-Service-Type-X25DChan 17
//! VALUE    Ascend-Session-Type        Ascend-Session-G711-Alaw 3
//! VALUE    Ascend-Session-Type        Ascend-Session-G711-Ulaw 2
//! VALUE    Ascend-Session-Type        Ascend-Session-G723    4
//! VALUE    Ascend-Session-Type        Ascend-Session-G723-64KPS 6
//! VALUE    Ascend-Session-Type        Ascend-Session-G728    7
//! VALUE    Ascend-Session-Type        Ascend-Session-G729    5
//! VALUE    Ascend-Session-Type        Ascend-Session-RT24    8
//! VALUE    Ascend-Session-Type        Ascend-Session-Unknown    1
//! VALUE    Ascend-Session-Type        Ascend-Session-Unused    0
//!
//! VALUE    Ascend-Tunneling-Protocol    ATMP-Tunnel        0
//! VALUE    Ascend-Tunneling-Protocol    VTP-Tunnel        1
//!
//! VALUE    Ascend-X25-Pad-X3-Profile    CC_SSP            4
//! VALUE    Ascend-X25-Pad-X3-Profile    CC_TSP            5
//! VALUE    Ascend-X25-Pad-X3-Profile    CRT            0
//! VALUE    Ascend-X25-Pad-X3-Profile    CUSTOM            11
//! VALUE    Ascend-X25-Pad-X3-Profile    DEFAULT            2
//! VALUE    Ascend-X25-Pad-X3-Profile    HARDCOPY        6
//! VALUE    Ascend-X25-Pad-X3-Profile    HDX            7
//! VALUE    Ascend-X25-Pad-X3-Profile    INFONET            1
//! VALUE    Ascend-X25-Pad-X3-Profile    NULL            10
//! VALUE    Ascend-X25-Pad-X3-Profile    POS            9
//! VALUE    Ascend-X25-Pad-X3-Profile    SCEN            3
//! VALUE    Ascend-X25-Pad-X3-Profile    SHARK            8
//! VALUE    Ascend-X25-Reverse-Charging    Reverse-Charging-No    0
//! VALUE    Ascend-X25-Reverse-Charging    Reverse-Charging-Yes    1
//! END-VENDOR Ascend
//! ```

use std::net::Ipv4Addr;

use crate::core::avp::{AVPError, AVPType, AVP};
use crate::core::packet::Packet;

pub const ASCEND_MAX_SHARED_USERS_TYPE: AVPType = 2;
/// Delete all of `ascend_max_shared_users` values from a packet.
pub fn delete_ascend_max_shared_users(packet: &mut Packet) {
    packet.delete(ASCEND_MAX_SHARED_USERS_TYPE);
}
/// Add `ascend_max_shared_users` integer value to a packet.
pub fn add_ascend_max_shared_users(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MAX_SHARED_USERS_TYPE, value));
}
/// Lookup a `ascend_max_shared_users` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_max_shared_users`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_max_shared_users(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_MAX_SHARED_USERS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_max_shared_users` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_max_shared_users(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MAX_SHARED_USERS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_UU_INFO_TYPE: AVPType = 7;
/// Delete all of `ascend_uu_info` values from a packet.
pub fn delete_ascend_uu_info(packet: &mut Packet) {
    packet.delete(ASCEND_UU_INFO_TYPE);
}
/// Add `ascend_uu_info` string value to a packet.
pub fn add_ascend_uu_info(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_UU_INFO_TYPE, value));
}
/// Lookup a `ascend_uu_info` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_uu_info`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_uu_info(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_UU_INFO_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_uu_info` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_uu_info(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_UU_INFO_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_CIR_TIMER_TYPE: AVPType = 9;
/// Delete all of `ascend_cir_timer` values from a packet.
pub fn delete_ascend_cir_timer(packet: &mut Packet) {
    packet.delete(ASCEND_CIR_TIMER_TYPE);
}
/// Add `ascend_cir_timer` integer value to a packet.
pub fn add_ascend_cir_timer(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_CIR_TIMER_TYPE, value));
}
/// Lookup a `ascend_cir_timer` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_cir_timer`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_cir_timer(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_CIR_TIMER_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_cir_timer` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_cir_timer(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CIR_TIMER_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_08_MODE_TYPE: AVPType = 10;
/// Delete all of `ascend_fr_08_mode` values from a packet.
pub fn delete_ascend_fr_08_mode(packet: &mut Packet) {
    packet.delete(ASCEND_FR_08_MODE_TYPE);
}
/// Add `ascend_fr_08_mode` integer value to a packet.
pub fn add_ascend_fr_08_mode(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_08_MODE_TYPE, value));
}
/// Lookup a `ascend_fr_08_mode` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_08_mode`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_08_mode(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_FR_08_MODE_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_08_mode` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_08_mode(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_08_MODE_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_DESTINATION_NAS_PORT_TYPE: AVPType = 11;
/// Delete all of `ascend_destination_nas_port` values from a packet.
pub fn delete_ascend_destination_nas_port(packet: &mut Packet) {
    packet.delete(ASCEND_DESTINATION_NAS_PORT_TYPE);
}
/// Add `ascend_destination_nas_port` integer value to a packet.
pub fn add_ascend_destination_nas_port(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_DESTINATION_NAS_PORT_TYPE, value));
}
/// Lookup a `ascend_destination_nas_port` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_destination_nas_port`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_destination_nas_port(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_DESTINATION_NAS_PORT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_destination_nas_port` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_destination_nas_port(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DESTINATION_NAS_PORT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_SVC_ADDR_TYPE: AVPType = 12;
/// Delete all of `ascend_fr_svc_addr` values from a packet.
pub fn delete_ascend_fr_svc_addr(packet: &mut Packet) {
    packet.delete(ASCEND_FR_SVC_ADDR_TYPE);
}
/// Add `ascend_fr_svc_addr` string value to a packet.
pub fn add_ascend_fr_svc_addr(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_FR_SVC_ADDR_TYPE, value));
}
/// Lookup a `ascend_fr_svc_addr` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_svc_addr`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_svc_addr(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_FR_SVC_ADDR_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_fr_svc_addr` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_svc_addr(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_SVC_ADDR_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_NAS_PORT_FORMAT_TYPE: AVPType = 13;
/// Delete all of `ascend_nas_port_format` values from a packet.
pub fn delete_ascend_nas_port_format(packet: &mut Packet) {
    packet.delete(ASCEND_NAS_PORT_FORMAT_TYPE);
}
/// Add `ascend_nas_port_format` value-defined integer value to a packet.
pub fn add_ascend_nas_port_format(packet: &mut Packet, value: AscendNASPortFormat) {
    packet.add(AVP::from_u32(ASCEND_NAS_PORT_FORMAT_TYPE, value));
}
/// Lookup a `ascend_nas_port_format` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_nas_port_format`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_nas_port_format(
    packet: &Packet,
) -> Option<Result<AscendNASPortFormat, AVPError>> {
    packet
        .lookup(ASCEND_NAS_PORT_FORMAT_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendNASPortFormat))
}
/// Lookup all of the `ascend_nas_port_format` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_nas_port_format(
    packet: &Packet,
) -> Result<Vec<AscendNASPortFormat>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_NAS_PORT_FORMAT_TYPE) {
        vec.push(avp.encode_u32()? as AscendNASPortFormat);
    }
    Ok(vec)
}

pub const ASCEND_ATM_FAULT_MANAGEMENT_TYPE: AVPType = 14;
/// Delete all of `ascend_atm_fault_management` values from a packet.
pub fn delete_ascend_atm_fault_management(packet: &mut Packet) {
    packet.delete(ASCEND_ATM_FAULT_MANAGEMENT_TYPE);
}
/// Add `ascend_atm_fault_management` value-defined integer value to a packet.
pub fn add_ascend_atm_fault_management(packet: &mut Packet, value: AscendATMFaultManagement) {
    packet.add(AVP::from_u32(ASCEND_ATM_FAULT_MANAGEMENT_TYPE, value));
}
/// Lookup a `ascend_atm_fault_management` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_atm_fault_management`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_atm_fault_management(
    packet: &Packet,
) -> Option<Result<AscendATMFaultManagement, AVPError>> {
    packet
        .lookup(ASCEND_ATM_FAULT_MANAGEMENT_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendATMFaultManagement))
}
/// Lookup all of the `ascend_atm_fault_management` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_atm_fault_management(
    packet: &Packet,
) -> Result<Vec<AscendATMFaultManagement>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ATM_FAULT_MANAGEMENT_TYPE) {
        vec.push(avp.encode_u32()? as AscendATMFaultManagement);
    }
    Ok(vec)
}

pub const ASCEND_ATM_LOOPBACK_CELL_LOSS_TYPE: AVPType = 15;
/// Delete all of `ascend_atm_loopback_cell_loss` values from a packet.
pub fn delete_ascend_atm_loopback_cell_loss(packet: &mut Packet) {
    packet.delete(ASCEND_ATM_LOOPBACK_CELL_LOSS_TYPE);
}
/// Add `ascend_atm_loopback_cell_loss` integer value to a packet.
pub fn add_ascend_atm_loopback_cell_loss(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_ATM_LOOPBACK_CELL_LOSS_TYPE, value));
}
/// Lookup a `ascend_atm_loopback_cell_loss` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_atm_loopback_cell_loss`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_atm_loopback_cell_loss(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_ATM_LOOPBACK_CELL_LOSS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_atm_loopback_cell_loss` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_atm_loopback_cell_loss(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ATM_LOOPBACK_CELL_LOSS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_CKT_TYPE_TYPE: AVPType = 16;
/// Delete all of `ascend_ckt_type` values from a packet.
pub fn delete_ascend_ckt_type(packet: &mut Packet) {
    packet.delete(ASCEND_CKT_TYPE_TYPE);
}
/// Add `ascend_ckt_type` value-defined integer value to a packet.
pub fn add_ascend_ckt_type(packet: &mut Packet, value: AscendCktType) {
    packet.add(AVP::from_u32(ASCEND_CKT_TYPE_TYPE, value));
}
/// Lookup a `ascend_ckt_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ckt_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ckt_type(packet: &Packet) -> Option<Result<AscendCktType, AVPError>> {
    packet
        .lookup(ASCEND_CKT_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCktType))
}
/// Lookup all of the `ascend_ckt_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ckt_type(packet: &Packet) -> Result<Vec<AscendCktType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CKT_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendCktType);
    }
    Ok(vec)
}

pub const ASCEND_SVC_ENABLED_TYPE: AVPType = 17;
/// Delete all of `ascend_svc_enabled` values from a packet.
pub fn delete_ascend_svc_enabled(packet: &mut Packet) {
    packet.delete(ASCEND_SVC_ENABLED_TYPE);
}
/// Add `ascend_svc_enabled` value-defined integer value to a packet.
pub fn add_ascend_svc_enabled(packet: &mut Packet, value: AscendSVCEnabled) {
    packet.add(AVP::from_u32(ASCEND_SVC_ENABLED_TYPE, value));
}
/// Lookup a `ascend_svc_enabled` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_svc_enabled`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_svc_enabled(packet: &Packet) -> Option<Result<AscendSVCEnabled, AVPError>> {
    packet
        .lookup(ASCEND_SVC_ENABLED_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendSVCEnabled))
}
/// Lookup all of the `ascend_svc_enabled` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_svc_enabled(packet: &Packet) -> Result<Vec<AscendSVCEnabled>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SVC_ENABLED_TYPE) {
        vec.push(avp.encode_u32()? as AscendSVCEnabled);
    }
    Ok(vec)
}

pub const ASCEND_SESSION_TYPE_TYPE: AVPType = 18;
/// Delete all of `ascend_session_type` values from a packet.
pub fn delete_ascend_session_type(packet: &mut Packet) {
    packet.delete(ASCEND_SESSION_TYPE_TYPE);
}
/// Add `ascend_session_type` value-defined integer value to a packet.
pub fn add_ascend_session_type(packet: &mut Packet, value: AscendSessionType) {
    packet.add(AVP::from_u32(ASCEND_SESSION_TYPE_TYPE, value));
}
/// Lookup a `ascend_session_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_session_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_session_type(packet: &Packet) -> Option<Result<AscendSessionType, AVPError>> {
    packet
        .lookup(ASCEND_SESSION_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendSessionType))
}
/// Lookup all of the `ascend_session_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_session_type(packet: &Packet) -> Result<Vec<AscendSessionType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SESSION_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendSessionType);
    }
    Ok(vec)
}

pub const ASCEND_H323_GATEKEEPER_TYPE: AVPType = 19;
/// Delete all of `ascend_h323_gatekeeper` values from a packet.
pub fn delete_ascend_h323_gatekeeper(packet: &mut Packet) {
    packet.delete(ASCEND_H323_GATEKEEPER_TYPE);
}
/// Add `ascend_h323_gatekeeper` ipaddr value to a packet.
pub fn add_ascend_h323_gatekeeper(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_H323_GATEKEEPER_TYPE, value));
}
/// Lookup a `ascend_h323_gatekeeper` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_h323_gatekeeper`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_h323_gatekeeper(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_H323_GATEKEEPER_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_h323_gatekeeper` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_h323_gatekeeper(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_H323_GATEKEEPER_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_GLOBAL_CALL_ID_TYPE: AVPType = 20;
/// Delete all of `ascend_global_call_id` values from a packet.
pub fn delete_ascend_global_call_id(packet: &mut Packet) {
    packet.delete(ASCEND_GLOBAL_CALL_ID_TYPE);
}
/// Add `ascend_global_call_id` string value to a packet.
pub fn add_ascend_global_call_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_GLOBAL_CALL_ID_TYPE, value));
}
/// Lookup a `ascend_global_call_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_global_call_id`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_global_call_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_GLOBAL_CALL_ID_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_global_call_id` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_global_call_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_GLOBAL_CALL_ID_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_H323_CONFERENCE_ID_TYPE: AVPType = 21;
/// Delete all of `ascend_h323_conference_id` values from a packet.
pub fn delete_ascend_h323_conference_id(packet: &mut Packet) {
    packet.delete(ASCEND_H323_CONFERENCE_ID_TYPE);
}
/// Add `ascend_h323_conference_id` integer value to a packet.
pub fn add_ascend_h323_conference_id(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_H323_CONFERENCE_ID_TYPE, value));
}
/// Lookup a `ascend_h323_conference_id` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_h323_conference_id`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_h323_conference_id(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_H323_CONFERENCE_ID_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_h323_conference_id` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_h323_conference_id(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_H323_CONFERENCE_ID_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_H323_FEGW_ADDRESS_TYPE: AVPType = 22;
/// Delete all of `ascend_h323_fegw_address` values from a packet.
pub fn delete_ascend_h323_fegw_address(packet: &mut Packet) {
    packet.delete(ASCEND_H323_FEGW_ADDRESS_TYPE);
}
/// Add `ascend_h323_fegw_address` ipaddr value to a packet.
pub fn add_ascend_h323_fegw_address(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_H323_FEGW_ADDRESS_TYPE, value));
}
/// Lookup a `ascend_h323_fegw_address` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_h323_fegw_address`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_h323_fegw_address(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_H323_FEGW_ADDRESS_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_h323_fegw_address` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_h323_fegw_address(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_H323_FEGW_ADDRESS_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_H323_DIALED_TIME_TYPE: AVPType = 23;
/// Delete all of `ascend_h323_dialed_time` values from a packet.
pub fn delete_ascend_h323_dialed_time(packet: &mut Packet) {
    packet.delete(ASCEND_H323_DIALED_TIME_TYPE);
}
/// Add `ascend_h323_dialed_time` integer value to a packet.
pub fn add_ascend_h323_dialed_time(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_H323_DIALED_TIME_TYPE, value));
}
/// Lookup a `ascend_h323_dialed_time` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_h323_dialed_time`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_h323_dialed_time(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_H323_DIALED_TIME_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_h323_dialed_time` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_h323_dialed_time(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_H323_DIALED_TIME_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_DIALED_NUMBER_TYPE: AVPType = 24;
/// Delete all of `ascend_dialed_number` values from a packet.
pub fn delete_ascend_dialed_number(packet: &mut Packet) {
    packet.delete(ASCEND_DIALED_NUMBER_TYPE);
}
/// Add `ascend_dialed_number` string value to a packet.
pub fn add_ascend_dialed_number(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_DIALED_NUMBER_TYPE, value));
}
/// Lookup a `ascend_dialed_number` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dialed_number`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dialed_number(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_DIALED_NUMBER_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_dialed_number` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dialed_number(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DIALED_NUMBER_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_INTER_ARRIVAL_JITTER_TYPE: AVPType = 25;
/// Delete all of `ascend_inter_arrival_jitter` values from a packet.
pub fn delete_ascend_inter_arrival_jitter(packet: &mut Packet) {
    packet.delete(ASCEND_INTER_ARRIVAL_JITTER_TYPE);
}
/// Add `ascend_inter_arrival_jitter` integer value to a packet.
pub fn add_ascend_inter_arrival_jitter(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_INTER_ARRIVAL_JITTER_TYPE, value));
}
/// Lookup a `ascend_inter_arrival_jitter` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_inter_arrival_jitter`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_inter_arrival_jitter(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_INTER_ARRIVAL_JITTER_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_inter_arrival_jitter` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_inter_arrival_jitter(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_INTER_ARRIVAL_JITTER_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_DROPPED_OCTETS_TYPE: AVPType = 26;
/// Delete all of `ascend_dropped_octets` values from a packet.
pub fn delete_ascend_dropped_octets(packet: &mut Packet) {
    packet.delete(ASCEND_DROPPED_OCTETS_TYPE);
}
/// Add `ascend_dropped_octets` integer value to a packet.
pub fn add_ascend_dropped_octets(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_DROPPED_OCTETS_TYPE, value));
}
/// Lookup a `ascend_dropped_octets` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dropped_octets`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dropped_octets(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_DROPPED_OCTETS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_dropped_octets` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dropped_octets(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DROPPED_OCTETS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_DROPPED_PACKETS_TYPE: AVPType = 27;
/// Delete all of `ascend_dropped_packets` values from a packet.
pub fn delete_ascend_dropped_packets(packet: &mut Packet) {
    packet.delete(ASCEND_DROPPED_PACKETS_TYPE);
}
/// Add `ascend_dropped_packets` integer value to a packet.
pub fn add_ascend_dropped_packets(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_DROPPED_PACKETS_TYPE, value));
}
/// Lookup a `ascend_dropped_packets` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dropped_packets`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dropped_packets(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_DROPPED_PACKETS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_dropped_packets` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dropped_packets(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DROPPED_PACKETS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_AUTH_DELAY_TYPE: AVPType = 28;
/// Delete all of `ascend_auth_delay` values from a packet.
pub fn delete_ascend_auth_delay(packet: &mut Packet) {
    packet.delete(ASCEND_AUTH_DELAY_TYPE);
}
/// Add `ascend_auth_delay` integer value to a packet.
pub fn add_ascend_auth_delay(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_AUTH_DELAY_TYPE, value));
}
/// Lookup a `ascend_auth_delay` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_auth_delay`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_auth_delay(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_AUTH_DELAY_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_auth_delay` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_auth_delay(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_AUTH_DELAY_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_PAD_X3_PROFILE_TYPE: AVPType = 29;
/// Delete all of `ascend_x25_pad_x3_profile` values from a packet.
pub fn delete_ascend_x25_pad_x3_profile(packet: &mut Packet) {
    packet.delete(ASCEND_X25_PAD_X3_PROFILE_TYPE);
}
/// Add `ascend_x25_pad_x3_profile` value-defined integer value to a packet.
pub fn add_ascend_x25_pad_x3_profile(packet: &mut Packet, value: AscendX25PadX3Profile) {
    packet.add(AVP::from_u32(ASCEND_X25_PAD_X3_PROFILE_TYPE, value));
}
/// Lookup a `ascend_x25_pad_x3_profile` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_pad_x3_profile`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_pad_x3_profile(
    packet: &Packet,
) -> Option<Result<AscendX25PadX3Profile, AVPError>> {
    packet
        .lookup(ASCEND_X25_PAD_X3_PROFILE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendX25PadX3Profile))
}
/// Lookup all of the `ascend_x25_pad_x3_profile` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_pad_x3_profile(
    packet: &Packet,
) -> Result<Vec<AscendX25PadX3Profile>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_PAD_X3_PROFILE_TYPE) {
        vec.push(avp.encode_u32()? as AscendX25PadX3Profile);
    }
    Ok(vec)
}

pub const ASCEND_X25_PAD_X3_PARAMETERS_TYPE: AVPType = 30;
/// Delete all of `ascend_x25_pad_x3_parameters` values from a packet.
pub fn delete_ascend_x25_pad_x3_parameters(packet: &mut Packet) {
    packet.delete(ASCEND_X25_PAD_X3_PARAMETERS_TYPE);
}
/// Add `ascend_x25_pad_x3_parameters` string value to a packet.
pub fn add_ascend_x25_pad_x3_parameters(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_PAD_X3_PARAMETERS_TYPE, value));
}
/// Lookup a `ascend_x25_pad_x3_parameters` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_pad_x3_parameters`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_pad_x3_parameters(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_X25_PAD_X3_PARAMETERS_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_pad_x3_parameters` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_pad_x3_parameters(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_PAD_X3_PARAMETERS_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_TUNNEL_V_ROUTER_NAME_TYPE: AVPType = 31;
/// Delete all of `ascend_tunnel_v_router_name` values from a packet.
pub fn delete_ascend_tunnel_v_router_name(packet: &mut Packet) {
    packet.delete(ASCEND_TUNNEL_V_ROUTER_NAME_TYPE);
}
/// Add `ascend_tunnel_v_router_name` string value to a packet.
pub fn add_ascend_tunnel_v_router_name(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_TUNNEL_V_ROUTER_NAME_TYPE, value));
}
/// Lookup a `ascend_tunnel_v_router_name` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_tunnel_v_router_name`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_tunnel_v_router_name(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_TUNNEL_V_ROUTER_NAME_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_tunnel_v_router_name` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_tunnel_v_router_name(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TUNNEL_V_ROUTER_NAME_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_REVERSE_CHARGING_TYPE: AVPType = 32;
/// Delete all of `ascend_x25_reverse_charging` values from a packet.
pub fn delete_ascend_x25_reverse_charging(packet: &mut Packet) {
    packet.delete(ASCEND_X25_REVERSE_CHARGING_TYPE);
}
/// Add `ascend_x25_reverse_charging` value-defined integer value to a packet.
pub fn add_ascend_x25_reverse_charging(packet: &mut Packet, value: AscendX25ReverseCharging) {
    packet.add(AVP::from_u32(ASCEND_X25_REVERSE_CHARGING_TYPE, value));
}
/// Lookup a `ascend_x25_reverse_charging` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_reverse_charging`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_reverse_charging(
    packet: &Packet,
) -> Option<Result<AscendX25ReverseCharging, AVPError>> {
    packet
        .lookup(ASCEND_X25_REVERSE_CHARGING_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendX25ReverseCharging))
}
/// Lookup all of the `ascend_x25_reverse_charging` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_reverse_charging(
    packet: &Packet,
) -> Result<Vec<AscendX25ReverseCharging>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_REVERSE_CHARGING_TYPE) {
        vec.push(avp.encode_u32()? as AscendX25ReverseCharging);
    }
    Ok(vec)
}

pub const ASCEND_X25_NUI_PROMPT_TYPE: AVPType = 33;
/// Delete all of `ascend_x25_nui_prompt` values from a packet.
pub fn delete_ascend_x25_nui_prompt(packet: &mut Packet) {
    packet.delete(ASCEND_X25_NUI_PROMPT_TYPE);
}
/// Add `ascend_x25_nui_prompt` string value to a packet.
pub fn add_ascend_x25_nui_prompt(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_NUI_PROMPT_TYPE, value));
}
/// Lookup a `ascend_x25_nui_prompt` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_nui_prompt`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_nui_prompt(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_X25_NUI_PROMPT_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_nui_prompt` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_nui_prompt(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_NUI_PROMPT_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_NUI_PASSWORD_PROMPT_TYPE: AVPType = 34;
/// Delete all of `ascend_x25_nui_password_prompt` values from a packet.
pub fn delete_ascend_x25_nui_password_prompt(packet: &mut Packet) {
    packet.delete(ASCEND_X25_NUI_PASSWORD_PROMPT_TYPE);
}
/// Add `ascend_x25_nui_password_prompt` string value to a packet.
pub fn add_ascend_x25_nui_password_prompt(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_NUI_PASSWORD_PROMPT_TYPE, value));
}
/// Lookup a `ascend_x25_nui_password_prompt` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_nui_password_prompt`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_nui_password_prompt(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_X25_NUI_PASSWORD_PROMPT_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_nui_password_prompt` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_nui_password_prompt(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_NUI_PASSWORD_PROMPT_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_CUG_TYPE: AVPType = 35;
/// Delete all of `ascend_x25_cug` values from a packet.
pub fn delete_ascend_x25_cug(packet: &mut Packet) {
    packet.delete(ASCEND_X25_CUG_TYPE);
}
/// Add `ascend_x25_cug` string value to a packet.
pub fn add_ascend_x25_cug(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_CUG_TYPE, value));
}
/// Lookup a `ascend_x25_cug` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_cug`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_cug(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_X25_CUG_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_cug` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_cug(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_CUG_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_PAD_ALIAS_1_TYPE: AVPType = 36;
/// Delete all of `ascend_x25_pad_alias_1` values from a packet.
pub fn delete_ascend_x25_pad_alias_1(packet: &mut Packet) {
    packet.delete(ASCEND_X25_PAD_ALIAS_1_TYPE);
}
/// Add `ascend_x25_pad_alias_1` string value to a packet.
pub fn add_ascend_x25_pad_alias_1(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_PAD_ALIAS_1_TYPE, value));
}
/// Lookup a `ascend_x25_pad_alias_1` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_pad_alias_1`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_pad_alias_1(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_X25_PAD_ALIAS_1_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_pad_alias_1` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_pad_alias_1(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_PAD_ALIAS_1_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_PAD_ALIAS_2_TYPE: AVPType = 37;
/// Delete all of `ascend_x25_pad_alias_2` values from a packet.
pub fn delete_ascend_x25_pad_alias_2(packet: &mut Packet) {
    packet.delete(ASCEND_X25_PAD_ALIAS_2_TYPE);
}
/// Add `ascend_x25_pad_alias_2` string value to a packet.
pub fn add_ascend_x25_pad_alias_2(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_PAD_ALIAS_2_TYPE, value));
}
/// Lookup a `ascend_x25_pad_alias_2` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_pad_alias_2`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_pad_alias_2(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_X25_PAD_ALIAS_2_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_pad_alias_2` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_pad_alias_2(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_PAD_ALIAS_2_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_PAD_ALIAS_3_TYPE: AVPType = 38;
/// Delete all of `ascend_x25_pad_alias_3` values from a packet.
pub fn delete_ascend_x25_pad_alias_3(packet: &mut Packet) {
    packet.delete(ASCEND_X25_PAD_ALIAS_3_TYPE);
}
/// Add `ascend_x25_pad_alias_3` string value to a packet.
pub fn add_ascend_x25_pad_alias_3(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_PAD_ALIAS_3_TYPE, value));
}
/// Lookup a `ascend_x25_pad_alias_3` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_pad_alias_3`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_pad_alias_3(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_X25_PAD_ALIAS_3_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_pad_alias_3` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_pad_alias_3(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_PAD_ALIAS_3_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_X121_ADDRESS_TYPE: AVPType = 39;
/// Delete all of `ascend_x25_x121_address` values from a packet.
pub fn delete_ascend_x25_x121_address(packet: &mut Packet) {
    packet.delete(ASCEND_X25_X121_ADDRESS_TYPE);
}
/// Add `ascend_x25_x121_address` string value to a packet.
pub fn add_ascend_x25_x121_address(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_X121_ADDRESS_TYPE, value));
}
/// Lookup a `ascend_x25_x121_address` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_x121_address`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_x121_address(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_X25_X121_ADDRESS_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_x121_address` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_x121_address(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_X121_ADDRESS_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_NUI_TYPE: AVPType = 40;
/// Delete all of `ascend_x25_nui` values from a packet.
pub fn delete_ascend_x25_nui(packet: &mut Packet) {
    packet.delete(ASCEND_X25_NUI_TYPE);
}
/// Add `ascend_x25_nui` string value to a packet.
pub fn add_ascend_x25_nui(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_NUI_TYPE, value));
}
/// Lookup a `ascend_x25_nui` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_nui`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_nui(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_X25_NUI_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_nui` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_nui(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_NUI_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_RPOA_TYPE: AVPType = 41;
/// Delete all of `ascend_x25_rpoa` values from a packet.
pub fn delete_ascend_x25_rpoa(packet: &mut Packet) {
    packet.delete(ASCEND_X25_RPOA_TYPE);
}
/// Add `ascend_x25_rpoa` string value to a packet.
pub fn add_ascend_x25_rpoa(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_RPOA_TYPE, value));
}
/// Lookup a `ascend_x25_rpoa` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_rpoa`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_rpoa(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_X25_RPOA_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_rpoa` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_rpoa(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_RPOA_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_PAD_PROMPT_TYPE: AVPType = 42;
/// Delete all of `ascend_x25_pad_prompt` values from a packet.
pub fn delete_ascend_x25_pad_prompt(packet: &mut Packet) {
    packet.delete(ASCEND_X25_PAD_PROMPT_TYPE);
}
/// Add `ascend_x25_pad_prompt` string value to a packet.
pub fn add_ascend_x25_pad_prompt(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_PAD_PROMPT_TYPE, value));
}
/// Lookup a `ascend_x25_pad_prompt` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_pad_prompt`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_pad_prompt(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_X25_PAD_PROMPT_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_pad_prompt` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_pad_prompt(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_PAD_PROMPT_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_PAD_BANNER_TYPE: AVPType = 43;
/// Delete all of `ascend_x25_pad_banner` values from a packet.
pub fn delete_ascend_x25_pad_banner(packet: &mut Packet) {
    packet.delete(ASCEND_X25_PAD_BANNER_TYPE);
}
/// Add `ascend_x25_pad_banner` string value to a packet.
pub fn add_ascend_x25_pad_banner(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_PAD_BANNER_TYPE, value));
}
/// Lookup a `ascend_x25_pad_banner` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_pad_banner`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_pad_banner(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_X25_PAD_BANNER_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_pad_banner` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_pad_banner(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_PAD_BANNER_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_X25_PROFILE_NAME_TYPE: AVPType = 44;
/// Delete all of `ascend_x25_profile_name` values from a packet.
pub fn delete_ascend_x25_profile_name(packet: &mut Packet) {
    packet.delete(ASCEND_X25_PROFILE_NAME_TYPE);
}
/// Add `ascend_x25_profile_name` string value to a packet.
pub fn add_ascend_x25_profile_name(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_X25_PROFILE_NAME_TYPE, value));
}
/// Lookup a `ascend_x25_profile_name` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_x25_profile_name`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_x25_profile_name(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_X25_PROFILE_NAME_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_x25_profile_name` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_x25_profile_name(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_X25_PROFILE_NAME_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_RECV_NAME_TYPE: AVPType = 45;
/// Delete all of `ascend_recv_name` values from a packet.
pub fn delete_ascend_recv_name(packet: &mut Packet) {
    packet.delete(ASCEND_RECV_NAME_TYPE);
}
/// Add `ascend_recv_name` string value to a packet.
pub fn add_ascend_recv_name(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_RECV_NAME_TYPE, value));
}
/// Lookup a `ascend_recv_name` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_recv_name`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_recv_name(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_RECV_NAME_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_recv_name` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_recv_name(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_RECV_NAME_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_BI_DIRECTIONAL_AUTH_TYPE: AVPType = 46;
/// Delete all of `ascend_bi_directional_auth` values from a packet.
pub fn delete_ascend_bi_directional_auth(packet: &mut Packet) {
    packet.delete(ASCEND_BI_DIRECTIONAL_AUTH_TYPE);
}
/// Add `ascend_bi_directional_auth` value-defined integer value to a packet.
pub fn add_ascend_bi_directional_auth(packet: &mut Packet, value: AscendBiDirectionalAuth) {
    packet.add(AVP::from_u32(ASCEND_BI_DIRECTIONAL_AUTH_TYPE, value));
}
/// Lookup a `ascend_bi_directional_auth` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_bi_directional_auth`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_bi_directional_auth(
    packet: &Packet,
) -> Option<Result<AscendBiDirectionalAuth, AVPError>> {
    packet
        .lookup(ASCEND_BI_DIRECTIONAL_AUTH_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendBiDirectionalAuth))
}
/// Lookup all of the `ascend_bi_directional_auth` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_bi_directional_auth(
    packet: &Packet,
) -> Result<Vec<AscendBiDirectionalAuth>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BI_DIRECTIONAL_AUTH_TYPE) {
        vec.push(avp.encode_u32()? as AscendBiDirectionalAuth);
    }
    Ok(vec)
}

pub const ASCEND_MTU_TYPE: AVPType = 47;
/// Delete all of `ascend_mtu` values from a packet.
pub fn delete_ascend_mtu(packet: &mut Packet) {
    packet.delete(ASCEND_MTU_TYPE);
}
/// Add `ascend_mtu` integer value to a packet.
pub fn add_ascend_mtu(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MTU_TYPE, value));
}
/// Lookup a `ascend_mtu` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_mtu`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_mtu(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_MTU_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_mtu` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_mtu(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MTU_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_CALL_DIRECTION_TYPE: AVPType = 48;
/// Delete all of `ascend_call_direction` values from a packet.
pub fn delete_ascend_call_direction(packet: &mut Packet) {
    packet.delete(ASCEND_CALL_DIRECTION_TYPE);
}
/// Add `ascend_call_direction` value-defined integer value to a packet.
pub fn add_ascend_call_direction(packet: &mut Packet, value: AscendCallDirection) {
    packet.add(AVP::from_u32(ASCEND_CALL_DIRECTION_TYPE, value));
}
/// Lookup a `ascend_call_direction` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_call_direction`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_call_direction(
    packet: &Packet,
) -> Option<Result<AscendCallDirection, AVPError>> {
    packet
        .lookup(ASCEND_CALL_DIRECTION_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCallDirection))
}
/// Lookup all of the `ascend_call_direction` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_call_direction(
    packet: &Packet,
) -> Result<Vec<AscendCallDirection>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALL_DIRECTION_TYPE) {
        vec.push(avp.encode_u32()? as AscendCallDirection);
    }
    Ok(vec)
}

pub const ASCEND_SERVICE_TYPE_TYPE: AVPType = 49;
/// Delete all of `ascend_service_type` values from a packet.
pub fn delete_ascend_service_type(packet: &mut Packet) {
    packet.delete(ASCEND_SERVICE_TYPE_TYPE);
}
/// Add `ascend_service_type` value-defined integer value to a packet.
pub fn add_ascend_service_type(packet: &mut Packet, value: AscendServiceType) {
    packet.add(AVP::from_u32(ASCEND_SERVICE_TYPE_TYPE, value));
}
/// Lookup a `ascend_service_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_service_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_service_type(packet: &Packet) -> Option<Result<AscendServiceType, AVPError>> {
    packet
        .lookup(ASCEND_SERVICE_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendServiceType))
}
/// Lookup all of the `ascend_service_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_service_type(packet: &Packet) -> Result<Vec<AscendServiceType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SERVICE_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendServiceType);
    }
    Ok(vec)
}

pub const ASCEND_FILTER_REQUIRED_TYPE: AVPType = 50;
/// Delete all of `ascend_filter_required` values from a packet.
pub fn delete_ascend_filter_required(packet: &mut Packet) {
    packet.delete(ASCEND_FILTER_REQUIRED_TYPE);
}
/// Add `ascend_filter_required` value-defined integer value to a packet.
pub fn add_ascend_filter_required(packet: &mut Packet, value: AscendFilterRequired) {
    packet.add(AVP::from_u32(ASCEND_FILTER_REQUIRED_TYPE, value));
}
/// Lookup a `ascend_filter_required` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_filter_required`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_filter_required(
    packet: &Packet,
) -> Option<Result<AscendFilterRequired, AVPError>> {
    packet
        .lookup(ASCEND_FILTER_REQUIRED_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendFilterRequired))
}
/// Lookup all of the `ascend_filter_required` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_filter_required(
    packet: &Packet,
) -> Result<Vec<AscendFilterRequired>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FILTER_REQUIRED_TYPE) {
        vec.push(avp.encode_u32()? as AscendFilterRequired);
    }
    Ok(vec)
}

pub const ASCEND_TRAFFIC_SHAPER_TYPE: AVPType = 51;
/// Delete all of `ascend_traffic_shaper` values from a packet.
pub fn delete_ascend_traffic_shaper(packet: &mut Packet) {
    packet.delete(ASCEND_TRAFFIC_SHAPER_TYPE);
}
/// Add `ascend_traffic_shaper` integer value to a packet.
pub fn add_ascend_traffic_shaper(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_TRAFFIC_SHAPER_TYPE, value));
}
/// Lookup a `ascend_traffic_shaper` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_traffic_shaper`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_traffic_shaper(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_TRAFFIC_SHAPER_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_traffic_shaper` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_traffic_shaper(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TRAFFIC_SHAPER_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_ACCESS_INTERCEPT_LEA_TYPE: AVPType = 52;
/// Delete all of `ascend_access_intercept_lea` values from a packet.
pub fn delete_ascend_access_intercept_lea(packet: &mut Packet) {
    packet.delete(ASCEND_ACCESS_INTERCEPT_LEA_TYPE);
}
/// Add `ascend_access_intercept_lea` string value to a packet.
pub fn add_ascend_access_intercept_lea(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_ACCESS_INTERCEPT_LEA_TYPE, value));
}
/// Lookup a `ascend_access_intercept_lea` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_access_intercept_lea`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_access_intercept_lea(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_ACCESS_INTERCEPT_LEA_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_access_intercept_lea` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_access_intercept_lea(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ACCESS_INTERCEPT_LEA_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_ACCESS_INTERCEPT_LOG_TYPE: AVPType = 53;
/// Delete all of `ascend_access_intercept_log` values from a packet.
pub fn delete_ascend_access_intercept_log(packet: &mut Packet) {
    packet.delete(ASCEND_ACCESS_INTERCEPT_LOG_TYPE);
}
/// Add `ascend_access_intercept_log` string value to a packet.
pub fn add_ascend_access_intercept_log(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_ACCESS_INTERCEPT_LOG_TYPE, value));
}
/// Lookup a `ascend_access_intercept_log` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_access_intercept_log`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_access_intercept_log(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_ACCESS_INTERCEPT_LOG_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_access_intercept_log` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_access_intercept_log(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ACCESS_INTERCEPT_LOG_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_PRIVATE_ROUTE_TABLE_ID_TYPE: AVPType = 54;
/// Delete all of `ascend_private_route_table_id` values from a packet.
pub fn delete_ascend_private_route_table_id(packet: &mut Packet) {
    packet.delete(ASCEND_PRIVATE_ROUTE_TABLE_ID_TYPE);
}
/// Add `ascend_private_route_table_id` string value to a packet.
pub fn add_ascend_private_route_table_id(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_PRIVATE_ROUTE_TABLE_ID_TYPE, value));
}
/// Lookup a `ascend_private_route_table_id` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_private_route_table_id`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_private_route_table_id(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_PRIVATE_ROUTE_TABLE_ID_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_private_route_table_id` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_private_route_table_id(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PRIVATE_ROUTE_TABLE_ID_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_PRIVATE_ROUTE_REQUIRED_TYPE: AVPType = 55;
/// Delete all of `ascend_private_route_required` values from a packet.
pub fn delete_ascend_private_route_required(packet: &mut Packet) {
    packet.delete(ASCEND_PRIVATE_ROUTE_REQUIRED_TYPE);
}
/// Add `ascend_private_route_required` value-defined integer value to a packet.
pub fn add_ascend_private_route_required(packet: &mut Packet, value: AscendPrivateRouteRequired) {
    packet.add(AVP::from_u32(ASCEND_PRIVATE_ROUTE_REQUIRED_TYPE, value));
}
/// Lookup a `ascend_private_route_required` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_private_route_required`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_private_route_required(
    packet: &Packet,
) -> Option<Result<AscendPrivateRouteRequired, AVPError>> {
    packet
        .lookup(ASCEND_PRIVATE_ROUTE_REQUIRED_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendPrivateRouteRequired))
}
/// Lookup all of the `ascend_private_route_required` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_private_route_required(
    packet: &Packet,
) -> Result<Vec<AscendPrivateRouteRequired>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PRIVATE_ROUTE_REQUIRED_TYPE) {
        vec.push(avp.encode_u32()? as AscendPrivateRouteRequired);
    }
    Ok(vec)
}

pub const ASCEND_CACHE_REFRESH_TYPE: AVPType = 56;
/// Delete all of `ascend_cache_refresh` values from a packet.
pub fn delete_ascend_cache_refresh(packet: &mut Packet) {
    packet.delete(ASCEND_CACHE_REFRESH_TYPE);
}
/// Add `ascend_cache_refresh` value-defined integer value to a packet.
pub fn add_ascend_cache_refresh(packet: &mut Packet, value: AscendCacheRefresh) {
    packet.add(AVP::from_u32(ASCEND_CACHE_REFRESH_TYPE, value));
}
/// Lookup a `ascend_cache_refresh` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_cache_refresh`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_cache_refresh(
    packet: &Packet,
) -> Option<Result<AscendCacheRefresh, AVPError>> {
    packet
        .lookup(ASCEND_CACHE_REFRESH_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCacheRefresh))
}
/// Lookup all of the `ascend_cache_refresh` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_cache_refresh(
    packet: &Packet,
) -> Result<Vec<AscendCacheRefresh>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CACHE_REFRESH_TYPE) {
        vec.push(avp.encode_u32()? as AscendCacheRefresh);
    }
    Ok(vec)
}

pub const ASCEND_CACHE_TIME_TYPE: AVPType = 57;
/// Delete all of `ascend_cache_time` values from a packet.
pub fn delete_ascend_cache_time(packet: &mut Packet) {
    packet.delete(ASCEND_CACHE_TIME_TYPE);
}
/// Add `ascend_cache_time` integer value to a packet.
pub fn add_ascend_cache_time(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_CACHE_TIME_TYPE, value));
}
/// Lookup a `ascend_cache_time` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_cache_time`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_cache_time(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_CACHE_TIME_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_cache_time` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_cache_time(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CACHE_TIME_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_EGRESS_ENABLED_TYPE: AVPType = 58;
/// Delete all of `ascend_egress_enabled` values from a packet.
pub fn delete_ascend_egress_enabled(packet: &mut Packet) {
    packet.delete(ASCEND_EGRESS_ENABLED_TYPE);
}
/// Add `ascend_egress_enabled` integer value to a packet.
pub fn add_ascend_egress_enabled(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_EGRESS_ENABLED_TYPE, value));
}
/// Lookup a `ascend_egress_enabled` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_egress_enabled`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_egress_enabled(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_EGRESS_ENABLED_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_egress_enabled` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_egress_enabled(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_EGRESS_ENABLED_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_QOS_UPSTREAM_TYPE: AVPType = 59;
/// Delete all of `ascend_qos_upstream` values from a packet.
pub fn delete_ascend_qos_upstream(packet: &mut Packet) {
    packet.delete(ASCEND_QOS_UPSTREAM_TYPE);
}
/// Add `ascend_qos_upstream` string value to a packet.
pub fn add_ascend_qos_upstream(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_QOS_UPSTREAM_TYPE, value));
}
/// Lookup a `ascend_qos_upstream` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_qos_upstream`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_qos_upstream(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_QOS_UPSTREAM_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_qos_upstream` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_qos_upstream(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_QOS_UPSTREAM_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_QOS_DOWNSTREAM_TYPE: AVPType = 60;
/// Delete all of `ascend_qos_downstream` values from a packet.
pub fn delete_ascend_qos_downstream(packet: &mut Packet) {
    packet.delete(ASCEND_QOS_DOWNSTREAM_TYPE);
}
/// Add `ascend_qos_downstream` string value to a packet.
pub fn add_ascend_qos_downstream(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_QOS_DOWNSTREAM_TYPE, value));
}
/// Lookup a `ascend_qos_downstream` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_qos_downstream`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_qos_downstream(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_QOS_DOWNSTREAM_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_qos_downstream` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_qos_downstream(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_QOS_DOWNSTREAM_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_ATM_CONNECT_VPI_TYPE: AVPType = 61;
/// Delete all of `ascend_atm_connect_vpi` values from a packet.
pub fn delete_ascend_atm_connect_vpi(packet: &mut Packet) {
    packet.delete(ASCEND_ATM_CONNECT_VPI_TYPE);
}
/// Add `ascend_atm_connect_vpi` integer value to a packet.
pub fn add_ascend_atm_connect_vpi(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_ATM_CONNECT_VPI_TYPE, value));
}
/// Lookup a `ascend_atm_connect_vpi` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_atm_connect_vpi`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_atm_connect_vpi(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_ATM_CONNECT_VPI_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_atm_connect_vpi` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_atm_connect_vpi(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ATM_CONNECT_VPI_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_ATM_CONNECT_VCI_TYPE: AVPType = 62;
/// Delete all of `ascend_atm_connect_vci` values from a packet.
pub fn delete_ascend_atm_connect_vci(packet: &mut Packet) {
    packet.delete(ASCEND_ATM_CONNECT_VCI_TYPE);
}
/// Add `ascend_atm_connect_vci` integer value to a packet.
pub fn add_ascend_atm_connect_vci(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_ATM_CONNECT_VCI_TYPE, value));
}
/// Lookup a `ascend_atm_connect_vci` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_atm_connect_vci`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_atm_connect_vci(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_ATM_CONNECT_VCI_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_atm_connect_vci` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_atm_connect_vci(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ATM_CONNECT_VCI_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_ATM_CONNECT_GROUP_TYPE: AVPType = 63;
/// Delete all of `ascend_atm_connect_group` values from a packet.
pub fn delete_ascend_atm_connect_group(packet: &mut Packet) {
    packet.delete(ASCEND_ATM_CONNECT_GROUP_TYPE);
}
/// Add `ascend_atm_connect_group` integer value to a packet.
pub fn add_ascend_atm_connect_group(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_ATM_CONNECT_GROUP_TYPE, value));
}
/// Lookup a `ascend_atm_connect_group` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_atm_connect_group`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_atm_connect_group(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_ATM_CONNECT_GROUP_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_atm_connect_group` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_atm_connect_group(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ATM_CONNECT_GROUP_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_ATM_GROUP_TYPE: AVPType = 64;
/// Delete all of `ascend_atm_group` values from a packet.
pub fn delete_ascend_atm_group(packet: &mut Packet) {
    packet.delete(ASCEND_ATM_GROUP_TYPE);
}
/// Add `ascend_atm_group` integer value to a packet.
pub fn add_ascend_atm_group(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_ATM_GROUP_TYPE, value));
}
/// Lookup a `ascend_atm_group` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_atm_group`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_atm_group(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_ATM_GROUP_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_atm_group` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_atm_group(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ATM_GROUP_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_IPX_HEADER_COMPRESSION_TYPE: AVPType = 65;
/// Delete all of `ascend_ipx_header_compression` values from a packet.
pub fn delete_ascend_ipx_header_compression(packet: &mut Packet) {
    packet.delete(ASCEND_IPX_HEADER_COMPRESSION_TYPE);
}
/// Add `ascend_ipx_header_compression` value-defined integer value to a packet.
pub fn add_ascend_ipx_header_compression(packet: &mut Packet, value: AscendIPXHeaderCompression) {
    packet.add(AVP::from_u32(ASCEND_IPX_HEADER_COMPRESSION_TYPE, value));
}
/// Lookup a `ascend_ipx_header_compression` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ipx_header_compression`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ipx_header_compression(
    packet: &Packet,
) -> Option<Result<AscendIPXHeaderCompression, AVPError>> {
    packet
        .lookup(ASCEND_IPX_HEADER_COMPRESSION_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendIPXHeaderCompression))
}
/// Lookup all of the `ascend_ipx_header_compression` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ipx_header_compression(
    packet: &Packet,
) -> Result<Vec<AscendIPXHeaderCompression>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IPX_HEADER_COMPRESSION_TYPE) {
        vec.push(avp.encode_u32()? as AscendIPXHeaderCompression);
    }
    Ok(vec)
}

pub const ASCEND_CALLING_ID_TYPE_OF_NUM_TYPE: AVPType = 66;
/// Delete all of `ascend_calling_id_type_of_num` values from a packet.
pub fn delete_ascend_calling_id_type_of_num(packet: &mut Packet) {
    packet.delete(ASCEND_CALLING_ID_TYPE_OF_NUM_TYPE);
}
/// Add `ascend_calling_id_type_of_num` value-defined integer value to a packet.
pub fn add_ascend_calling_id_type_of_num(packet: &mut Packet, value: AscendCallingIdTypeOfNum) {
    packet.add(AVP::from_u32(ASCEND_CALLING_ID_TYPE_OF_NUM_TYPE, value));
}
/// Lookup a `ascend_calling_id_type_of_num` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_calling_id_type_of_num`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_calling_id_type_of_num(
    packet: &Packet,
) -> Option<Result<AscendCallingIdTypeOfNum, AVPError>> {
    packet
        .lookup(ASCEND_CALLING_ID_TYPE_OF_NUM_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCallingIdTypeOfNum))
}
/// Lookup all of the `ascend_calling_id_type_of_num` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_calling_id_type_of_num(
    packet: &Packet,
) -> Result<Vec<AscendCallingIdTypeOfNum>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALLING_ID_TYPE_OF_NUM_TYPE) {
        vec.push(avp.encode_u32()? as AscendCallingIdTypeOfNum);
    }
    Ok(vec)
}

pub const ASCEND_CALLING_ID_NUMBER_PLAN_TYPE: AVPType = 67;
/// Delete all of `ascend_calling_id_number_plan` values from a packet.
pub fn delete_ascend_calling_id_number_plan(packet: &mut Packet) {
    packet.delete(ASCEND_CALLING_ID_NUMBER_PLAN_TYPE);
}
/// Add `ascend_calling_id_number_plan` value-defined integer value to a packet.
pub fn add_ascend_calling_id_number_plan(packet: &mut Packet, value: AscendCallingIdNumberPlan) {
    packet.add(AVP::from_u32(ASCEND_CALLING_ID_NUMBER_PLAN_TYPE, value));
}
/// Lookup a `ascend_calling_id_number_plan` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_calling_id_number_plan`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_calling_id_number_plan(
    packet: &Packet,
) -> Option<Result<AscendCallingIdNumberPlan, AVPError>> {
    packet
        .lookup(ASCEND_CALLING_ID_NUMBER_PLAN_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCallingIdNumberPlan))
}
/// Lookup all of the `ascend_calling_id_number_plan` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_calling_id_number_plan(
    packet: &Packet,
) -> Result<Vec<AscendCallingIdNumberPlan>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALLING_ID_NUMBER_PLAN_TYPE) {
        vec.push(avp.encode_u32()? as AscendCallingIdNumberPlan);
    }
    Ok(vec)
}

pub const ASCEND_CALLING_ID_PRESENTATN_TYPE: AVPType = 68;
/// Delete all of `ascend_calling_id_presentatn` values from a packet.
pub fn delete_ascend_calling_id_presentatn(packet: &mut Packet) {
    packet.delete(ASCEND_CALLING_ID_PRESENTATN_TYPE);
}
/// Add `ascend_calling_id_presentatn` value-defined integer value to a packet.
pub fn add_ascend_calling_id_presentatn(packet: &mut Packet, value: AscendCallingIdPresentatn) {
    packet.add(AVP::from_u32(ASCEND_CALLING_ID_PRESENTATN_TYPE, value));
}
/// Lookup a `ascend_calling_id_presentatn` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_calling_id_presentatn`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_calling_id_presentatn(
    packet: &Packet,
) -> Option<Result<AscendCallingIdPresentatn, AVPError>> {
    packet
        .lookup(ASCEND_CALLING_ID_PRESENTATN_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCallingIdPresentatn))
}
/// Lookup all of the `ascend_calling_id_presentatn` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_calling_id_presentatn(
    packet: &Packet,
) -> Result<Vec<AscendCallingIdPresentatn>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALLING_ID_PRESENTATN_TYPE) {
        vec.push(avp.encode_u32()? as AscendCallingIdPresentatn);
    }
    Ok(vec)
}

pub const ASCEND_CALLING_ID_SCREENING_TYPE: AVPType = 69;
/// Delete all of `ascend_calling_id_screening` values from a packet.
pub fn delete_ascend_calling_id_screening(packet: &mut Packet) {
    packet.delete(ASCEND_CALLING_ID_SCREENING_TYPE);
}
/// Add `ascend_calling_id_screening` value-defined integer value to a packet.
pub fn add_ascend_calling_id_screening(packet: &mut Packet, value: AscendCallingIdScreening) {
    packet.add(AVP::from_u32(ASCEND_CALLING_ID_SCREENING_TYPE, value));
}
/// Lookup a `ascend_calling_id_screening` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_calling_id_screening`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_calling_id_screening(
    packet: &Packet,
) -> Option<Result<AscendCallingIdScreening, AVPError>> {
    packet
        .lookup(ASCEND_CALLING_ID_SCREENING_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCallingIdScreening))
}
/// Lookup all of the `ascend_calling_id_screening` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_calling_id_screening(
    packet: &Packet,
) -> Result<Vec<AscendCallingIdScreening>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALLING_ID_SCREENING_TYPE) {
        vec.push(avp.encode_u32()? as AscendCallingIdScreening);
    }
    Ok(vec)
}

pub const ASCEND_BIR_ENABLE_TYPE: AVPType = 70;
/// Delete all of `ascend_bir_enable` values from a packet.
pub fn delete_ascend_bir_enable(packet: &mut Packet) {
    packet.delete(ASCEND_BIR_ENABLE_TYPE);
}
/// Add `ascend_bir_enable` value-defined integer value to a packet.
pub fn add_ascend_bir_enable(packet: &mut Packet, value: AscendBIREnable) {
    packet.add(AVP::from_u32(ASCEND_BIR_ENABLE_TYPE, value));
}
/// Lookup a `ascend_bir_enable` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_bir_enable`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_bir_enable(packet: &Packet) -> Option<Result<AscendBIREnable, AVPError>> {
    packet
        .lookup(ASCEND_BIR_ENABLE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendBIREnable))
}
/// Lookup all of the `ascend_bir_enable` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_bir_enable(packet: &Packet) -> Result<Vec<AscendBIREnable>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BIR_ENABLE_TYPE) {
        vec.push(avp.encode_u32()? as AscendBIREnable);
    }
    Ok(vec)
}

pub const ASCEND_BIR_PROXY_TYPE: AVPType = 71;
/// Delete all of `ascend_bir_proxy` values from a packet.
pub fn delete_ascend_bir_proxy(packet: &mut Packet) {
    packet.delete(ASCEND_BIR_PROXY_TYPE);
}
/// Add `ascend_bir_proxy` value-defined integer value to a packet.
pub fn add_ascend_bir_proxy(packet: &mut Packet, value: AscendBIRProxy) {
    packet.add(AVP::from_u32(ASCEND_BIR_PROXY_TYPE, value));
}
/// Lookup a `ascend_bir_proxy` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_bir_proxy`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_bir_proxy(packet: &Packet) -> Option<Result<AscendBIRProxy, AVPError>> {
    packet
        .lookup(ASCEND_BIR_PROXY_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendBIRProxy))
}
/// Lookup all of the `ascend_bir_proxy` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_bir_proxy(packet: &Packet) -> Result<Vec<AscendBIRProxy>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BIR_PROXY_TYPE) {
        vec.push(avp.encode_u32()? as AscendBIRProxy);
    }
    Ok(vec)
}

pub const ASCEND_BIR_BRIDGE_GROUP_TYPE: AVPType = 72;
/// Delete all of `ascend_bir_bridge_group` values from a packet.
pub fn delete_ascend_bir_bridge_group(packet: &mut Packet) {
    packet.delete(ASCEND_BIR_BRIDGE_GROUP_TYPE);
}
/// Add `ascend_bir_bridge_group` integer value to a packet.
pub fn add_ascend_bir_bridge_group(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_BIR_BRIDGE_GROUP_TYPE, value));
}
/// Lookup a `ascend_bir_bridge_group` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_bir_bridge_group`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_bir_bridge_group(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_BIR_BRIDGE_GROUP_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_bir_bridge_group` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_bir_bridge_group(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BIR_BRIDGE_GROUP_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_IPSEC_PROFILE_TYPE: AVPType = 73;
/// Delete all of `ascend_ipsec_profile` values from a packet.
pub fn delete_ascend_ipsec_profile(packet: &mut Packet) {
    packet.delete(ASCEND_IPSEC_PROFILE_TYPE);
}
/// Add `ascend_ipsec_profile` string value to a packet.
pub fn add_ascend_ipsec_profile(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_IPSEC_PROFILE_TYPE, value));
}
/// Lookup a `ascend_ipsec_profile` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ipsec_profile`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ipsec_profile(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_IPSEC_PROFILE_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_ipsec_profile` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ipsec_profile(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IPSEC_PROFILE_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_PP_PO_E_ENABLE_TYPE: AVPType = 74;
/// Delete all of `ascend_pp_po_e_enable` values from a packet.
pub fn delete_ascend_pp_po_e_enable(packet: &mut Packet) {
    packet.delete(ASCEND_PP_PO_E_ENABLE_TYPE);
}
/// Add `ascend_pp_po_e_enable` value-defined integer value to a packet.
pub fn add_ascend_pp_po_e_enable(packet: &mut Packet, value: AscendPPPoEEnable) {
    packet.add(AVP::from_u32(ASCEND_PP_PO_E_ENABLE_TYPE, value));
}
/// Lookup a `ascend_pp_po_e_enable` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_pp_po_e_enable`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_pp_po_e_enable(
    packet: &Packet,
) -> Option<Result<AscendPPPoEEnable, AVPError>> {
    packet
        .lookup(ASCEND_PP_PO_E_ENABLE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendPPPoEEnable))
}
/// Lookup all of the `ascend_pp_po_e_enable` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_pp_po_e_enable(
    packet: &Packet,
) -> Result<Vec<AscendPPPoEEnable>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PP_PO_E_ENABLE_TYPE) {
        vec.push(avp.encode_u32()? as AscendPPPoEEnable);
    }
    Ok(vec)
}

pub const ASCEND_BRIDGE_NON_PP_PO_E_TYPE: AVPType = 75;
/// Delete all of `ascend_bridge_non_pp_po_e` values from a packet.
pub fn delete_ascend_bridge_non_pp_po_e(packet: &mut Packet) {
    packet.delete(ASCEND_BRIDGE_NON_PP_PO_E_TYPE);
}
/// Add `ascend_bridge_non_pp_po_e` value-defined integer value to a packet.
pub fn add_ascend_bridge_non_pp_po_e(packet: &mut Packet, value: AscendBridgeNonPPPoE) {
    packet.add(AVP::from_u32(ASCEND_BRIDGE_NON_PP_PO_E_TYPE, value));
}
/// Lookup a `ascend_bridge_non_pp_po_e` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_bridge_non_pp_po_e`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_bridge_non_pp_po_e(
    packet: &Packet,
) -> Option<Result<AscendBridgeNonPPPoE, AVPError>> {
    packet
        .lookup(ASCEND_BRIDGE_NON_PP_PO_E_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendBridgeNonPPPoE))
}
/// Lookup all of the `ascend_bridge_non_pp_po_e` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_bridge_non_pp_po_e(
    packet: &Packet,
) -> Result<Vec<AscendBridgeNonPPPoE>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BRIDGE_NON_PP_PO_E_TYPE) {
        vec.push(avp.encode_u32()? as AscendBridgeNonPPPoE);
    }
    Ok(vec)
}

pub const ASCEND_ATM_DIRECT_TYPE: AVPType = 76;
/// Delete all of `ascend_atm_direct` values from a packet.
pub fn delete_ascend_atm_direct(packet: &mut Packet) {
    packet.delete(ASCEND_ATM_DIRECT_TYPE);
}
/// Add `ascend_atm_direct` value-defined integer value to a packet.
pub fn add_ascend_atm_direct(packet: &mut Packet, value: AscendATMDirect) {
    packet.add(AVP::from_u32(ASCEND_ATM_DIRECT_TYPE, value));
}
/// Lookup a `ascend_atm_direct` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_atm_direct`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_atm_direct(packet: &Packet) -> Option<Result<AscendATMDirect, AVPError>> {
    packet
        .lookup(ASCEND_ATM_DIRECT_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendATMDirect))
}
/// Lookup all of the `ascend_atm_direct` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_atm_direct(packet: &Packet) -> Result<Vec<AscendATMDirect>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ATM_DIRECT_TYPE) {
        vec.push(avp.encode_u32()? as AscendATMDirect);
    }
    Ok(vec)
}

pub const ASCEND_ATM_DIRECT_PROFILE_TYPE: AVPType = 77;
/// Delete all of `ascend_atm_direct_profile` values from a packet.
pub fn delete_ascend_atm_direct_profile(packet: &mut Packet) {
    packet.delete(ASCEND_ATM_DIRECT_PROFILE_TYPE);
}
/// Add `ascend_atm_direct_profile` string value to a packet.
pub fn add_ascend_atm_direct_profile(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_ATM_DIRECT_PROFILE_TYPE, value));
}
/// Lookup a `ascend_atm_direct_profile` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_atm_direct_profile`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_atm_direct_profile(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_ATM_DIRECT_PROFILE_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_atm_direct_profile` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_atm_direct_profile(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ATM_DIRECT_PROFILE_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_CLIENT_PRIMARY_WINS_TYPE: AVPType = 78;
/// Delete all of `ascend_client_primary_wins` values from a packet.
pub fn delete_ascend_client_primary_wins(packet: &mut Packet) {
    packet.delete(ASCEND_CLIENT_PRIMARY_WINS_TYPE);
}
/// Add `ascend_client_primary_wins` ipaddr value to a packet.
pub fn add_ascend_client_primary_wins(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_CLIENT_PRIMARY_WINS_TYPE, value));
}
/// Lookup a `ascend_client_primary_wins` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_client_primary_wins`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_client_primary_wins(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_CLIENT_PRIMARY_WINS_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_client_primary_wins` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_client_primary_wins(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CLIENT_PRIMARY_WINS_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_CLIENT_SECONDARY_WINS_TYPE: AVPType = 79;
/// Delete all of `ascend_client_secondary_wins` values from a packet.
pub fn delete_ascend_client_secondary_wins(packet: &mut Packet) {
    packet.delete(ASCEND_CLIENT_SECONDARY_WINS_TYPE);
}
/// Add `ascend_client_secondary_wins` ipaddr value to a packet.
pub fn add_ascend_client_secondary_wins(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_CLIENT_SECONDARY_WINS_TYPE, value));
}
/// Lookup a `ascend_client_secondary_wins` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_client_secondary_wins`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_client_secondary_wins(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_CLIENT_SECONDARY_WINS_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_client_secondary_wins` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_client_secondary_wins(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CLIENT_SECONDARY_WINS_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_CLIENT_ASSIGN_WINS_TYPE: AVPType = 80;
/// Delete all of `ascend_client_assign_wins` values from a packet.
pub fn delete_ascend_client_assign_wins(packet: &mut Packet) {
    packet.delete(ASCEND_CLIENT_ASSIGN_WINS_TYPE);
}
/// Add `ascend_client_assign_wins` value-defined integer value to a packet.
pub fn add_ascend_client_assign_wins(packet: &mut Packet, value: AscendClientAssignWINS) {
    packet.add(AVP::from_u32(ASCEND_CLIENT_ASSIGN_WINS_TYPE, value));
}
/// Lookup a `ascend_client_assign_wins` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_client_assign_wins`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_client_assign_wins(
    packet: &Packet,
) -> Option<Result<AscendClientAssignWINS, AVPError>> {
    packet
        .lookup(ASCEND_CLIENT_ASSIGN_WINS_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendClientAssignWINS))
}
/// Lookup all of the `ascend_client_assign_wins` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_client_assign_wins(
    packet: &Packet,
) -> Result<Vec<AscendClientAssignWINS>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CLIENT_ASSIGN_WINS_TYPE) {
        vec.push(avp.encode_u32()? as AscendClientAssignWINS);
    }
    Ok(vec)
}

pub const ASCEND_AUTH_TYPE_TYPE: AVPType = 81;
/// Delete all of `ascend_auth_type` values from a packet.
pub fn delete_ascend_auth_type(packet: &mut Packet) {
    packet.delete(ASCEND_AUTH_TYPE_TYPE);
}
/// Add `ascend_auth_type` value-defined integer value to a packet.
pub fn add_ascend_auth_type(packet: &mut Packet, value: AscendAuthType) {
    packet.add(AVP::from_u32(ASCEND_AUTH_TYPE_TYPE, value));
}
/// Lookup a `ascend_auth_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_auth_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_auth_type(packet: &Packet) -> Option<Result<AscendAuthType, AVPError>> {
    packet
        .lookup(ASCEND_AUTH_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendAuthType))
}
/// Lookup all of the `ascend_auth_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_auth_type(packet: &Packet) -> Result<Vec<AscendAuthType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_AUTH_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendAuthType);
    }
    Ok(vec)
}

pub const ASCEND_PORT_REDIR_PROTOCOL_TYPE: AVPType = 82;
/// Delete all of `ascend_port_redir_protocol` values from a packet.
pub fn delete_ascend_port_redir_protocol(packet: &mut Packet) {
    packet.delete(ASCEND_PORT_REDIR_PROTOCOL_TYPE);
}
/// Add `ascend_port_redir_protocol` value-defined integer value to a packet.
pub fn add_ascend_port_redir_protocol(packet: &mut Packet, value: AscendPortRedirProtocol) {
    packet.add(AVP::from_u32(ASCEND_PORT_REDIR_PROTOCOL_TYPE, value));
}
/// Lookup a `ascend_port_redir_protocol` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_port_redir_protocol`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_port_redir_protocol(
    packet: &Packet,
) -> Option<Result<AscendPortRedirProtocol, AVPError>> {
    packet
        .lookup(ASCEND_PORT_REDIR_PROTOCOL_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendPortRedirProtocol))
}
/// Lookup all of the `ascend_port_redir_protocol` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_port_redir_protocol(
    packet: &Packet,
) -> Result<Vec<AscendPortRedirProtocol>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PORT_REDIR_PROTOCOL_TYPE) {
        vec.push(avp.encode_u32()? as AscendPortRedirProtocol);
    }
    Ok(vec)
}

pub const ASCEND_PORT_REDIR_PORTNUM_TYPE: AVPType = 83;
/// Delete all of `ascend_port_redir_portnum` values from a packet.
pub fn delete_ascend_port_redir_portnum(packet: &mut Packet) {
    packet.delete(ASCEND_PORT_REDIR_PORTNUM_TYPE);
}
/// Add `ascend_port_redir_portnum` integer value to a packet.
pub fn add_ascend_port_redir_portnum(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_PORT_REDIR_PORTNUM_TYPE, value));
}
/// Lookup a `ascend_port_redir_portnum` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_port_redir_portnum`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_port_redir_portnum(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_PORT_REDIR_PORTNUM_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_port_redir_portnum` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_port_redir_portnum(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PORT_REDIR_PORTNUM_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_PORT_REDIR_SERVER_TYPE: AVPType = 84;
/// Delete all of `ascend_port_redir_server` values from a packet.
pub fn delete_ascend_port_redir_server(packet: &mut Packet) {
    packet.delete(ASCEND_PORT_REDIR_SERVER_TYPE);
}
/// Add `ascend_port_redir_server` ipaddr value to a packet.
pub fn add_ascend_port_redir_server(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_PORT_REDIR_SERVER_TYPE, value));
}
/// Lookup a `ascend_port_redir_server` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_port_redir_server`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_port_redir_server(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_PORT_REDIR_SERVER_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_port_redir_server` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_port_redir_server(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PORT_REDIR_SERVER_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_IP_POOL_CHAINING_TYPE: AVPType = 85;
/// Delete all of `ascend_ip_pool_chaining` values from a packet.
pub fn delete_ascend_ip_pool_chaining(packet: &mut Packet) {
    packet.delete(ASCEND_IP_POOL_CHAINING_TYPE);
}
/// Add `ascend_ip_pool_chaining` value-defined integer value to a packet.
pub fn add_ascend_ip_pool_chaining(packet: &mut Packet, value: AscendIPPoolChaining) {
    packet.add(AVP::from_u32(ASCEND_IP_POOL_CHAINING_TYPE, value));
}
/// Lookup a `ascend_ip_pool_chaining` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ip_pool_chaining`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ip_pool_chaining(
    packet: &Packet,
) -> Option<Result<AscendIPPoolChaining, AVPError>> {
    packet
        .lookup(ASCEND_IP_POOL_CHAINING_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendIPPoolChaining))
}
/// Lookup all of the `ascend_ip_pool_chaining` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ip_pool_chaining(
    packet: &Packet,
) -> Result<Vec<AscendIPPoolChaining>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IP_POOL_CHAINING_TYPE) {
        vec.push(avp.encode_u32()? as AscendIPPoolChaining);
    }
    Ok(vec)
}

pub const ASCEND_OWNER_IP_ADDR_TYPE: AVPType = 86;
/// Delete all of `ascend_owner_ip_addr` values from a packet.
pub fn delete_ascend_owner_ip_addr(packet: &mut Packet) {
    packet.delete(ASCEND_OWNER_IP_ADDR_TYPE);
}
/// Add `ascend_owner_ip_addr` ipaddr value to a packet.
pub fn add_ascend_owner_ip_addr(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_OWNER_IP_ADDR_TYPE, value));
}
/// Lookup a `ascend_owner_ip_addr` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_owner_ip_addr`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_owner_ip_addr(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_OWNER_IP_ADDR_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_owner_ip_addr` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_owner_ip_addr(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_OWNER_IP_ADDR_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_IP_TOS_TYPE: AVPType = 87;
/// Delete all of `ascend_ip_tos` values from a packet.
pub fn delete_ascend_ip_tos(packet: &mut Packet) {
    packet.delete(ASCEND_IP_TOS_TYPE);
}
/// Add `ascend_ip_tos` value-defined integer value to a packet.
pub fn add_ascend_ip_tos(packet: &mut Packet, value: AscendIPTOS) {
    packet.add(AVP::from_u32(ASCEND_IP_TOS_TYPE, value));
}
/// Lookup a `ascend_ip_tos` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ip_tos`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ip_tos(packet: &Packet) -> Option<Result<AscendIPTOS, AVPError>> {
    packet
        .lookup(ASCEND_IP_TOS_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendIPTOS))
}
/// Lookup all of the `ascend_ip_tos` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ip_tos(packet: &Packet) -> Result<Vec<AscendIPTOS>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IP_TOS_TYPE) {
        vec.push(avp.encode_u32()? as AscendIPTOS);
    }
    Ok(vec)
}

pub const ASCEND_IP_TOS_PRECEDENCE_TYPE: AVPType = 88;
/// Delete all of `ascend_ip_tos_precedence` values from a packet.
pub fn delete_ascend_ip_tos_precedence(packet: &mut Packet) {
    packet.delete(ASCEND_IP_TOS_PRECEDENCE_TYPE);
}
/// Add `ascend_ip_tos_precedence` value-defined integer value to a packet.
pub fn add_ascend_ip_tos_precedence(packet: &mut Packet, value: AscendIPTOSPrecedence) {
    packet.add(AVP::from_u32(ASCEND_IP_TOS_PRECEDENCE_TYPE, value));
}
/// Lookup a `ascend_ip_tos_precedence` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ip_tos_precedence`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ip_tos_precedence(
    packet: &Packet,
) -> Option<Result<AscendIPTOSPrecedence, AVPError>> {
    packet
        .lookup(ASCEND_IP_TOS_PRECEDENCE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendIPTOSPrecedence))
}
/// Lookup all of the `ascend_ip_tos_precedence` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ip_tos_precedence(
    packet: &Packet,
) -> Result<Vec<AscendIPTOSPrecedence>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IP_TOS_PRECEDENCE_TYPE) {
        vec.push(avp.encode_u32()? as AscendIPTOSPrecedence);
    }
    Ok(vec)
}

pub const ASCEND_IP_TOS_APPLY_TO_TYPE: AVPType = 89;
/// Delete all of `ascend_ip_tos_apply_to` values from a packet.
pub fn delete_ascend_ip_tos_apply_to(packet: &mut Packet) {
    packet.delete(ASCEND_IP_TOS_APPLY_TO_TYPE);
}
/// Add `ascend_ip_tos_apply_to` value-defined integer value to a packet.
pub fn add_ascend_ip_tos_apply_to(packet: &mut Packet, value: AscendIPTOSApplyTo) {
    packet.add(AVP::from_u32(ASCEND_IP_TOS_APPLY_TO_TYPE, value));
}
/// Lookup a `ascend_ip_tos_apply_to` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ip_tos_apply_to`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ip_tos_apply_to(
    packet: &Packet,
) -> Option<Result<AscendIPTOSApplyTo, AVPError>> {
    packet
        .lookup(ASCEND_IP_TOS_APPLY_TO_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendIPTOSApplyTo))
}
/// Lookup all of the `ascend_ip_tos_apply_to` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ip_tos_apply_to(
    packet: &Packet,
) -> Result<Vec<AscendIPTOSApplyTo>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IP_TOS_APPLY_TO_TYPE) {
        vec.push(avp.encode_u32()? as AscendIPTOSApplyTo);
    }
    Ok(vec)
}

pub const ASCEND_FILTER_TYPE: AVPType = 90;
/// Delete all of `ascend_filter` values from a packet.
pub fn delete_ascend_filter(packet: &mut Packet) {
    packet.delete(ASCEND_FILTER_TYPE);
}
/// Add `ascend_filter` string value to a packet.
pub fn add_ascend_filter(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_FILTER_TYPE, value));
}
/// Lookup a `ascend_filter` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_filter`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_filter(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_FILTER_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_filter` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_filter(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FILTER_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_TELNET_PROFILE_TYPE: AVPType = 91;
/// Delete all of `ascend_telnet_profile` values from a packet.
pub fn delete_ascend_telnet_profile(packet: &mut Packet) {
    packet.delete(ASCEND_TELNET_PROFILE_TYPE);
}
/// Add `ascend_telnet_profile` string value to a packet.
pub fn add_ascend_telnet_profile(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_TELNET_PROFILE_TYPE, value));
}
/// Lookup a `ascend_telnet_profile` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_telnet_profile`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_telnet_profile(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_TELNET_PROFILE_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_telnet_profile` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_telnet_profile(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TELNET_PROFILE_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_DSL_RATE_TYPE_TYPE: AVPType = 92;
/// Delete all of `ascend_dsl_rate_type` values from a packet.
pub fn delete_ascend_dsl_rate_type(packet: &mut Packet) {
    packet.delete(ASCEND_DSL_RATE_TYPE_TYPE);
}
/// Add `ascend_dsl_rate_type` value-defined integer value to a packet.
pub fn add_ascend_dsl_rate_type(packet: &mut Packet, value: AscendDslRateType) {
    packet.add(AVP::from_u32(ASCEND_DSL_RATE_TYPE_TYPE, value));
}
/// Lookup a `ascend_dsl_rate_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dsl_rate_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dsl_rate_type(packet: &Packet) -> Option<Result<AscendDslRateType, AVPError>> {
    packet
        .lookup(ASCEND_DSL_RATE_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendDslRateType))
}
/// Lookup all of the `ascend_dsl_rate_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dsl_rate_type(
    packet: &Packet,
) -> Result<Vec<AscendDslRateType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DSL_RATE_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendDslRateType);
    }
    Ok(vec)
}

pub const ASCEND_REDIRECT_NUMBER_TYPE: AVPType = 93;
/// Delete all of `ascend_redirect_number` values from a packet.
pub fn delete_ascend_redirect_number(packet: &mut Packet) {
    packet.delete(ASCEND_REDIRECT_NUMBER_TYPE);
}
/// Add `ascend_redirect_number` string value to a packet.
pub fn add_ascend_redirect_number(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_REDIRECT_NUMBER_TYPE, value));
}
/// Lookup a `ascend_redirect_number` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_redirect_number`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_redirect_number(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_REDIRECT_NUMBER_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_redirect_number` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_redirect_number(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_REDIRECT_NUMBER_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_ATM_VPI_TYPE: AVPType = 94;
/// Delete all of `ascend_atm_vpi` values from a packet.
pub fn delete_ascend_atm_vpi(packet: &mut Packet) {
    packet.delete(ASCEND_ATM_VPI_TYPE);
}
/// Add `ascend_atm_vpi` integer value to a packet.
pub fn add_ascend_atm_vpi(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_ATM_VPI_TYPE, value));
}
/// Lookup a `ascend_atm_vpi` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_atm_vpi`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_atm_vpi(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_ATM_VPI_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_atm_vpi` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_atm_vpi(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ATM_VPI_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_ATM_VCI_TYPE: AVPType = 95;
/// Delete all of `ascend_atm_vci` values from a packet.
pub fn delete_ascend_atm_vci(packet: &mut Packet) {
    packet.delete(ASCEND_ATM_VCI_TYPE);
}
/// Add `ascend_atm_vci` integer value to a packet.
pub fn add_ascend_atm_vci(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_ATM_VCI_TYPE, value));
}
/// Lookup a `ascend_atm_vci` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_atm_vci`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_atm_vci(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_ATM_VCI_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_atm_vci` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_atm_vci(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ATM_VCI_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_SOURCE_IP_CHECK_TYPE: AVPType = 96;
/// Delete all of `ascend_source_ip_check` values from a packet.
pub fn delete_ascend_source_ip_check(packet: &mut Packet) {
    packet.delete(ASCEND_SOURCE_IP_CHECK_TYPE);
}
/// Add `ascend_source_ip_check` value-defined integer value to a packet.
pub fn add_ascend_source_ip_check(packet: &mut Packet, value: AscendSourceIPCheck) {
    packet.add(AVP::from_u32(ASCEND_SOURCE_IP_CHECK_TYPE, value));
}
/// Lookup a `ascend_source_ip_check` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_source_ip_check`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_source_ip_check(
    packet: &Packet,
) -> Option<Result<AscendSourceIPCheck, AVPError>> {
    packet
        .lookup(ASCEND_SOURCE_IP_CHECK_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendSourceIPCheck))
}
/// Lookup all of the `ascend_source_ip_check` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_source_ip_check(
    packet: &Packet,
) -> Result<Vec<AscendSourceIPCheck>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SOURCE_IP_CHECK_TYPE) {
        vec.push(avp.encode_u32()? as AscendSourceIPCheck);
    }
    Ok(vec)
}

pub const ASCEND_DSL_RATE_MODE_TYPE: AVPType = 97;
/// Delete all of `ascend_dsl_rate_mode` values from a packet.
pub fn delete_ascend_dsl_rate_mode(packet: &mut Packet) {
    packet.delete(ASCEND_DSL_RATE_MODE_TYPE);
}
/// Add `ascend_dsl_rate_mode` value-defined integer value to a packet.
pub fn add_ascend_dsl_rate_mode(packet: &mut Packet, value: AscendDslRateMode) {
    packet.add(AVP::from_u32(ASCEND_DSL_RATE_MODE_TYPE, value));
}
/// Lookup a `ascend_dsl_rate_mode` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dsl_rate_mode`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dsl_rate_mode(packet: &Packet) -> Option<Result<AscendDslRateMode, AVPError>> {
    packet
        .lookup(ASCEND_DSL_RATE_MODE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendDslRateMode))
}
/// Lookup all of the `ascend_dsl_rate_mode` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dsl_rate_mode(
    packet: &Packet,
) -> Result<Vec<AscendDslRateMode>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DSL_RATE_MODE_TYPE) {
        vec.push(avp.encode_u32()? as AscendDslRateMode);
    }
    Ok(vec)
}

pub const ASCEND_DSL_UPSTREAM_LIMIT_TYPE: AVPType = 98;
/// Delete all of `ascend_dsl_upstream_limit` values from a packet.
pub fn delete_ascend_dsl_upstream_limit(packet: &mut Packet) {
    packet.delete(ASCEND_DSL_UPSTREAM_LIMIT_TYPE);
}
/// Add `ascend_dsl_upstream_limit` value-defined integer value to a packet.
pub fn add_ascend_dsl_upstream_limit(packet: &mut Packet, value: AscendDslUpstreamLimit) {
    packet.add(AVP::from_u32(ASCEND_DSL_UPSTREAM_LIMIT_TYPE, value));
}
/// Lookup a `ascend_dsl_upstream_limit` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dsl_upstream_limit`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dsl_upstream_limit(
    packet: &Packet,
) -> Option<Result<AscendDslUpstreamLimit, AVPError>> {
    packet
        .lookup(ASCEND_DSL_UPSTREAM_LIMIT_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendDslUpstreamLimit))
}
/// Lookup all of the `ascend_dsl_upstream_limit` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dsl_upstream_limit(
    packet: &Packet,
) -> Result<Vec<AscendDslUpstreamLimit>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DSL_UPSTREAM_LIMIT_TYPE) {
        vec.push(avp.encode_u32()? as AscendDslUpstreamLimit);
    }
    Ok(vec)
}

pub const ASCEND_DSL_DOWNSTREAM_LIMIT_TYPE: AVPType = 99;
/// Delete all of `ascend_dsl_downstream_limit` values from a packet.
pub fn delete_ascend_dsl_downstream_limit(packet: &mut Packet) {
    packet.delete(ASCEND_DSL_DOWNSTREAM_LIMIT_TYPE);
}
/// Add `ascend_dsl_downstream_limit` value-defined integer value to a packet.
pub fn add_ascend_dsl_downstream_limit(packet: &mut Packet, value: AscendDslDownstreamLimit) {
    packet.add(AVP::from_u32(ASCEND_DSL_DOWNSTREAM_LIMIT_TYPE, value));
}
/// Lookup a `ascend_dsl_downstream_limit` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dsl_downstream_limit`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dsl_downstream_limit(
    packet: &Packet,
) -> Option<Result<AscendDslDownstreamLimit, AVPError>> {
    packet
        .lookup(ASCEND_DSL_DOWNSTREAM_LIMIT_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendDslDownstreamLimit))
}
/// Lookup all of the `ascend_dsl_downstream_limit` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dsl_downstream_limit(
    packet: &Packet,
) -> Result<Vec<AscendDslDownstreamLimit>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DSL_DOWNSTREAM_LIMIT_TYPE) {
        vec.push(avp.encode_u32()? as AscendDslDownstreamLimit);
    }
    Ok(vec)
}

pub const ASCEND_DSL_CIR_RECV_LIMIT_TYPE: AVPType = 100;
/// Delete all of `ascend_dsl_cir_recv_limit` values from a packet.
pub fn delete_ascend_dsl_cir_recv_limit(packet: &mut Packet) {
    packet.delete(ASCEND_DSL_CIR_RECV_LIMIT_TYPE);
}
/// Add `ascend_dsl_cir_recv_limit` integer value to a packet.
pub fn add_ascend_dsl_cir_recv_limit(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_DSL_CIR_RECV_LIMIT_TYPE, value));
}
/// Lookup a `ascend_dsl_cir_recv_limit` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dsl_cir_recv_limit`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dsl_cir_recv_limit(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_DSL_CIR_RECV_LIMIT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_dsl_cir_recv_limit` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dsl_cir_recv_limit(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DSL_CIR_RECV_LIMIT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_DSL_CIR_XMIT_LIMIT_TYPE: AVPType = 101;
/// Delete all of `ascend_dsl_cir_xmit_limit` values from a packet.
pub fn delete_ascend_dsl_cir_xmit_limit(packet: &mut Packet) {
    packet.delete(ASCEND_DSL_CIR_XMIT_LIMIT_TYPE);
}
/// Add `ascend_dsl_cir_xmit_limit` integer value to a packet.
pub fn add_ascend_dsl_cir_xmit_limit(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_DSL_CIR_XMIT_LIMIT_TYPE, value));
}
/// Lookup a `ascend_dsl_cir_xmit_limit` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dsl_cir_xmit_limit`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dsl_cir_xmit_limit(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_DSL_CIR_XMIT_LIMIT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_dsl_cir_xmit_limit` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dsl_cir_xmit_limit(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DSL_CIR_XMIT_LIMIT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_V_ROUTER_NAME_TYPE: AVPType = 102;
/// Delete all of `ascend_v_router_name` values from a packet.
pub fn delete_ascend_v_router_name(packet: &mut Packet) {
    packet.delete(ASCEND_V_ROUTER_NAME_TYPE);
}
/// Add `ascend_v_router_name` string value to a packet.
pub fn add_ascend_v_router_name(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_V_ROUTER_NAME_TYPE, value));
}
/// Lookup a `ascend_v_router_name` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_v_router_name`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_v_router_name(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_V_ROUTER_NAME_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_v_router_name` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_v_router_name(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_V_ROUTER_NAME_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_SOURCE_AUTH_TYPE: AVPType = 103;
/// Delete all of `ascend_source_auth` values from a packet.
pub fn delete_ascend_source_auth(packet: &mut Packet) {
    packet.delete(ASCEND_SOURCE_AUTH_TYPE);
}
/// Add `ascend_source_auth` string value to a packet.
pub fn add_ascend_source_auth(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_SOURCE_AUTH_TYPE, value));
}
/// Lookup a `ascend_source_auth` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_source_auth`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_source_auth(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_SOURCE_AUTH_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_source_auth` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_source_auth(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SOURCE_AUTH_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_PRIVATE_ROUTE_TYPE: AVPType = 104;
/// Delete all of `ascend_private_route` values from a packet.
pub fn delete_ascend_private_route(packet: &mut Packet) {
    packet.delete(ASCEND_PRIVATE_ROUTE_TYPE);
}
/// Add `ascend_private_route` string value to a packet.
pub fn add_ascend_private_route(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_PRIVATE_ROUTE_TYPE, value));
}
/// Lookup a `ascend_private_route` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_private_route`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_private_route(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_PRIVATE_ROUTE_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_private_route` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_private_route(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PRIVATE_ROUTE_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_NUMBERING_PLAN_ID_TYPE: AVPType = 105;
/// Delete all of `ascend_numbering_plan_id` values from a packet.
pub fn delete_ascend_numbering_plan_id(packet: &mut Packet) {
    packet.delete(ASCEND_NUMBERING_PLAN_ID_TYPE);
}
/// Add `ascend_numbering_plan_id` value-defined integer value to a packet.
pub fn add_ascend_numbering_plan_id(packet: &mut Packet, value: AscendNumberingPlanID) {
    packet.add(AVP::from_u32(ASCEND_NUMBERING_PLAN_ID_TYPE, value));
}
/// Lookup a `ascend_numbering_plan_id` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_numbering_plan_id`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_numbering_plan_id(
    packet: &Packet,
) -> Option<Result<AscendNumberingPlanID, AVPError>> {
    packet
        .lookup(ASCEND_NUMBERING_PLAN_ID_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendNumberingPlanID))
}
/// Lookup all of the `ascend_numbering_plan_id` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_numbering_plan_id(
    packet: &Packet,
) -> Result<Vec<AscendNumberingPlanID>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_NUMBERING_PLAN_ID_TYPE) {
        vec.push(avp.encode_u32()? as AscendNumberingPlanID);
    }
    Ok(vec)
}

pub const ASCEND_FR_LINK_STATUS_DLCI_TYPE: AVPType = 106;
/// Delete all of `ascend_fr_link_status_dlci` values from a packet.
pub fn delete_ascend_fr_link_status_dlci(packet: &mut Packet) {
    packet.delete(ASCEND_FR_LINK_STATUS_DLCI_TYPE);
}
/// Add `ascend_fr_link_status_dlci` value-defined integer value to a packet.
pub fn add_ascend_fr_link_status_dlci(packet: &mut Packet, value: AscendFRLinkStatusDLCI) {
    packet.add(AVP::from_u32(ASCEND_FR_LINK_STATUS_DLCI_TYPE, value));
}
/// Lookup a `ascend_fr_link_status_dlci` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_link_status_dlci`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_link_status_dlci(
    packet: &Packet,
) -> Option<Result<AscendFRLinkStatusDLCI, AVPError>> {
    packet
        .lookup(ASCEND_FR_LINK_STATUS_DLCI_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendFRLinkStatusDLCI))
}
/// Lookup all of the `ascend_fr_link_status_dlci` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_link_status_dlci(
    packet: &Packet,
) -> Result<Vec<AscendFRLinkStatusDLCI>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_LINK_STATUS_DLCI_TYPE) {
        vec.push(avp.encode_u32()? as AscendFRLinkStatusDLCI);
    }
    Ok(vec)
}

pub const ASCEND_CALLING_SUBADDRESS_TYPE: AVPType = 107;
/// Delete all of `ascend_calling_subaddress` values from a packet.
pub fn delete_ascend_calling_subaddress(packet: &mut Packet) {
    packet.delete(ASCEND_CALLING_SUBADDRESS_TYPE);
}
/// Add `ascend_calling_subaddress` string value to a packet.
pub fn add_ascend_calling_subaddress(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_CALLING_SUBADDRESS_TYPE, value));
}
/// Lookup a `ascend_calling_subaddress` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_calling_subaddress`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_calling_subaddress(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_CALLING_SUBADDRESS_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_calling_subaddress` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_calling_subaddress(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALLING_SUBADDRESS_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_CALLBACK_DELAY_TYPE: AVPType = 108;
/// Delete all of `ascend_callback_delay` values from a packet.
pub fn delete_ascend_callback_delay(packet: &mut Packet) {
    packet.delete(ASCEND_CALLBACK_DELAY_TYPE);
}
/// Add `ascend_callback_delay` integer value to a packet.
pub fn add_ascend_callback_delay(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_CALLBACK_DELAY_TYPE, value));
}
/// Lookup a `ascend_callback_delay` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_callback_delay`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_callback_delay(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_CALLBACK_DELAY_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_callback_delay` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_callback_delay(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALLBACK_DELAY_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_ENDPOINT_DISC_TYPE: AVPType = 109;
/// Delete all of `ascend_endpoint_disc` values from a packet.
pub fn delete_ascend_endpoint_disc(packet: &mut Packet) {
    packet.delete(ASCEND_ENDPOINT_DISC_TYPE);
}
/// Add `ascend_endpoint_disc` string value to a packet.
pub fn add_ascend_endpoint_disc(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_ENDPOINT_DISC_TYPE, value));
}
/// Lookup a `ascend_endpoint_disc` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_endpoint_disc`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_endpoint_disc(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_ENDPOINT_DISC_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_endpoint_disc` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_endpoint_disc(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ENDPOINT_DISC_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_REMOTE_FW_TYPE: AVPType = 110;
/// Delete all of `ascend_remote_fw` values from a packet.
pub fn delete_ascend_remote_fw(packet: &mut Packet) {
    packet.delete(ASCEND_REMOTE_FW_TYPE);
}
/// Add `ascend_remote_fw` string value to a packet.
pub fn add_ascend_remote_fw(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_REMOTE_FW_TYPE, value));
}
/// Lookup a `ascend_remote_fw` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_remote_fw`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_remote_fw(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_REMOTE_FW_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_remote_fw` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_remote_fw(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_REMOTE_FW_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_MULTICAST_G_LEAVE_DELAY_TYPE: AVPType = 111;
/// Delete all of `ascend_multicast_g_leave_delay` values from a packet.
pub fn delete_ascend_multicast_g_leave_delay(packet: &mut Packet) {
    packet.delete(ASCEND_MULTICAST_G_LEAVE_DELAY_TYPE);
}
/// Add `ascend_multicast_g_leave_delay` integer value to a packet.
pub fn add_ascend_multicast_g_leave_delay(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MULTICAST_G_LEAVE_DELAY_TYPE, value));
}
/// Lookup a `ascend_multicast_g_leave_delay` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_multicast_g_leave_delay`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_multicast_g_leave_delay(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_MULTICAST_G_LEAVE_DELAY_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_multicast_g_leave_delay` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_multicast_g_leave_delay(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MULTICAST_G_LEAVE_DELAY_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_CBCP_ENABLE_TYPE: AVPType = 112;
/// Delete all of `ascend_cbcp_enable` values from a packet.
pub fn delete_ascend_cbcp_enable(packet: &mut Packet) {
    packet.delete(ASCEND_CBCP_ENABLE_TYPE);
}
/// Add `ascend_cbcp_enable` value-defined integer value to a packet.
pub fn add_ascend_cbcp_enable(packet: &mut Packet, value: AscendCBCPEnable) {
    packet.add(AVP::from_u32(ASCEND_CBCP_ENABLE_TYPE, value));
}
/// Lookup a `ascend_cbcp_enable` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_cbcp_enable`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_cbcp_enable(packet: &Packet) -> Option<Result<AscendCBCPEnable, AVPError>> {
    packet
        .lookup(ASCEND_CBCP_ENABLE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCBCPEnable))
}
/// Lookup all of the `ascend_cbcp_enable` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_cbcp_enable(packet: &Packet) -> Result<Vec<AscendCBCPEnable>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CBCP_ENABLE_TYPE) {
        vec.push(avp.encode_u32()? as AscendCBCPEnable);
    }
    Ok(vec)
}

pub const ASCEND_CBCP_MODE_TYPE: AVPType = 113;
/// Delete all of `ascend_cbcp_mode` values from a packet.
pub fn delete_ascend_cbcp_mode(packet: &mut Packet) {
    packet.delete(ASCEND_CBCP_MODE_TYPE);
}
/// Add `ascend_cbcp_mode` value-defined integer value to a packet.
pub fn add_ascend_cbcp_mode(packet: &mut Packet, value: AscendCBCPMode) {
    packet.add(AVP::from_u32(ASCEND_CBCP_MODE_TYPE, value));
}
/// Lookup a `ascend_cbcp_mode` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_cbcp_mode`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_cbcp_mode(packet: &Packet) -> Option<Result<AscendCBCPMode, AVPError>> {
    packet
        .lookup(ASCEND_CBCP_MODE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCBCPMode))
}
/// Lookup all of the `ascend_cbcp_mode` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_cbcp_mode(packet: &Packet) -> Result<Vec<AscendCBCPMode>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CBCP_MODE_TYPE) {
        vec.push(avp.encode_u32()? as AscendCBCPMode);
    }
    Ok(vec)
}

pub const ASCEND_CBCP_DELAY_TYPE: AVPType = 114;
/// Delete all of `ascend_cbcp_delay` values from a packet.
pub fn delete_ascend_cbcp_delay(packet: &mut Packet) {
    packet.delete(ASCEND_CBCP_DELAY_TYPE);
}
/// Add `ascend_cbcp_delay` integer value to a packet.
pub fn add_ascend_cbcp_delay(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_CBCP_DELAY_TYPE, value));
}
/// Lookup a `ascend_cbcp_delay` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_cbcp_delay`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_cbcp_delay(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_CBCP_DELAY_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_cbcp_delay` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_cbcp_delay(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CBCP_DELAY_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_CBCP_TRUNK_GROUP_TYPE: AVPType = 115;
/// Delete all of `ascend_cbcp_trunk_group` values from a packet.
pub fn delete_ascend_cbcp_trunk_group(packet: &mut Packet) {
    packet.delete(ASCEND_CBCP_TRUNK_GROUP_TYPE);
}
/// Add `ascend_cbcp_trunk_group` integer value to a packet.
pub fn add_ascend_cbcp_trunk_group(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_CBCP_TRUNK_GROUP_TYPE, value));
}
/// Lookup a `ascend_cbcp_trunk_group` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_cbcp_trunk_group`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_cbcp_trunk_group(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_CBCP_TRUNK_GROUP_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_cbcp_trunk_group` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_cbcp_trunk_group(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CBCP_TRUNK_GROUP_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_APPLETALK_ROUTE_TYPE: AVPType = 116;
/// Delete all of `ascend_appletalk_route` values from a packet.
pub fn delete_ascend_appletalk_route(packet: &mut Packet) {
    packet.delete(ASCEND_APPLETALK_ROUTE_TYPE);
}
/// Add `ascend_appletalk_route` string value to a packet.
pub fn add_ascend_appletalk_route(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_APPLETALK_ROUTE_TYPE, value));
}
/// Lookup a `ascend_appletalk_route` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_appletalk_route`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_appletalk_route(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_APPLETALK_ROUTE_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_appletalk_route` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_appletalk_route(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_APPLETALK_ROUTE_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_APPLETALK_PEER_MODE_TYPE: AVPType = 117;
/// Delete all of `ascend_appletalk_peer_mode` values from a packet.
pub fn delete_ascend_appletalk_peer_mode(packet: &mut Packet) {
    packet.delete(ASCEND_APPLETALK_PEER_MODE_TYPE);
}
/// Add `ascend_appletalk_peer_mode` value-defined integer value to a packet.
pub fn add_ascend_appletalk_peer_mode(packet: &mut Packet, value: AscendAppletalkPeerMode) {
    packet.add(AVP::from_u32(ASCEND_APPLETALK_PEER_MODE_TYPE, value));
}
/// Lookup a `ascend_appletalk_peer_mode` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_appletalk_peer_mode`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_appletalk_peer_mode(
    packet: &Packet,
) -> Option<Result<AscendAppletalkPeerMode, AVPError>> {
    packet
        .lookup(ASCEND_APPLETALK_PEER_MODE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendAppletalkPeerMode))
}
/// Lookup all of the `ascend_appletalk_peer_mode` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_appletalk_peer_mode(
    packet: &Packet,
) -> Result<Vec<AscendAppletalkPeerMode>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_APPLETALK_PEER_MODE_TYPE) {
        vec.push(avp.encode_u32()? as AscendAppletalkPeerMode);
    }
    Ok(vec)
}

pub const ASCEND_ROUTE_APPLETALK_TYPE: AVPType = 118;
/// Delete all of `ascend_route_appletalk` values from a packet.
pub fn delete_ascend_route_appletalk(packet: &mut Packet) {
    packet.delete(ASCEND_ROUTE_APPLETALK_TYPE);
}
/// Add `ascend_route_appletalk` value-defined integer value to a packet.
pub fn add_ascend_route_appletalk(packet: &mut Packet, value: AscendRouteAppletalk) {
    packet.add(AVP::from_u32(ASCEND_ROUTE_APPLETALK_TYPE, value));
}
/// Lookup a `ascend_route_appletalk` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_route_appletalk`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_route_appletalk(
    packet: &Packet,
) -> Option<Result<AscendRouteAppletalk, AVPError>> {
    packet
        .lookup(ASCEND_ROUTE_APPLETALK_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendRouteAppletalk))
}
/// Lookup all of the `ascend_route_appletalk` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_route_appletalk(
    packet: &Packet,
) -> Result<Vec<AscendRouteAppletalk>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ROUTE_APPLETALK_TYPE) {
        vec.push(avp.encode_u32()? as AscendRouteAppletalk);
    }
    Ok(vec)
}

pub const ASCEND_FCP_PARAMETER_TYPE: AVPType = 119;
/// Delete all of `ascend_fcp_parameter` values from a packet.
pub fn delete_ascend_fcp_parameter(packet: &mut Packet) {
    packet.delete(ASCEND_FCP_PARAMETER_TYPE);
}
/// Add `ascend_fcp_parameter` string value to a packet.
pub fn add_ascend_fcp_parameter(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_FCP_PARAMETER_TYPE, value));
}
/// Lookup a `ascend_fcp_parameter` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fcp_parameter`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fcp_parameter(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_FCP_PARAMETER_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_fcp_parameter` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fcp_parameter(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FCP_PARAMETER_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_MODEM_PORT_NO_TYPE: AVPType = 120;
/// Delete all of `ascend_modem_port_no` values from a packet.
pub fn delete_ascend_modem_port_no(packet: &mut Packet) {
    packet.delete(ASCEND_MODEM_PORT_NO_TYPE);
}
/// Add `ascend_modem_port_no` integer value to a packet.
pub fn add_ascend_modem_port_no(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MODEM_PORT_NO_TYPE, value));
}
/// Lookup a `ascend_modem_port_no` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_modem_port_no`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_modem_port_no(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_MODEM_PORT_NO_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_modem_port_no` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_modem_port_no(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MODEM_PORT_NO_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_MODEM_SLOT_NO_TYPE: AVPType = 121;
/// Delete all of `ascend_modem_slot_no` values from a packet.
pub fn delete_ascend_modem_slot_no(packet: &mut Packet) {
    packet.delete(ASCEND_MODEM_SLOT_NO_TYPE);
}
/// Add `ascend_modem_slot_no` integer value to a packet.
pub fn add_ascend_modem_slot_no(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MODEM_SLOT_NO_TYPE, value));
}
/// Lookup a `ascend_modem_slot_no` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_modem_slot_no`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_modem_slot_no(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_MODEM_SLOT_NO_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_modem_slot_no` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_modem_slot_no(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MODEM_SLOT_NO_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_MODEM_SHELF_NO_TYPE: AVPType = 122;
/// Delete all of `ascend_modem_shelf_no` values from a packet.
pub fn delete_ascend_modem_shelf_no(packet: &mut Packet) {
    packet.delete(ASCEND_MODEM_SHELF_NO_TYPE);
}
/// Add `ascend_modem_shelf_no` integer value to a packet.
pub fn add_ascend_modem_shelf_no(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MODEM_SHELF_NO_TYPE, value));
}
/// Lookup a `ascend_modem_shelf_no` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_modem_shelf_no`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_modem_shelf_no(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_MODEM_SHELF_NO_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_modem_shelf_no` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_modem_shelf_no(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MODEM_SHELF_NO_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_CALL_ATTEMPT_LIMIT_TYPE: AVPType = 123;
/// Delete all of `ascend_call_attempt_limit` values from a packet.
pub fn delete_ascend_call_attempt_limit(packet: &mut Packet) {
    packet.delete(ASCEND_CALL_ATTEMPT_LIMIT_TYPE);
}
/// Add `ascend_call_attempt_limit` integer value to a packet.
pub fn add_ascend_call_attempt_limit(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_CALL_ATTEMPT_LIMIT_TYPE, value));
}
/// Lookup a `ascend_call_attempt_limit` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_call_attempt_limit`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_call_attempt_limit(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_CALL_ATTEMPT_LIMIT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_call_attempt_limit` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_call_attempt_limit(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALL_ATTEMPT_LIMIT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_CALL_BLOCK_DURATION_TYPE: AVPType = 124;
/// Delete all of `ascend_call_block_duration` values from a packet.
pub fn delete_ascend_call_block_duration(packet: &mut Packet) {
    packet.delete(ASCEND_CALL_BLOCK_DURATION_TYPE);
}
/// Add `ascend_call_block_duration` integer value to a packet.
pub fn add_ascend_call_block_duration(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_CALL_BLOCK_DURATION_TYPE, value));
}
/// Lookup a `ascend_call_block_duration` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_call_block_duration`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_call_block_duration(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_CALL_BLOCK_DURATION_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_call_block_duration` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_call_block_duration(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALL_BLOCK_DURATION_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_MAXIMUM_CALL_DURATION_TYPE: AVPType = 125;
/// Delete all of `ascend_maximum_call_duration` values from a packet.
pub fn delete_ascend_maximum_call_duration(packet: &mut Packet) {
    packet.delete(ASCEND_MAXIMUM_CALL_DURATION_TYPE);
}
/// Add `ascend_maximum_call_duration` integer value to a packet.
pub fn add_ascend_maximum_call_duration(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MAXIMUM_CALL_DURATION_TYPE, value));
}
/// Lookup a `ascend_maximum_call_duration` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_maximum_call_duration`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_maximum_call_duration(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_MAXIMUM_CALL_DURATION_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_maximum_call_duration` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_maximum_call_duration(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MAXIMUM_CALL_DURATION_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_TEMPORARY_RTES_TYPE: AVPType = 126;
/// Delete all of `ascend_temporary_rtes` values from a packet.
pub fn delete_ascend_temporary_rtes(packet: &mut Packet) {
    packet.delete(ASCEND_TEMPORARY_RTES_TYPE);
}
/// Add `ascend_temporary_rtes` value-defined integer value to a packet.
pub fn add_ascend_temporary_rtes(packet: &mut Packet, value: AscendTemporaryRtes) {
    packet.add(AVP::from_u32(ASCEND_TEMPORARY_RTES_TYPE, value));
}
/// Lookup a `ascend_temporary_rtes` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_temporary_rtes`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_temporary_rtes(
    packet: &Packet,
) -> Option<Result<AscendTemporaryRtes, AVPError>> {
    packet
        .lookup(ASCEND_TEMPORARY_RTES_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendTemporaryRtes))
}
/// Lookup all of the `ascend_temporary_rtes` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_temporary_rtes(
    packet: &Packet,
) -> Result<Vec<AscendTemporaryRtes>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TEMPORARY_RTES_TYPE) {
        vec.push(avp.encode_u32()? as AscendTemporaryRtes);
    }
    Ok(vec)
}

pub const ASCEND_TUNNELING_PROTOCOL_TYPE: AVPType = 127;
/// Delete all of `ascend_tunneling_protocol` values from a packet.
pub fn delete_ascend_tunneling_protocol(packet: &mut Packet) {
    packet.delete(ASCEND_TUNNELING_PROTOCOL_TYPE);
}
/// Add `ascend_tunneling_protocol` value-defined integer value to a packet.
pub fn add_ascend_tunneling_protocol(packet: &mut Packet, value: AscendTunnelingProtocol) {
    packet.add(AVP::from_u32(ASCEND_TUNNELING_PROTOCOL_TYPE, value));
}
/// Lookup a `ascend_tunneling_protocol` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_tunneling_protocol`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_tunneling_protocol(
    packet: &Packet,
) -> Option<Result<AscendTunnelingProtocol, AVPError>> {
    packet
        .lookup(ASCEND_TUNNELING_PROTOCOL_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendTunnelingProtocol))
}
/// Lookup all of the `ascend_tunneling_protocol` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_tunneling_protocol(
    packet: &Packet,
) -> Result<Vec<AscendTunnelingProtocol>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TUNNELING_PROTOCOL_TYPE) {
        vec.push(avp.encode_u32()? as AscendTunnelingProtocol);
    }
    Ok(vec)
}

pub const ASCEND_SHARED_PROFILE_ENABLE_TYPE: AVPType = 128;
/// Delete all of `ascend_shared_profile_enable` values from a packet.
pub fn delete_ascend_shared_profile_enable(packet: &mut Packet) {
    packet.delete(ASCEND_SHARED_PROFILE_ENABLE_TYPE);
}
/// Add `ascend_shared_profile_enable` value-defined integer value to a packet.
pub fn add_ascend_shared_profile_enable(packet: &mut Packet, value: AscendSharedProfileEnable) {
    packet.add(AVP::from_u32(ASCEND_SHARED_PROFILE_ENABLE_TYPE, value));
}
/// Lookup a `ascend_shared_profile_enable` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_shared_profile_enable`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_shared_profile_enable(
    packet: &Packet,
) -> Option<Result<AscendSharedProfileEnable, AVPError>> {
    packet
        .lookup(ASCEND_SHARED_PROFILE_ENABLE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendSharedProfileEnable))
}
/// Lookup all of the `ascend_shared_profile_enable` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_shared_profile_enable(
    packet: &Packet,
) -> Result<Vec<AscendSharedProfileEnable>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SHARED_PROFILE_ENABLE_TYPE) {
        vec.push(avp.encode_u32()? as AscendSharedProfileEnable);
    }
    Ok(vec)
}

pub const ASCEND_PRIMARY_HOME_AGENT_TYPE: AVPType = 129;
/// Delete all of `ascend_primary_home_agent` values from a packet.
pub fn delete_ascend_primary_home_agent(packet: &mut Packet) {
    packet.delete(ASCEND_PRIMARY_HOME_AGENT_TYPE);
}
/// Add `ascend_primary_home_agent` string value to a packet.
pub fn add_ascend_primary_home_agent(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_PRIMARY_HOME_AGENT_TYPE, value));
}
/// Lookup a `ascend_primary_home_agent` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_primary_home_agent`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_primary_home_agent(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_PRIMARY_HOME_AGENT_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_primary_home_agent` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_primary_home_agent(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PRIMARY_HOME_AGENT_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_SECONDARY_HOME_AGENT_TYPE: AVPType = 130;
/// Delete all of `ascend_secondary_home_agent` values from a packet.
pub fn delete_ascend_secondary_home_agent(packet: &mut Packet) {
    packet.delete(ASCEND_SECONDARY_HOME_AGENT_TYPE);
}
/// Add `ascend_secondary_home_agent` string value to a packet.
pub fn add_ascend_secondary_home_agent(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_SECONDARY_HOME_AGENT_TYPE, value));
}
/// Lookup a `ascend_secondary_home_agent` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_secondary_home_agent`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_secondary_home_agent(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_SECONDARY_HOME_AGENT_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_secondary_home_agent` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_secondary_home_agent(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SECONDARY_HOME_AGENT_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_DIALOUT_ALLOWED_TYPE: AVPType = 131;
/// Delete all of `ascend_dialout_allowed` values from a packet.
pub fn delete_ascend_dialout_allowed(packet: &mut Packet) {
    packet.delete(ASCEND_DIALOUT_ALLOWED_TYPE);
}
/// Add `ascend_dialout_allowed` value-defined integer value to a packet.
pub fn add_ascend_dialout_allowed(packet: &mut Packet, value: AscendDialoutAllowed) {
    packet.add(AVP::from_u32(ASCEND_DIALOUT_ALLOWED_TYPE, value));
}
/// Lookup a `ascend_dialout_allowed` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dialout_allowed`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dialout_allowed(
    packet: &Packet,
) -> Option<Result<AscendDialoutAllowed, AVPError>> {
    packet
        .lookup(ASCEND_DIALOUT_ALLOWED_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendDialoutAllowed))
}
/// Lookup all of the `ascend_dialout_allowed` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dialout_allowed(
    packet: &Packet,
) -> Result<Vec<AscendDialoutAllowed>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DIALOUT_ALLOWED_TYPE) {
        vec.push(avp.encode_u32()? as AscendDialoutAllowed);
    }
    Ok(vec)
}

pub const ASCEND_CLIENT_GATEWAY_TYPE: AVPType = 132;
/// Delete all of `ascend_client_gateway` values from a packet.
pub fn delete_ascend_client_gateway(packet: &mut Packet) {
    packet.delete(ASCEND_CLIENT_GATEWAY_TYPE);
}
/// Add `ascend_client_gateway` ipaddr value to a packet.
pub fn add_ascend_client_gateway(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_CLIENT_GATEWAY_TYPE, value));
}
/// Lookup a `ascend_client_gateway` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_client_gateway`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_client_gateway(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_CLIENT_GATEWAY_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_client_gateway` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_client_gateway(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CLIENT_GATEWAY_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_BACP_ENABLE_TYPE: AVPType = 133;
/// Delete all of `ascend_bacp_enable` values from a packet.
pub fn delete_ascend_bacp_enable(packet: &mut Packet) {
    packet.delete(ASCEND_BACP_ENABLE_TYPE);
}
/// Add `ascend_bacp_enable` value-defined integer value to a packet.
pub fn add_ascend_bacp_enable(packet: &mut Packet, value: AscendBACPEnable) {
    packet.add(AVP::from_u32(ASCEND_BACP_ENABLE_TYPE, value));
}
/// Lookup a `ascend_bacp_enable` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_bacp_enable`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_bacp_enable(packet: &Packet) -> Option<Result<AscendBACPEnable, AVPError>> {
    packet
        .lookup(ASCEND_BACP_ENABLE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendBACPEnable))
}
/// Lookup all of the `ascend_bacp_enable` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_bacp_enable(packet: &Packet) -> Result<Vec<AscendBACPEnable>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BACP_ENABLE_TYPE) {
        vec.push(avp.encode_u32()? as AscendBACPEnable);
    }
    Ok(vec)
}

pub const ASCEND_DHCP_MAXIMUM_LEASES_TYPE: AVPType = 134;
/// Delete all of `ascend_dhcp_maximum_leases` values from a packet.
pub fn delete_ascend_dhcp_maximum_leases(packet: &mut Packet) {
    packet.delete(ASCEND_DHCP_MAXIMUM_LEASES_TYPE);
}
/// Add `ascend_dhcp_maximum_leases` integer value to a packet.
pub fn add_ascend_dhcp_maximum_leases(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_DHCP_MAXIMUM_LEASES_TYPE, value));
}
/// Lookup a `ascend_dhcp_maximum_leases` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dhcp_maximum_leases`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dhcp_maximum_leases(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_DHCP_MAXIMUM_LEASES_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_dhcp_maximum_leases` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dhcp_maximum_leases(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DHCP_MAXIMUM_LEASES_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_CLIENT_PRIMARY_DNS_TYPE: AVPType = 135;
/// Delete all of `ascend_client_primary_dns` values from a packet.
pub fn delete_ascend_client_primary_dns(packet: &mut Packet) {
    packet.delete(ASCEND_CLIENT_PRIMARY_DNS_TYPE);
}
/// Add `ascend_client_primary_dns` ipaddr value to a packet.
pub fn add_ascend_client_primary_dns(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_CLIENT_PRIMARY_DNS_TYPE, value));
}
/// Lookup a `ascend_client_primary_dns` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_client_primary_dns`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_client_primary_dns(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_CLIENT_PRIMARY_DNS_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_client_primary_dns` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_client_primary_dns(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CLIENT_PRIMARY_DNS_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_CLIENT_SECONDARY_DNS_TYPE: AVPType = 136;
/// Delete all of `ascend_client_secondary_dns` values from a packet.
pub fn delete_ascend_client_secondary_dns(packet: &mut Packet) {
    packet.delete(ASCEND_CLIENT_SECONDARY_DNS_TYPE);
}
/// Add `ascend_client_secondary_dns` ipaddr value to a packet.
pub fn add_ascend_client_secondary_dns(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_CLIENT_SECONDARY_DNS_TYPE, value));
}
/// Lookup a `ascend_client_secondary_dns` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_client_secondary_dns`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_client_secondary_dns(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_CLIENT_SECONDARY_DNS_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_client_secondary_dns` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_client_secondary_dns(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CLIENT_SECONDARY_DNS_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_CLIENT_ASSIGN_DNS_TYPE: AVPType = 137;
/// Delete all of `ascend_client_assign_dns` values from a packet.
pub fn delete_ascend_client_assign_dns(packet: &mut Packet) {
    packet.delete(ASCEND_CLIENT_ASSIGN_DNS_TYPE);
}
/// Add `ascend_client_assign_dns` value-defined integer value to a packet.
pub fn add_ascend_client_assign_dns(packet: &mut Packet, value: AscendClientAssignDNS) {
    packet.add(AVP::from_u32(ASCEND_CLIENT_ASSIGN_DNS_TYPE, value));
}
/// Lookup a `ascend_client_assign_dns` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_client_assign_dns`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_client_assign_dns(
    packet: &Packet,
) -> Option<Result<AscendClientAssignDNS, AVPError>> {
    packet
        .lookup(ASCEND_CLIENT_ASSIGN_DNS_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendClientAssignDNS))
}
/// Lookup all of the `ascend_client_assign_dns` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_client_assign_dns(
    packet: &Packet,
) -> Result<Vec<AscendClientAssignDNS>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CLIENT_ASSIGN_DNS_TYPE) {
        vec.push(avp.encode_u32()? as AscendClientAssignDNS);
    }
    Ok(vec)
}

pub const ASCEND_USER_ACCT_TYPE_TYPE: AVPType = 138;
/// Delete all of `ascend_user_acct_type` values from a packet.
pub fn delete_ascend_user_acct_type(packet: &mut Packet) {
    packet.delete(ASCEND_USER_ACCT_TYPE_TYPE);
}
/// Add `ascend_user_acct_type` value-defined integer value to a packet.
pub fn add_ascend_user_acct_type(packet: &mut Packet, value: AscendUserAcctType) {
    packet.add(AVP::from_u32(ASCEND_USER_ACCT_TYPE_TYPE, value));
}
/// Lookup a `ascend_user_acct_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_user_acct_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_user_acct_type(
    packet: &Packet,
) -> Option<Result<AscendUserAcctType, AVPError>> {
    packet
        .lookup(ASCEND_USER_ACCT_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendUserAcctType))
}
/// Lookup all of the `ascend_user_acct_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_user_acct_type(
    packet: &Packet,
) -> Result<Vec<AscendUserAcctType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_USER_ACCT_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendUserAcctType);
    }
    Ok(vec)
}

pub const ASCEND_USER_ACCT_HOST_TYPE: AVPType = 139;
/// Delete all of `ascend_user_acct_host` values from a packet.
pub fn delete_ascend_user_acct_host(packet: &mut Packet) {
    packet.delete(ASCEND_USER_ACCT_HOST_TYPE);
}
/// Add `ascend_user_acct_host` ipaddr value to a packet.
pub fn add_ascend_user_acct_host(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_USER_ACCT_HOST_TYPE, value));
}
/// Lookup a `ascend_user_acct_host` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_user_acct_host`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_user_acct_host(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_USER_ACCT_HOST_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_user_acct_host` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_user_acct_host(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_USER_ACCT_HOST_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_USER_ACCT_PORT_TYPE: AVPType = 140;
/// Delete all of `ascend_user_acct_port` values from a packet.
pub fn delete_ascend_user_acct_port(packet: &mut Packet) {
    packet.delete(ASCEND_USER_ACCT_PORT_TYPE);
}
/// Add `ascend_user_acct_port` integer value to a packet.
pub fn add_ascend_user_acct_port(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_USER_ACCT_PORT_TYPE, value));
}
/// Lookup a `ascend_user_acct_port` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_user_acct_port`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_user_acct_port(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_USER_ACCT_PORT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_user_acct_port` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_user_acct_port(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_USER_ACCT_PORT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_USER_ACCT_KEY_TYPE: AVPType = 141;
/// Delete all of `ascend_user_acct_key` values from a packet.
pub fn delete_ascend_user_acct_key(packet: &mut Packet) {
    packet.delete(ASCEND_USER_ACCT_KEY_TYPE);
}
/// Add `ascend_user_acct_key` string value to a packet.
pub fn add_ascend_user_acct_key(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_USER_ACCT_KEY_TYPE, value));
}
/// Lookup a `ascend_user_acct_key` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_user_acct_key`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_user_acct_key(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_USER_ACCT_KEY_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_user_acct_key` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_user_acct_key(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_USER_ACCT_KEY_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_USER_ACCT_BASE_TYPE: AVPType = 142;
/// Delete all of `ascend_user_acct_base` values from a packet.
pub fn delete_ascend_user_acct_base(packet: &mut Packet) {
    packet.delete(ASCEND_USER_ACCT_BASE_TYPE);
}
/// Add `ascend_user_acct_base` value-defined integer value to a packet.
pub fn add_ascend_user_acct_base(packet: &mut Packet, value: AscendUserAcctBase) {
    packet.add(AVP::from_u32(ASCEND_USER_ACCT_BASE_TYPE, value));
}
/// Lookup a `ascend_user_acct_base` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_user_acct_base`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_user_acct_base(
    packet: &Packet,
) -> Option<Result<AscendUserAcctBase, AVPError>> {
    packet
        .lookup(ASCEND_USER_ACCT_BASE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendUserAcctBase))
}
/// Lookup all of the `ascend_user_acct_base` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_user_acct_base(
    packet: &Packet,
) -> Result<Vec<AscendUserAcctBase>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_USER_ACCT_BASE_TYPE) {
        vec.push(avp.encode_u32()? as AscendUserAcctBase);
    }
    Ok(vec)
}

pub const ASCEND_USER_ACCT_TIME_TYPE: AVPType = 143;
/// Delete all of `ascend_user_acct_time` values from a packet.
pub fn delete_ascend_user_acct_time(packet: &mut Packet) {
    packet.delete(ASCEND_USER_ACCT_TIME_TYPE);
}
/// Add `ascend_user_acct_time` integer value to a packet.
pub fn add_ascend_user_acct_time(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_USER_ACCT_TIME_TYPE, value));
}
/// Lookup a `ascend_user_acct_time` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_user_acct_time`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_user_acct_time(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_USER_ACCT_TIME_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_user_acct_time` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_user_acct_time(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_USER_ACCT_TIME_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_ASSIGN_IP_CLIENT_TYPE: AVPType = 144;
/// Delete all of `ascend_assign_ip_client` values from a packet.
pub fn delete_ascend_assign_ip_client(packet: &mut Packet) {
    packet.delete(ASCEND_ASSIGN_IP_CLIENT_TYPE);
}
/// Add `ascend_assign_ip_client` ipaddr value to a packet.
pub fn add_ascend_assign_ip_client(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_ASSIGN_IP_CLIENT_TYPE, value));
}
/// Lookup a `ascend_assign_ip_client` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_assign_ip_client`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_assign_ip_client(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_ASSIGN_IP_CLIENT_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_assign_ip_client` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_assign_ip_client(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ASSIGN_IP_CLIENT_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_ASSIGN_IP_SERVER_TYPE: AVPType = 145;
/// Delete all of `ascend_assign_ip_server` values from a packet.
pub fn delete_ascend_assign_ip_server(packet: &mut Packet) {
    packet.delete(ASCEND_ASSIGN_IP_SERVER_TYPE);
}
/// Add `ascend_assign_ip_server` ipaddr value to a packet.
pub fn add_ascend_assign_ip_server(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_ASSIGN_IP_SERVER_TYPE, value));
}
/// Lookup a `ascend_assign_ip_server` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_assign_ip_server`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_assign_ip_server(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_ASSIGN_IP_SERVER_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_assign_ip_server` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_assign_ip_server(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ASSIGN_IP_SERVER_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_ASSIGN_IP_GLOBAL_POOL_TYPE: AVPType = 146;
/// Delete all of `ascend_assign_ip_global_pool` values from a packet.
pub fn delete_ascend_assign_ip_global_pool(packet: &mut Packet) {
    packet.delete(ASCEND_ASSIGN_IP_GLOBAL_POOL_TYPE);
}
/// Add `ascend_assign_ip_global_pool` string value to a packet.
pub fn add_ascend_assign_ip_global_pool(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_ASSIGN_IP_GLOBAL_POOL_TYPE, value));
}
/// Lookup a `ascend_assign_ip_global_pool` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_assign_ip_global_pool`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_assign_ip_global_pool(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_ASSIGN_IP_GLOBAL_POOL_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_assign_ip_global_pool` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_assign_ip_global_pool(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ASSIGN_IP_GLOBAL_POOL_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_DHCP_REPLY_TYPE: AVPType = 147;
/// Delete all of `ascend_dhcp_reply` values from a packet.
pub fn delete_ascend_dhcp_reply(packet: &mut Packet) {
    packet.delete(ASCEND_DHCP_REPLY_TYPE);
}
/// Add `ascend_dhcp_reply` value-defined integer value to a packet.
pub fn add_ascend_dhcp_reply(packet: &mut Packet, value: AscendDHCPReply) {
    packet.add(AVP::from_u32(ASCEND_DHCP_REPLY_TYPE, value));
}
/// Lookup a `ascend_dhcp_reply` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dhcp_reply`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dhcp_reply(packet: &Packet) -> Option<Result<AscendDHCPReply, AVPError>> {
    packet
        .lookup(ASCEND_DHCP_REPLY_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendDHCPReply))
}
/// Lookup all of the `ascend_dhcp_reply` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dhcp_reply(packet: &Packet) -> Result<Vec<AscendDHCPReply>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DHCP_REPLY_TYPE) {
        vec.push(avp.encode_u32()? as AscendDHCPReply);
    }
    Ok(vec)
}

pub const ASCEND_DHCP_POOL_NUMBER_TYPE: AVPType = 148;
/// Delete all of `ascend_dhcp_pool_number` values from a packet.
pub fn delete_ascend_dhcp_pool_number(packet: &mut Packet) {
    packet.delete(ASCEND_DHCP_POOL_NUMBER_TYPE);
}
/// Add `ascend_dhcp_pool_number` integer value to a packet.
pub fn add_ascend_dhcp_pool_number(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_DHCP_POOL_NUMBER_TYPE, value));
}
/// Lookup a `ascend_dhcp_pool_number` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dhcp_pool_number`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dhcp_pool_number(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_DHCP_POOL_NUMBER_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_dhcp_pool_number` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dhcp_pool_number(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DHCP_POOL_NUMBER_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_EXPECT_CALLBACK_TYPE: AVPType = 149;
/// Delete all of `ascend_expect_callback` values from a packet.
pub fn delete_ascend_expect_callback(packet: &mut Packet) {
    packet.delete(ASCEND_EXPECT_CALLBACK_TYPE);
}
/// Add `ascend_expect_callback` value-defined integer value to a packet.
pub fn add_ascend_expect_callback(packet: &mut Packet, value: AscendExpectCallback) {
    packet.add(AVP::from_u32(ASCEND_EXPECT_CALLBACK_TYPE, value));
}
/// Lookup a `ascend_expect_callback` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_expect_callback`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_expect_callback(
    packet: &Packet,
) -> Option<Result<AscendExpectCallback, AVPError>> {
    packet
        .lookup(ASCEND_EXPECT_CALLBACK_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendExpectCallback))
}
/// Lookup all of the `ascend_expect_callback` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_expect_callback(
    packet: &Packet,
) -> Result<Vec<AscendExpectCallback>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_EXPECT_CALLBACK_TYPE) {
        vec.push(avp.encode_u32()? as AscendExpectCallback);
    }
    Ok(vec)
}

pub const ASCEND_EVENT_TYPE_TYPE: AVPType = 150;
/// Delete all of `ascend_event_type` values from a packet.
pub fn delete_ascend_event_type(packet: &mut Packet) {
    packet.delete(ASCEND_EVENT_TYPE_TYPE);
}
/// Add `ascend_event_type` value-defined integer value to a packet.
pub fn add_ascend_event_type(packet: &mut Packet, value: AscendEventType) {
    packet.add(AVP::from_u32(ASCEND_EVENT_TYPE_TYPE, value));
}
/// Lookup a `ascend_event_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_event_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_event_type(packet: &Packet) -> Option<Result<AscendEventType, AVPError>> {
    packet
        .lookup(ASCEND_EVENT_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendEventType))
}
/// Lookup all of the `ascend_event_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_event_type(packet: &Packet) -> Result<Vec<AscendEventType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_EVENT_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendEventType);
    }
    Ok(vec)
}

pub const ASCEND_SESSION_SVR_KEY_TYPE: AVPType = 151;
/// Delete all of `ascend_session_svr_key` values from a packet.
pub fn delete_ascend_session_svr_key(packet: &mut Packet) {
    packet.delete(ASCEND_SESSION_SVR_KEY_TYPE);
}
/// Add `ascend_session_svr_key` string value to a packet.
pub fn add_ascend_session_svr_key(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_SESSION_SVR_KEY_TYPE, value));
}
/// Lookup a `ascend_session_svr_key` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_session_svr_key`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_session_svr_key(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_SESSION_SVR_KEY_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_session_svr_key` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_session_svr_key(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SESSION_SVR_KEY_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_MULTICAST_RATE_LIMIT_TYPE: AVPType = 152;
/// Delete all of `ascend_multicast_rate_limit` values from a packet.
pub fn delete_ascend_multicast_rate_limit(packet: &mut Packet) {
    packet.delete(ASCEND_MULTICAST_RATE_LIMIT_TYPE);
}
/// Add `ascend_multicast_rate_limit` integer value to a packet.
pub fn add_ascend_multicast_rate_limit(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MULTICAST_RATE_LIMIT_TYPE, value));
}
/// Lookup a `ascend_multicast_rate_limit` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_multicast_rate_limit`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_multicast_rate_limit(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_MULTICAST_RATE_LIMIT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_multicast_rate_limit` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_multicast_rate_limit(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MULTICAST_RATE_LIMIT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_IF_NETMASK_TYPE: AVPType = 153;
/// Delete all of `ascend_if_netmask` values from a packet.
pub fn delete_ascend_if_netmask(packet: &mut Packet) {
    packet.delete(ASCEND_IF_NETMASK_TYPE);
}
/// Add `ascend_if_netmask` ipaddr value to a packet.
pub fn add_ascend_if_netmask(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_IF_NETMASK_TYPE, value));
}
/// Lookup a `ascend_if_netmask` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_if_netmask`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_if_netmask(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet.lookup(ASCEND_IF_NETMASK_TYPE).map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_if_netmask` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_if_netmask(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IF_NETMASK_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_REMOTE_ADDR_TYPE: AVPType = 154;
/// Delete all of `ascend_remote_addr` values from a packet.
pub fn delete_ascend_remote_addr(packet: &mut Packet) {
    packet.delete(ASCEND_REMOTE_ADDR_TYPE);
}
/// Add `ascend_remote_addr` ipaddr value to a packet.
pub fn add_ascend_remote_addr(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_REMOTE_ADDR_TYPE, value));
}
/// Lookup a `ascend_remote_addr` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_remote_addr`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_remote_addr(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet.lookup(ASCEND_REMOTE_ADDR_TYPE).map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_remote_addr` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_remote_addr(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_REMOTE_ADDR_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_MULTICAST_CLIENT_TYPE: AVPType = 155;
/// Delete all of `ascend_multicast_client` values from a packet.
pub fn delete_ascend_multicast_client(packet: &mut Packet) {
    packet.delete(ASCEND_MULTICAST_CLIENT_TYPE);
}
/// Add `ascend_multicast_client` value-defined integer value to a packet.
pub fn add_ascend_multicast_client(packet: &mut Packet, value: AscendMulticastClient) {
    packet.add(AVP::from_u32(ASCEND_MULTICAST_CLIENT_TYPE, value));
}
/// Lookup a `ascend_multicast_client` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_multicast_client`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_multicast_client(
    packet: &Packet,
) -> Option<Result<AscendMulticastClient, AVPError>> {
    packet
        .lookup(ASCEND_MULTICAST_CLIENT_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendMulticastClient))
}
/// Lookup all of the `ascend_multicast_client` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_multicast_client(
    packet: &Packet,
) -> Result<Vec<AscendMulticastClient>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MULTICAST_CLIENT_TYPE) {
        vec.push(avp.encode_u32()? as AscendMulticastClient);
    }
    Ok(vec)
}

pub const ASCEND_FR_CIRCUIT_NAME_TYPE: AVPType = 156;
/// Delete all of `ascend_fr_circuit_name` values from a packet.
pub fn delete_ascend_fr_circuit_name(packet: &mut Packet) {
    packet.delete(ASCEND_FR_CIRCUIT_NAME_TYPE);
}
/// Add `ascend_fr_circuit_name` string value to a packet.
pub fn add_ascend_fr_circuit_name(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_FR_CIRCUIT_NAME_TYPE, value));
}
/// Lookup a `ascend_fr_circuit_name` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_circuit_name`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_circuit_name(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_FR_CIRCUIT_NAME_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_fr_circuit_name` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_circuit_name(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_CIRCUIT_NAME_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_LINK_UP_TYPE: AVPType = 157;
/// Delete all of `ascend_fr_link_up` values from a packet.
pub fn delete_ascend_fr_link_up(packet: &mut Packet) {
    packet.delete(ASCEND_FR_LINK_UP_TYPE);
}
/// Add `ascend_fr_link_up` value-defined integer value to a packet.
pub fn add_ascend_fr_link_up(packet: &mut Packet, value: AscendFRLinkUp) {
    packet.add(AVP::from_u32(ASCEND_FR_LINK_UP_TYPE, value));
}
/// Lookup a `ascend_fr_link_up` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_link_up`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_link_up(packet: &Packet) -> Option<Result<AscendFRLinkUp, AVPError>> {
    packet
        .lookup(ASCEND_FR_LINK_UP_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendFRLinkUp))
}
/// Lookup all of the `ascend_fr_link_up` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_link_up(packet: &Packet) -> Result<Vec<AscendFRLinkUp>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_LINK_UP_TYPE) {
        vec.push(avp.encode_u32()? as AscendFRLinkUp);
    }
    Ok(vec)
}

pub const ASCEND_FR_NAILED_GRP_TYPE: AVPType = 158;
/// Delete all of `ascend_fr_nailed_grp` values from a packet.
pub fn delete_ascend_fr_nailed_grp(packet: &mut Packet) {
    packet.delete(ASCEND_FR_NAILED_GRP_TYPE);
}
/// Add `ascend_fr_nailed_grp` integer value to a packet.
pub fn add_ascend_fr_nailed_grp(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_NAILED_GRP_TYPE, value));
}
/// Lookup a `ascend_fr_nailed_grp` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_nailed_grp`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_nailed_grp(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_FR_NAILED_GRP_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_nailed_grp` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_nailed_grp(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_NAILED_GRP_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_TYPE_TYPE: AVPType = 159;
/// Delete all of `ascend_fr_type` values from a packet.
pub fn delete_ascend_fr_type(packet: &mut Packet) {
    packet.delete(ASCEND_FR_TYPE_TYPE);
}
/// Add `ascend_fr_type` value-defined integer value to a packet.
pub fn add_ascend_fr_type(packet: &mut Packet, value: AscendFRType) {
    packet.add(AVP::from_u32(ASCEND_FR_TYPE_TYPE, value));
}
/// Lookup a `ascend_fr_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_type(packet: &Packet) -> Option<Result<AscendFRType, AVPError>> {
    packet
        .lookup(ASCEND_FR_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendFRType))
}
/// Lookup all of the `ascend_fr_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_type(packet: &Packet) -> Result<Vec<AscendFRType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendFRType);
    }
    Ok(vec)
}

pub const ASCEND_FR_LINK_MGT_TYPE: AVPType = 160;
/// Delete all of `ascend_fr_link_mgt` values from a packet.
pub fn delete_ascend_fr_link_mgt(packet: &mut Packet) {
    packet.delete(ASCEND_FR_LINK_MGT_TYPE);
}
/// Add `ascend_fr_link_mgt` value-defined integer value to a packet.
pub fn add_ascend_fr_link_mgt(packet: &mut Packet, value: AscendFRLinkMgt) {
    packet.add(AVP::from_u32(ASCEND_FR_LINK_MGT_TYPE, value));
}
/// Lookup a `ascend_fr_link_mgt` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_link_mgt`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_link_mgt(packet: &Packet) -> Option<Result<AscendFRLinkMgt, AVPError>> {
    packet
        .lookup(ASCEND_FR_LINK_MGT_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendFRLinkMgt))
}
/// Lookup all of the `ascend_fr_link_mgt` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_link_mgt(packet: &Packet) -> Result<Vec<AscendFRLinkMgt>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_LINK_MGT_TYPE) {
        vec.push(avp.encode_u32()? as AscendFRLinkMgt);
    }
    Ok(vec)
}

pub const ASCEND_FR_N391_TYPE: AVPType = 161;
/// Delete all of `ascend_fr_n391` values from a packet.
pub fn delete_ascend_fr_n391(packet: &mut Packet) {
    packet.delete(ASCEND_FR_N391_TYPE);
}
/// Add `ascend_fr_n391` integer value to a packet.
pub fn add_ascend_fr_n391(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_N391_TYPE, value));
}
/// Lookup a `ascend_fr_n391` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_n391`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_n391(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_FR_N391_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_n391` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_n391(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_N391_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_DCE_N392_TYPE: AVPType = 162;
/// Delete all of `ascend_fr_dce_n392` values from a packet.
pub fn delete_ascend_fr_dce_n392(packet: &mut Packet) {
    packet.delete(ASCEND_FR_DCE_N392_TYPE);
}
/// Add `ascend_fr_dce_n392` integer value to a packet.
pub fn add_ascend_fr_dce_n392(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_DCE_N392_TYPE, value));
}
/// Lookup a `ascend_fr_dce_n392` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_dce_n392`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_dce_n392(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_FR_DCE_N392_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_dce_n392` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_dce_n392(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_DCE_N392_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_DTE_N392_TYPE: AVPType = 163;
/// Delete all of `ascend_fr_dte_n392` values from a packet.
pub fn delete_ascend_fr_dte_n392(packet: &mut Packet) {
    packet.delete(ASCEND_FR_DTE_N392_TYPE);
}
/// Add `ascend_fr_dte_n392` integer value to a packet.
pub fn add_ascend_fr_dte_n392(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_DTE_N392_TYPE, value));
}
/// Lookup a `ascend_fr_dte_n392` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_dte_n392`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_dte_n392(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_FR_DTE_N392_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_dte_n392` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_dte_n392(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_DTE_N392_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_DCE_N393_TYPE: AVPType = 164;
/// Delete all of `ascend_fr_dce_n393` values from a packet.
pub fn delete_ascend_fr_dce_n393(packet: &mut Packet) {
    packet.delete(ASCEND_FR_DCE_N393_TYPE);
}
/// Add `ascend_fr_dce_n393` integer value to a packet.
pub fn add_ascend_fr_dce_n393(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_DCE_N393_TYPE, value));
}
/// Lookup a `ascend_fr_dce_n393` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_dce_n393`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_dce_n393(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_FR_DCE_N393_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_dce_n393` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_dce_n393(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_DCE_N393_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_DTE_N393_TYPE: AVPType = 165;
/// Delete all of `ascend_fr_dte_n393` values from a packet.
pub fn delete_ascend_fr_dte_n393(packet: &mut Packet) {
    packet.delete(ASCEND_FR_DTE_N393_TYPE);
}
/// Add `ascend_fr_dte_n393` integer value to a packet.
pub fn add_ascend_fr_dte_n393(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_DTE_N393_TYPE, value));
}
/// Lookup a `ascend_fr_dte_n393` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_dte_n393`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_dte_n393(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_FR_DTE_N393_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_dte_n393` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_dte_n393(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_DTE_N393_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_T391_TYPE: AVPType = 166;
/// Delete all of `ascend_fr_t391` values from a packet.
pub fn delete_ascend_fr_t391(packet: &mut Packet) {
    packet.delete(ASCEND_FR_T391_TYPE);
}
/// Add `ascend_fr_t391` integer value to a packet.
pub fn add_ascend_fr_t391(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_T391_TYPE, value));
}
/// Lookup a `ascend_fr_t391` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_t391`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_t391(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_FR_T391_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_t391` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_t391(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_T391_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_T392_TYPE: AVPType = 167;
/// Delete all of `ascend_fr_t392` values from a packet.
pub fn delete_ascend_fr_t392(packet: &mut Packet) {
    packet.delete(ASCEND_FR_T392_TYPE);
}
/// Add `ascend_fr_t392` integer value to a packet.
pub fn add_ascend_fr_t392(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_T392_TYPE, value));
}
/// Lookup a `ascend_fr_t392` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_t392`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_t392(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_FR_T392_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_t392` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_t392(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_T392_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_BRIDGE_ADDRESS_TYPE: AVPType = 168;
/// Delete all of `ascend_bridge_address` values from a packet.
pub fn delete_ascend_bridge_address(packet: &mut Packet) {
    packet.delete(ASCEND_BRIDGE_ADDRESS_TYPE);
}
/// Add `ascend_bridge_address` string value to a packet.
pub fn add_ascend_bridge_address(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_BRIDGE_ADDRESS_TYPE, value));
}
/// Lookup a `ascend_bridge_address` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_bridge_address`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_bridge_address(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_BRIDGE_ADDRESS_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_bridge_address` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_bridge_address(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BRIDGE_ADDRESS_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_TS_IDLE_LIMIT_TYPE: AVPType = 169;
/// Delete all of `ascend_ts_idle_limit` values from a packet.
pub fn delete_ascend_ts_idle_limit(packet: &mut Packet) {
    packet.delete(ASCEND_TS_IDLE_LIMIT_TYPE);
}
/// Add `ascend_ts_idle_limit` integer value to a packet.
pub fn add_ascend_ts_idle_limit(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_TS_IDLE_LIMIT_TYPE, value));
}
/// Lookup a `ascend_ts_idle_limit` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ts_idle_limit`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ts_idle_limit(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_TS_IDLE_LIMIT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_ts_idle_limit` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ts_idle_limit(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TS_IDLE_LIMIT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_TS_IDLE_MODE_TYPE: AVPType = 170;
/// Delete all of `ascend_ts_idle_mode` values from a packet.
pub fn delete_ascend_ts_idle_mode(packet: &mut Packet) {
    packet.delete(ASCEND_TS_IDLE_MODE_TYPE);
}
/// Add `ascend_ts_idle_mode` value-defined integer value to a packet.
pub fn add_ascend_ts_idle_mode(packet: &mut Packet, value: AscendTSIdleMode) {
    packet.add(AVP::from_u32(ASCEND_TS_IDLE_MODE_TYPE, value));
}
/// Lookup a `ascend_ts_idle_mode` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ts_idle_mode`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ts_idle_mode(packet: &Packet) -> Option<Result<AscendTSIdleMode, AVPError>> {
    packet
        .lookup(ASCEND_TS_IDLE_MODE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendTSIdleMode))
}
/// Lookup all of the `ascend_ts_idle_mode` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ts_idle_mode(packet: &Packet) -> Result<Vec<AscendTSIdleMode>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TS_IDLE_MODE_TYPE) {
        vec.push(avp.encode_u32()? as AscendTSIdleMode);
    }
    Ok(vec)
}

pub const ASCEND_DBA_MONITOR_TYPE: AVPType = 171;
/// Delete all of `ascend_dba_monitor` values from a packet.
pub fn delete_ascend_dba_monitor(packet: &mut Packet) {
    packet.delete(ASCEND_DBA_MONITOR_TYPE);
}
/// Add `ascend_dba_monitor` value-defined integer value to a packet.
pub fn add_ascend_dba_monitor(packet: &mut Packet, value: AscendDBAMonitor) {
    packet.add(AVP::from_u32(ASCEND_DBA_MONITOR_TYPE, value));
}
/// Lookup a `ascend_dba_monitor` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dba_monitor`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dba_monitor(packet: &Packet) -> Option<Result<AscendDBAMonitor, AVPError>> {
    packet
        .lookup(ASCEND_DBA_MONITOR_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendDBAMonitor))
}
/// Lookup all of the `ascend_dba_monitor` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dba_monitor(packet: &Packet) -> Result<Vec<AscendDBAMonitor>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DBA_MONITOR_TYPE) {
        vec.push(avp.encode_u32()? as AscendDBAMonitor);
    }
    Ok(vec)
}

pub const ASCEND_BASE_CHANNEL_COUNT_TYPE: AVPType = 172;
/// Delete all of `ascend_base_channel_count` values from a packet.
pub fn delete_ascend_base_channel_count(packet: &mut Packet) {
    packet.delete(ASCEND_BASE_CHANNEL_COUNT_TYPE);
}
/// Add `ascend_base_channel_count` integer value to a packet.
pub fn add_ascend_base_channel_count(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_BASE_CHANNEL_COUNT_TYPE, value));
}
/// Lookup a `ascend_base_channel_count` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_base_channel_count`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_base_channel_count(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_BASE_CHANNEL_COUNT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_base_channel_count` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_base_channel_count(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BASE_CHANNEL_COUNT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_MINIMUM_CHANNELS_TYPE: AVPType = 173;
/// Delete all of `ascend_minimum_channels` values from a packet.
pub fn delete_ascend_minimum_channels(packet: &mut Packet) {
    packet.delete(ASCEND_MINIMUM_CHANNELS_TYPE);
}
/// Add `ascend_minimum_channels` integer value to a packet.
pub fn add_ascend_minimum_channels(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MINIMUM_CHANNELS_TYPE, value));
}
/// Lookup a `ascend_minimum_channels` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_minimum_channels`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_minimum_channels(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_MINIMUM_CHANNELS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_minimum_channels` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_minimum_channels(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MINIMUM_CHANNELS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_IPX_ROUTE_TYPE: AVPType = 174;
/// Delete all of `ascend_ipx_route` values from a packet.
pub fn delete_ascend_ipx_route(packet: &mut Packet) {
    packet.delete(ASCEND_IPX_ROUTE_TYPE);
}
/// Add `ascend_ipx_route` string value to a packet.
pub fn add_ascend_ipx_route(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_IPX_ROUTE_TYPE, value));
}
/// Lookup a `ascend_ipx_route` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ipx_route`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ipx_route(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_IPX_ROUTE_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_ipx_route` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ipx_route(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IPX_ROUTE_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_FT1_CALLER_TYPE: AVPType = 175;
/// Delete all of `ascend_ft1_caller` values from a packet.
pub fn delete_ascend_ft1_caller(packet: &mut Packet) {
    packet.delete(ASCEND_FT1_CALLER_TYPE);
}
/// Add `ascend_ft1_caller` value-defined integer value to a packet.
pub fn add_ascend_ft1_caller(packet: &mut Packet, value: AscendFT1Caller) {
    packet.add(AVP::from_u32(ASCEND_FT1_CALLER_TYPE, value));
}
/// Lookup a `ascend_ft1_caller` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ft1_caller`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ft1_caller(packet: &Packet) -> Option<Result<AscendFT1Caller, AVPError>> {
    packet
        .lookup(ASCEND_FT1_CALLER_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendFT1Caller))
}
/// Lookup all of the `ascend_ft1_caller` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ft1_caller(packet: &Packet) -> Result<Vec<AscendFT1Caller>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FT1_CALLER_TYPE) {
        vec.push(avp.encode_u32()? as AscendFT1Caller);
    }
    Ok(vec)
}

pub const ASCEND_BACKUP_TYPE: AVPType = 176;
/// Delete all of `ascend_backup` values from a packet.
pub fn delete_ascend_backup(packet: &mut Packet) {
    packet.delete(ASCEND_BACKUP_TYPE);
}
/// Add `ascend_backup` string value to a packet.
pub fn add_ascend_backup(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_BACKUP_TYPE, value));
}
/// Lookup a `ascend_backup` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_backup`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_backup(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_BACKUP_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_backup` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_backup(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BACKUP_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_CALL_TYPE_TYPE: AVPType = 177;
/// Delete all of `ascend_call_type` values from a packet.
pub fn delete_ascend_call_type(packet: &mut Packet) {
    packet.delete(ASCEND_CALL_TYPE_TYPE);
}
/// Add `ascend_call_type` value-defined integer value to a packet.
pub fn add_ascend_call_type(packet: &mut Packet, value: AscendCallType) {
    packet.add(AVP::from_u32(ASCEND_CALL_TYPE_TYPE, value));
}
/// Lookup a `ascend_call_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_call_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_call_type(packet: &Packet) -> Option<Result<AscendCallType, AVPError>> {
    packet
        .lookup(ASCEND_CALL_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCallType))
}
/// Lookup all of the `ascend_call_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_call_type(packet: &Packet) -> Result<Vec<AscendCallType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALL_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendCallType);
    }
    Ok(vec)
}

pub const ASCEND_GROUP_TYPE: AVPType = 178;
/// Delete all of `ascend_group` values from a packet.
pub fn delete_ascend_group(packet: &mut Packet) {
    packet.delete(ASCEND_GROUP_TYPE);
}
/// Add `ascend_group` string value to a packet.
pub fn add_ascend_group(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_GROUP_TYPE, value));
}
/// Lookup a `ascend_group` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_group`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_group(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_GROUP_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_group` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_group(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_GROUP_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_DLCI_TYPE: AVPType = 179;
/// Delete all of `ascend_fr_dlci` values from a packet.
pub fn delete_ascend_fr_dlci(packet: &mut Packet) {
    packet.delete(ASCEND_FR_DLCI_TYPE);
}
/// Add `ascend_fr_dlci` integer value to a packet.
pub fn add_ascend_fr_dlci(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_DLCI_TYPE, value));
}
/// Lookup a `ascend_fr_dlci` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_dlci`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_dlci(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_FR_DLCI_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_dlci` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_dlci(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_DLCI_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_PROFILE_NAME_TYPE: AVPType = 180;
/// Delete all of `ascend_fr_profile_name` values from a packet.
pub fn delete_ascend_fr_profile_name(packet: &mut Packet) {
    packet.delete(ASCEND_FR_PROFILE_NAME_TYPE);
}
/// Add `ascend_fr_profile_name` string value to a packet.
pub fn add_ascend_fr_profile_name(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_FR_PROFILE_NAME_TYPE, value));
}
/// Lookup a `ascend_fr_profile_name` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_profile_name`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_profile_name(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_FR_PROFILE_NAME_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_fr_profile_name` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_profile_name(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_PROFILE_NAME_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_ARA_PW_TYPE: AVPType = 181;
/// Delete all of `ascend_ara_pw` values from a packet.
pub fn delete_ascend_ara_pw(packet: &mut Packet) {
    packet.delete(ASCEND_ARA_PW_TYPE);
}
/// Add `ascend_ara_pw` string value to a packet.
pub fn add_ascend_ara_pw(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_ARA_PW_TYPE, value));
}
/// Lookup a `ascend_ara_pw` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ara_pw`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ara_pw(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_ARA_PW_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_ara_pw` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ara_pw(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ARA_PW_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_IPX_NODE_ADDR_TYPE: AVPType = 182;
/// Delete all of `ascend_ipx_node_addr` values from a packet.
pub fn delete_ascend_ipx_node_addr(packet: &mut Packet) {
    packet.delete(ASCEND_IPX_NODE_ADDR_TYPE);
}
/// Add `ascend_ipx_node_addr` string value to a packet.
pub fn add_ascend_ipx_node_addr(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_IPX_NODE_ADDR_TYPE, value));
}
/// Lookup a `ascend_ipx_node_addr` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ipx_node_addr`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ipx_node_addr(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_IPX_NODE_ADDR_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_ipx_node_addr` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ipx_node_addr(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IPX_NODE_ADDR_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_HOME_AGENT_IP_ADDR_TYPE: AVPType = 183;
/// Delete all of `ascend_home_agent_ip_addr` values from a packet.
pub fn delete_ascend_home_agent_ip_addr(packet: &mut Packet) {
    packet.delete(ASCEND_HOME_AGENT_IP_ADDR_TYPE);
}
/// Add `ascend_home_agent_ip_addr` ipaddr value to a packet.
pub fn add_ascend_home_agent_ip_addr(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_HOME_AGENT_IP_ADDR_TYPE, value));
}
/// Lookup a `ascend_home_agent_ip_addr` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_home_agent_ip_addr`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_home_agent_ip_addr(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet
        .lookup(ASCEND_HOME_AGENT_IP_ADDR_TYPE)
        .map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_home_agent_ip_addr` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_home_agent_ip_addr(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_HOME_AGENT_IP_ADDR_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_HOME_AGENT_PASSWORD_TYPE: AVPType = 184;
/// Delete all of `ascend_home_agent_password` values from a packet.
pub fn delete_ascend_home_agent_password(packet: &mut Packet) {
    packet.delete(ASCEND_HOME_AGENT_PASSWORD_TYPE);
}
/// Add `ascend_home_agent_password` string value to a packet.
pub fn add_ascend_home_agent_password(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_HOME_AGENT_PASSWORD_TYPE, value));
}
/// Lookup a `ascend_home_agent_password` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_home_agent_password`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_home_agent_password(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_HOME_AGENT_PASSWORD_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_home_agent_password` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_home_agent_password(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_HOME_AGENT_PASSWORD_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_HOME_NETWORK_NAME_TYPE: AVPType = 185;
/// Delete all of `ascend_home_network_name` values from a packet.
pub fn delete_ascend_home_network_name(packet: &mut Packet) {
    packet.delete(ASCEND_HOME_NETWORK_NAME_TYPE);
}
/// Add `ascend_home_network_name` string value to a packet.
pub fn add_ascend_home_network_name(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_HOME_NETWORK_NAME_TYPE, value));
}
/// Lookup a `ascend_home_network_name` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_home_network_name`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_home_network_name(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_HOME_NETWORK_NAME_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_home_network_name` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_home_network_name(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_HOME_NETWORK_NAME_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_HOME_AGENT_UDP_PORT_TYPE: AVPType = 186;
/// Delete all of `ascend_home_agent_udp_port` values from a packet.
pub fn delete_ascend_home_agent_udp_port(packet: &mut Packet) {
    packet.delete(ASCEND_HOME_AGENT_UDP_PORT_TYPE);
}
/// Add `ascend_home_agent_udp_port` integer value to a packet.
pub fn add_ascend_home_agent_udp_port(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_HOME_AGENT_UDP_PORT_TYPE, value));
}
/// Lookup a `ascend_home_agent_udp_port` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_home_agent_udp_port`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_home_agent_udp_port(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_HOME_AGENT_UDP_PORT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_home_agent_udp_port` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_home_agent_udp_port(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_HOME_AGENT_UDP_PORT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_MULTILINK_ID_TYPE: AVPType = 187;
/// Delete all of `ascend_multilink_id` values from a packet.
pub fn delete_ascend_multilink_id(packet: &mut Packet) {
    packet.delete(ASCEND_MULTILINK_ID_TYPE);
}
/// Add `ascend_multilink_id` integer value to a packet.
pub fn add_ascend_multilink_id(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MULTILINK_ID_TYPE, value));
}
/// Lookup a `ascend_multilink_id` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_multilink_id`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_multilink_id(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_MULTILINK_ID_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_multilink_id` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_multilink_id(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MULTILINK_ID_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_NUM_IN_MULTILINK_TYPE: AVPType = 188;
/// Delete all of `ascend_num_in_multilink` values from a packet.
pub fn delete_ascend_num_in_multilink(packet: &mut Packet) {
    packet.delete(ASCEND_NUM_IN_MULTILINK_TYPE);
}
/// Add `ascend_num_in_multilink` integer value to a packet.
pub fn add_ascend_num_in_multilink(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_NUM_IN_MULTILINK_TYPE, value));
}
/// Lookup a `ascend_num_in_multilink` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_num_in_multilink`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_num_in_multilink(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_NUM_IN_MULTILINK_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_num_in_multilink` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_num_in_multilink(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_NUM_IN_MULTILINK_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FIRST_DEST_TYPE: AVPType = 189;
/// Delete all of `ascend_first_dest` values from a packet.
pub fn delete_ascend_first_dest(packet: &mut Packet) {
    packet.delete(ASCEND_FIRST_DEST_TYPE);
}
/// Add `ascend_first_dest` ipaddr value to a packet.
pub fn add_ascend_first_dest(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_FIRST_DEST_TYPE, value));
}
/// Lookup a `ascend_first_dest` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_first_dest`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_first_dest(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet.lookup(ASCEND_FIRST_DEST_TYPE).map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_first_dest` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_first_dest(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FIRST_DEST_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_PRE_INPUT_OCTETS_TYPE: AVPType = 190;
/// Delete all of `ascend_pre_input_octets` values from a packet.
pub fn delete_ascend_pre_input_octets(packet: &mut Packet) {
    packet.delete(ASCEND_PRE_INPUT_OCTETS_TYPE);
}
/// Add `ascend_pre_input_octets` integer value to a packet.
pub fn add_ascend_pre_input_octets(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_PRE_INPUT_OCTETS_TYPE, value));
}
/// Lookup a `ascend_pre_input_octets` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_pre_input_octets`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_pre_input_octets(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_PRE_INPUT_OCTETS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_pre_input_octets` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_pre_input_octets(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PRE_INPUT_OCTETS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_PRE_OUTPUT_OCTETS_TYPE: AVPType = 191;
/// Delete all of `ascend_pre_output_octets` values from a packet.
pub fn delete_ascend_pre_output_octets(packet: &mut Packet) {
    packet.delete(ASCEND_PRE_OUTPUT_OCTETS_TYPE);
}
/// Add `ascend_pre_output_octets` integer value to a packet.
pub fn add_ascend_pre_output_octets(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_PRE_OUTPUT_OCTETS_TYPE, value));
}
/// Lookup a `ascend_pre_output_octets` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_pre_output_octets`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_pre_output_octets(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_PRE_OUTPUT_OCTETS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_pre_output_octets` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_pre_output_octets(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PRE_OUTPUT_OCTETS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_PRE_INPUT_PACKETS_TYPE: AVPType = 192;
/// Delete all of `ascend_pre_input_packets` values from a packet.
pub fn delete_ascend_pre_input_packets(packet: &mut Packet) {
    packet.delete(ASCEND_PRE_INPUT_PACKETS_TYPE);
}
/// Add `ascend_pre_input_packets` integer value to a packet.
pub fn add_ascend_pre_input_packets(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_PRE_INPUT_PACKETS_TYPE, value));
}
/// Lookup a `ascend_pre_input_packets` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_pre_input_packets`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_pre_input_packets(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_PRE_INPUT_PACKETS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_pre_input_packets` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_pre_input_packets(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PRE_INPUT_PACKETS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_PRE_OUTPUT_PACKETS_TYPE: AVPType = 193;
/// Delete all of `ascend_pre_output_packets` values from a packet.
pub fn delete_ascend_pre_output_packets(packet: &mut Packet) {
    packet.delete(ASCEND_PRE_OUTPUT_PACKETS_TYPE);
}
/// Add `ascend_pre_output_packets` integer value to a packet.
pub fn add_ascend_pre_output_packets(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_PRE_OUTPUT_PACKETS_TYPE, value));
}
/// Lookup a `ascend_pre_output_packets` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_pre_output_packets`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_pre_output_packets(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_PRE_OUTPUT_PACKETS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_pre_output_packets` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_pre_output_packets(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PRE_OUTPUT_PACKETS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_MAXIMUM_TIME_TYPE: AVPType = 194;
/// Delete all of `ascend_maximum_time` values from a packet.
pub fn delete_ascend_maximum_time(packet: &mut Packet) {
    packet.delete(ASCEND_MAXIMUM_TIME_TYPE);
}
/// Add `ascend_maximum_time` integer value to a packet.
pub fn add_ascend_maximum_time(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MAXIMUM_TIME_TYPE, value));
}
/// Lookup a `ascend_maximum_time` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_maximum_time`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_maximum_time(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_MAXIMUM_TIME_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_maximum_time` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_maximum_time(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MAXIMUM_TIME_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_DISCONNECT_CAUSE_TYPE: AVPType = 195;
/// Delete all of `ascend_disconnect_cause` values from a packet.
pub fn delete_ascend_disconnect_cause(packet: &mut Packet) {
    packet.delete(ASCEND_DISCONNECT_CAUSE_TYPE);
}
/// Add `ascend_disconnect_cause` value-defined integer value to a packet.
pub fn add_ascend_disconnect_cause(packet: &mut Packet, value: AscendDisconnectCause) {
    packet.add(AVP::from_u32(ASCEND_DISCONNECT_CAUSE_TYPE, value));
}
/// Lookup a `ascend_disconnect_cause` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_disconnect_cause`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_disconnect_cause(
    packet: &Packet,
) -> Option<Result<AscendDisconnectCause, AVPError>> {
    packet
        .lookup(ASCEND_DISCONNECT_CAUSE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendDisconnectCause))
}
/// Lookup all of the `ascend_disconnect_cause` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_disconnect_cause(
    packet: &Packet,
) -> Result<Vec<AscendDisconnectCause>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DISCONNECT_CAUSE_TYPE) {
        vec.push(avp.encode_u32()? as AscendDisconnectCause);
    }
    Ok(vec)
}

pub const ASCEND_CONNECT_PROGRESS_TYPE: AVPType = 196;
/// Delete all of `ascend_connect_progress` values from a packet.
pub fn delete_ascend_connect_progress(packet: &mut Packet) {
    packet.delete(ASCEND_CONNECT_PROGRESS_TYPE);
}
/// Add `ascend_connect_progress` value-defined integer value to a packet.
pub fn add_ascend_connect_progress(packet: &mut Packet, value: AscendConnectProgress) {
    packet.add(AVP::from_u32(ASCEND_CONNECT_PROGRESS_TYPE, value));
}
/// Lookup a `ascend_connect_progress` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_connect_progress`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_connect_progress(
    packet: &Packet,
) -> Option<Result<AscendConnectProgress, AVPError>> {
    packet
        .lookup(ASCEND_CONNECT_PROGRESS_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendConnectProgress))
}
/// Lookup all of the `ascend_connect_progress` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_connect_progress(
    packet: &Packet,
) -> Result<Vec<AscendConnectProgress>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CONNECT_PROGRESS_TYPE) {
        vec.push(avp.encode_u32()? as AscendConnectProgress);
    }
    Ok(vec)
}

pub const ASCEND_DATA_RATE_TYPE: AVPType = 197;
/// Delete all of `ascend_data_rate` values from a packet.
pub fn delete_ascend_data_rate(packet: &mut Packet) {
    packet.delete(ASCEND_DATA_RATE_TYPE);
}
/// Add `ascend_data_rate` integer value to a packet.
pub fn add_ascend_data_rate(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_DATA_RATE_TYPE, value));
}
/// Lookup a `ascend_data_rate` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_data_rate`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_data_rate(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_DATA_RATE_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_data_rate` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_data_rate(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DATA_RATE_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_PRE_SESSION_TIME_TYPE: AVPType = 198;
/// Delete all of `ascend_pre_session_time` values from a packet.
pub fn delete_ascend_pre_session_time(packet: &mut Packet) {
    packet.delete(ASCEND_PRE_SESSION_TIME_TYPE);
}
/// Add `ascend_pre_session_time` integer value to a packet.
pub fn add_ascend_pre_session_time(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_PRE_SESSION_TIME_TYPE, value));
}
/// Lookup a `ascend_pre_session_time` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_pre_session_time`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_pre_session_time(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_PRE_SESSION_TIME_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_pre_session_time` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_pre_session_time(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PRE_SESSION_TIME_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_TOKEN_IDLE_TYPE: AVPType = 199;
/// Delete all of `ascend_token_idle` values from a packet.
pub fn delete_ascend_token_idle(packet: &mut Packet) {
    packet.delete(ASCEND_TOKEN_IDLE_TYPE);
}
/// Add `ascend_token_idle` integer value to a packet.
pub fn add_ascend_token_idle(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_TOKEN_IDLE_TYPE, value));
}
/// Lookup a `ascend_token_idle` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_token_idle`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_token_idle(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_TOKEN_IDLE_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_token_idle` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_token_idle(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TOKEN_IDLE_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_TOKEN_IMMEDIATE_TYPE: AVPType = 200;
/// Delete all of `ascend_token_immediate` values from a packet.
pub fn delete_ascend_token_immediate(packet: &mut Packet) {
    packet.delete(ASCEND_TOKEN_IMMEDIATE_TYPE);
}
/// Add `ascend_token_immediate` value-defined integer value to a packet.
pub fn add_ascend_token_immediate(packet: &mut Packet, value: AscendTokenImmediate) {
    packet.add(AVP::from_u32(ASCEND_TOKEN_IMMEDIATE_TYPE, value));
}
/// Lookup a `ascend_token_immediate` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_token_immediate`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_token_immediate(
    packet: &Packet,
) -> Option<Result<AscendTokenImmediate, AVPError>> {
    packet
        .lookup(ASCEND_TOKEN_IMMEDIATE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendTokenImmediate))
}
/// Lookup all of the `ascend_token_immediate` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_token_immediate(
    packet: &Packet,
) -> Result<Vec<AscendTokenImmediate>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TOKEN_IMMEDIATE_TYPE) {
        vec.push(avp.encode_u32()? as AscendTokenImmediate);
    }
    Ok(vec)
}

pub const ASCEND_REQUIRE_AUTH_TYPE: AVPType = 201;
/// Delete all of `ascend_require_auth` values from a packet.
pub fn delete_ascend_require_auth(packet: &mut Packet) {
    packet.delete(ASCEND_REQUIRE_AUTH_TYPE);
}
/// Add `ascend_require_auth` value-defined integer value to a packet.
pub fn add_ascend_require_auth(packet: &mut Packet, value: AscendRequireAuth) {
    packet.add(AVP::from_u32(ASCEND_REQUIRE_AUTH_TYPE, value));
}
/// Lookup a `ascend_require_auth` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_require_auth`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_require_auth(packet: &Packet) -> Option<Result<AscendRequireAuth, AVPError>> {
    packet
        .lookup(ASCEND_REQUIRE_AUTH_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendRequireAuth))
}
/// Lookup all of the `ascend_require_auth` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_require_auth(packet: &Packet) -> Result<Vec<AscendRequireAuth>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_REQUIRE_AUTH_TYPE) {
        vec.push(avp.encode_u32()? as AscendRequireAuth);
    }
    Ok(vec)
}

pub const ASCEND_NUMBER_SESSIONS_TYPE: AVPType = 202;
/// Delete all of `ascend_number_sessions` values from a packet.
pub fn delete_ascend_number_sessions(packet: &mut Packet) {
    packet.delete(ASCEND_NUMBER_SESSIONS_TYPE);
}
/// Add `ascend_number_sessions` string value to a packet.
pub fn add_ascend_number_sessions(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_NUMBER_SESSIONS_TYPE, value));
}
/// Lookup a `ascend_number_sessions` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_number_sessions`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_number_sessions(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_NUMBER_SESSIONS_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_number_sessions` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_number_sessions(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_NUMBER_SESSIONS_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_AUTHEN_ALIAS_TYPE: AVPType = 203;
/// Delete all of `ascend_authen_alias` values from a packet.
pub fn delete_ascend_authen_alias(packet: &mut Packet) {
    packet.delete(ASCEND_AUTHEN_ALIAS_TYPE);
}
/// Add `ascend_authen_alias` string value to a packet.
pub fn add_ascend_authen_alias(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_AUTHEN_ALIAS_TYPE, value));
}
/// Lookup a `ascend_authen_alias` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_authen_alias`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_authen_alias(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_AUTHEN_ALIAS_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_authen_alias` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_authen_alias(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_AUTHEN_ALIAS_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_TOKEN_EXPIRY_TYPE: AVPType = 204;
/// Delete all of `ascend_token_expiry` values from a packet.
pub fn delete_ascend_token_expiry(packet: &mut Packet) {
    packet.delete(ASCEND_TOKEN_EXPIRY_TYPE);
}
/// Add `ascend_token_expiry` integer value to a packet.
pub fn add_ascend_token_expiry(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_TOKEN_EXPIRY_TYPE, value));
}
/// Lookup a `ascend_token_expiry` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_token_expiry`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_token_expiry(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_TOKEN_EXPIRY_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_token_expiry` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_token_expiry(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TOKEN_EXPIRY_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_MENU_SELECTOR_TYPE: AVPType = 205;
/// Delete all of `ascend_menu_selector` values from a packet.
pub fn delete_ascend_menu_selector(packet: &mut Packet) {
    packet.delete(ASCEND_MENU_SELECTOR_TYPE);
}
/// Add `ascend_menu_selector` string value to a packet.
pub fn add_ascend_menu_selector(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_MENU_SELECTOR_TYPE, value));
}
/// Lookup a `ascend_menu_selector` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_menu_selector`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_menu_selector(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_MENU_SELECTOR_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_menu_selector` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_menu_selector(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MENU_SELECTOR_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_MENU_ITEM_TYPE: AVPType = 206;
/// Delete all of `ascend_menu_item` values from a packet.
pub fn delete_ascend_menu_item(packet: &mut Packet) {
    packet.delete(ASCEND_MENU_ITEM_TYPE);
}
/// Add `ascend_menu_item` string value to a packet.
pub fn add_ascend_menu_item(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_MENU_ITEM_TYPE, value));
}
/// Lookup a `ascend_menu_item` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_menu_item`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_menu_item(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_MENU_ITEM_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_menu_item` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_menu_item(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MENU_ITEM_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_PW_WARNTIME_TYPE: AVPType = 207;
/// Delete all of `ascend_pw_warntime` values from a packet.
pub fn delete_ascend_pw_warntime(packet: &mut Packet) {
    packet.delete(ASCEND_PW_WARNTIME_TYPE);
}
/// Add `ascend_pw_warntime` value-defined integer value to a packet.
pub fn add_ascend_pw_warntime(packet: &mut Packet, value: AscendPWWarntime) {
    packet.add(AVP::from_u32(ASCEND_PW_WARNTIME_TYPE, value));
}
/// Lookup a `ascend_pw_warntime` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_pw_warntime`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_pw_warntime(packet: &Packet) -> Option<Result<AscendPWWarntime, AVPError>> {
    packet
        .lookup(ASCEND_PW_WARNTIME_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendPWWarntime))
}
/// Lookup all of the `ascend_pw_warntime` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_pw_warntime(packet: &Packet) -> Result<Vec<AscendPWWarntime>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PW_WARNTIME_TYPE) {
        vec.push(avp.encode_u32()? as AscendPWWarntime);
    }
    Ok(vec)
}

pub const ASCEND_PW_LIFETIME_TYPE: AVPType = 208;
/// Delete all of `ascend_pw_lifetime` values from a packet.
pub fn delete_ascend_pw_lifetime(packet: &mut Packet) {
    packet.delete(ASCEND_PW_LIFETIME_TYPE);
}
/// Add `ascend_pw_lifetime` value-defined integer value to a packet.
pub fn add_ascend_pw_lifetime(packet: &mut Packet, value: AscendPWLifetime) {
    packet.add(AVP::from_u32(ASCEND_PW_LIFETIME_TYPE, value));
}
/// Lookup a `ascend_pw_lifetime` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_pw_lifetime`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_pw_lifetime(packet: &Packet) -> Option<Result<AscendPWLifetime, AVPError>> {
    packet
        .lookup(ASCEND_PW_LIFETIME_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendPWLifetime))
}
/// Lookup all of the `ascend_pw_lifetime` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_pw_lifetime(packet: &Packet) -> Result<Vec<AscendPWLifetime>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PW_LIFETIME_TYPE) {
        vec.push(avp.encode_u32()? as AscendPWLifetime);
    }
    Ok(vec)
}

pub const ASCEND_IP_DIRECT_TYPE: AVPType = 209;
/// Delete all of `ascend_ip_direct` values from a packet.
pub fn delete_ascend_ip_direct(packet: &mut Packet) {
    packet.delete(ASCEND_IP_DIRECT_TYPE);
}
/// Add `ascend_ip_direct` ipaddr value to a packet.
pub fn add_ascend_ip_direct(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_IP_DIRECT_TYPE, value));
}
/// Lookup a `ascend_ip_direct` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ip_direct`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ip_direct(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet.lookup(ASCEND_IP_DIRECT_TYPE).map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_ip_direct` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ip_direct(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IP_DIRECT_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_PPP_VJ_SLOT_COMP_TYPE: AVPType = 210;
/// Delete all of `ascend_ppp_vj_slot_comp` values from a packet.
pub fn delete_ascend_ppp_vj_slot_comp(packet: &mut Packet) {
    packet.delete(ASCEND_PPP_VJ_SLOT_COMP_TYPE);
}
/// Add `ascend_ppp_vj_slot_comp` value-defined integer value to a packet.
pub fn add_ascend_ppp_vj_slot_comp(packet: &mut Packet, value: AscendPPPVJSlotComp) {
    packet.add(AVP::from_u32(ASCEND_PPP_VJ_SLOT_COMP_TYPE, value));
}
/// Lookup a `ascend_ppp_vj_slot_comp` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ppp_vj_slot_comp`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ppp_vj_slot_comp(
    packet: &Packet,
) -> Option<Result<AscendPPPVJSlotComp, AVPError>> {
    packet
        .lookup(ASCEND_PPP_VJ_SLOT_COMP_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendPPPVJSlotComp))
}
/// Lookup all of the `ascend_ppp_vj_slot_comp` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ppp_vj_slot_comp(
    packet: &Packet,
) -> Result<Vec<AscendPPPVJSlotComp>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PPP_VJ_SLOT_COMP_TYPE) {
        vec.push(avp.encode_u32()? as AscendPPPVJSlotComp);
    }
    Ok(vec)
}

pub const ASCEND_PPP_VJ_1172_TYPE: AVPType = 211;
/// Delete all of `ascend_ppp_vj_1172` values from a packet.
pub fn delete_ascend_ppp_vj_1172(packet: &mut Packet) {
    packet.delete(ASCEND_PPP_VJ_1172_TYPE);
}
/// Add `ascend_ppp_vj_1172` value-defined integer value to a packet.
pub fn add_ascend_ppp_vj_1172(packet: &mut Packet, value: AscendPPPVJ1172) {
    packet.add(AVP::from_u32(ASCEND_PPP_VJ_1172_TYPE, value));
}
/// Lookup a `ascend_ppp_vj_1172` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ppp_vj_1172`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ppp_vj_1172(packet: &Packet) -> Option<Result<AscendPPPVJ1172, AVPError>> {
    packet
        .lookup(ASCEND_PPP_VJ_1172_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendPPPVJ1172))
}
/// Lookup all of the `ascend_ppp_vj_1172` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ppp_vj_1172(packet: &Packet) -> Result<Vec<AscendPPPVJ1172>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PPP_VJ_1172_TYPE) {
        vec.push(avp.encode_u32()? as AscendPPPVJ1172);
    }
    Ok(vec)
}

pub const ASCEND_PPP_ASYNC_MAP_TYPE: AVPType = 212;
/// Delete all of `ascend_ppp_async_map` values from a packet.
pub fn delete_ascend_ppp_async_map(packet: &mut Packet) {
    packet.delete(ASCEND_PPP_ASYNC_MAP_TYPE);
}
/// Add `ascend_ppp_async_map` integer value to a packet.
pub fn add_ascend_ppp_async_map(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_PPP_ASYNC_MAP_TYPE, value));
}
/// Lookup a `ascend_ppp_async_map` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ppp_async_map`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ppp_async_map(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_PPP_ASYNC_MAP_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_ppp_async_map` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ppp_async_map(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PPP_ASYNC_MAP_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_THIRD_PROMPT_TYPE: AVPType = 213;
/// Delete all of `ascend_third_prompt` values from a packet.
pub fn delete_ascend_third_prompt(packet: &mut Packet) {
    packet.delete(ASCEND_THIRD_PROMPT_TYPE);
}
/// Add `ascend_third_prompt` string value to a packet.
pub fn add_ascend_third_prompt(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_THIRD_PROMPT_TYPE, value));
}
/// Lookup a `ascend_third_prompt` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_third_prompt`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_third_prompt(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_THIRD_PROMPT_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_third_prompt` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_third_prompt(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_THIRD_PROMPT_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_SEND_SECRET_TYPE: AVPType = 214;
/// Delete all of `ascend_send_secret` values from a packet.
pub fn delete_ascend_send_secret(packet: &mut Packet) {
    packet.delete(ASCEND_SEND_SECRET_TYPE);
}
/// Add `ascend_send_secret` string value to a packet.
pub fn add_ascend_send_secret(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_SEND_SECRET_TYPE, value));
}
/// Lookup a `ascend_send_secret` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_send_secret`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_send_secret(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_SEND_SECRET_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_send_secret` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_send_secret(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SEND_SECRET_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_RECEIVE_SECRET_TYPE: AVPType = 215;
/// Delete all of `ascend_receive_secret` values from a packet.
pub fn delete_ascend_receive_secret(packet: &mut Packet) {
    packet.delete(ASCEND_RECEIVE_SECRET_TYPE);
}
/// Add `ascend_receive_secret` string value to a packet.
pub fn add_ascend_receive_secret(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_RECEIVE_SECRET_TYPE, value));
}
/// Lookup a `ascend_receive_secret` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_receive_secret`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_receive_secret(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_RECEIVE_SECRET_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_receive_secret` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_receive_secret(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_RECEIVE_SECRET_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_IPX_PEER_MODE_TYPE: AVPType = 216;
/// Delete all of `ascend_ipx_peer_mode` values from a packet.
pub fn delete_ascend_ipx_peer_mode(packet: &mut Packet) {
    packet.delete(ASCEND_IPX_PEER_MODE_TYPE);
}
/// Add `ascend_ipx_peer_mode` value-defined integer value to a packet.
pub fn add_ascend_ipx_peer_mode(packet: &mut Packet, value: AscendIPXPeerMode) {
    packet.add(AVP::from_u32(ASCEND_IPX_PEER_MODE_TYPE, value));
}
/// Lookup a `ascend_ipx_peer_mode` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ipx_peer_mode`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ipx_peer_mode(packet: &Packet) -> Option<Result<AscendIPXPeerMode, AVPError>> {
    packet
        .lookup(ASCEND_IPX_PEER_MODE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendIPXPeerMode))
}
/// Lookup all of the `ascend_ipx_peer_mode` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ipx_peer_mode(
    packet: &Packet,
) -> Result<Vec<AscendIPXPeerMode>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IPX_PEER_MODE_TYPE) {
        vec.push(avp.encode_u32()? as AscendIPXPeerMode);
    }
    Ok(vec)
}

pub const ASCEND_IP_POOL_DEFINITION_TYPE: AVPType = 217;
/// Delete all of `ascend_ip_pool_definition` values from a packet.
pub fn delete_ascend_ip_pool_definition(packet: &mut Packet) {
    packet.delete(ASCEND_IP_POOL_DEFINITION_TYPE);
}
/// Add `ascend_ip_pool_definition` string value to a packet.
pub fn add_ascend_ip_pool_definition(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_IP_POOL_DEFINITION_TYPE, value));
}
/// Lookup a `ascend_ip_pool_definition` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ip_pool_definition`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ip_pool_definition(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_IP_POOL_DEFINITION_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_ip_pool_definition` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ip_pool_definition(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IP_POOL_DEFINITION_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_ASSIGN_IP_POOL_TYPE: AVPType = 218;
/// Delete all of `ascend_assign_ip_pool` values from a packet.
pub fn delete_ascend_assign_ip_pool(packet: &mut Packet) {
    packet.delete(ASCEND_ASSIGN_IP_POOL_TYPE);
}
/// Add `ascend_assign_ip_pool` integer value to a packet.
pub fn add_ascend_assign_ip_pool(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_ASSIGN_IP_POOL_TYPE, value));
}
/// Lookup a `ascend_assign_ip_pool` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_assign_ip_pool`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_assign_ip_pool(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_ASSIGN_IP_POOL_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_assign_ip_pool` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_assign_ip_pool(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ASSIGN_IP_POOL_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_DIRECT_TYPE: AVPType = 219;
/// Delete all of `ascend_fr_direct` values from a packet.
pub fn delete_ascend_fr_direct(packet: &mut Packet) {
    packet.delete(ASCEND_FR_DIRECT_TYPE);
}
/// Add `ascend_fr_direct` value-defined integer value to a packet.
pub fn add_ascend_fr_direct(packet: &mut Packet, value: AscendFRDirect) {
    packet.add(AVP::from_u32(ASCEND_FR_DIRECT_TYPE, value));
}
/// Lookup a `ascend_fr_direct` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_direct`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_direct(packet: &Packet) -> Option<Result<AscendFRDirect, AVPError>> {
    packet
        .lookup(ASCEND_FR_DIRECT_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendFRDirect))
}
/// Lookup all of the `ascend_fr_direct` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_direct(packet: &Packet) -> Result<Vec<AscendFRDirect>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_DIRECT_TYPE) {
        vec.push(avp.encode_u32()? as AscendFRDirect);
    }
    Ok(vec)
}

pub const ASCEND_FR_DIRECT_PROFILE_TYPE: AVPType = 220;
/// Delete all of `ascend_fr_direct_profile` values from a packet.
pub fn delete_ascend_fr_direct_profile(packet: &mut Packet) {
    packet.delete(ASCEND_FR_DIRECT_PROFILE_TYPE);
}
/// Add `ascend_fr_direct_profile` string value to a packet.
pub fn add_ascend_fr_direct_profile(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_FR_DIRECT_PROFILE_TYPE, value));
}
/// Lookup a `ascend_fr_direct_profile` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_direct_profile`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_direct_profile(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_FR_DIRECT_PROFILE_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_fr_direct_profile` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_direct_profile(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_DIRECT_PROFILE_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_FR_DIRECT_DLCI_TYPE: AVPType = 221;
/// Delete all of `ascend_fr_direct_dlci` values from a packet.
pub fn delete_ascend_fr_direct_dlci(packet: &mut Packet) {
    packet.delete(ASCEND_FR_DIRECT_DLCI_TYPE);
}
/// Add `ascend_fr_direct_dlci` integer value to a packet.
pub fn add_ascend_fr_direct_dlci(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_FR_DIRECT_DLCI_TYPE, value));
}
/// Lookup a `ascend_fr_direct_dlci` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_fr_direct_dlci`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_fr_direct_dlci(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_FR_DIRECT_DLCI_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_fr_direct_dlci` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_fr_direct_dlci(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FR_DIRECT_DLCI_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_HANDLE_IPX_TYPE: AVPType = 222;
/// Delete all of `ascend_handle_ipx` values from a packet.
pub fn delete_ascend_handle_ipx(packet: &mut Packet) {
    packet.delete(ASCEND_HANDLE_IPX_TYPE);
}
/// Add `ascend_handle_ipx` value-defined integer value to a packet.
pub fn add_ascend_handle_ipx(packet: &mut Packet, value: AscendHandleIPX) {
    packet.add(AVP::from_u32(ASCEND_HANDLE_IPX_TYPE, value));
}
/// Lookup a `ascend_handle_ipx` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_handle_ipx`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_handle_ipx(packet: &Packet) -> Option<Result<AscendHandleIPX, AVPError>> {
    packet
        .lookup(ASCEND_HANDLE_IPX_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendHandleIPX))
}
/// Lookup all of the `ascend_handle_ipx` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_handle_ipx(packet: &Packet) -> Result<Vec<AscendHandleIPX>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_HANDLE_IPX_TYPE) {
        vec.push(avp.encode_u32()? as AscendHandleIPX);
    }
    Ok(vec)
}

pub const ASCEND_NETWARE_TIMEOUT_TYPE: AVPType = 223;
/// Delete all of `ascend_netware_timeout` values from a packet.
pub fn delete_ascend_netware_timeout(packet: &mut Packet) {
    packet.delete(ASCEND_NETWARE_TIMEOUT_TYPE);
}
/// Add `ascend_netware_timeout` integer value to a packet.
pub fn add_ascend_netware_timeout(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_NETWARE_TIMEOUT_TYPE, value));
}
/// Lookup a `ascend_netware_timeout` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_netware_timeout`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_netware_timeout(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_NETWARE_TIMEOUT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_netware_timeout` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_netware_timeout(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_NETWARE_TIMEOUT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_IPX_ALIAS_TYPE: AVPType = 224;
/// Delete all of `ascend_ipx_alias` values from a packet.
pub fn delete_ascend_ipx_alias(packet: &mut Packet) {
    packet.delete(ASCEND_IPX_ALIAS_TYPE);
}
/// Add `ascend_ipx_alias` integer value to a packet.
pub fn add_ascend_ipx_alias(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_IPX_ALIAS_TYPE, value));
}
/// Lookup a `ascend_ipx_alias` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ipx_alias`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ipx_alias(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_IPX_ALIAS_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_ipx_alias` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ipx_alias(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IPX_ALIAS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_METRIC_TYPE: AVPType = 225;
/// Delete all of `ascend_metric` values from a packet.
pub fn delete_ascend_metric(packet: &mut Packet) {
    packet.delete(ASCEND_METRIC_TYPE);
}
/// Add `ascend_metric` integer value to a packet.
pub fn add_ascend_metric(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_METRIC_TYPE, value));
}
/// Lookup a `ascend_metric` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_metric`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_metric(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_METRIC_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_metric` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_metric(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_METRIC_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_PRI_NUMBER_TYPE_TYPE: AVPType = 226;
/// Delete all of `ascend_pri_number_type` values from a packet.
pub fn delete_ascend_pri_number_type(packet: &mut Packet) {
    packet.delete(ASCEND_PRI_NUMBER_TYPE_TYPE);
}
/// Add `ascend_pri_number_type` value-defined integer value to a packet.
pub fn add_ascend_pri_number_type(packet: &mut Packet, value: AscendPRINumberType) {
    packet.add(AVP::from_u32(ASCEND_PRI_NUMBER_TYPE_TYPE, value));
}
/// Lookup a `ascend_pri_number_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_pri_number_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_pri_number_type(
    packet: &Packet,
) -> Option<Result<AscendPRINumberType, AVPError>> {
    packet
        .lookup(ASCEND_PRI_NUMBER_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendPRINumberType))
}
/// Lookup all of the `ascend_pri_number_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_pri_number_type(
    packet: &Packet,
) -> Result<Vec<AscendPRINumberType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PRI_NUMBER_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendPRINumberType);
    }
    Ok(vec)
}

pub const ASCEND_DIAL_NUMBER_TYPE: AVPType = 227;
/// Delete all of `ascend_dial_number` values from a packet.
pub fn delete_ascend_dial_number(packet: &mut Packet) {
    packet.delete(ASCEND_DIAL_NUMBER_TYPE);
}
/// Add `ascend_dial_number` string value to a packet.
pub fn add_ascend_dial_number(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_DIAL_NUMBER_TYPE, value));
}
/// Lookup a `ascend_dial_number` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dial_number`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dial_number(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_DIAL_NUMBER_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_dial_number` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dial_number(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DIAL_NUMBER_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_ROUTE_IP_TYPE: AVPType = 228;
/// Delete all of `ascend_route_ip` values from a packet.
pub fn delete_ascend_route_ip(packet: &mut Packet) {
    packet.delete(ASCEND_ROUTE_IP_TYPE);
}
/// Add `ascend_route_ip` value-defined integer value to a packet.
pub fn add_ascend_route_ip(packet: &mut Packet, value: AscendRouteIP) {
    packet.add(AVP::from_u32(ASCEND_ROUTE_IP_TYPE, value));
}
/// Lookup a `ascend_route_ip` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_route_ip`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_route_ip(packet: &Packet) -> Option<Result<AscendRouteIP, AVPError>> {
    packet
        .lookup(ASCEND_ROUTE_IP_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendRouteIP))
}
/// Lookup all of the `ascend_route_ip` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_route_ip(packet: &Packet) -> Result<Vec<AscendRouteIP>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ROUTE_IP_TYPE) {
        vec.push(avp.encode_u32()? as AscendRouteIP);
    }
    Ok(vec)
}

pub const ASCEND_ROUTE_IPX_TYPE: AVPType = 229;
/// Delete all of `ascend_route_ipx` values from a packet.
pub fn delete_ascend_route_ipx(packet: &mut Packet) {
    packet.delete(ASCEND_ROUTE_IPX_TYPE);
}
/// Add `ascend_route_ipx` value-defined integer value to a packet.
pub fn add_ascend_route_ipx(packet: &mut Packet, value: AscendRouteIPX) {
    packet.add(AVP::from_u32(ASCEND_ROUTE_IPX_TYPE, value));
}
/// Lookup a `ascend_route_ipx` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_route_ipx`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_route_ipx(packet: &Packet) -> Option<Result<AscendRouteIPX, AVPError>> {
    packet
        .lookup(ASCEND_ROUTE_IPX_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendRouteIPX))
}
/// Lookup all of the `ascend_route_ipx` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_route_ipx(packet: &Packet) -> Result<Vec<AscendRouteIPX>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ROUTE_IPX_TYPE) {
        vec.push(avp.encode_u32()? as AscendRouteIPX);
    }
    Ok(vec)
}

pub const ASCEND_BRIDGE_TYPE: AVPType = 230;
/// Delete all of `ascend_bridge` values from a packet.
pub fn delete_ascend_bridge(packet: &mut Packet) {
    packet.delete(ASCEND_BRIDGE_TYPE);
}
/// Add `ascend_bridge` value-defined integer value to a packet.
pub fn add_ascend_bridge(packet: &mut Packet, value: AscendBridge) {
    packet.add(AVP::from_u32(ASCEND_BRIDGE_TYPE, value));
}
/// Lookup a `ascend_bridge` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_bridge`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_bridge(packet: &Packet) -> Option<Result<AscendBridge, AVPError>> {
    packet
        .lookup(ASCEND_BRIDGE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendBridge))
}
/// Lookup all of the `ascend_bridge` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_bridge(packet: &Packet) -> Result<Vec<AscendBridge>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BRIDGE_TYPE) {
        vec.push(avp.encode_u32()? as AscendBridge);
    }
    Ok(vec)
}

pub const ASCEND_SEND_AUTH_TYPE: AVPType = 231;
/// Delete all of `ascend_send_auth` values from a packet.
pub fn delete_ascend_send_auth(packet: &mut Packet) {
    packet.delete(ASCEND_SEND_AUTH_TYPE);
}
/// Add `ascend_send_auth` value-defined integer value to a packet.
pub fn add_ascend_send_auth(packet: &mut Packet, value: AscendSendAuth) {
    packet.add(AVP::from_u32(ASCEND_SEND_AUTH_TYPE, value));
}
/// Lookup a `ascend_send_auth` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_send_auth`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_send_auth(packet: &Packet) -> Option<Result<AscendSendAuth, AVPError>> {
    packet
        .lookup(ASCEND_SEND_AUTH_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendSendAuth))
}
/// Lookup all of the `ascend_send_auth` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_send_auth(packet: &Packet) -> Result<Vec<AscendSendAuth>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SEND_AUTH_TYPE) {
        vec.push(avp.encode_u32()? as AscendSendAuth);
    }
    Ok(vec)
}

pub const ASCEND_SEND_PASSWD_TYPE: AVPType = 232;
/// Delete all of `ascend_send_passwd` values from a packet.
pub fn delete_ascend_send_passwd(packet: &mut Packet) {
    packet.delete(ASCEND_SEND_PASSWD_TYPE);
}
/// Add `ascend_send_passwd` string value to a packet.
pub fn add_ascend_send_passwd(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_SEND_PASSWD_TYPE, value));
}
/// Lookup a `ascend_send_passwd` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_send_passwd`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_send_passwd(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_SEND_PASSWD_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_send_passwd` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_send_passwd(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SEND_PASSWD_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_LINK_COMPRESSION_TYPE: AVPType = 233;
/// Delete all of `ascend_link_compression` values from a packet.
pub fn delete_ascend_link_compression(packet: &mut Packet) {
    packet.delete(ASCEND_LINK_COMPRESSION_TYPE);
}
/// Add `ascend_link_compression` value-defined integer value to a packet.
pub fn add_ascend_link_compression(packet: &mut Packet, value: AscendLinkCompression) {
    packet.add(AVP::from_u32(ASCEND_LINK_COMPRESSION_TYPE, value));
}
/// Lookup a `ascend_link_compression` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_link_compression`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_link_compression(
    packet: &Packet,
) -> Option<Result<AscendLinkCompression, AVPError>> {
    packet
        .lookup(ASCEND_LINK_COMPRESSION_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendLinkCompression))
}
/// Lookup all of the `ascend_link_compression` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_link_compression(
    packet: &Packet,
) -> Result<Vec<AscendLinkCompression>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_LINK_COMPRESSION_TYPE) {
        vec.push(avp.encode_u32()? as AscendLinkCompression);
    }
    Ok(vec)
}

pub const ASCEND_TARGET_UTIL_TYPE: AVPType = 234;
/// Delete all of `ascend_target_util` values from a packet.
pub fn delete_ascend_target_util(packet: &mut Packet) {
    packet.delete(ASCEND_TARGET_UTIL_TYPE);
}
/// Add `ascend_target_util` integer value to a packet.
pub fn add_ascend_target_util(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_TARGET_UTIL_TYPE, value));
}
/// Lookup a `ascend_target_util` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_target_util`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_target_util(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_TARGET_UTIL_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_target_util` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_target_util(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TARGET_UTIL_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_MAXIMUM_CHANNELS_TYPE: AVPType = 235;
/// Delete all of `ascend_maximum_channels` values from a packet.
pub fn delete_ascend_maximum_channels(packet: &mut Packet) {
    packet.delete(ASCEND_MAXIMUM_CHANNELS_TYPE);
}
/// Add `ascend_maximum_channels` integer value to a packet.
pub fn add_ascend_maximum_channels(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MAXIMUM_CHANNELS_TYPE, value));
}
/// Lookup a `ascend_maximum_channels` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_maximum_channels`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_maximum_channels(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_MAXIMUM_CHANNELS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_maximum_channels` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_maximum_channels(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MAXIMUM_CHANNELS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_INC_CHANNEL_COUNT_TYPE: AVPType = 236;
/// Delete all of `ascend_inc_channel_count` values from a packet.
pub fn delete_ascend_inc_channel_count(packet: &mut Packet) {
    packet.delete(ASCEND_INC_CHANNEL_COUNT_TYPE);
}
/// Add `ascend_inc_channel_count` integer value to a packet.
pub fn add_ascend_inc_channel_count(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_INC_CHANNEL_COUNT_TYPE, value));
}
/// Lookup a `ascend_inc_channel_count` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_inc_channel_count`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_inc_channel_count(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_INC_CHANNEL_COUNT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_inc_channel_count` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_inc_channel_count(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_INC_CHANNEL_COUNT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_DEC_CHANNEL_COUNT_TYPE: AVPType = 237;
/// Delete all of `ascend_dec_channel_count` values from a packet.
pub fn delete_ascend_dec_channel_count(packet: &mut Packet) {
    packet.delete(ASCEND_DEC_CHANNEL_COUNT_TYPE);
}
/// Add `ascend_dec_channel_count` integer value to a packet.
pub fn add_ascend_dec_channel_count(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_DEC_CHANNEL_COUNT_TYPE, value));
}
/// Lookup a `ascend_dec_channel_count` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_dec_channel_count`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_dec_channel_count(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_DEC_CHANNEL_COUNT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_dec_channel_count` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_dec_channel_count(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DEC_CHANNEL_COUNT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_SECONDS_OF_HISTORY_TYPE: AVPType = 238;
/// Delete all of `ascend_seconds_of_history` values from a packet.
pub fn delete_ascend_seconds_of_history(packet: &mut Packet) {
    packet.delete(ASCEND_SECONDS_OF_HISTORY_TYPE);
}
/// Add `ascend_seconds_of_history` integer value to a packet.
pub fn add_ascend_seconds_of_history(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_SECONDS_OF_HISTORY_TYPE, value));
}
/// Lookup a `ascend_seconds_of_history` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_seconds_of_history`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_seconds_of_history(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_SECONDS_OF_HISTORY_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_seconds_of_history` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_seconds_of_history(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_SECONDS_OF_HISTORY_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_HISTORY_WEIGH_TYPE_TYPE: AVPType = 239;
/// Delete all of `ascend_history_weigh_type` values from a packet.
pub fn delete_ascend_history_weigh_type(packet: &mut Packet) {
    packet.delete(ASCEND_HISTORY_WEIGH_TYPE_TYPE);
}
/// Add `ascend_history_weigh_type` value-defined integer value to a packet.
pub fn add_ascend_history_weigh_type(packet: &mut Packet, value: AscendHistoryWeighType) {
    packet.add(AVP::from_u32(ASCEND_HISTORY_WEIGH_TYPE_TYPE, value));
}
/// Lookup a `ascend_history_weigh_type` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_history_weigh_type`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_history_weigh_type(
    packet: &Packet,
) -> Option<Result<AscendHistoryWeighType, AVPError>> {
    packet
        .lookup(ASCEND_HISTORY_WEIGH_TYPE_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendHistoryWeighType))
}
/// Lookup all of the `ascend_history_weigh_type` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_history_weigh_type(
    packet: &Packet,
) -> Result<Vec<AscendHistoryWeighType>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_HISTORY_WEIGH_TYPE_TYPE) {
        vec.push(avp.encode_u32()? as AscendHistoryWeighType);
    }
    Ok(vec)
}

pub const ASCEND_ADD_SECONDS_TYPE: AVPType = 240;
/// Delete all of `ascend_add_seconds` values from a packet.
pub fn delete_ascend_add_seconds(packet: &mut Packet) {
    packet.delete(ASCEND_ADD_SECONDS_TYPE);
}
/// Add `ascend_add_seconds` integer value to a packet.
pub fn add_ascend_add_seconds(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_ADD_SECONDS_TYPE, value));
}
/// Lookup a `ascend_add_seconds` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_add_seconds`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_add_seconds(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_ADD_SECONDS_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_add_seconds` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_add_seconds(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_ADD_SECONDS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_REMOVE_SECONDS_TYPE: AVPType = 241;
/// Delete all of `ascend_remove_seconds` values from a packet.
pub fn delete_ascend_remove_seconds(packet: &mut Packet) {
    packet.delete(ASCEND_REMOVE_SECONDS_TYPE);
}
/// Add `ascend_remove_seconds` integer value to a packet.
pub fn add_ascend_remove_seconds(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_REMOVE_SECONDS_TYPE, value));
}
/// Lookup a `ascend_remove_seconds` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_remove_seconds`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_remove_seconds(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_REMOVE_SECONDS_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_remove_seconds` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_remove_seconds(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_REMOVE_SECONDS_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_DATA_FILTER_TYPE: AVPType = 242;
/// Delete all of `ascend_data_filter` values from a packet.
pub fn delete_ascend_data_filter(packet: &mut Packet) {
    packet.delete(ASCEND_DATA_FILTER_TYPE);
}
/// Add `ascend_data_filter` octets value to a packet.
pub fn add_ascend_data_filter(packet: &mut Packet, value: &[u8]) {
    packet.add(AVP::from_bytes(ASCEND_DATA_FILTER_TYPE, value));
}
/// Lookup a `ascend_data_filter` octets value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_data_filter`, it returns `None`.
#[must_use]
pub fn lookup_ascend_data_filter(packet: &Packet) -> Option<Vec<u8>> {
    packet
        .lookup(ASCEND_DATA_FILTER_TYPE)
        .map(AVP::encode_bytes)
}
/// Lookup all of the `ascend_data_filter` octets value from a packet.
#[must_use]
pub fn lookup_all_ascend_data_filter(packet: &Packet) -> Vec<Vec<u8>> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DATA_FILTER_TYPE) {
        vec.push(avp.encode_bytes());
    }
    vec
}

pub const ASCEND_CALL_FILTER_TYPE: AVPType = 243;
/// Delete all of `ascend_call_filter` values from a packet.
pub fn delete_ascend_call_filter(packet: &mut Packet) {
    packet.delete(ASCEND_CALL_FILTER_TYPE);
}
/// Add `ascend_call_filter` octets value to a packet.
pub fn add_ascend_call_filter(packet: &mut Packet, value: &[u8]) {
    packet.add(AVP::from_bytes(ASCEND_CALL_FILTER_TYPE, value));
}
/// Lookup a `ascend_call_filter` octets value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_call_filter`, it returns `None`.
#[must_use]
pub fn lookup_ascend_call_filter(packet: &Packet) -> Option<Vec<u8>> {
    packet
        .lookup(ASCEND_CALL_FILTER_TYPE)
        .map(AVP::encode_bytes)
}
/// Lookup all of the `ascend_call_filter` octets value from a packet.
#[must_use]
pub fn lookup_all_ascend_call_filter(packet: &Packet) -> Vec<Vec<u8>> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALL_FILTER_TYPE) {
        vec.push(avp.encode_bytes());
    }
    vec
}

pub const ASCEND_IDLE_LIMIT_TYPE: AVPType = 244;
/// Delete all of `ascend_idle_limit` values from a packet.
pub fn delete_ascend_idle_limit(packet: &mut Packet) {
    packet.delete(ASCEND_IDLE_LIMIT_TYPE);
}
/// Add `ascend_idle_limit` integer value to a packet.
pub fn add_ascend_idle_limit(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_IDLE_LIMIT_TYPE, value));
}
/// Lookup a `ascend_idle_limit` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_idle_limit`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_idle_limit(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_IDLE_LIMIT_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_idle_limit` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_idle_limit(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_IDLE_LIMIT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_PREEMPT_LIMIT_TYPE: AVPType = 245;
/// Delete all of `ascend_preempt_limit` values from a packet.
pub fn delete_ascend_preempt_limit(packet: &mut Packet) {
    packet.delete(ASCEND_PREEMPT_LIMIT_TYPE);
}
/// Add `ascend_preempt_limit` integer value to a packet.
pub fn add_ascend_preempt_limit(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_PREEMPT_LIMIT_TYPE, value));
}
/// Lookup a `ascend_preempt_limit` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_preempt_limit`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_preempt_limit(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_PREEMPT_LIMIT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_preempt_limit` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_preempt_limit(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PREEMPT_LIMIT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_CALLBACK_TYPE: AVPType = 246;
/// Delete all of `ascend_callback` values from a packet.
pub fn delete_ascend_callback(packet: &mut Packet) {
    packet.delete(ASCEND_CALLBACK_TYPE);
}
/// Add `ascend_callback` value-defined integer value to a packet.
pub fn add_ascend_callback(packet: &mut Packet, value: AscendCallback) {
    packet.add(AVP::from_u32(ASCEND_CALLBACK_TYPE, value));
}
/// Lookup a `ascend_callback` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_callback`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_callback(packet: &Packet) -> Option<Result<AscendCallback, AVPError>> {
    packet
        .lookup(ASCEND_CALLBACK_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendCallback))
}
/// Lookup all of the `ascend_callback` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_callback(packet: &Packet) -> Result<Vec<AscendCallback>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALLBACK_TYPE) {
        vec.push(avp.encode_u32()? as AscendCallback);
    }
    Ok(vec)
}

pub const ASCEND_DATA_SVC_TYPE: AVPType = 247;
/// Delete all of `ascend_data_svc` values from a packet.
pub fn delete_ascend_data_svc(packet: &mut Packet) {
    packet.delete(ASCEND_DATA_SVC_TYPE);
}
/// Add `ascend_data_svc` value-defined integer value to a packet.
pub fn add_ascend_data_svc(packet: &mut Packet, value: AscendDataSvc) {
    packet.add(AVP::from_u32(ASCEND_DATA_SVC_TYPE, value));
}
/// Lookup a `ascend_data_svc` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_data_svc`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_data_svc(packet: &Packet) -> Option<Result<AscendDataSvc, AVPError>> {
    packet
        .lookup(ASCEND_DATA_SVC_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendDataSvc))
}
/// Lookup all of the `ascend_data_svc` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_data_svc(packet: &Packet) -> Result<Vec<AscendDataSvc>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_DATA_SVC_TYPE) {
        vec.push(avp.encode_u32()? as AscendDataSvc);
    }
    Ok(vec)
}

pub const ASCEND_FORCE_56_TYPE: AVPType = 248;
/// Delete all of `ascend_force_56` values from a packet.
pub fn delete_ascend_force_56(packet: &mut Packet) {
    packet.delete(ASCEND_FORCE_56_TYPE);
}
/// Add `ascend_force_56` value-defined integer value to a packet.
pub fn add_ascend_force_56(packet: &mut Packet, value: AscendForce56) {
    packet.add(AVP::from_u32(ASCEND_FORCE_56_TYPE, value));
}
/// Lookup a `ascend_force_56` value-defined integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_force_56`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_force_56(packet: &Packet) -> Option<Result<AscendForce56, AVPError>> {
    packet
        .lookup(ASCEND_FORCE_56_TYPE)
        .map(|v| Ok(v.encode_u32()? as AscendForce56))
}
/// Lookup all of the `ascend_force_56` value-defined integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_force_56(packet: &Packet) -> Result<Vec<AscendForce56>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_FORCE_56_TYPE) {
        vec.push(avp.encode_u32()? as AscendForce56);
    }
    Ok(vec)
}

pub const ASCEND_BILLING_NUMBER_TYPE: AVPType = 249;
/// Delete all of `ascend_billing_number` values from a packet.
pub fn delete_ascend_billing_number(packet: &mut Packet) {
    packet.delete(ASCEND_BILLING_NUMBER_TYPE);
}
/// Add `ascend_billing_number` string value to a packet.
pub fn add_ascend_billing_number(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_BILLING_NUMBER_TYPE, value));
}
/// Lookup a `ascend_billing_number` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_billing_number`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_billing_number(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_BILLING_NUMBER_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_billing_number` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_billing_number(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_BILLING_NUMBER_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_CALL_BY_CALL_TYPE: AVPType = 250;
/// Delete all of `ascend_call_by_call` values from a packet.
pub fn delete_ascend_call_by_call(packet: &mut Packet) {
    packet.delete(ASCEND_CALL_BY_CALL_TYPE);
}
/// Add `ascend_call_by_call` integer value to a packet.
pub fn add_ascend_call_by_call(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_CALL_BY_CALL_TYPE, value));
}
/// Lookup a `ascend_call_by_call` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_call_by_call`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_call_by_call(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_CALL_BY_CALL_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_call_by_call` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_call_by_call(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_CALL_BY_CALL_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_TRANSIT_NUMBER_TYPE: AVPType = 251;
/// Delete all of `ascend_transit_number` values from a packet.
pub fn delete_ascend_transit_number(packet: &mut Packet) {
    packet.delete(ASCEND_TRANSIT_NUMBER_TYPE);
}
/// Add `ascend_transit_number` string value to a packet.
pub fn add_ascend_transit_number(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_TRANSIT_NUMBER_TYPE, value));
}
/// Lookup a `ascend_transit_number` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_transit_number`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_transit_number(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet
        .lookup(ASCEND_TRANSIT_NUMBER_TYPE)
        .map(AVP::encode_string)
}
/// Lookup all of the `ascend_transit_number` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_transit_number(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_TRANSIT_NUMBER_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_HOST_INFO_TYPE: AVPType = 252;
/// Delete all of `ascend_host_info` values from a packet.
pub fn delete_ascend_host_info(packet: &mut Packet) {
    packet.delete(ASCEND_HOST_INFO_TYPE);
}
/// Add `ascend_host_info` string value to a packet.
pub fn add_ascend_host_info(packet: &mut Packet, value: &str) {
    packet.add(AVP::from_string(ASCEND_HOST_INFO_TYPE, value));
}
/// Lookup a `ascend_host_info` string value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_host_info`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_host_info(packet: &Packet) -> Option<Result<String, AVPError>> {
    packet.lookup(ASCEND_HOST_INFO_TYPE).map(AVP::encode_string)
}
/// Lookup all of the `ascend_host_info` string value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_host_info(packet: &Packet) -> Result<Vec<String>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_HOST_INFO_TYPE) {
        vec.push(avp.encode_string()?);
    }
    Ok(vec)
}

pub const ASCEND_PPP_ADDRESS_TYPE: AVPType = 253;
/// Delete all of `ascend_ppp_address` values from a packet.
pub fn delete_ascend_ppp_address(packet: &mut Packet) {
    packet.delete(ASCEND_PPP_ADDRESS_TYPE);
}
/// Add `ascend_ppp_address` ipaddr value to a packet.
pub fn add_ascend_ppp_address(packet: &mut Packet, value: &Ipv4Addr) {
    packet.add(AVP::from_ipv4(ASCEND_PPP_ADDRESS_TYPE, value));
}
/// Lookup a `ascend_ppp_address` ipaddr value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_ppp_address`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_ppp_address(packet: &Packet) -> Option<Result<Ipv4Addr, AVPError>> {
    packet.lookup(ASCEND_PPP_ADDRESS_TYPE).map(AVP::encode_ipv4)
}
/// Lookup all of the `ascend_ppp_address` ipaddr value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_ppp_address(packet: &Packet) -> Result<Vec<Ipv4Addr>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_PPP_ADDRESS_TYPE) {
        vec.push(avp.encode_ipv4()?);
    }
    Ok(vec)
}

pub const ASCEND_MPP_IDLE_PERCENT_TYPE: AVPType = 254;
/// Delete all of `ascend_mpp_idle_percent` values from a packet.
pub fn delete_ascend_mpp_idle_percent(packet: &mut Packet) {
    packet.delete(ASCEND_MPP_IDLE_PERCENT_TYPE);
}
/// Add `ascend_mpp_idle_percent` integer value to a packet.
pub fn add_ascend_mpp_idle_percent(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_MPP_IDLE_PERCENT_TYPE, value));
}
/// Lookup a `ascend_mpp_idle_percent` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_mpp_idle_percent`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_mpp_idle_percent(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet
        .lookup(ASCEND_MPP_IDLE_PERCENT_TYPE)
        .map(AVP::encode_u32)
}
/// Lookup all of the `ascend_mpp_idle_percent` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_mpp_idle_percent(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_MPP_IDLE_PERCENT_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub const ASCEND_XMIT_RATE_TYPE: AVPType = 255;
/// Delete all of `ascend_xmit_rate` values from a packet.
pub fn delete_ascend_xmit_rate(packet: &mut Packet) {
    packet.delete(ASCEND_XMIT_RATE_TYPE);
}
/// Add `ascend_xmit_rate` integer value to a packet.
pub fn add_ascend_xmit_rate(packet: &mut Packet, value: u32) {
    packet.add(AVP::from_u32(ASCEND_XMIT_RATE_TYPE, value));
}
/// Lookup a `ascend_xmit_rate` integer value from a packet.
///
/// It returns the first looked up value. If there is no associated value with `ascend_xmit_rate`, it returns `None`.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_ascend_xmit_rate(packet: &Packet) -> Option<Result<u32, AVPError>> {
    packet.lookup(ASCEND_XMIT_RATE_TYPE).map(AVP::encode_u32)
}
/// Lookup all of the `ascend_xmit_rate` integer value from a packet.
///
/// # Errors
///
/// Returns an `AVPError` if decoding fails.
#[must_use]
pub fn lookup_all_ascend_xmit_rate(packet: &Packet) -> Result<Vec<u32>, AVPError> {
    let mut vec = Vec::new();
    for avp in packet.lookup_all(ASCEND_XMIT_RATE_TYPE) {
        vec.push(avp.encode_u32()?);
    }
    Ok(vec)
}

pub type AscendATMDirect = u32;
pub const ASCEND_ATM_DIRECT_ATM_DIRECT_NO: AscendATMDirect = 0;
pub const ASCEND_ATM_DIRECT_ATM_DIRECT_YES: AscendATMDirect = 1;

pub type AscendATMFaultManagement = u32;
pub const ASCEND_ATM_FAULT_MANAGEMENT_VC_END_TO_END_LOOPBACK: AscendATMFaultManagement = 2;
pub const ASCEND_ATM_FAULT_MANAGEMENT_VC_NO_LOOPBACK: AscendATMFaultManagement = 0;
pub const ASCEND_ATM_FAULT_MANAGEMENT_VC_SEGMENT_LOOPBACK: AscendATMFaultManagement = 1;

pub type AscendAppletalkPeerMode = u32;
pub const ASCEND_APPLETALK_PEER_MODE_APPLETALK_PEER_DIALIN: AscendAppletalkPeerMode = 1;
pub const ASCEND_APPLETALK_PEER_MODE_APPLETALK_PEER_ROUTER: AscendAppletalkPeerMode = 0;

pub type AscendAuthType = u32;
pub const ASCEND_AUTH_TYPE_AUTH_ANY: AscendAuthType = 2;
pub const ASCEND_AUTH_TYPE_AUTH_CHAP: AscendAuthType = 4;
pub const ASCEND_AUTH_TYPE_AUTH_DEFAULT: AscendAuthType = 1;
pub const ASCEND_AUTH_TYPE_AUTH_MS_CHAP: AscendAuthType = 5;
pub const ASCEND_AUTH_TYPE_AUTH_NONE: AscendAuthType = 0;
pub const ASCEND_AUTH_TYPE_AUTH_PAP: AscendAuthType = 3;

pub type AscendBACPEnable = u32;
pub const ASCEND_BACP_ENABLE_BACP_NO: AscendBACPEnable = 0;
pub const ASCEND_BACP_ENABLE_BACP_YES: AscendBACPEnable = 1;

pub type AscendBIREnable = u32;
pub const ASCEND_BIR_ENABLE_BIR_ENABLE_NO: AscendBIREnable = 0;
pub const ASCEND_BIR_ENABLE_BIR_ENABLE_YES: AscendBIREnable = 1;

pub type AscendBIRProxy = u32;
pub const ASCEND_BIR_PROXY_BIR_PROXY_NO: AscendBIRProxy = 0;
pub const ASCEND_BIR_PROXY_BIR_PROXY_YES: AscendBIRProxy = 1;

pub type AscendBiDirectionalAuth = u32;
pub const ASCEND_BI_DIRECTIONAL_AUTH_BI_DIRECTIONAL_AUTH_ALLOWED: AscendBiDirectionalAuth = 1;
pub const ASCEND_BI_DIRECTIONAL_AUTH_BI_DIRECTIONAL_AUTH_NONE: AscendBiDirectionalAuth = 0;
pub const ASCEND_BI_DIRECTIONAL_AUTH_BI_DIRECTIONAL_AUTH_REQUIRED: AscendBiDirectionalAuth = 2;

pub type AscendBridge = u32;
pub const ASCEND_BRIDGE_BRIDGE_NO: AscendBridge = 0;
pub const ASCEND_BRIDGE_BRIDGE_YES: AscendBridge = 1;

pub type AscendBridgeNonPPPoE = u32;
pub const ASCEND_BRIDGE_NON_PP_PO_E_BRIDGE_NON_PP_PO_E_NO: AscendBridgeNonPPPoE = 0;
pub const ASCEND_BRIDGE_NON_PP_PO_E_BRIDGE_NON_PP_PO_E_YES: AscendBridgeNonPPPoE = 1;

pub type AscendCBCPEnable = u32;
pub const ASCEND_CBCP_ENABLE_CBCP_NOT_ENABLED: AscendCBCPEnable = 0;
pub const ASCEND_CBCP_ENABLE_CBCP_ENABLED: AscendCBCPEnable = 1;

pub type AscendCBCPMode = u32;
pub const ASCEND_CBCP_MODE_CBCP_NO_CALLBACK: AscendCBCPMode = 1;
pub const ASCEND_CBCP_MODE_CBCP_USER_CALLBACK: AscendCBCPMode = 2;
pub const ASCEND_CBCP_MODE_CBCP_PROFILE_CALLBACK: AscendCBCPMode = 3;
pub const ASCEND_CBCP_MODE_CBCP_ANY_OR_NO: AscendCBCPMode = 7;
pub const ASCEND_CBCP_MODE_CBCP_OFF: AscendCBCPMode = 8;

pub type AscendCacheRefresh = u32;
pub const ASCEND_CACHE_REFRESH_REFRESH_NO: AscendCacheRefresh = 0;
pub const ASCEND_CACHE_REFRESH_REFRESH_YES: AscendCacheRefresh = 1;

pub type AscendCallDirection = u32;
pub const ASCEND_CALL_DIRECTION_ASCEND_CALL_DIRECTION_INCOMING: AscendCallDirection = 0;
pub const ASCEND_CALL_DIRECTION_ASCEND_CALL_DIRECTION_OUTGOING: AscendCallDirection = 1;

pub type AscendCallType = u32;
pub const ASCEND_CALL_TYPE_SWITCHED: AscendCallType = 0;
pub const ASCEND_CALL_TYPE_NAILED: AscendCallType = 1;
pub const ASCEND_CALL_TYPE_NAILED_MPP: AscendCallType = 2;
pub const ASCEND_CALL_TYPE_PERM_SWITCHED: AscendCallType = 3;
pub const ASCEND_CALL_TYPE_AO_DI: AscendCallType = 6;
pub const ASCEND_CALL_TYPE_MEGA_MAX: AscendCallType = 7;

pub type AscendCallback = u32;
pub const ASCEND_CALLBACK_CALLBACK_NO: AscendCallback = 0;
pub const ASCEND_CALLBACK_CALLBACK_YES: AscendCallback = 1;

pub type AscendCallingIdNumberPlan = u32;
pub const ASCEND_CALLING_ID_NUMBER_PLAN_DATA: AscendCallingIdNumberPlan = 3;
pub const ASCEND_CALLING_ID_NUMBER_PLAN_ISDN_TELEPHONY: AscendCallingIdNumberPlan = 1;
pub const ASCEND_CALLING_ID_NUMBER_PLAN_NATIONAL: AscendCallingIdNumberPlan = 8;
pub const ASCEND_CALLING_ID_NUMBER_PLAN_PRIVATE: AscendCallingIdNumberPlan = 9;
pub const ASCEND_CALLING_ID_NUMBER_PLAN_TELEX: AscendCallingIdNumberPlan = 4;
pub const ASCEND_CALLING_ID_NUMBER_PLAN_UNKNOWN: AscendCallingIdNumberPlan = 0;

pub type AscendCallingIdPresentatn = u32;
pub const ASCEND_CALLING_ID_PRESENTATN_ALLOWED: AscendCallingIdPresentatn = 0;
pub const ASCEND_CALLING_ID_PRESENTATN_NUMBER_NOT_AVAILABLE: AscendCallingIdPresentatn = 2;
pub const ASCEND_CALLING_ID_PRESENTATN_RESTRICTED: AscendCallingIdPresentatn = 1;

pub type AscendCallingIdScreening = u32;
pub const ASCEND_CALLING_ID_SCREENING_NETWORK_PROVIDED: AscendCallingIdScreening = 3;
pub const ASCEND_CALLING_ID_SCREENING_USER_NOT_SCREENED: AscendCallingIdScreening = 0;
pub const ASCEND_CALLING_ID_SCREENING_USER_PROVIDED_FAILED: AscendCallingIdScreening = 2;
pub const ASCEND_CALLING_ID_SCREENING_USER_PROVIDED_PASSED: AscendCallingIdScreening = 1;

pub type AscendCallingIdTypeOfNum = u32;
pub const ASCEND_CALLING_ID_TYPE_OF_NUM_ABBREVIATED_NUMBER: AscendCallingIdTypeOfNum = 6;
pub const ASCEND_CALLING_ID_TYPE_OF_NUM_INTERNATIONAL_NUMBER: AscendCallingIdTypeOfNum = 1;
pub const ASCEND_CALLING_ID_TYPE_OF_NUM_NATIONAL_NUMBER: AscendCallingIdTypeOfNum = 2;
pub const ASCEND_CALLING_ID_TYPE_OF_NUM_NETWORK_SPECIFIC: AscendCallingIdTypeOfNum = 3;
pub const ASCEND_CALLING_ID_TYPE_OF_NUM_SUBSCRIBER_NUMBER: AscendCallingIdTypeOfNum = 4;
pub const ASCEND_CALLING_ID_TYPE_OF_NUM_UNKNOWN: AscendCallingIdTypeOfNum = 0;

pub type AscendCktType = u32;
pub const ASCEND_CKT_TYPE_ASCEND_PVC: AscendCktType = 0;
pub const ASCEND_CKT_TYPE_ASCEND_SVC: AscendCktType = 1;

pub type AscendClientAssignDNS = u32;
pub const ASCEND_CLIENT_ASSIGN_DNS_DNS_ASSIGN_NO: AscendClientAssignDNS = 0;
pub const ASCEND_CLIENT_ASSIGN_DNS_DNS_ASSIGN_YES: AscendClientAssignDNS = 1;

pub type AscendClientAssignWINS = u32;
pub const ASCEND_CLIENT_ASSIGN_WINS_WINS_ASSIGN_NO: AscendClientAssignWINS = 0;
pub const ASCEND_CLIENT_ASSIGN_WINS_WINS_ASSIGN_YES: AscendClientAssignWINS = 1;

pub type AscendConnectProgress = u32;
pub const ASCEND_CONNECT_PROGRESS_NO_PROGRESS: AscendConnectProgress = 0;
pub const ASCEND_CONNECT_PROGRESS_CALL_UP: AscendConnectProgress = 10;
pub const ASCEND_CONNECT_PROGRESS_MODEM_UP: AscendConnectProgress = 30;
pub const ASCEND_CONNECT_PROGRESS_MODEM_AWAITING_DCD: AscendConnectProgress = 31;
pub const ASCEND_CONNECT_PROGRESS_MODEM_AWAITING_CODES: AscendConnectProgress = 32;
pub const ASCEND_CONNECT_PROGRESS_TERM_SRV_STARTED: AscendConnectProgress = 40;
pub const ASCEND_CONNECT_PROGRESS_TERM_SRV_RAW_TCP_STARTED: AscendConnectProgress = 41;
pub const ASCEND_CONNECT_PROGRESS_TERM_SRV_TELNET_STARTED: AscendConnectProgress = 42;
pub const ASCEND_CONNECT_PROGRESS_TERM_SRV_RAW_TCP_CONNECTED: AscendConnectProgress = 43;
pub const ASCEND_CONNECT_PROGRESS_TERM_SRV_TELNET_CONNECTED: AscendConnectProgress = 44;
pub const ASCEND_CONNECT_PROGRESS_TERM_SRV_RLOGIN_STARTED: AscendConnectProgress = 45;
pub const ASCEND_CONNECT_PROGRESS_TERM_SRV_RLOGIN_CONNECTED: AscendConnectProgress = 46;
pub const ASCEND_CONNECT_PROGRESS_MODEM_OUTDIAL_CALL_UP: AscendConnectProgress = 50;
pub const ASCEND_CONNECT_PROGRESS_LAN_SESSION_UP: AscendConnectProgress = 60;
pub const ASCEND_CONNECT_PROGRESS_LCP_OPENING: AscendConnectProgress = 61;
pub const ASCEND_CONNECT_PROGRESS_CCP_OPENING: AscendConnectProgress = 62;
pub const ASCEND_CONNECT_PROGRESS_IPNCP_OPENING: AscendConnectProgress = 63;
pub const ASCEND_CONNECT_PROGRESS_BNCP_OPENING: AscendConnectProgress = 64;
pub const ASCEND_CONNECT_PROGRESS_LCP_OPENED: AscendConnectProgress = 65;
pub const ASCEND_CONNECT_PROGRESS_CCP_OPENED: AscendConnectProgress = 66;
pub const ASCEND_CONNECT_PROGRESS_IPNCP_OPENED: AscendConnectProgress = 67;
pub const ASCEND_CONNECT_PROGRESS_BNCP_OPENED: AscendConnectProgress = 68;
pub const ASCEND_CONNECT_PROGRESS_LCP_STATE_INITIAL: AscendConnectProgress = 69;
pub const ASCEND_CONNECT_PROGRESS_LCP_STATE_STARTING: AscendConnectProgress = 70;
pub const ASCEND_CONNECT_PROGRESS_LCP_STATE_CLOSED: AscendConnectProgress = 71;
pub const ASCEND_CONNECT_PROGRESS_LCP_STATE_STOPPED: AscendConnectProgress = 72;
pub const ASCEND_CONNECT_PROGRESS_LCP_STATE_CLOSING: AscendConnectProgress = 73;
pub const ASCEND_CONNECT_PROGRESS_LCP_STATE_STOPPING: AscendConnectProgress = 74;
pub const ASCEND_CONNECT_PROGRESS_LCP_STATE_REQUEST_SENT: AscendConnectProgress = 75;
pub const ASCEND_CONNECT_PROGRESS_LCP_STATE_ACK_RECEIVED: AscendConnectProgress = 76;
pub const ASCEND_CONNECT_PROGRESS_LCP_STATE_ACK_SENT: AscendConnectProgress = 77;
pub const ASCEND_CONNECT_PROGRESS_IPXNCP_OPENED: AscendConnectProgress = 80;
pub const ASCEND_CONNECT_PROGRESS_ATNCP_OPENED: AscendConnectProgress = 81;
pub const ASCEND_CONNECT_PROGRESS_BACP_OPENING: AscendConnectProgress = 82;
pub const ASCEND_CONNECT_PROGRESS_BACP_OPENED: AscendConnectProgress = 83;
pub const ASCEND_CONNECT_PROGRESS_V110_UP: AscendConnectProgress = 90;
pub const ASCEND_CONNECT_PROGRESS_V110_STATE_OPENED: AscendConnectProgress = 91;
pub const ASCEND_CONNECT_PROGRESS_V110_STATE_CARRIER: AscendConnectProgress = 92;
pub const ASCEND_CONNECT_PROGRESS_V110_STATE_RESET: AscendConnectProgress = 93;
pub const ASCEND_CONNECT_PROGRESS_V110_STATE_CLOSED: AscendConnectProgress = 94;

pub type AscendDBAMonitor = u32;
pub const ASCEND_DBA_MONITOR_DBA_TRANSMIT: AscendDBAMonitor = 0;
pub const ASCEND_DBA_MONITOR_DBA_TRANSMIT_RECV: AscendDBAMonitor = 1;
pub const ASCEND_DBA_MONITOR_DBA_NONE: AscendDBAMonitor = 2;

pub type AscendDHCPReply = u32;
pub const ASCEND_DHCP_REPLY_DHCP_REPLY_NO: AscendDHCPReply = 0;
pub const ASCEND_DHCP_REPLY_DHCP_REPLY_YES: AscendDHCPReply = 1;

pub type AscendDataSvc = u32;
pub const ASCEND_DATA_SVC_SWITCHED_VOICE_BEARER: AscendDataSvc = 0;
pub const ASCEND_DATA_SVC_NAILED_56KR: AscendDataSvc = 1;
pub const ASCEND_DATA_SVC_NAILED_64K: AscendDataSvc = 2;
pub const ASCEND_DATA_SVC_SWITCHED_64KR: AscendDataSvc = 3;
pub const ASCEND_DATA_SVC_SWITCHED_56K: AscendDataSvc = 4;
pub const ASCEND_DATA_SVC_SWITCHED_384KR: AscendDataSvc = 5;
pub const ASCEND_DATA_SVC_SWITCHED_384K: AscendDataSvc = 6;
pub const ASCEND_DATA_SVC_SWITCHED_1536K: AscendDataSvc = 7;
pub const ASCEND_DATA_SVC_SWITCHED_1536KR: AscendDataSvc = 8;
pub const ASCEND_DATA_SVC_SWITCHED_128K: AscendDataSvc = 9;
pub const ASCEND_DATA_SVC_SWITCHED_192K: AscendDataSvc = 10;
pub const ASCEND_DATA_SVC_SWITCHED_256K: AscendDataSvc = 11;
pub const ASCEND_DATA_SVC_SWITCHED_320K: AscendDataSvc = 12;
pub const ASCEND_DATA_SVC_SWITCHED_384K_MR: AscendDataSvc = 13;
pub const ASCEND_DATA_SVC_SWITCHED_448K: AscendDataSvc = 14;
pub const ASCEND_DATA_SVC_SWITCHED_512K: AscendDataSvc = 15;
pub const ASCEND_DATA_SVC_SWITCHED_576K: AscendDataSvc = 16;
pub const ASCEND_DATA_SVC_SWITCHED_640K: AscendDataSvc = 17;
pub const ASCEND_DATA_SVC_SWITCHED_704K: AscendDataSvc = 18;
pub const ASCEND_DATA_SVC_SWITCHED_768K: AscendDataSvc = 19;
pub const ASCEND_DATA_SVC_SWITCHED_832K: AscendDataSvc = 20;
pub const ASCEND_DATA_SVC_SWITCHED_896K: AscendDataSvc = 21;
pub const ASCEND_DATA_SVC_SWITCHED_960K: AscendDataSvc = 22;
pub const ASCEND_DATA_SVC_SWITCHED_1024K: AscendDataSvc = 23;
pub const ASCEND_DATA_SVC_SWITCHED_1088K: AscendDataSvc = 24;
pub const ASCEND_DATA_SVC_SWITCHED_1152K: AscendDataSvc = 25;
pub const ASCEND_DATA_SVC_SWITCHED_1216K: AscendDataSvc = 26;
pub const ASCEND_DATA_SVC_SWITCHED_1280K: AscendDataSvc = 27;
pub const ASCEND_DATA_SVC_SWITCHED_1344K: AscendDataSvc = 28;
pub const ASCEND_DATA_SVC_SWITCHED_1408K: AscendDataSvc = 29;
pub const ASCEND_DATA_SVC_SWITCHED_1472K: AscendDataSvc = 30;
pub const ASCEND_DATA_SVC_SWITCHED_1600K: AscendDataSvc = 31;
pub const ASCEND_DATA_SVC_SWITCHED_1664K: AscendDataSvc = 32;
pub const ASCEND_DATA_SVC_SWITCHED_1728K: AscendDataSvc = 33;
pub const ASCEND_DATA_SVC_SWITCHED_1792K: AscendDataSvc = 34;
pub const ASCEND_DATA_SVC_SWITCHED_1856K: AscendDataSvc = 35;
pub const ASCEND_DATA_SVC_SWITCHED_1920K: AscendDataSvc = 36;
pub const ASCEND_DATA_SVC_SWITCHED_INHERITED: AscendDataSvc = 37;
pub const ASCEND_DATA_SVC_SWITCHED_RESTRICTED_BEARER_X_30: AscendDataSvc = 38;
pub const ASCEND_DATA_SVC_SWITCHED_CLEAR_BEARER_V_110: AscendDataSvc = 39;
pub const ASCEND_DATA_SVC_SWITCHED_RESTRICTED_64_X_30: AscendDataSvc = 40;
pub const ASCEND_DATA_SVC_SWITCHED_CLEAR_56_V_110: AscendDataSvc = 41;
pub const ASCEND_DATA_SVC_SWITCHED_MODEM: AscendDataSvc = 42;
pub const ASCEND_DATA_SVC_SWITCHED_ATMODEM: AscendDataSvc = 43;
pub const ASCEND_DATA_SVC_SWITCHED_V110_24_56: AscendDataSvc = 45;
pub const ASCEND_DATA_SVC_SWITCHED_V110_48_56: AscendDataSvc = 46;
pub const ASCEND_DATA_SVC_SWITCHED_V110_96_56: AscendDataSvc = 47;
pub const ASCEND_DATA_SVC_SWITCHED_V110_192_56: AscendDataSvc = 48;
pub const ASCEND_DATA_SVC_SWITCHED_V110_384_56: AscendDataSvc = 49;
pub const ASCEND_DATA_SVC_SWITCHED_V110_24_56R: AscendDataSvc = 50;
pub const ASCEND_DATA_SVC_SWITCHED_V110_48_56R: AscendDataSvc = 51;
pub const ASCEND_DATA_SVC_SWITCHED_V110_96_56R: AscendDataSvc = 52;
pub const ASCEND_DATA_SVC_SWITCHED_V110_192_56R: AscendDataSvc = 53;
pub const ASCEND_DATA_SVC_SWITCHED_V110_384_56R: AscendDataSvc = 54;
pub const ASCEND_DATA_SVC_SWITCHED_V110_24_64: AscendDataSvc = 55;
pub const ASCEND_DATA_SVC_SWITCHED_V110_48_64: AscendDataSvc = 56;
pub const ASCEND_DATA_SVC_SWITCHED_V110_96_64: AscendDataSvc = 57;
pub const ASCEND_DATA_SVC_SWITCHED_V110_192_64: AscendDataSvc = 58;
pub const ASCEND_DATA_SVC_SWITCHED_V110_384_64: AscendDataSvc = 59;
pub const ASCEND_DATA_SVC_SWITCHED_V110_24_64R: AscendDataSvc = 60;
pub const ASCEND_DATA_SVC_SWITCHED_V110_48_64R: AscendDataSvc = 61;
pub const ASCEND_DATA_SVC_SWITCHED_V110_96_64R: AscendDataSvc = 62;
pub const ASCEND_DATA_SVC_SWITCHED_V110_384_64R: AscendDataSvc = 64;
pub const ASCEND_DATA_SVC_SWITCHED_V110_192_64R: AscendDataSvc = 63;
pub const ASCEND_DATA_SVC_SWITCHED_POTS: AscendDataSvc = 68;
pub const ASCEND_DATA_SVC_SWITCHED_ATM: AscendDataSvc = 69;
pub const ASCEND_DATA_SVC_SWITCHED_FR: AscendDataSvc = 70;

pub type AscendDialoutAllowed = u32;
pub const ASCEND_DIALOUT_ALLOWED_DIALOUT_NOT_ALLOWED: AscendDialoutAllowed = 0;
pub const ASCEND_DIALOUT_ALLOWED_DIALOUT_ALLOWED: AscendDialoutAllowed = 1;

pub type AscendDisconnectCause = u32;
pub const ASCEND_DISCONNECT_CAUSE_NO_REASON: AscendDisconnectCause = 0;
pub const ASCEND_DISCONNECT_CAUSE_NOT_APPLICABLE: AscendDisconnectCause = 1;
pub const ASCEND_DISCONNECT_CAUSE_UNKNOWN: AscendDisconnectCause = 2;
pub const ASCEND_DISCONNECT_CAUSE_CALL_DISCONNECTED: AscendDisconnectCause = 3;
pub const ASCEND_DISCONNECT_CAUSE_CLID_AUTHENTICATION_FAILED: AscendDisconnectCause = 4;
pub const ASCEND_DISCONNECT_CAUSE_CLID_RADIUS_TIMEOUT: AscendDisconnectCause = 5;
pub const ASCEND_DISCONNECT_CAUSE_MODEM_NO_DCD: AscendDisconnectCause = 10;
pub const ASCEND_DISCONNECT_CAUSE_DCD_DETECTED_THEN_INACTIVE: AscendDisconnectCause = 11;
pub const ASCEND_DISCONNECT_CAUSE_MODEM_INVALID_RESULT_CODES: AscendDisconnectCause = 12;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_USER_QUIT: AscendDisconnectCause = 20;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_IDLE_TIMEOUT: AscendDisconnectCause = 21;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_EXIT_TELNET: AscendDisconnectCause = 22;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_NO_I_PADDR: AscendDisconnectCause = 23;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_EXIT_RAW_TCP: AscendDisconnectCause = 24;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_EXIT_LOGIN_FAILED: AscendDisconnectCause = 25;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_EXIT_RAW_TCP_DISABLED: AscendDisconnectCause = 26;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_CTRL_C_IN_LOGIN: AscendDisconnectCause = 27;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_DESTROYED: AscendDisconnectCause = 28;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_USER_CLOSED_V_CON: AscendDisconnectCause = 29;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_V_CON_DESTROYED: AscendDisconnectCause = 30;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_EXIT_RLOGIN: AscendDisconnectCause = 31;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_BAD_RLOGIN_OPTION: AscendDisconnectCause = 32;
pub const ASCEND_DISCONNECT_CAUSE_TERM_SRV_NOT_ENOUGH_RESOURCES: AscendDisconnectCause = 33;
pub const ASCEND_DISCONNECT_CAUSE_MPP_NO_NULL_MSG_TIMEOUT: AscendDisconnectCause = 35;
pub const ASCEND_DISCONNECT_CAUSE_PPP_LCP_TIMEOUT: AscendDisconnectCause = 40;
pub const ASCEND_DISCONNECT_CAUSE_PPP_LCP_NEGOTION_FAILED: AscendDisconnectCause = 41;
pub const ASCEND_DISCONNECT_CAUSE_PPP_PAP_AUTH_FAILED: AscendDisconnectCause = 42;
pub const ASCEND_DISCONNECT_CAUSE_PPP_CHAP_AUTH_FAILED: AscendDisconnectCause = 43;
pub const ASCEND_DISCONNECT_CAUSE_PPP_RMT_AUTH_FAILED: AscendDisconnectCause = 44;
pub const ASCEND_DISCONNECT_CAUSE_PPP_RCV_TERMINATE_REQ: AscendDisconnectCause = 45;
pub const ASCEND_DISCONNECT_CAUSE_PPP_RCV_CLOSE_EVENT: AscendDisconnectCause = 46;
pub const ASCEND_DISCONNECT_CAUSE_PPP_NO_NC_PS_OPEN: AscendDisconnectCause = 47;
pub const ASCEND_DISCONNECT_CAUSE_PPP_MP_BUNDLE_UNKNOWN: AscendDisconnectCause = 48;
pub const ASCEND_DISCONNECT_CAUSE_PPP_LCP_CLOSE_MP_ADD_FAIL: AscendDisconnectCause = 49;
pub const ASCEND_DISCONNECT_CAUSE_SESSION_TABLE_FULL: AscendDisconnectCause = 50;
pub const ASCEND_DISCONNECT_CAUSE_OUT_OF_RESOURCES: AscendDisconnectCause = 51;
pub const ASCEND_DISCONNECT_CAUSE_INVALID_IP_ADDRESS: AscendDisconnectCause = 52;
pub const ASCEND_DISCONNECT_CAUSE_HOSTNAME_RESOLUTION_FAILED: AscendDisconnectCause = 53;
pub const ASCEND_DISCONNECT_CAUSE_BAD_OR_MISSING_PORT_NUMBER: AscendDisconnectCause = 54;
pub const ASCEND_DISCONNECT_CAUSE_HOST_RESET: AscendDisconnectCause = 60;
pub const ASCEND_DISCONNECT_CAUSE_CONNECTION_REFUSED: AscendDisconnectCause = 61;
pub const ASCEND_DISCONNECT_CAUSE_CONNECTION_TIMEOUT: AscendDisconnectCause = 62;
pub const ASCEND_DISCONNECT_CAUSE_CONNECTION_CLOSED: AscendDisconnectCause = 63;
pub const ASCEND_DISCONNECT_CAUSE_NETWORK_UNREACHABLE: AscendDisconnectCause = 64;
pub const ASCEND_DISCONNECT_CAUSE_HOST_UNREACHABLE: AscendDisconnectCause = 65;
pub const ASCEND_DISCONNECT_CAUSE_NETWORK_UNREACHABLE_ADMIN: AscendDisconnectCause = 66;
pub const ASCEND_DISCONNECT_CAUSE_HOST_UNREACHABLE_ADMIN: AscendDisconnectCause = 67;
pub const ASCEND_DISCONNECT_CAUSE_PORT_UNREACHABLE: AscendDisconnectCause = 68;
pub const ASCEND_DISCONNECT_CAUSE_SESSION_TIMEOUT: AscendDisconnectCause = 100;
pub const ASCEND_DISCONNECT_CAUSE_INVALID_INCOMING_USER: AscendDisconnectCause = 101;
pub const ASCEND_DISCONNECT_CAUSE_DISCONNECT_DUE_TO_CALLBACK: AscendDisconnectCause = 102;
pub const ASCEND_DISCONNECT_CAUSE_PROTO_DISABLED_OR_UNSUPPORTED: AscendDisconnectCause = 120;
pub const ASCEND_DISCONNECT_CAUSE_DISCONNECT_REQ_BY_RADIUS: AscendDisconnectCause = 150;
pub const ASCEND_DISCONNECT_CAUSE_DISCONNECT_REQ_BY_LOCAL_ADMIN: AscendDisconnectCause = 151;
pub const ASCEND_DISCONNECT_CAUSE_V110_TIMEOUT_SYNC_RETRY_EXCEED: AscendDisconnectCause = 160;
pub const ASCEND_DISCONNECT_CAUSE_PPP_AUTH_TIMEOUT_EXCEEDED: AscendDisconnectCause = 170;
pub const ASCEND_DISCONNECT_CAUSE_USER_EXECUTED_DO_HANGUP: AscendDisconnectCause = 180;
pub const ASCEND_DISCONNECT_CAUSE_REMOTE_END_HUNG_UP: AscendDisconnectCause = 185;
pub const ASCEND_DISCONNECT_CAUSE_RESOURCE_HAS_BEEN_QUIESCED: AscendDisconnectCause = 190;
pub const ASCEND_DISCONNECT_CAUSE_MAX_CALL_DURATION_REACHED: AscendDisconnectCause = 195;

pub type AscendDslDownstreamLimit = u32;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_1280000: AscendDslDownstreamLimit = 10;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_1600000: AscendDslDownstreamLimit = 9;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_1920000: AscendDslDownstreamLimit = 8;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_2240000: AscendDslDownstreamLimit = 7;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_2560000: AscendDslDownstreamLimit = 6;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_2688000: AscendDslDownstreamLimit = 5;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_3200000: AscendDslDownstreamLimit = 4;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_4480000: AscendDslDownstreamLimit = 3;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_5120000: AscendDslDownstreamLimit = 2;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_6272000: AscendDslDownstreamLimit = 1;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_640000: AscendDslDownstreamLimit = 12;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_7168000: AscendDslDownstreamLimit = 0;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLCAP_DN_960000: AscendDslDownstreamLimit = 11;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_128000: AscendDslDownstreamLimit = 121;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_1280000: AscendDslDownstreamLimit = 114;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_1600000: AscendDslDownstreamLimit = 113;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_1920000: AscendDslDownstreamLimit = 112;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_2240000: AscendDslDownstreamLimit = 111;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_256000: AscendDslDownstreamLimit = 120;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_2560000: AscendDslDownstreamLimit = 110;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_2688000: AscendDslDownstreamLimit = 109;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_3200000: AscendDslDownstreamLimit = 108;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_384000: AscendDslDownstreamLimit = 119;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_4480000: AscendDslDownstreamLimit = 107;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_512000: AscendDslDownstreamLimit = 118;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_5120000: AscendDslDownstreamLimit = 106;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_6272000: AscendDslDownstreamLimit = 105;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_640000: AscendDslDownstreamLimit = 117;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_7168000: AscendDslDownstreamLimit = 104;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_768000: AscendDslDownstreamLimit = 116;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_8000000: AscendDslDownstreamLimit = 103;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_8960000: AscendDslDownstreamLimit = 102;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_9504000: AscendDslDownstreamLimit = 101;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_960000: AscendDslDownstreamLimit = 115;
pub const ASCEND_DSL_DOWNSTREAM_LIMIT_ADSLDMT_DN_AUTO: AscendDslDownstreamLimit = 100;

pub type AscendDslRateMode = u32;
pub const ASCEND_DSL_RATE_MODE_RATE_MODE_AUTO_BAUD: AscendDslRateMode = 1;
pub const ASCEND_DSL_RATE_MODE_RATE_MODE_SINGLE: AscendDslRateMode = 2;

pub type AscendDslRateType = u32;
pub const ASCEND_DSL_RATE_TYPE_RATE_TYPE_ADSL_CAP: AscendDslRateType = 2;
pub const ASCEND_DSL_RATE_TYPE_RATE_TYPE_ADSL_DMT: AscendDslRateType = 4;
pub const ASCEND_DSL_RATE_TYPE_RATE_TYPE_ADSL_DMT_CELL: AscendDslRateType = 3;
pub const ASCEND_DSL_RATE_TYPE_RATE_TYPE_DISABLED: AscendDslRateType = 0;
pub const ASCEND_DSL_RATE_TYPE_RATE_TYPE_SDSL: AscendDslRateType = 1;

pub type AscendDslUpstreamLimit = u32;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_896000: AscendDslUpstreamLimit = 153;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLCAP_UP_1088000: AscendDslUpstreamLimit = 50;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLCAP_UP_272000: AscendDslUpstreamLimit = 56;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLCAP_UP_408000: AscendDslUpstreamLimit = 55;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLCAP_UP_544000: AscendDslUpstreamLimit = 54;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLCAP_UP_680000: AscendDslUpstreamLimit = 53;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLCAP_UP_816000: AscendDslUpstreamLimit = 52;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLCAP_UP_952000: AscendDslUpstreamLimit = 51;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_1088000: AscendDslUpstreamLimit = 151;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_128000: AscendDslUpstreamLimit = 160;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_256000: AscendDslUpstreamLimit = 159;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_384000: AscendDslUpstreamLimit = 158;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_512000: AscendDslUpstreamLimit = 157;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_640000: AscendDslUpstreamLimit = 156;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_768000: AscendDslUpstreamLimit = 155;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_800000: AscendDslUpstreamLimit = 154;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_928000: AscendDslUpstreamLimit = 152;
pub const ASCEND_DSL_UPSTREAM_LIMIT_ADSLDMT_UP_AUTO: AscendDslUpstreamLimit = 150;
pub const ASCEND_DSL_UPSTREAM_LIMIT_SDSL_1168000: AscendDslUpstreamLimit = 5;
pub const ASCEND_DSL_UPSTREAM_LIMIT_SDSL_144000: AscendDslUpstreamLimit = 0;
pub const ASCEND_DSL_UPSTREAM_LIMIT_SDSL_1552000: AscendDslUpstreamLimit = 6;
pub const ASCEND_DSL_UPSTREAM_LIMIT_SDSL_2320000: AscendDslUpstreamLimit = 7;
pub const ASCEND_DSL_UPSTREAM_LIMIT_SDSL_272000: AscendDslUpstreamLimit = 1;
pub const ASCEND_DSL_UPSTREAM_LIMIT_SDSL_400000: AscendDslUpstreamLimit = 2;
pub const ASCEND_DSL_UPSTREAM_LIMIT_SDSL_528000: AscendDslUpstreamLimit = 3;
pub const ASCEND_DSL_UPSTREAM_LIMIT_SDSL_784000: AscendDslUpstreamLimit = 4;

pub type AscendEventType = u32;
pub const ASCEND_EVENT_TYPE_ASCEND_COLD_START: AscendEventType = 1;
pub const ASCEND_EVENT_TYPE_ASCEND_SESSION_EVENT: AscendEventType = 2;

pub type AscendExpectCallback = u32;
pub const ASCEND_EXPECT_CALLBACK_EXPECT_CALLBACK_NO: AscendExpectCallback = 0;
pub const ASCEND_EXPECT_CALLBACK_EXPECT_CALLBACK_YES: AscendExpectCallback = 1;

pub type AscendFRDirect = u32;
pub const ASCEND_FR_DIRECT_FR_DIRECT_NO: AscendFRDirect = 0;
pub const ASCEND_FR_DIRECT_FR_DIRECT_YES: AscendFRDirect = 1;

pub type AscendFRLinkMgt = u32;
pub const ASCEND_FR_LINK_MGT_ASCEND_FR_NO_LINK_MGT: AscendFRLinkMgt = 0;
pub const ASCEND_FR_LINK_MGT_ASCEND_FR_T1_617D: AscendFRLinkMgt = 1;
pub const ASCEND_FR_LINK_MGT_ASCEND_FR_Q_933A: AscendFRLinkMgt = 2;

pub type AscendFRLinkStatusDLCI = u32;
pub const ASCEND_FR_LINK_STATUS_DLCI_ASCEND_FR_LMI_DLCI_0: AscendFRLinkStatusDLCI = 0;
pub const ASCEND_FR_LINK_STATUS_DLCI_ASCEND_FR_LMI_DLCI_1023: AscendFRLinkStatusDLCI = 1023;

pub type AscendFRLinkUp = u32;
pub const ASCEND_FR_LINK_UP_ASCEND_LINK_UP_DEFAULT: AscendFRLinkUp = 0;
pub const ASCEND_FR_LINK_UP_ASCEND_LINK_UP_ALWAYS_UP: AscendFRLinkUp = 1;

pub type AscendFRType = u32;
pub const ASCEND_FR_TYPE_ASCEND_FR_DTE: AscendFRType = 0;
pub const ASCEND_FR_TYPE_ASCEND_FR_DCE: AscendFRType = 1;
pub const ASCEND_FR_TYPE_ASCEND_FR_NNI: AscendFRType = 2;

pub type AscendFT1Caller = u32;
pub const ASCEND_FT1_CALLER_FT1_NO: AscendFT1Caller = 0;
pub const ASCEND_FT1_CALLER_FT1_YES: AscendFT1Caller = 1;

pub type AscendFilterRequired = u32;
pub const ASCEND_FILTER_REQUIRED_REQUIRED_NO: AscendFilterRequired = 0;
pub const ASCEND_FILTER_REQUIRED_REQUIRED_YES: AscendFilterRequired = 1;

pub type AscendForce56 = u32;
pub const ASCEND_FORCE_56_FORCE_56_NO: AscendForce56 = 0;
pub const ASCEND_FORCE_56_FORCE_56_YES: AscendForce56 = 1;

pub type AscendHandleIPX = u32;
pub const ASCEND_HANDLE_IPX_HANDLE_IPX_NONE: AscendHandleIPX = 0;
pub const ASCEND_HANDLE_IPX_HANDLE_IPX_CLIENT: AscendHandleIPX = 1;
pub const ASCEND_HANDLE_IPX_HANDLE_IPX_SERVER: AscendHandleIPX = 2;

pub type AscendHistoryWeighType = u32;
pub const ASCEND_HISTORY_WEIGH_TYPE_HISTORY_CONSTANT: AscendHistoryWeighType = 0;
pub const ASCEND_HISTORY_WEIGH_TYPE_HISTORY_LINEAR: AscendHistoryWeighType = 1;
pub const ASCEND_HISTORY_WEIGH_TYPE_HISTORY_QUADRATIC: AscendHistoryWeighType = 2;

pub type AscendIPPoolChaining = u32;
pub const ASCEND_IP_POOL_CHAINING_IP_POOL_CHAINING_NO: AscendIPPoolChaining = 0;
pub const ASCEND_IP_POOL_CHAINING_IP_POOL_CHAINING_YES: AscendIPPoolChaining = 1;

pub type AscendIPTOS = u32;
pub const ASCEND_IPTOS_IP_TOS_COST: AscendIPTOS = 2;
pub const ASCEND_IPTOS_IP_TOS_DISABLED: AscendIPTOS = 1;
pub const ASCEND_IPTOS_IP_TOS_LATENCY: AscendIPTOS = 16;
pub const ASCEND_IPTOS_IP_TOS_NORMAL: AscendIPTOS = 0;
pub const ASCEND_IPTOS_IP_TOS_RELIABILITY: AscendIPTOS = 4;
pub const ASCEND_IPTOS_IP_TOS_THROUGHPUT: AscendIPTOS = 8;

pub type AscendIPTOSApplyTo = u32;
pub const ASCEND_IPTOS_APPLY_TO_IP_TOS_APPLY_TO_BOTH: AscendIPTOSApplyTo = 3072;
pub const ASCEND_IPTOS_APPLY_TO_IP_TOS_APPLY_TO_INCOMING: AscendIPTOSApplyTo = 1024;
pub const ASCEND_IPTOS_APPLY_TO_IP_TOS_APPLY_TO_OUTGOING: AscendIPTOSApplyTo = 2048;

pub type AscendIPTOSPrecedence = u32;
pub const ASCEND_IPTOS_PRECEDENCE_IP_TOS_PRECEDENCE_PRI_FIVE: AscendIPTOSPrecedence = 160;
pub const ASCEND_IPTOS_PRECEDENCE_IP_TOS_PRECEDENCE_PRI_FOUR: AscendIPTOSPrecedence = 128;
pub const ASCEND_IPTOS_PRECEDENCE_IP_TOS_PRECEDENCE_PRI_NORMAL: AscendIPTOSPrecedence = 0;
pub const ASCEND_IPTOS_PRECEDENCE_IP_TOS_PRECEDENCE_PRI_ONE: AscendIPTOSPrecedence = 32;
pub const ASCEND_IPTOS_PRECEDENCE_IP_TOS_PRECEDENCE_PRI_SEVEN: AscendIPTOSPrecedence = 224;
pub const ASCEND_IPTOS_PRECEDENCE_IP_TOS_PRECEDENCE_PRI_SIX: AscendIPTOSPrecedence = 192;
pub const ASCEND_IPTOS_PRECEDENCE_IP_TOS_PRECEDENCE_PRI_THREE: AscendIPTOSPrecedence = 96;
pub const ASCEND_IPTOS_PRECEDENCE_IP_TOS_PRECEDENCE_PRI_TWO: AscendIPTOSPrecedence = 64;

pub type AscendIPXHeaderCompression = u32;
pub const ASCEND_IPX_HEADER_COMPRESSION_IPX_HEADER_COMPRESSION_NO: AscendIPXHeaderCompression = 0;
pub const ASCEND_IPX_HEADER_COMPRESSION_IPX_HEADER_COMPRESSION_YES: AscendIPXHeaderCompression = 1;

pub type AscendIPXPeerMode = u32;
pub const ASCEND_IPX_PEER_MODE_IPX_PEER_ROUTER: AscendIPXPeerMode = 0;
pub const ASCEND_IPX_PEER_MODE_IPX_PEER_DIALIN: AscendIPXPeerMode = 1;

pub type AscendLinkCompression = u32;
pub const ASCEND_LINK_COMPRESSION_LINK_COMP_NONE: AscendLinkCompression = 0;
pub const ASCEND_LINK_COMPRESSION_LINK_COMP_STAC: AscendLinkCompression = 1;
pub const ASCEND_LINK_COMPRESSION_LINK_COMP_STAC_DRAFT_9: AscendLinkCompression = 2;
pub const ASCEND_LINK_COMPRESSION_LINK_COMP_MS_STAC: AscendLinkCompression = 3;

pub type AscendMulticastClient = u32;
pub const ASCEND_MULTICAST_CLIENT_MULTICAST_NO: AscendMulticastClient = 0;
pub const ASCEND_MULTICAST_CLIENT_MULTICAST_YES: AscendMulticastClient = 1;

pub type AscendNASPortFormat = u32;
pub const ASCEND_NAS_PORT_FORMAT_1_2_2: AscendNASPortFormat = 3;
pub const ASCEND_NAS_PORT_FORMAT_2_4_5_5: AscendNASPortFormat = 2;
pub const ASCEND_NAS_PORT_FORMAT_2_4_6_4: AscendNASPortFormat = 1;
pub const ASCEND_NAS_PORT_FORMAT_UNKNOWN: AscendNASPortFormat = 0;

pub type AscendNumberingPlanID = u32;
pub const ASCEND_NUMBERING_PLAN_ID_ISDN_NUMBERING_PLAN: AscendNumberingPlanID = 1;
pub const ASCEND_NUMBERING_PLAN_ID_PRIVATE_NUMBERING_PLAN: AscendNumberingPlanID = 9;
pub const ASCEND_NUMBERING_PLAN_ID_UNKNOWN_NUMBERING_PLAN: AscendNumberingPlanID = 0;

pub type AscendPPPVJ1172 = u32;
pub const ASCEND_PPPVJ1172_PPP_VJ_1172: AscendPPPVJ1172 = 1;

pub type AscendPPPVJSlotComp = u32;
pub const ASCEND_PPPVJ_SLOT_COMP_VJ_SLOT_COMP_NO: AscendPPPVJSlotComp = 1;

pub type AscendPPPoEEnable = u32;
pub const ASCEND_PP_PO_E_ENABLE_PP_PO_E_NO: AscendPPPoEEnable = 0;
pub const ASCEND_PP_PO_E_ENABLE_PP_PO_E_YES: AscendPPPoEEnable = 1;

pub type AscendPRINumberType = u32;
pub const ASCEND_PRI_NUMBER_TYPE_UNKNOWN_NUMBER: AscendPRINumberType = 0;
pub const ASCEND_PRI_NUMBER_TYPE_INTL_NUMBER: AscendPRINumberType = 1;
pub const ASCEND_PRI_NUMBER_TYPE_NATIONAL_NUMBER: AscendPRINumberType = 2;
pub const ASCEND_PRI_NUMBER_TYPE_NET_SPECIFIC_NUMBER: AscendPRINumberType = 3;
pub const ASCEND_PRI_NUMBER_TYPE_LOCAL_NUMBER: AscendPRINumberType = 4;
pub const ASCEND_PRI_NUMBER_TYPE_ABBREV_NUMBER: AscendPRINumberType = 5;

pub type AscendPWLifetime = u32;
pub const ASCEND_PW_LIFETIME_LIFETIME_IN_DAYS: AscendPWLifetime = 0;

pub type AscendPWWarntime = u32;
pub const ASCEND_PW_WARNTIME_DAYS_OF_WARNING: AscendPWWarntime = 0;

pub type AscendPortRedirProtocol = u32;
pub const ASCEND_PORT_REDIR_PROTOCOL_ASCEND_PROTO_TCP: AscendPortRedirProtocol = 6;
pub const ASCEND_PORT_REDIR_PROTOCOL_ASCEND_PROTO_UDP: AscendPortRedirProtocol = 17;

pub type AscendPrivateRouteRequired = u32;
pub const ASCEND_PRIVATE_ROUTE_REQUIRED_REQUIRED_NO: AscendPrivateRouteRequired = 0;
pub const ASCEND_PRIVATE_ROUTE_REQUIRED_REQUIRED_YES: AscendPrivateRouteRequired = 1;

pub type AscendRequireAuth = u32;
pub const ASCEND_REQUIRE_AUTH_NOT_REQUIRE_AUTH: AscendRequireAuth = 0;
pub const ASCEND_REQUIRE_AUTH_REQUIRE_AUTH: AscendRequireAuth = 1;

pub type AscendRouteAppletalk = u32;
pub const ASCEND_ROUTE_APPLETALK_ROUTE_APPLETALK_NO: AscendRouteAppletalk = 0;
pub const ASCEND_ROUTE_APPLETALK_ROUTE_APPLETALK_YES: AscendRouteAppletalk = 1;

pub type AscendRouteIP = u32;
pub const ASCEND_ROUTE_IP_ROUTE_IP_NO: AscendRouteIP = 0;
pub const ASCEND_ROUTE_IP_ROUTE_IP_YES: AscendRouteIP = 1;

pub type AscendRouteIPX = u32;
pub const ASCEND_ROUTE_IPX_ROUTE_IPX_NO: AscendRouteIPX = 0;
pub const ASCEND_ROUTE_IPX_ROUTE_IPX_YES: AscendRouteIPX = 1;

pub type AscendSVCEnabled = u32;
pub const ASCEND_SVC_ENABLED_ASCEND_SVC_ENABLED_NO: AscendSVCEnabled = 0;
pub const ASCEND_SVC_ENABLED_ASCEND_SVC_ENABLED_YES: AscendSVCEnabled = 1;

pub type AscendSendAuth = u32;
pub const ASCEND_SEND_AUTH_SEND_AUTH_NONE: AscendSendAuth = 0;
pub const ASCEND_SEND_AUTH_SEND_AUTH_PAP: AscendSendAuth = 1;
pub const ASCEND_SEND_AUTH_SEND_AUTH_CHAP: AscendSendAuth = 2;
pub const ASCEND_SEND_AUTH_SEND_AUTH_MS_CHAP: AscendSendAuth = 3;

pub type AscendServiceType = u32;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_ATM: AscendServiceType = 20;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_COMBINET: AscendServiceType = 7;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_EU_RAW: AscendServiceType = 9;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_EU_UI: AscendServiceType = 10;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_FR: AscendServiceType = 8;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_HDLC_NRM: AscendServiceType = 21;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_IP_FAX: AscendServiceType = 19;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_MP: AscendServiceType = 15;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_MPP: AscendServiceType = 5;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_NET_TO_NET: AscendServiceType = 25;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_NONE: AscendServiceType = 1;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_NOT_USED: AscendServiceType = 0;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_OTHER: AscendServiceType = 2;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_PPP: AscendServiceType = 3;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_PSEU_TUN_PPP: AscendServiceType = 18;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_RAW_TCP: AscendServiceType = 13;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_SLIP: AscendServiceType = 4;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_TELNET: AscendServiceType = 11;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_TELNET_BIN: AscendServiceType = 12;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_TERM_SERVER: AscendServiceType = 14;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_VIRTUAL_CONN: AscendServiceType = 16;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_VISA_2: AscendServiceType = 23;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_VO_IP: AscendServiceType = 22;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_X25: AscendServiceType = 6;
pub const ASCEND_SERVICE_TYPE_ASCEND_SERVICE_TYPE_X25D_CHAN: AscendServiceType = 17;

pub type AscendSessionType = u32;
pub const ASCEND_SESSION_TYPE_ASCEND_SESSION_G711_ALAW: AscendSessionType = 3;
pub const ASCEND_SESSION_TYPE_ASCEND_SESSION_G711_ULAW: AscendSessionType = 2;
pub const ASCEND_SESSION_TYPE_ASCEND_SESSION_G723: AscendSessionType = 4;
pub const ASCEND_SESSION_TYPE_ASCEND_SESSION_G723_64KPS: AscendSessionType = 6;
pub const ASCEND_SESSION_TYPE_ASCEND_SESSION_G728: AscendSessionType = 7;
pub const ASCEND_SESSION_TYPE_ASCEND_SESSION_G729: AscendSessionType = 5;
pub const ASCEND_SESSION_TYPE_ASCEND_SESSION_RT24: AscendSessionType = 8;
pub const ASCEND_SESSION_TYPE_ASCEND_SESSION_UNKNOWN: AscendSessionType = 1;
pub const ASCEND_SESSION_TYPE_ASCEND_SESSION_UNUSED: AscendSessionType = 0;

pub type AscendSharedProfileEnable = u32;
pub const ASCEND_SHARED_PROFILE_ENABLE_SHARED_PROFILE_NO: AscendSharedProfileEnable = 0;
pub const ASCEND_SHARED_PROFILE_ENABLE_SHARED_PROFILE_YES: AscendSharedProfileEnable = 1;

pub type AscendSourceIPCheck = u32;
pub const ASCEND_SOURCE_IP_CHECK_SOURCE_IP_CHECK_NO: AscendSourceIPCheck = 0;
pub const ASCEND_SOURCE_IP_CHECK_SOURCE_IP_CHECK_YES: AscendSourceIPCheck = 1;

pub type AscendTSIdleMode = u32;
pub const ASCEND_TS_IDLE_MODE_TS_IDLE_NONE: AscendTSIdleMode = 0;
pub const ASCEND_TS_IDLE_MODE_TS_IDLE_INPUT: AscendTSIdleMode = 1;
pub const ASCEND_TS_IDLE_MODE_TS_IDLE_INPUT_OUTPUT: AscendTSIdleMode = 2;

pub type AscendTemporaryRtes = u32;
pub const ASCEND_TEMPORARY_RTES_TEMP_RTES_NO: AscendTemporaryRtes = 0;
pub const ASCEND_TEMPORARY_RTES_TEMP_RTES_YES: AscendTemporaryRtes = 1;

pub type AscendTokenImmediate = u32;
pub const ASCEND_TOKEN_IMMEDIATE_TOK_IMM_NO: AscendTokenImmediate = 0;
pub const ASCEND_TOKEN_IMMEDIATE_TOK_IMM_YES: AscendTokenImmediate = 1;

pub type AscendTunnelingProtocol = u32;
pub const ASCEND_TUNNELING_PROTOCOL_ATMP_TUNNEL: AscendTunnelingProtocol = 0;
pub const ASCEND_TUNNELING_PROTOCOL_VTP_TUNNEL: AscendTunnelingProtocol = 1;

pub type AscendUserAcctBase = u32;
pub const ASCEND_USER_ACCT_BASE_BASE_10: AscendUserAcctBase = 0;
pub const ASCEND_USER_ACCT_BASE_BASE_16: AscendUserAcctBase = 1;

pub type AscendUserAcctType = u32;
pub const ASCEND_USER_ACCT_TYPE_ASCEND_USER_ACCT_NONE: AscendUserAcctType = 0;
pub const ASCEND_USER_ACCT_TYPE_ASCEND_USER_ACCT_USER: AscendUserAcctType = 1;
pub const ASCEND_USER_ACCT_TYPE_ASCEND_USER_ACCT_USER_DEFAULT: AscendUserAcctType = 2;

pub type AscendX25PadX3Profile = u32;
pub const ASCEND_X25_PAD_X3_PROFILE_CC_SSP: AscendX25PadX3Profile = 4;
pub const ASCEND_X25_PAD_X3_PROFILE_CC_TSP: AscendX25PadX3Profile = 5;
pub const ASCEND_X25_PAD_X3_PROFILE_CRT: AscendX25PadX3Profile = 0;
pub const ASCEND_X25_PAD_X3_PROFILE_CUSTOM: AscendX25PadX3Profile = 11;
pub const ASCEND_X25_PAD_X3_PROFILE_DEFAULT: AscendX25PadX3Profile = 2;
pub const ASCEND_X25_PAD_X3_PROFILE_HARDCOPY: AscendX25PadX3Profile = 6;
pub const ASCEND_X25_PAD_X3_PROFILE_HDX: AscendX25PadX3Profile = 7;
pub const ASCEND_X25_PAD_X3_PROFILE_INFONET: AscendX25PadX3Profile = 1;
pub const ASCEND_X25_PAD_X3_PROFILE_NULL: AscendX25PadX3Profile = 10;
pub const ASCEND_X25_PAD_X3_PROFILE_POS: AscendX25PadX3Profile = 9;
pub const ASCEND_X25_PAD_X3_PROFILE_SCEN: AscendX25PadX3Profile = 3;
pub const ASCEND_X25_PAD_X3_PROFILE_SHARK: AscendX25PadX3Profile = 8;

pub type AscendX25ReverseCharging = u32;
pub const ASCEND_X25_REVERSE_CHARGING_REVERSE_CHARGING_NO: AscendX25ReverseCharging = 0;
pub const ASCEND_X25_REVERSE_CHARGING_REVERSE_CHARGING_YES: AscendX25ReverseCharging = 1;

pub type FramedProtocol = u32;
pub const FRAMED_PROTOCOL_ASCEND_ARA: FramedProtocol = 255;
pub const FRAMED_PROTOCOL_ASCEND_MPP: FramedProtocol = 256;
pub const FRAMED_PROTOCOL_ASCEND_EURAW: FramedProtocol = 257;
pub const FRAMED_PROTOCOL_ASCEND_EUUI: FramedProtocol = 258;
pub const FRAMED_PROTOCOL_ASCEND_X25: FramedProtocol = 259;
pub const FRAMED_PROTOCOL_ASCEND_COMB: FramedProtocol = 260;
pub const FRAMED_PROTOCOL_ASCEND_FR: FramedProtocol = 261;
pub const FRAMED_PROTOCOL_ASCEND_MP: FramedProtocol = 262;
pub const FRAMED_PROTOCOL_ASCEND_FR_CIR: FramedProtocol = 263;
